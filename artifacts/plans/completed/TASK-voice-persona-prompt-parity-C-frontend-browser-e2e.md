# Task: Voice-Persona-Prompt Parity C — frontend clone/persona/prompt UI and deterministic browser-style journeys

## Problem Statement

This is part C of the implementation plan for `artifacts/designs/pending/DD-voice-persona-prompt-parity-browser-external-validation.md`. Parts A and B (backend) are prerequisites. This part delivers the browser-visible parity: (1) clone reference upload/list/inline-preview/download/delete UI integrated with the existing Voices tab, (2) a manual persona editor surfaced from the Characters & Scenes workbench (structured fields/evidence/aliases/scene-scope/review/protection/voice-consequences), and (3) an effective-prompt/settings viewer+editor (structured and guarded raw modes) wired to the prompt-config API, plus (4) deterministic browser-style (Playwright-*analogous*) Vitest/jsdom journeys using fake TTS engines and fixed media fixtures. No new real-engine dependency and no legacy manifest/route. This part also wires committed-dist build output so the deterministic frontend build gate stays green.

## Dependencies

- **Prerequisite:** `artifacts/plans/pending/TASK-voice-persona-prompt-parity-A-...` and `-B-...` (backend contracts + routes).
- Design: `artifacts/designs/pending/DD-voice-persona-prompt-parity-browser-external-validation.md`.
- Frontend: `frontend/src/api.ts` (`get/post/put/putWithRetryOnce/delWithRetryOnce/postWithRetryOnce`), `frontend/src/state.ts` (`WorkbenchState`, `WorkbenchConfig`, selectors, `Voice`/`VoiceConfig`), `frontend/src/tabs/voices.ts` (895 lines), `frontend/src/tabs/workbench.ts` (815 lines; Characters & Scenes), `frontend/src/main.ts`, `frontend/index.html`, `frontend/vitest.config.ts` (jsdom, `http://localhost:3000/`), `frontend/vitest.setup.ts` (HTMLMediaElement play/pause recording stubs already installed), `frontend/tests/frontend/test_workbench.test.ts` (773 lines convention).
- Vitest 4.1.10, jsdom 30, vite 8.2.0; `npm run build` emits into `app/static/dist/` (committed-dist equality gate `git diff --exit-code app/static/dist/`).
- Invariants: no real engines/fixtures in CI; deterministic fake engines + fixed media fixtures; `app/tts.py` byte-identical; combined-walks workbench UI (workbench.ts) unchanged where protected, extended only by the persona editor hook; no new tab family in index.html beyond wiring; accessible (keyboard, non-color, confirmation, undo, no secret/path/raw prompt exposure).

## Phases

### Phase 1: Clone reference UI (S1)
- [x] Extend `frontend/src/tabs/voices.ts` with a clone-reference panel (collapsible when voice type is `clone` or always for clone-capable voices): upload via `<input type="file">` (multipart) + optional `ref_text`, listing owned references (metadata: filename, media_type, byte_size, duration_ms, created), inline preview via the `/preview` audio endpoint (HTMLAudioElement), download via attachment endpoint (`<a download>` or blob), delete with explicit confirmation (owner-safe; cross-owner error surfaced). Resolve display-name→resolved-ID before any assignment. Surface size/duration/material limits and accessible error toasts.
    **Verified:** Clone-reference panel in frontend/src/tabs/voices.ts verified against plan+backend (app/pipeline/api_voices.py, PipelineVoiceCloneReferenceAPI.v1). Present: multipart upload (<input type=file accept=audio/*> + optional ref_text), owned-reference listing, inline preview via GET .../preview as <audio src> with load/loadedmetadata-before-currentTime seek ordering (playCloneReference), attachment download via <a download href=.../download>, delete with explicit showConfirm gate (owner-safe; cross-owner 404 surfaces as error toast), display-name→resolved-id voice selector (option values are voice_config ids, NARRATOR excluded), size/duration limits surfaced (CLONE_REF_MAX_BYTES=100MiB, CLONE_REF_MAX_DURATION_MS=10min) and accessible error toasts. Route paths + payload field names match backend exactly. GAP FIXED (within scope): the row did not surface the plan-enumerated `created` metadata — added `formatCloneCreated(ms)` helper and appended it to the metadata line in createCloneReferenceRow.
    **Notes:** Reuse `API.post/get/del`; upload is multipart (FormData), not JSON. Inline preview must use the existing player seam (`frontend/src/player.ts`) or a scoped audio element; assert `load`/`loadedmetadata` ordering before `currentTime` for seek (vitest media stubs already installed in `vitest.setup.ts`).
- [x] Add `frontend/src/api.ts` typed helpers for clone references (`uploadCloneReference(voiceId, file, refText)`, `listCloneReferences(voiceId)`, `previewCloneReferenceUrl(voiceId, referenceId)`, `downloadCloneReference(voiceId, referenceId)`, `deleteCloneReference(voiceId, referenceId)`), persona, and prompt-config endpoints to keep `state.ts`/tabs thin. Update `state.ts` types (`CloneReference`, `Persona`, `PersonaRevision`, `EffectiveWalkConfig`, `PromptConfigRevision`).
    **Verified:** frontend/src/api.ts typed helpers + frontend/src/state.ts types verified, no changes needed. api.ts: uploadCloneReference (multipart FormData audio+ref_text via postFormWithRetryOnce), listCloneReferences, cloneReferencePreviewUrl, cloneReferenceDownloadUrl, deleteCloneReference (delNoContentWithRetryOnce, 204-tolerant), plus persona (getPersona/savePersona/listPersonaRevisions/validatePersona/rerunPersona) and prompt-config (getEffectiveWalkConfig/validatePromptConfig/savePromptConfigRevision/rerunScopedWalk) helpers, 503 Retry-After convention honored. state.ts types present: CloneReference/CloneReferenceListResponse/CloneReferenceUploadResponse, Persona/PersonaEvidence/VoiceConsequences/PersonaRevision/PersonaWriteRequest/PersonaValidationResponse/PersonaRerunRequest, EffectiveWalkConfig/EffectiveWalkTask/PromptConfigWriteRequest/PromptConfigValidationResponse/PromptConfigRevision/ScopedWalkRerunRequest. All route paths, payload shapes, and DTO field names match the backend contract exactly (reference_id/voice_id/owner_id/relative_path/original_filename/media_type/byte_size/duration_ms/sha256/created_ms). Deviation (noted, not changed): helper names cloneReferencePreviewUrl/cloneReferenceDownloadUrl differ from the plan's suggested previewCloneReferenceUrl/downloadCloneReference — internal module naming, semantically equivalent, no contract impact.
    **Notes:** Follow the existing `putWithRetryOnce`/`delWithRetryOnce` 503-retry convention for persona/prompt writes; `get` for reads.
- [x] Verify: `cd frontend && npx tsc --noEmit -p tsconfig.json` exit 0; new `frontend/tests/frontend/test_clone_references.test.ts` covers upload (FixtureAudio file), inline preview load/`loadedmetadata`/seek ordering, download attachment, delete confirm + owner-error, limits display, keyboard reachability.
    **Verified:** Verification gate passed. `cd frontend && npx tsc --noEmit -p tsconfig.json` exit 0 (no pre-existing errors). `npx vitest run tests/frontend/test_clone_references.test.ts`: 19 passed / 19 (Test Files 1 passed, v4.1.10). Coverage matches plan: upload (File multipart with ref_text, oversized-file pre-network rejection, failure toast), inline preview load/loadedmetadata-before-currentTime seek ordering (playCloneReference asserts src set + currentTime=0 + play after metadata), download attachment (<a download> to /download/... URL), delete explicit confirm + decline-abort, limits display (byte/duration format helpers + limits text), owner/cross-owner delete error surfacing, and keyboard reachability (native focusable controls, no tabindex traps). GAP FIXED (within scope, test file): added `formatCloneCreated` test, delete owner-error surfacing test, and keyboard-reachability test (3 new tests, 16→19).

### Phase 2: Persona editor UI (S2)
- [x] Wire a persona editor into the workbench tab (`frontend/src/tabs/workbench.ts` / a new focused module imported by it): open a character from the Characters & Scenes ledger → edit structured fields (identity/appearance/manner/speech/role), evidence (anchor/quote/source/confidence using reachable stable anchors), aliases (normalized + provenance), scene scope (book|scenes + reachable scene_ids), review state (draft|needs_review|accepted|rejected), protected flag, and preview derived voice consequences. Save via PUT with `base_revision`; show validation/revision history; handle stale 409 (refresh + merge), 422, 503 retry.
    **Verified:** Persona editor in frontend/src/tabs/persona.ts (from cf4bca5) verified against plan + backend (app/pipeline/api_characters.py, PipelineCharacterPersonaAPI.v1). Present: open-from-ledger wiring in workbench.ts (Edit persona button, data-action=persona-open, initPersonaEditor on workbench-tab), structured fields (identity/appearance/manner/speech/role), evidence rows (anchor/quote/source/confidence with reachable scene anchors), normalized aliases (comma-separated), scene scope (book|scenes + reachable scene_ids checkboxes from selectWorkbench), review state (draft/needs_review/accepted/rejected), protected flag (+ banner, rerun disabled, edits preserve protection), derived voice consequences (renderVoiceConsequences, show-only, never an implicit assignment), save via PUT with base_revision (buildWriteRequest always sends base_revision + book_id), validation (POST /validate, side-effect-free), revision history (GET /revisions), stale 409 refresh/merge + protected rejection, 422 validation, 503 one-retry. GAP FIXED (the task-critical structured-conflict gap): the editor did NOT parse the structured RevisionConflictDTO {error,code,message,detail} (commit a720ca3 + app/pipeline/revision_conflict.py) — savePersonaWrite showed a generic 409 message and the rerun path degraded to '[object Object]'. Replaced dead errorDetail helper with readPersonaError (parses structured code+message, falls back to plain detail string / FastAPI field-array), savePersonaChecked now returns parsed error body, savePersonaWrite surfaces STALE_BASE_REVISION vs PROTECTED_REVISION code-specific messages, and added rerunPersonaChecked (local structured-aware POST) so rerunPersonaConfirmed surfaces ALREADY_RAN / PROTECTED_REVISION / CROSS_BOOK codes instead of [object Object]. No scope creep: only persona.ts + test file touched.
    **Notes:** The editor is a separately addressable capability integrated with the workbench ledger — it does NOT become a second discovery pipeline and does not modify `character.voice_assignment_id` (voice consequence is show-only unless the user explicitly assigns). Protected-edit is rejected with a clear message; rerun requires explicit confirmation naming affected scenes.
- [x] Verify: `cd frontend && npx tsc --noEmit -p tsconfig.json` exit 0; extend workbench or new `frontend/tests/frontend/test_persona.test.ts` covering open-from-ledger, field/evidence/alias/scope/protection editing, validation, save with base_revision, stale 409, protected rejection, revision list, side-effect-free validate call, keyboard + non-color state encoding.
    **Verified:** Verification gate passed. `cd frontend && npx tsc --noEmit` exit 0 (no errors). `npx vitest run tests/frontend/test_persona.test.ts`: 21 passed / 21. Full suite `npx vitest run`: 464 passed (11 files, incl. 378 base + clone tests). Coverage matches plan: open-from-ledger (openPersonaEditor head+history, 404 empty-editor), field/evidence/alias/scope/protection editing (renderPersonaEditor + buildWriteRequest round-trip with base_revision=3), validation (side-effect-free validate call renders consequences), save with base_revision (asserts PUT body base_revision), stale 409 (structured STALE_BASE_REVISION parsed), structured PROTECTED_REVISION 409, 422, 503 one-retry (Retry-After), revision list, protected rerun rejection (pre-request), explicit-confirmation scoped rerun (decline-abort, confirm:true body, no voice_assignment_id), keyboard/non-color state (real buttons/selects, text+badge encoding, aria-live errors). GAP FIXED (tests): replaced plain {detail:'stale'} 409 assertion with structured-shape tests — added STALE_BASE_REVISION + PROTECTED_REVISION parse tests for savePersonaChecked, and ALREADY_RAN + PROTECTED_REVISION structured-conflict tests for rerunPersonaConfirmed asserting the code/message surfaces and no '[object Object]' degradation. Rerun tests reworked from API.rerunPersona mock to fetch stubs (code path now uses local rerunPersonaChecked). Test count 17→21.

### Phase 3: Effective prompt/settings editor UI (S3)
- [x] Implement a prompt/settings viewer+editor (new module `frontend/src/tabs/prompt-config.ts` or within an existing tab pane): for each of the nine fixed walks, show effective values + provenance/source badges (reuse `sourceLabel`/`resolve_effective_config` shape from `state.ts`); toggle structured vs guarded-raw JSON mode (raw is validated JSON only, unknown keys rejected); validate before save (side-effect-free POST); save a revision with optimistic conflict protection (base_revision); list revisions; explicit scoped-rerun confirmation naming affected walk scope and scene scope, with `confirm:true`. Inline `temperature=0.0` handling.
    **Verified:** frontend/src/tabs/prompt-config.ts (from cf4bca5) verified against plan + backend (app/pipeline/api_walks.py, PipelineWalkPromptConfigRevisionAPI.v1, revision_conflict.py). Present: nine fixed walks via PROMPT_TASK_ORDER (WALK_ORDER→WALK_TASK_NAMES), effective values + source-tier badges (promptSourceLabel: row=DB override/config=on-disk/task=task override/global=global/fallback=default) via renderEffectiveRow/renderTaskRow, structured vs guarded-raw JSON mode (safeParseRawJson enforces exact allow-list {model_name,reasoning_effort,temperature,prompt}; unknown keys rejected client-side), side-effect-free validate (POST /config/validate), revision save with base_revision conflict protection (savePromptConfigChecked), session-tracked revision list + head (backend exposes no list route), explicit scoped-rerun with showConfirm + confirm:true + scenes reachability + script_alias_resolution book-global rejection, temperature=0.0 honored (empty = inherit). No legacy prompt-file import. GAP FIXED (structured-409 parity): the rerun path used shared API.rerunScopedWalk→post→handleError which reads body.detail (an object for RevisionConflictDTO) and degrades ALREADY_RAN/CROSS_BOOK to '[object Object]'. Added readPromptError (parses {error,code,message,detail}, falls back to plain detail string / FastAPI field-array) and rerunScopedWalkChecked (local structured-aware POST, mirrors persona's rerunPersonaChecked); rerunPromptConfirmed now surfaces ALREADY_RAN/CROSS_BOOK code+message, no auto-retry. savePromptConfigChecked now returns parsed error body; savePromptWrite surfaces STALE_BASE_REVISION/CROSS_BOOK/ALREADY_RAN code-specific messages. api.ts, persona.ts, voices.ts, main.ts untouched (persona.ts/voices.ts M-status is pre-existing from phases 1/2).
    **Notes:** Do NOT import any legacy prompt-file module. Provenance badges mirror the workbench `_source_for` tiers (row/config/task/global/fallback) surfaced via the GET effective config. Writes use `postWithRetryOnce`-style 503 retry. Rerun never auto-fires from a save.
- [x] Verify: `cd frontend && npx tsc --noEmit -p tsconfig.json` exit 0; new `frontend/tests/frontend/test_prompt_config.test.ts` covering effective/source display for the nine tasks, structured/raw toggle, guarded-raw rejection of unknown keys, validate call, revision save + conflict, rerun confirmation + already-ran error, keyboard/reachability.
    **Verified:** Verification gate passed. `cd frontend && npx tsc --noEmit` exit 0. `npx vitest run tests/frontend/test_prompt_config.test.ts`: 28 passed / 28 (test file reworked from 21→28). Full suite `npx vitest run`: 471 passed (11 files). Coverage matches plan: effective/source display for all nine tasks (promptSourceLabel tiers, renderEffectiveRow/renderTaskRow badges), structured/raw toggle, guarded-raw rejection of unknown keys + malformed/non-object JSON, validate call (side-effect-free), revision save + conflict (201/409/422/503 one-retry), rerun confirmation + already-ran error, keyboard/reachability (real buttons/selects/inputs, delegated actions). GAP FIXED (tests): rerun tests reworked from API.rerunScopedWalk mocks to fetch stubs (code path now uses local rerunScopedWalkChecked) — added structured-shape tests: ALREADY_RAN 409, CROSS_BOOK 409, generic 409 no-auto-retry, STALE_BASE_REVISION save parse, plain-detail + field-array readPromptError, non-JSON error, and a savePromptWrite CROSS_BOOK surfacing test asserting no '[object Object]' degradation. Test count 21→28.

### Phase 4: Deterministic browser-style journeys + build gate (S4)
- [x] Add a browser-journey test file (`frontend/tests/frontend/test_browser_journeys.test.ts`) using deterministic fake engines and fixed media fixtures (a fixture `FixtureAudio`/`MediaFixture` module under `frontend/tests/` providing stable byte content, duration metadata, and fake `generate` behavior implementing the existing TTS generate contract). Cover the user journeys: upload+preview+assign+delete clone voice; persona edit→validate→protect→explicit scoped rerun; prompt compare→structured edit→raw validate→save→confirm rerun; deterministic playback/download/form/marker/error behavior incl. seek ordering and 4xx/5xx error states. No unavailable-engine false-green: if a capability is unavailable it renders unavailable, never passed.
    **Verified:** Created frontend/tests/fixtures/media.ts (deterministic media fixture: createWavBytes via DataView 44-byte PCM WAV header + 440Hz sine, wavDurationMs header parse, fakeAudioUrl, FakeGenerateEngine implementing a minimal TTS generate contract with togglable `unavailable` → throws EngineUnavailableError) and frontend/tests/frontend/test_browser_journeys.test.ts (27 tests) covering all five DD journeys with mocked API/utils + fetch stubs + fixture WAV bytes (no real binaries, no timing assertions). Journey coverage: (1) clone voice upload(multipart+ref_text)→preview→assign(display-name→resolved-id)→delete incl. decline-abort + 5xx error; (2) persona edit→validate(side-effect-free)→protect→explicit scoped rerun confirm:true, protected-head rerun blocked pre-request, PROTECTED_REVISION structured save conflict; (3) prompt compare(all 9 walks+badges)→structured edit temp=0.0→raw validate(allow-list, unknown-key pre-network reject)→save→confirm rerun confirm:true no auto_run; (4) deterministic playback seek ordering(load/loadedmetadata before currentTime), download attachment link, upload form guards, workbench non-color selected-scene marker(aria-current+visually-hidden text), 5xx/4xx error surfacing; (5) no unavailable-engine false-green: FakeGenerateEngine unavailable → error toast surfaced, no play/success, never a silent pass; plus 4 fixture-determinism tests. Gates: npx tsc --noEmit exit 0; journeys test 27/27 passed on two consecutive runs (deterministic); full suite 498 passed (471 existing + 27 new, 12 files). No src changes required — journeys exposed no real bugs. Fixture note: no pre-existing fixtures dir existed; created one matching the DD's 'fixed media fixtures' requirement.
    **Notes:** "Playwright-style" here means journey-level Vitest/jsdom tests (no real Playwright dependency exists in the repo; the DD's "Playwright-style" = browser-visible journeys with deterministic fixtures). Media stubs already present in `vitest.setup.ts`.
- [x] Build and committed-dist: `cd frontend && npm run build` then `git diff --exit-code app/static/dist/` (new assets under `app/static/dist/`). Confirm `npm run build` is deterministic and the committed-dist gate passes.
    **Verified:** `cd frontend && npm run build` succeeds (vite 8.2.0, ~25 modules) and is deterministic — two consecutive builds both emitted `assets/index-lVnMUa2G.js` (149.56 kB) + index.html. The committed dist was STALE: HEAD held `index-DT_gpgas.js` (from ebec982, unverified); the fresh build replaced it with `index-lVnMUa2G.js` (index.html script src updated accordingly). Committed ONLY app/static/dist as one scoped commit: 1aa780d "build(frontend): rebuild dist for parity UI" (D index-DT_gpgas.js, A index-lVnMUa2G.js, M index.html). Post-commit `npm run build && git diff --exit-code app/static/dist/` exits 0 (dist gate clean). src/test changes intentionally left uncommitted for P5-S2.
    **Notes:** Commit the built `index-*.js` + `index.html` under `app/static/dist/` exactly as the combined-walks delivery did.

### Phase 5: Gates and scoped commit (S5)
- [x] Run gates: `cd frontend && npx tsc --noEmit -p tsconfig.json` (exit 0), `cd frontend && npx vitest run` (all frontend tests incl. 378 existing base + new), `cd frontend && npm run build && git diff --exit-code app/static/dist/`, `cd /repo-root && pytest -q` (backend still green) + legacy guard 12/12.
    **Verified:** All final gates passed. (1) `cd frontend && npx tsc --noEmit -p tsconfig.json` exit 0 — no type errors. (2) `cd frontend && npx vitest run`: 498 passed (498) / 12 test files, v4.1.10, 19.29s — matches expected 498 (471 existing + 27 journeys). (3) `cd frontend && npm run build` exit 0 + `git diff --exit-code app/static/dist/` exit 0 — committed dist (1aa780d) is current, no drift. (4) Backend full suite `/tmp/qa-venv/bin/pytest -q`: 1625 passed in 47.84s — green. (5) `pytest -q tests/pipeline/test_legacy_removed.py`: 12 passed / 12. (6) `git diff --exit-code app/tts.py` exit 0 — byte-identical, untouched. (7) Combined-walks workbench tests (`pytest -q tests/ -k workbench`): 77 passed, 1548 deselected — green (test_api_workbench, test_api_review_workbench, test_workbench_walks all pass). No real-engine dependency, no backend surface changes. Working tree ready for P5-S2 scoped commits.
    **Notes:** Record exact vitest counts and dist gate output. No real-engine dependency; no backend surface changes in part C.
- [x] Commit part C as scoped commits (e.g. `feat(frontend): clone reference UI`, `feat(frontend): persona editor`, `feat(frontend): prompt config editor`, `test(frontend): deterministic browser journeys + committed dist`). Note commit SHAs.
    **Verified:** Committed part C as 5 scoped commits (exact files, no git add -A, nothing outside the allowed list; app/tts.py, backend, CONTRACTS.md, app/app.py, legacy modules untouched). Commits: (1) 79e5bf9 `feat(frontend): clone reference UI panel + tests` — frontend/src/tabs/voices.ts + frontend/tests/frontend/test_clone_references.test.ts. (2) a509f94 `feat(frontend): persona editor + structured conflict handling + tests` — frontend/src/tabs/persona.ts + frontend/tests/frontend/test_persona.test.ts. (3) 8b9f663 `feat(frontend): prompt config editor + structured conflict handling + tests` — frontend/src/tabs/prompt-config.ts + frontend/tests/frontend/test_prompt_config.test.ts. (4) 20437b8 `test(frontend): browser journeys + deterministic media fixtures` — frontend/tests/frontend/test_browser_journeys.test.ts + frontend/tests/fixtures/media.ts. (5) docs commit (below) for artifacts/plans/pending/TASK-voice-persona-prompt-parity-C-frontend-browser-e2e.md. Post-commit `git status --short` clean (verified). All P5-S1 gates green: tsc exit 0, vitest 498/498, dist gate clean, pytest 1625, legacy guard 12/12, tts.py byte-identical, workbench 77.

## Completion Criteria

- Clone upload/list/inline-preview/download/delete UI enforces ownership/bounded-validated-media/limits, resolves display→resolved-ID before assignment, and surfaces errors accessibly.
- Persona editor round-trips exact structured fields/evidence/aliases/scope/protection/consequences with revisions, validation, stale-409, protected-rejection, and explicit confirmed scoped rerun (no auto-cascade).
- Every fixed walk shows effective prompt/settings + provenance; structured and guarded-raw modes; exact allow-list validation; versioned save with conflict protection; explicit confirmed rerun (already-ran handled).
- Deterministic browser journeys cover all five DD user journeys with fake engines/media; seek ordering, downloads, forms, errors; no unavailable-engine false-green.
- `tsc --noEmit` exit 0; `vitest run` green (base 378 + new); `npm run build && git diff --exit-code app/static/dist/` clean; backend suite + legacy guard green.
- No `app/tts.py` change, no legacy module import, no real-engine dependency, no walk-tab reorganization.

## References

- `artifacts/designs/pending/DD-voice-persona-prompt-parity-browser-external-validation.md`
- `artifacts/plans/pending/TASK-voice-persona-prompt-parity-A-...` / `-B-...`
- `artifacts/plans/completed/TASK-combined-walks-workbench-workbench-foundation.md` (frontend + dist conventions)
- `frontend/vitest.setup.ts`, `frontend/tests/frontend/test_workbench.test.ts`
