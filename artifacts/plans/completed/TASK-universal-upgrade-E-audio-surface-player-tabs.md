# Task: Audio Surface, Singleton Player & Tab Navigation

## Problem Statement

Plan Q removed the legacy Result tab and audio.ts; today there is no way to play rendered audio in the pipeline UI. `editor-pipeline.ts` renders spans into a static table with a fake progress bar, the `#btn-pipeline-download` button referenced in code does not exist in index.html, and no `<audio>` element exists for playback. The Universal Upgrade design (DD cap1, FP1/FP2/FP4/FP7) restores the audio surface pipeline-natively: a singleton player with an injectable `createPreviewPlayer()` factory, per-span preview in individual render mode (seeking `GET /export/chunk/{job_id}/{idx}`), whole-book playback (`GET /export/audio/{job_id}`), and sequence playback (player queue of span URLs) — with stopThenPlay semantics, benign AbortError/NotAllowedError handling, and tap-to-continue autoplay.

**Evidence-based addition:** the frontend has no working tab navigation at all. Nav links are plain `<a data-tab="...">` with no click handler and no bootstrap tab wiring; every pane except `#setup-tab` is `display:none` in the DOM. All new UI in this and later plans lives in hidden tabs, so a tab-navigation foundation is required first (small, in main.ts + index.html). The DD's vitest media-stub claim is also false (only `MockAudio` in test_voices.test.ts exists), so media stubs are added to `vitest.setup.ts` here. Batch-mode renders show a tooltip "preview differs from final — whole-book playback only" because batch mode has no per-chunk rows.

## Dependencies

- Plan B (render_job/render_chunk rows, render_status per-chunk counts, chunk/audio endpoints in Plan C) and Plan C (GET /export/chunk/{job_id}/{idx}, GET /export/audio/{job_id}) — completed and archived
- `artifacts/designs/parts/epub-audiobook-pipeline-rewrite/CONTRACTS.md` § Universal Upgrade — endpoint registration

## Phases

### Phase 1: Tab-navigation foundation (evidence-based)
- [x] TDD RED: write frontend/tests/frontend/test_main.test.ts (or extend an existing test) asserting that clicking a nav link with `data-tab="voices"` shows the voices pane and hides others, and that the active nav class moves
    **Done:** Created frontend/tests/frontend/test_main.test.ts (6 tests): builds a minimal jsdom DOM mirroring index.html nav links + {tab}-tab panes; asserts (a) clicking data-tab="voices" shows #voices-tab, (b) hides previously visible #setup-tab, (c) .nav-link.active moves to clicked link, (d) a pre-existing click listener on [data-tab="editor"] (simulating editor.ts's loadSpans/loadReviewItems hook) still fires after the global handler is registered, plus fail-silently-missing-pane and non-nav-click tests. RED confirmed: 6/6 failed (switchTab/initTabNavigation not exported from main.ts).
- [x] Implement: add a global tab-switch handler in main.ts (DOMContentLoaded) that listens on nav links `[data-tab]`, toggles `.tab-content` visibility by id (`{tab}-tab`), sets the `.nav-link.active` class, and calls the per-tab load hook (e.g. editor's existing loadSpans/loadReviewItems listener contract preserved)
    **Done:** Added export function switchTab(tabName) + export function initTabNavigation() to frontend/src/main.ts. switchTab hides all .tab-content panes, shows #${tabName}-tab (fail-silent no-op if missing), and moves .nav-link.active to the matching [data-tab] link. initTabNavigation attaches one delegated click listener on document (idempotent guard), resolving the clicked link via closest('[data-tab]'); it toggles visibility + active class ONLY — editor.ts's per-tab listener on [data-tab="editor"] is untouched (no stopPropagation/removeEventListener, listeners coexist). Registered initTabNavigation() inside the existing DOMContentLoaded callback. No index.html changes needed — nav links already carry data-tab.
- [x] TDD GREEN: run `npx tsc --noEmit` (exit 0) and `npm test` (vitest) with the new tab test; assert panes toggle correctly with jsdom
    **Done:** GREEN: npx tsc --noEmit exit 0; full vitest run green — 5 files, 173 tests passed (167 baseline + 6 new in test_main.test.ts). No existing test assertions modified. No new npm dependencies.
- [x] Verify: `npm run build` succeeds and regenerates app/static/dist; run `git diff --exit-code app/static/dist/` and commit the dist update
    **Done:** npm run build succeeded (vite 8.2.0, 20 modules, 271ms). dist regenerated: app/static/dist/index.html now references new hashed bundle assets/index-CwSKHhKw.js (70.78 kB, gzip 17.44 kB); stale index-44F8ZaNx.js removed by emptyOutDir. Bundle verified to contain the new tab-navigation code (switchTab/tab-content present). Per task direction the dist update is NOT committed — the single plan-level commit happens at P7-S6; git diff --exit-code app/static/dist/ will be verified clean after that commit. Working tree intentionally left dirty (main.ts, test_main.test.ts, dist/index.html + assets). No index.html source change needed (nav links already carry data-tab).

### Phase 2: Media stubs in vitest.setup.ts (evidence-based)
- [x] TDD RED: write a test that instantiates the player via the factory (Phase 3) under jsdom and asserts play()/pause()/currentTime/seek calls work without the real HTMLMediaElement (jsdom does not implement play())
    **Done:** Created frontend/tests/frontend/test_player.test.ts (10 tests, spec-first). Block A "vitest media stubs (jsdom HTMLMediaElement)" (5 tests, no player import): pins HTMLMediaElement.prototype.play callable+resolving on document.createElement('audio') and call-recording (vi.mocked prototype mock), pause() callable, currentTime get/set native behavior, new Audio(url) inheriting stubbed play. Block B "createPreviewPlayer() factory" (5 tests) locks the Phase 3 spec: module exports createPreviewPlayer(audio?: HTMLAudioElement) factory returning player with play(url):Promise<void>/pause()/seek(seconds)/stop(), and getPreviewPlayer() singleton accessor returning same instance across calls; injected-element tests assert play sets src + calls play, pause calls pause, seek sets currentTime. Module loaded via NON-LITERAL dynamic import (const PLAYER_MODULE: string = '../../src/player'; await import(PLAYER_MODULE)) so the file loads and tsc skips static resolution — no ambient declaration or vi.mock needed; tests fail only at runtime with module-not-found (RED). RED confirmed: all 5 factory tests fail with "Cannot find module '/src/player'"; the 5 stub tests pass immediately (no stubs were in place when the file was first written — jsdom's real play() throws, so the stub tests only turn green after P2-S2).
- [x] Implement: add media stubs to frontend/vitest.setup.ts (HTMLMediaElement.prototype.play/pause/currentTime mocks, Audio constructor stub if needed) per the DD test strategy; keep the existing localStorage fix
    **Done:** Modified frontend/vitest.setup.ts: added `import { vi } from 'vitest'` + media-stub section AFTER the untouched localStorage/sessionStorage fix. Empirical jsdom ^30.0.1 probe (node script, before writing): Audio EXISTS in jsdom (typeof function; new Audio(url) instanceof HTMLMediaElement with native src) so NO global Audio stub added — instances inherit the stubbed prototype; only play()/pause() throw "Not implemented". Installed Object.defineProperty(HTMLMediaElement.prototype, 'play', { value: vi.fn(() => Promise.resolve()), configurable: true }) and 'pause' ({ value: vi.fn() }) — descriptors verified writable+configurable so redefinition is safe. currentTime left native (verified working: get 0 / set 12.5 round-trips). Stubs installed via defineProperty, deliberately NOT vi.stubGlobal, so test_voices.test.ts's vi.stubGlobal('Audio', MockAudio) + vi.unstubAllGlobals() cycle restores OUR stubs as the captured original — coexistence verified by the full suite run (see P2-S3). Stub tests in test_player turned green after this change.
- [x] TDD GREEN: run vitest; assert player tests pass with the stubs in place and the existing 167 tests stay green
    **Done:** Verified from frontend/: (a) `npm test` (vitest ^4.1.10, jsdom) → 6 test files, 183 tests total: 178 passed / 5 failed. The 5 failures are EXACTLY the spec-first player-factory tests in test_player.test.ts, all `Error: Cannot find module '/src/player'` (expected RED — Phase 3 implements src/player.ts and turns them green; WatchOut logged: confirm dynamic-import resolution on the first P3-S3 run). (b) 5 stub-verification tests PASS (178 = 173 baseline + 5 stub tests). (c) All 173 existing tests stay green unchanged — 5/5 existing files pass, no assertions touched (test_voices MockAudio coexistence confirmed). (d) `npx tsc --noEmit` exit 0 (tsconfig includes only src/, so setup + test files are not type-checked — no ambient declarations needed; non-literal dynamic import sidesteps TS2307). No new npm dependencies; no src/player.ts created (Phase 3 owns it); no Phase 1 files touched; nothing committed — working tree intentionally dirty for the P7-S6 commit.

### Phase 3: Singleton player + injectable createPreviewPlayer factory
- [x] TDD RED: write frontend/tests/frontend/test_player.test.ts asserting createPreviewPlayer() returns a player with stop(), play(url), seek(seconds), pause(), and that the factory is injectable (can be swapped in tests); a singleton accessor returns the same instance across calls
    **Done:** Extended frontend/tests/frontend/test_player.test.ts (the Phase 2 locked spec, 10 tests) with 4 new spec assertions covering what the original 5 factory tests did not: (1) stopThenPlay ordering — play(url2) must not load url2 while the previous stop() is pending (controllable via gating the injected element's pause() to return a manual promise; asserts src unchanged + play() uncalled until the gate opens, then src=url2 + play() once); (2) benign AbortError — audio.play() rejecting with DOMException('interrupted','AbortError') leaves play() resolving (resolves.toBeUndefined, no throw, src still set); (3) benign NotAllowedError + tap-to-continue — first play() rejects with NotAllowedError, play() still resolves, then a dispatched pointerdown event triggers the one-time retry (play mock called 2x, src preserved) and a SECOND gesture does NOT replay (still 2x); (4) singleton identity — createPreviewPlayer() without an element returns the SAME instance as getPreviewPlayer() across repeated calls (pins the factory-as-singleton-constructor contract). Also tightened the locked contract: PreviewPlayer.stop(): void → stop(): Promise<void> in the test docblock + interface (matches DD 'stopThenPlay: awaits stop before play' — play() awaits an awaitable stop). No existing assertion weakened. RED confirmed before implementation: npx vitest run tests/frontend/test_player.test.ts → 9 failed (5 prior + 4 new, all 'Cannot find module /src/player') | 5 passed (stub block). Baseline full suite before extension: 178 passed / 5 failed.
- [x] Implement: create frontend/src/player.ts with the createPreviewPlayer factory (returns a singleton-backed player wrapping a single shared `<audio>` element), stopThenPlay semantics (await stop before play; AbortError/NotAllowedError treated as benign; tap-to-continue autoplay resume), and a module-level singleton accessor
    **Done:** Created frontend/src/player.ts (NEW, ~180 lines). Exports: `interface PreviewPlayer { play(url: string): Promise<void>; pause(): void; seek(seconds: number): void; stop(): Promise<void> }`, `createPreviewPlayer(audio?: HTMLAudioElement): PreviewPlayer`, `getPreviewPlayer(): PreviewPlayer`. Design: (1) Factory = singleton constructor when `audio` omitted — lazily creates ONE shared <audio> element via document.createElement('audio'), NOT attached to the DOM (no document.body requirement; browsers play unattached audio); caches the instance in module-level `let singletonPlayer`; with an element argument it returns a fresh player wrapping THAT element (dependency injection for Phase 4's editor-pipeline wiring + tests). (2) getPreviewPlayer() = createPreviewPlayer() — the module-level singleton accessor, same instance across calls. (3) createPlayer(audio) closure: `play(url)` = stopThenPlay — awaits `stop()` before loading src and calling play(); `stop()` awaits `audio.pause()` (synchronous in browsers; awaitable so tests can gate the teardown by overriding the element's pause()), drops tap-to-continue state, removes the src attribute; `pause()` → audio.pause(); `seek(seconds)` → audio.currentTime. (4) Benign errors in playInternal(): AbortError swallowed; NotAllowedError arms one-time tap-to-continue — document-level pointerdown+keydown listeners that remove themselves, then re-invoke playInternal(url) inside the gesture; other errors rethrow. (5) Error detection via name-string check (errorName helper) rather than instanceof — robust to cross-realm DOMExceptions. (6) Phase 6 readiness: play path centralized in playInternal() and element held in closure so the sequence queue (auto-advance on 'ended', stop clears queue) can hook in without restructuring — queue NOT implemented (Phase 6 scope). No new npm dependencies; no other files touched.
- [x] TDD GREEN: run player tests with fake timers and media stubs; assert stop-then-play ordering, benign error swallowing, and singleton identity
    **Done:** TDD GREEN achieved. npx vitest run tests/frontend/test_player.test.ts → 14/14 passed (5 stub-verification + 9 factory/spec tests, incl. the 4 new spec assertions from P3-S1). The Phase 2 WatchOut did not materialize: the non-literal dynamic import (const PLAYER_MODULE: string = '../../src/player') resolved cleanly against the real src/player.ts on the first run — no static-import switch needed. Full suite: `npm test` → 6 test files, 187 tests passed / 0 failed (167 baseline + 6 tab-nav + 5 media-stub + 9 player factory/spec). Fake timers were NOT required: stopThenPlay gating used a manual promise on the injected element's pause() (controllable stop), and benign-error/tap-to-continue tests used mockRejectedValueOnce + mockReset on the prototype play mock + a real microtask flush (await Promise.resolve()) — the retry play() call is synchronous within dispatchEvent. Assertions verified green: stop-then-play ordering (url2 not loaded until stop resolves), benign AbortError swallow, benign NotAllowedError + one-time tap-to-continue retry (2nd gesture does not replay), singleton identity (getPreviewPlayer() === createPreviewPlayer() no-arg). All 173 pre-existing tests stay green untouched.
- [x] Verify: `npx tsc --noEmit` exit 0; vitest green; `npm run build` + dist commit
    **Done:** Verification complete. (a) `npx tsc --noEmit` exit 0 (tsconfig includes only src/ — src/player.ts type-checks clean under strict; test files transpiled by vitest, not type-checked). (b) Full `npm test` → 6 test files, 187 tests passed / 0 failed (167 baseline + 6 tab-nav + 5 media-stub + 9 player factory/spec; the 5 RED factory tests from Phase 2 are now green, plus 4 new spec tests added in P3-S1). (c) `npm run build` → vite 8.2.0, 20 modules, 242ms, success; regenerated app/static/dist/index.html + assets/index-CwSKHhKw.js — byte-identical hash to Phase 1's output. NOTE (as predicted): dist does NOT change in this phase because src/player.ts is not yet imported by main.ts — git status on app/static/dist/ shows only the Phase 1 delta (stale index-44F8ZaNx.js deleted, index-CwSKHhKw.js added); the dist will update when Phase 4 wires the player into editor-pipeline.ts. (d) Nothing committed — working tree intentionally dirty for the P7-S6 plan-level commit. Scope check: only frontend/src/player.ts (NEW) and frontend/tests/frontend/test_player.test.ts (extended, never weakened) touched; no Phase 1/2 files, no backend, no index.html, no new npm dependencies, no sequence queue (Phase 6).

### Phase 4: Per-span preview in editor-pipeline.ts
- [x] TDD RED: write/extend frontend/tests/frontend/test_editor.test.ts asserting each span row shows a ▶ preview affordance that resolves `GET /export/chunk/{job_id}/{idx}` and plays via the player; in batch mode (job.mode=batch) the ▶ is disabled or shows the tooltip "preview differs from final — whole-book playback only"
    **Note:** TDD RED: extended tests/frontend/test_editor.test.ts with describe 'Editor Tab — Per-Span Audio Preview (Audio Surface)' — 8 new tests: (a) renderSpanRow emits ▶ button (btn-span-preview) with data-job-id + data-chunk-idx (0-based = global_index−1) + fa-play; disabled when no job known; (b) handlePreviewSpan(2) resolves GET /api/pipeline/render_status/job-preview-1 + GET /api/pipeline/export/jobs/job-preview-1/chunks then plays /api/pipeline/export/chunk/job-preview-1/1 via injected player; chunk-list-unavailable fallback; (c) batch mode (render_status mode='batch') → showToast('Preview differs from final — whole-book playback only','warning'), play NOT called, chunk list NOT fetched; no-job warning; plus a delegated click test (initEditor + DOMContentLoaded dispatch, btn.click() → play called, via vi.waitFor). RED confirmed: 8 failed (setPreviewPlayer is not a function), 57 existing passed. Player injection uses setPreviewPlayer (plan-endorsed injectable pattern) rather than vi.mock; mock player {play,pause,seek,stop} re-injected per test, real singleton restored in afterEach. No existing assertions touched.
- [x] Implement: extend renderSpanRow in editor-pipeline.ts with a preview button wired to the singleton player; resolve chunk idx per span (span index ↔ chunk idx mapping per DD open item #4 — use the render_chunk row list from GET /export/jobs/{job_id}/chunks when available); disable per-span preview for batch jobs with the tooltip
    **Note:** CHUNK-IDX CONVENTION VERIFIED FROM BACKEND CODE (documented): app/pipeline/tts_integration.py render_audiobook individual mode L626 'for i, entry in enumerate(script)' — i is 0-BASED index into script; L632 final_path = chunk_{i:04d}.wav; L634 _insert_chunk_row(storage, resolved_job_id, i, final_path) stores render_chunk idx=i. script = export_annotated_script(book_id, storage) — the SAME array GET /api/pipeline/export/{book_id} serves (api_export.py L146 export_script → export_annotated_script). Therefore ONE chunk per span in presentation order, chunk idx = span.global_index − 1 (0-based). L619-623: batch mode writes NO per-chunk rows. API shapes confirmed: GET /api/pipeline/export/jobs/{job_id}/chunks (api_export.py L355-376) returns list of {job_id, idx, status, wav_path, error} ordered by idx (empty for batch); GET /api/pipeline/render_status/{job_id} (L273-319) returns mode field ('individual'|'batch') + per-chunk counts. IMPLEMENTED in frontend/src/tabs/editor-pipeline.ts: (1) imports { getPreviewPlayer } + type PreviewPlayer from ../player; (2) new ChunkRow + RenderStatus interfaces (RenderStatus widens pipelineRenderStatus return with mode?: 'individual'|'batch' + total/completed/failed_chunks — additive, existing TTS Rendering tests with mode-less mocks unaffected); (3) module state: BATCH_PREVIEW_TOOLTIP const (exact DD string 'Preview differs from final — whole-book playback only'), let _previewPlayer: PreviewPlayer = getPreviewPlayer(), exported setPreviewPlayer(player) for test injection; (4) renderSpanRow: leading cell gains btn-outline-success btn-span-preview with data-index, data-job-id (state.pipelineRenderJobId ?? _currentRenderJobId), data-chunk-idx (global_index−1), fa-play icon; disabled + title 'No render job to preview' when no job; (5) new exported handlePreviewSpan(index): resolves job (state.pipelineRenderJobId ?? _currentRenderJobId, warns if none), fetches render_status for mode (batch → showToast(BATCH_PREVIEW_TOOLTIP,'warning') and return, no chunk fetch), individual → fetches chunk row list, uses row.idx when found else falls back to presentation-order index−1, then await _previewPlayer.play(`/api/pipeline/export/chunk/${jobId}/${chunkIdx}`). frontend/src/tabs/editor.ts: re-export block for handlePreviewSpan + setPreviewPlayer, and btn-span-preview branch in the existing spansTableBody delegated click handler (consistent with btn-span-*). No HTML change needed (button lives in existing leading cell). src/player.ts NOT modified.
- [x] TDD GREEN: run test_editor tests with fully mocked fetch (render_status, chunk URLs) and fake timers; assert preview click plays the right URL and batch-mode disables it
    **Note:** TDD GREEN: full vitest run — 6 test files, 195/195 passed (baseline 187 + 8 new preview tests). test_editor.test.ts alone: 65/65 (57 existing assertions unchanged, never weakened). New tests use fully mocked API (vi.mocked(API.get) with mockResolvedValueOnce chains for render_status + chunk rows; mockReset in beforeEach clears once-queues so no cross-test leakage), injected mock player via setPreviewPlayer (asserts play() called with exact chunk URL; play NOT called for batch/no-job), and the delegated-click test uses vi.waitFor to flush the async handler. Fake timers not needed for preview path (no polling involved); existing TTS Rendering tests keep their vi.useFakeTimers + advanceTimersByTimeAsync(2000) pattern untouched.
- [x] Verify: `npx tsc --noEmit` exit 0; vitest green; `npm run build` + dist commit
    **Note:** Verify: `npx tsc --noEmit` exit 0 (clean type-check of src/). Full `npm test` green: 6 files, 195/195. `npm run build` (vite 8.2.0) succeeded in 236ms: 21 modules transformed (up from 19 — player.ts + preview wiring now bundled), output ../app/static/dist/assets/index-nKJV80kk.js (72.57 kB, gzip 17.99 kB). Dist DID change as expected: old Phase-1-3 bundle index-CwSKHhKw.js replaced, new bundle grep-verified to contain the batch tooltip string and btn-span-preview wiring. Git status confirms only in-scope files changed: M frontend/src/tabs/editor-pipeline.ts, M frontend/src/tabs/editor.ts, M frontend/tests/frontend/test_editor.test.ts, plus dist artifact (?? app/static/dist/assets/index-nKJV80kk.js, D app/static/dist/assets/index-44F8ZaNx.js). frontend/dist cleaned by vite outDir. NOT committed — tree intentionally left dirty for the P7 commit. src/player.ts, main.ts, vitest.setup.ts, backend, index.html untouched.

### Phase 5: Whole-book playback
- [x] TDD RED: write a test asserting the result surface (to be built in Plan F; here the player wiring) plays the whole book via `GET /export/audio/{job_id}` with correct URL and singleton seek support
    **Note:** TDD RED: extended tests/frontend/test_editor.test.ts with describe 'Editor Tab — Whole-Book Playback (Audio Surface)' — 6 new tests: (a) index.html declares the play-book affordance (fs read via resolve(process.cwd(),'index.html') — import.meta.url is not file:// under vitest jsdom) asserting id="btn-pipeline-play-book", btn-outline-info, fa-play, title "Play whole book", and that it sits in the editor-tab card header next to #pipeline-render-job; (b) playPipelineAudiobook() resolves GET /api/pipeline/export/audio/{job_id} and calls injected player's play() with it (state.pipelineRenderJobId path); explicit jobId arg takes precedence; (c) singleton seek support: after play, mockPlayer.seek(120) asserts seek call with 120 and same player instance serves play+seek (real impl delegates to audio.currentTime); (d) no-job guard: state.pipelineRenderJobId=null → showToast('No render job to play. Render the audiobook first.','warning'), play NOT called; (e) click wiring: initEditor + DOMContentLoaded, #btn-pipeline-play-book click → play called (vi.waitFor). Follows Phase 4 pattern: mock player {play,pause,seek,stop} injected via setPreviewPlayer per test, afterEach restores getPreviewPlayer() singleton; vi.mocked(API.get).mockReset() in beforeEach; state.pipelineRenderJobId set/cleared explicitly. RED confirmed: 6 failed | 65 passed (71 total) — failures all 'playPipelineAudiobook is not a function' / toContain miss / click not wired. No existing assertions touched.
- [x] Implement: add a "Play book" affordance in editor-pipeline.ts (near the render job badge) that loads `GET /export/audio/{job_id}` into the singleton player; store the URL in state.pipelineRenderJobId flow
    **Note:** Implemented whole-book playback wiring. (1) frontend/src/tabs/editor-pipeline.ts: new exported async playPipelineAudiobook(jobId?: string): Promise<void> placed after mergePipelineAudiobook — resolves id = jobId ?? state.pipelineRenderJobId ?? _currentRenderJobId (identical resolution to downloadPipelineRender/mergePipelineAudiobook, per task spec); no id → showToast('No render job to play. Render the audiobook first.','warning') and return; else await _previewPlayer.play(`/api/pipeline/export/audio/${id}`) via the Phase 4 injectable singleton (_previewPlayer/setPreviewPlayer). Header docstring endpoint list extended with '- GET /api/pipeline/export/audio/{job_id} (whole-book playback)'. No new state stored — reuses state.pipelineRenderJobId flow exactly as specified. (2) frontend/index.html: added <button class="btn btn-outline-info btn-sm" id="btn-pipeline-play-book" title="Play whole book"><i class="fas fa-play me-1"></i>Play Book</button> in the editor tab card header (L987), immediately after #btn-pipeline-merge-audiobook and before the #pipeline-render-job badge. (3) frontend/src/tabs/editor.ts: playPipelineAudiobook added to the editor-pipeline import list (grouped under 'Whole-book playback (audio surface)'), added to a new re-export block '// Re-export whole-book playback (audio surface)', and wired in initEditor() DOMContentLoaded: btnPipelinePlayBook click → playPipelineAudiobook() (guard if element missing), placed after the Merge Audiobook wiring. src/player.ts NOT modified. No Plan F result-surface UI, no Phase 6 sequence playback.
- [x] TDD GREEN: run the playback tests; assert the audio element src is the /export/audio URL and play() is called
    **Note:** TDD GREEN: npx vitest run tests/frontend/test_editor.test.ts → 71/71 passed (65 prior + 6 new whole-book playback tests). Play asserted with exact URL /api/pipeline/export/audio/job-play-1 (state-resolved) and /api/pipeline/export/audio/job-explicit (explicit arg); seek(120) recorded on injected spy player with same-instance play+seek; no-job guard toast + no play verified; click-wiring test (initEditor + DOMContentLoaded dispatch + btn.click + vi.waitFor) passes. One mid-run fix: the index.html fs-read test originally used new URL('../../index.html', import.meta.url) which throws 'The URL must be of scheme file' under vitest jsdom — switched to resolve(process.cwd(), 'index.html') (vitest cwd = frontend/). All 65 existing assertions unchanged, never weakened. Fake timers not needed (no polling in play path).
- [x] Verify: `npx tsc --noEmit` exit 0; vitest green
    **Note:** Verify: npx tsc --noEmit exit 0 (clean type-check of src/, includes new playPipelineAudiobook). Full npm test → 6 files, 201/201 passed (195 baseline + 6 new whole-book playback tests). Optional sanity npm run build succeeded (vite 8.2.0, 219ms): bundle index-CMj-NkGl.js (72.85 kB, gzip 18.02 kB) replaces Phase 4's index-nKJV80kk.js, grep-confirmed to contain playPipelineAudiobook/export/audio/no-job-toast strings. dist NOT committed (per task: P7-S6 plan-level commit; tree intentionally dirty). Scope check: only frontend/src/tabs/editor-pipeline.ts, frontend/src/tabs/editor.ts, frontend/index.html, frontend/tests/frontend/test_editor.test.ts (+ dist artifact) touched this phase; main.ts/test_main.test.ts/vitest.setup.ts/test_player.test.ts/src/player.ts/backend untouched; no new npm dependencies; no commits.

### Phase 6: Sequence playback (player queue)
- [x] TDD RED: write tests asserting sequence playback queues a list of span chunk URLs and plays them in order (presentation order of loaded spans), advancing on 'ended' events, and stopping cleanly on stop()
    **Done:** TDD RED. Extended test_player.test.ts: (a) new describe 'playSequence() queue (spec-first — Phase 6)' with 5 tests — ordered playback (3 URLs play in order, src/play-mock asserted after each dispatched 'ended' event, no advancement after last), stop() during sequence clears queue + tears down (src attribute removed, late 'ended' does not advance, play called once), playSequence([]) resolves cleanly without calling play() or setting src, stopThenPlay-for-sequences (gated pause: nothing loads until stop resolves), and playSequence exposed on the shared singleton. Also hoisted loadPlayerModule() to module scope (was nested in the Phase 3 describe) so both describes share it; extended the PreviewPlayer interface + docblock SPEC with playSequence(urls: string[]): Promise<void>. (b/c/d) covered. Extended test_editor.test.ts: new describe 'Editor Tab — Sequence Playback (Audio Surface)' with 7 tests — playSpanSequence queues all cached spans in presentation order (render_status individual + chunks list fetched, playSequence called with 3 chunk URLs), selection-filtered sequence sorted by global_index, chunks-list-unavailable fallback to presentation-order idx, batch-mode tooltip (BATCH_PREVIEW_TOOLTIP toast, no playSequence, no chunks fetch), no-job warning toast, index.html #btn-pipeline-play-sequence near #pipeline-render-job, click wiring via initEditor+DOMContentLoaded. RED confirmed: 12 failed (5 player: 'player.playSequence is not a function'; 7 editor: 'playSpanSequence is not a function' + index.html toContain miss + waitFor timeout) | 85 passed in these 2 files (all prior assertions untouched).
- [x] Implement: add sequence playback to the player module (queue + auto-advance on ended + stop clears the queue); wire an entry point in editor-pipeline.ts for the selected spans
    **Done:** Implemented sequence playback. (1) frontend/src/player.ts: PreviewPlayer gains `playSequence(urls: string[]): Promise<void>` (additive — play/pause/seek/stop untouched). createPlayer() adds Phase 6 state: `sequenceActive: boolean` + `sequenceQueue: string[]`; playSequence() = stopThenPlay (`await stop()` first — cancels any in-flight sequence or single playback), empty list returns immediately (no-op), else copies the list, sets sequenceActive, registers an 'ended' listener on the wrapped element, and drives the FIRST url through the existing centralized playInternal() (resolves when it starts). onSequenceEnded() auto-advances: shift next url → playInternal(next); last-url end removes the listener and ends the sequence; a hard (non-benign) advance failure aborts the sequence (logs, clears, removes listener — no unhandled rejection). stop() now clears the queue + removes the listener BEFORE awaiting pause, so no advancement can happen after stop and a late 'ended' no-ops. Single play()/stop() cancel an active sequence via the same stop() path. API shape: `playSequence(urls: string[]): Promise<void>`. (2) frontend/src/tabs/editor-pipeline.ts: new exported `playSpanSequence(jobId?: string): Promise<void>` — resolves id (jobId ?? state.pipelineRenderJobId ?? _currentRenderJobId, same as playPipelineAudiobook; no id → toast 'No render job to play. Render the audiobook first.'), fetches render_status for mode (batch → showToast(BATCH_PREVIEW_TOOLTIP,'warning') and return — sequence requires individual mode), builds span set per v1 semantics: `_selectedIndices.size > 0 ? _cachedSpans.filter(selected) : _cachedSpans`, sorted by global_index (presentation order — selection is a filter, never a reorder; review-filtered spans NOT excluded: _cachedSpans is the full export, review items never remove spans — documented in the docstring per DD open item #5), empty span set → toast 'No spans to play', then fetches GET /api/pipeline/export/jobs/{job_id}/chunks (fallback to presentation-order idx = global_index−1 on failure, mirroring handlePreviewSpan) and calls `_previewPlayer.playSequence(urls)` with /api/pipeline/export/chunk/{id}/{idx} URLs. (3) frontend/index.html: `#btn-pipeline-play-sequence` (btn-outline-primary, fa-list-ul, title 'Play spans in sequence (selected, or all)') in the editor card header after #btn-pipeline-play-book, before the #pipeline-render-job badge. (4) frontend/src/tabs/editor.ts: import + re-export block 'Re-export sequence playback (audio surface)' + initEditor wiring (click → playSpanSequence(), guard if element missing), placed after the Play Book wiring. No Plan F result-surface UI; no new npm dependencies; no backend touched.
- [x] TDD GREEN: run sequence tests with fake timers; assert ordered playback and clean stop
    **Done:** TDD GREEN. npx vitest run on the two touched files: 97/97 (85 prior + 12 new sequence tests). Full `npm test`: 6 files, 213/213 passed (201 baseline + 5 player queue tests + 7 editor entry-point tests; all 201 prior assertions untouched, never weakened). `npx tsc --noEmit` exit 0 (clean type-check of src/ incl. playSequence on PreviewPlayer + playSpanSequence). Sequence tests verified green: ordered playback (src + play-mock call order asserted after each dispatched 'ended', no advancement past last), clean stop (queue cleared, src attribute removed, late 'ended' does not advance, play called exactly once), empty-list no-op (resolves, play never called), stopThenPlay-for-sequences (gated pause — nothing loads until stop resolves), singleton exposes playSequence; entry point tests: all-spans presentation order, selection-filtered sorted by global_index, chunk-list fallback, batch tooltip guard, no-job toast, index.html button presence, click wiring. No existing test assertion modified; no fake timers needed (real timers + dispatched 'ended' events, per task guidance).
- [x] Verify: `npx tsc --noEmit` exit 0; vitest green; `npm run build` + dist commit
    **Done:** Verified. (1) `npx tsc --noEmit` exit 0 (re-checked after build — clean). (2) Full `npm test` 213/213 green (6 files) — re-run after build to confirm nothing regressed. (3) `npm run build` from frontend/ succeeded (vite v8.2.0, 21 modules, 226ms) regenerating app/static/dist: new bundle app/static/dist/assets/index-CvzQuOJH.js (74.10 kB gzip 18.27 kB) + app/static/dist/index.html. Sequence code confirmed IN the bundle: `async playSequence(t){...}` (player queue impl) and the entry-point body (`/api/pipeline/export/chunk/${n}/${t}` URL mapping + `Kt.playSequence(s)` call) present; the exported name playSpanSequence was minified by esbuild (it is re-exported only for tests, no runtime consumer outside the bundle — expected). dist/index.html contains #btn-pipeline-play-sequence with title 'Play spans in sequence (selected, or all)'. Old asset index-44F8ZaNx.js deleted, new index-CvzQuOJH.js added (emptyOutDir:true). NOT committed — working tree stays dirty for the P7 final commit (task constraint honored; dist/index.html + bundle show as modified/untracked alongside Phases 1-5 changes). No Plan F UI implemented; no backend touched; no new npm dependencies.

### Phase 7: Regression gate, guard suite and verification
- [x] Run `npm test` (vitest) and record the pass count (167 baseline + new); run `npx tsc --noEmit` (exit 0)
    **Done:** REGRESSION GATE: `npm test` (vitest v4.1.10, jsdom) from frontend/ → 6 test files, 213/213 passed, 0 failed (167 baseline + 46 new across Phases 1-6: 6 tab-nav + 5 media-stub + 9 player factory/spec + 8 per-span preview + 6 whole-book + 12 sequence = 46). `npx tsc --noEmit` → exit 0 (clean type-check of src/, incl. src/player.ts + tab modules). No existing test assertions modified; no new npm dependencies; nothing committed.
- [x] Run `npm run build` and verify `git diff --exit-code app/static/dist/` is clean after committing dist
    **Done:** BUILD + DIST GATE: `npm run build` (vite 8.2.0) from frontend/ → exit 0, 21 modules, 224ms; regenerated app/static/dist: assets/index-CvzQuOJH.js (74.10 kB, gzip 18.27 kB) + index.html (now references index-CvzQuOJH.js). `git status --short app/static/dist/` shows EXACTLY the expected set and nothing else: D assets/index-44F8ZaNx.js (stale pre-plan bundle), M index.html (rebuilt), ?? assets/index-CvzQuOJH.js (new hashed bundle). Bundle grep-verified to contain: tab-navigation code (tab-content), player singleton impl (AbortError, NotAllowedError, pointerdown tap-to-continue — esbuild renames the getPreviewPlayer export, so presence is verified via these distinctive impl strings), preview wiring (btn-span-preview x2, /api/pipeline/export/chunk x2, 'differs from final' tooltip), whole-book wiring (/api/pipeline/export/audio, 'No render job to play' toast), sequence wiring (playSequence x2, /api/pipeline/export/jobs x2). dist/index.html carries the static button labels: #btn-pipeline-play-book title 'Play whole book' + #btn-pipeline-play-sequence title 'Play spans in sequence' (static HTML, not in JS bundle). NOT committed per task — P7-S6 owns the commit; `git diff --exit-code app/static/dist/` verified clean after that commit per the CONTRACTS Rule #13 gate.
- [x] Run `pytest tests/pipeline/test_legacy_removed.py -q` and verify 12/12 still green (frontend-only plan; guard must not regress via any HTML/JS references to legacy audio.ts / editor-legacy.ts)
    **Done:** GUARD SUITE: `python -m pytest tests/pipeline/test_legacy_removed.py -q` from repo root → 12 passed in 1.65s (12/12). No guard regression from the frontend audio-surface work: legacy-file-absent (audio.ts / editor-legacy.ts), legacy-route absence (live route scan of app.app incl. family prefix check), state.ts pipelineEnabled absence, and project_manager-identifier checks all green. No frontend fix required — the guard's HTML/JS legacy-reference scan finds nothing introduced by Phases 1-6.
- [x] Security review: chunk/audio URLs are server-constructed paths (no user-supplied paths in the frontend); no new auth surface; audio element srcs come from the API only; document findings
    **Done:** SECURITY REVIEW (documentation pass) — audio-surface artifact-access safety, 4 checks. (1) SERVER-CONSTRUCTED PATHS: every audio URL is a template literal into the documented Plan C endpoints — `/api/pipeline/export/chunk/${jobId}/${chunkIdx}` (editor-pipeline.ts handlePreviewSpan L405, playSpanSequence L965) and `/api/pipeline/export/audio/${id}` (playPipelineAudiobook L889). The only interpolated values: jobId = state.pipelineRenderJobId ?? _currentRenderJobId, where _currentRenderJobId = result.job_id from the render POST response (L738, server-assigned UUID) and state.pipelineRenderJobId is set from that same flow (L756); chunkIdx is a NUMBER derived from span presentation position (index-1 / global_index-1), confirmed against the server-provided chunk row list (GET /api/pipeline/export/jobs/{job_id}/chunks) when available. No user-supplied file path reaches any frontend audio code path. (2) NO NEW AUTH SURFACE: this plan registers no endpoints, adds no middleware and no credential handling — playback reuses the existing Plan C GET /export/chunk + GET /export/audio handlers on the existing pipeline router; the frontend delta is only play()/playSequence() calls on the singleton player. (3) AUDIO SRC ORIGIN: player.ts playInternal L178 `audio.src = url` is the sole src assignment in the audio surface; url always originates from the endpoint literals above — never from user input or arbitrary paths (pre-existing audio.play() call sites in voices.ts/training.ts/designer.ts are other tabs, untouched by this plan). (4) BACKEND CONTAINMENT CONFIRMED (read-only; not edited): export_chunk (api_export.py L384-482) is rows=truth — the render_chunk row supplies status + wav_path, the request path never names a file; non-int/out-of-range idx → 404, pending/failed → 409, evicted → 410; wav_path resolved against the run dir (row output_dir, or derived RENDER_ROOT/book-{id}/{job_id}/ when NULL) and realpath-containment-checked (escape → 404); BoundedRangeFileResponse caps any single request at PIPELINE_MAX_RANGE_BYTES. export_audio (L913+) — the render_job row decides: expired → 410, non-completed → 404, artifact paths pass _resolve_within_run_dir containment, individual-mode chunks from rows, batch-mode from sorted *.wav in the run dir. The frontend cannot bypass this: endpoint signatures accept only job_id + idx and all path resolution is server-side. RESIDUAL RISK (accepted): job_id is not validated client-side — acceptable, the backend owns authorization (unknown job_id → 404); chunk idx is bounded by the backend's 404/409/410 + 206/416 range handling per Plan C; /export/* is unauthenticated like the rest of the local pipeline UI (pre-existing Plan C contract, not widened here). CONCLUSION: no artifact-access, path-traversal, or auth findings attributable to the Plan E frontend audio surface.
- [x] Code review pass via exec-manager QA-Reviewer; fix MINOR findings
    **Done:** QA GATE (exec-manager): QA-Reviewer 3 rounds. R1: 11 MINOR (3 code-quality, 3 test-gap, 2 test-flakiness, 3 doc) → fixer round 1 (all fixed, +4 gap tests). R2: 6 MINOR (5 doc incl. tooltip case drift 'Preview differs...'→lowercase byte-exact vs DD L113, stale vitest note, 'Two blocks:'→'Three blocks:', stale Phase-6 comment tense, missing PUT /span/{id}/text header row; 1 code-quality unguarded tap-to-continue retry) → fixer round 2 (all fixed). R3: 2 trivial MINOR (delegated tab handler missing e.preventDefault() — R2 a11y href="#" made nav links real anchors, jsdom cannot observe anchor nav so tests spy the dispatched event; setPreviewPlayer docstring under-scoping its 3 consumers) → fixer round 3 (all fixed, +1 preventDefault regression test). Final verified state: tsc exit 0, vitest 218/218 (167 baseline + 51 new across 6 files), guard 12/12, build reproducible (dist bundle index-Dx_qAJuB.js sha256 b0ee5741...), tooltip byte-exact vs DD, contracts/layer-compliance/completeness all PASS per R3. QA-Reviewer R3 certified all checks PASS with only the 2 inline fixes (fixer-confirmed) — no re-review needed. QA gate CLOSED.
- [x] Commit: `feat(frontend): pipeline audio surface with singleton player and tab navigation`
    **Done:** COMMIT: 5082b5f on main — `feat(frontend): pipeline audio surface with singleton player and tab navigation` (exact message, empty body, no trailers). Staged exactly 11 implementation files, nothing else: R051 app/static/dist/assets/index-44F8ZaNx.js -> index-Dx_qAJuB.js (git rename-detection of stale-bundle-deleted + new-bundle-added; the actual tree delta is D index-44F8ZaNx.js + ?? index-Dx_qAJuB.js), M app/static/dist/index.html, M frontend/index.html, M frontend/src/main.ts, A frontend/src/player.ts, M frontend/src/tabs/editor-pipeline.ts, M frontend/src/tabs/editor.ts, M frontend/tests/frontend/test_editor.test.ts, A frontend/tests/frontend/test_main.test.ts, A frontend/tests/frontend/test_player.test.ts, M frontend/vitest.setup.ts — 1881 insertions / 36 deletions. Plan file (artifacts/plans/pending/...) deliberately NOT staged (exec-manager contract-stamp + archive commits own it). DIST GATE (CONTRACTS Rule #13) VERIFIED FOR REAL: re-ran `npm run build` from frontend/ (vite 8.2.0, 21 modules, 224ms) AFTER committing — regenerated dist/index.html + assets/index-Dx_qAJuB.js (74.70 kB, gzip 18.37 kB), then `git diff --exit-code app/static/dist/` exited 0 with zero output: committed dist is byte-identical to a fresh build, so CI `npm run build && git diff --exit-code app/static/dist/` passes. Working tree now contains only the plan file modification (unstaged, pending exec-manager stamp).

## Completion Criteria

- Tab navigation works: clicking any nav link shows its pane; active class moves; per-tab load hooks preserved
- Media stubs present in vitest.setup.ts; player tests green under jsdom
- createPreviewPlayer() factory + singleton player implemented with stopThenPlay, benign error handling, tap-to-continue
- Per-span ▶ preview resolves /export/chunk/{job_id}/{idx} (individual mode); batch mode disabled with the "preview differs from final" tooltip
- Whole-book playback via /export/audio/{job_id}; sequence playback queue advances on ended
- tsc exit 0, vitest green (167 + new), build + dist committed, guard 12/12

## References

- `artifacts/designs/pending/DD-universal-upgrade.md` — P4 C1 phase, cap1, FP1/FP2/FP4/FP7, UX workflow, decision #2 (batch tooltip), open item #4 (span↔chunk offset)
- `artifacts/designs/parts/universal-upgrade/CONTRACTS.md` — createPreviewPlayer, per-span preview, whole-book playback
- `artifacts/designs/parts/universal-upgrade/README.md` — Plan E row + evidence-based adjustments #1/#2
- Prior: `TASK-universal-upgrade-C-artifacts-gc-export-backend.md` (completed)
