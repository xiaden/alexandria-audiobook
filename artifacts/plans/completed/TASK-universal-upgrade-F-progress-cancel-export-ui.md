# Task: Progress, Cancel & Export UI

## Problem Statement

The pipeline frontend shows a static progress bar (`#full-progress-bar` set to 100% with "{total} spans loaded" — editor-pipeline.ts:233-239), polls render status without per-chunk counts, and has no working cancel affordance in the result flow. The `#btn-pipeline-download` button referenced by editor.ts:147 and editor-pipeline.ts:653/730 is absent from index.html — the download/export surface is unreachable. There is no export UI at all: no metadata form, no cover upload, no chapter markers, no MP3/Audacity options. The Script tab shows static per-walk status badges with no run history.

This plan (DD cap3, FP6+FP2) builds the real progress/cancel/export UI on top of the Plan B/C backend: per-chunk progress (completed/total/failures) for individual renders, job-level for batch; Cancel buttons wired to `cancel_walks`/`cancel_render` with 503+Retry-After retry-once; the result surface playing the whole book; the Export M4B form (title/author/narrator/year/description + cover upload + auto chapter markers); MP3 + Audacity ZIP_STORED buttons where supported (feature-detect libmp3lame, degrade to M4B-only with clear messaging); the walk-runs list in the Script tab; and the README doc-drift correction (progress-UI overstatement). It also restores a reachable download button.

## Dependencies

- Plan C (export/chunk, export/audio, export/m4b endpoints, range serving) and Plan E (singleton player, tab navigation, media stubs) — completed and archived
- `artifacts/designs/parts/epub-audiobook-pipeline-rewrite/CONTRACTS.md` § Universal Upgrade — endpoint registration

## Phases

### Phase 1: Real per-chunk progress + result surface
- [x] TDD RED: write/extend frontend/tests/frontend/test_editor.test.ts asserting the render poll consumes render_status per-chunk counts (completed_chunks/total_chunks/failed_chunks) and renders a real progress bar (width = completed/total, failures shown as a count/error badge) for individual mode; job-level progress for batch mode
    **RED:** P1-S1 RED: added describe 'Editor Tab — Render Progress + Result Surface (Plan F)' to test_editor.test.ts — 8 tests: (1) individual-mode per-chunk width/label from render_status counts, (2) failure count/error badge, (3) badge hidden when no failures, (4) batch-mode indeterminate animated bar then 100%, (5) index.html restores reachable #btn-pipeline-download + #render-failures-badge inside editor tab, (6) download button click wiring → anchor GET /api/pipeline/download/{job_id} (spy on HTMLAnchorElement.prototype.click, NOT HTMLElement — buttons share that prototype), (7) render completion exposes download button (data-job-id) + Play Book affordance, (8) loadSpans keeps bar neutral 'Ready' 0% (no static 100% 'N spans loaded'). Verified RED: 7 new tests fail for the right reasons (bar stays 0%, no badge element in index.html), all 81 pre-existing tests in the file pass. Isolation: afterEach adds vi.mocked(API.get/post).mockReset() (clearAllMocks keeps implementations — leaked Once chains from failing tests broke 8 later tests until fixed).
- [x] Implement: replace the static bar logic in editor-pipeline.ts (loadSpans + pipelineRenderAll poll) with per-chunk progress rendering; poll GET /render_status/{job_id} on the existing 2000ms timer (_renderPollTimer), stop when terminal, surface failures per chunk
    **GREEN:** P1-S2: editor-pipeline.ts — replaced static bar logic. (a) loadSpans: removed `progressBar.style.width='100%'` + `${total} spans loaded`; now calls resetRenderProgress() (0%, neutral 'Ready', badge hidden) — bar owned by render progress. (b) New internal helpers resetRenderProgress() + updateRenderProgress(status: RenderStatus) placed before pipelineRenderAll. updateRenderProgress: individual mode (mode==='individual' && total_chunks>0) → width=Math.round(completed/total*100)+'%' (clamped ≤100), label `${completed}/${total} chunks`, failures badge `bg-danger` shows `${failed} chunk(s) failed` (innerText, count + job error as title) when failed_chunks>0 else hidden; batch/mode-less → job-level only: completed→100%, failed/cancelled→'Failed'/'Cancelled', running/pending→progress-bar-animated striped 100% 'Rendering...'. (c) pipelineRenderAll: resetRenderProgress() on render start (after button swaps); updateRenderProgress(status) on every poll tick BEFORE terminal checks; terminal handling (completed/failed/cancelled → clearInterval + resolve/reject) preserved untouched. No backend changes; app/tts.py untouched.
- [x] Add the result surface: a "Play book" area using GET /export/audio/{job_id} via the singleton player from Plan E, plus a reachable Download button (restore #btn-pipeline-download in index.html wired to GET /download/{job_id})
    **GREEN:** P1-S3: frontend/index.html — result surface restored inside pipeline-editor-section card header: added `<button class="btn btn-outline-primary btn-sm" id="btn-pipeline-download" style="display:none;" title="Download rendered audiobook" aria-label="Download audiobook">` (fa-download + 'Download') between #btn-pipeline-play-sequence and #pipeline-render-job badge — keeps Plan E positional assertions (playBookIdx < renderJobIdx, playSeqIdx < renderJobIdx, editorTabStart < playBookIdx). Added `<span id="render-failures-badge" class="badge bg-danger mb-3" style="display:none;"></span>` right after the .progress div. No initEditor() change needed — btn-pipeline-download→downloadPipelineRender and btn-pipeline-play-book→playPipelineAudiobook listeners already existed in editor.ts initEditor (L179-182, L191-194). pipelineRenderAll completion now ALSO reveals #btn-pipeline-play-book (display inline-block) alongside the existing download reveal (data-job-id).
- [x] TDD GREEN: run test_editor tests with fake timers (vi.advanceTimersByTimeAsync(2000)) and mocked fetch (render_status with counts); assert progress bar width, failure badge, and download/play wiring
    **GREEN:** P1-S4: all 8 new Plan F tests GREEN — test_editor.test.ts 88/88 passing (was 80 before additions + 8 new). Fake timers vi.advanceTimersByTimeAsync(2000) drive the 2000ms poll; mocked API.get render_status payloads (Once chains running→completed) assert: bar width '30%' + '12/40 chunks' label, failure badge '3 chunks failed' + error title, badge hidden when failed=0, batch indeterminate animated bar 'Rendering...' → '100%', download button restore in index.html + click wiring → anchor href '/api/pipeline/download/job-dl-1' (HTMLAnchorElement.prototype.click spy), completion exposes download (data-job-id) + play book, loadSpans neutral 'Ready' 0%. Existing tests stay green. Isolation fix along the way: fixture now includes #spans-table-body (loadSpans early-returns without it).
- [x] Verify: `npx tsc --noEmit` exit 0; vitest green; `npm run build` + dist commit
    **DONE:** P1-S5: verification complete — npx tsc --noEmit exit 0; npx vitest run 226/226 across 6 files (218 baseline + 8 new, no regressions); npm run build exit 0 (vite 8.2.0, 21 modules, dist regenerated: index.html 69.84 kB + assets/index-Cq8Yc-HN.js 75.84 kB). Dist NOT committed per plan (single implementation commit is P7-S6); bundle smoke-checked for new strings (btn-pipeline-download, render-failures-badge, 'Rendering...', 'Ready', progress-bar-animated). No backend files touched; app/tts.py untouched; no legacy endpoints/modules reintroduced. Working tree left dirty by design.

### Phase 2: Cancel wiring with retry-once
- [x] TDD RED: write tests asserting the Cancel button calls POST /cancel_render (render) and POST /cancel_walks (walks), and that a 503 + Retry-After response triggers exactly one automatic retry before surfacing an error
    **Done:** TDD RED written: 9 new tests across test_editor.test.ts (4: pipelineCancelRender 503→200 retry-once success + POST shape; both-503 rejects w/ exactly 2 attempts; 503-without-Retry-After no-retry contract pin; cancelPipelineRender() explicit-cancel wired to ACTIVE job id w/ both-503 error surfaced via console.error) and test_script.test.ts (5: pipelineCancelWalks 503→200 / both-503 / no-header + handleCancelWalks button-path success toast & error toast). Both files' vi.mock('../../src/api') factories converted to partial mocks via vi.importActual exposing the REAL postWithRetryOnce; tests spy on global.fetch (editor) / module-scope mockFetch (script). RED confirmed: 13 failures before wiring (9 new + 4 pre-existing poisoned by leaked _currentRenderJobId from a mid-test failure — fixed by reordering E4 cleanup before assertions).
- [x] Implement: wire cancel buttons in editor-pipeline.ts (render cancel) and script.ts (walk cancel) to the endpoints; add the retry-once wrapper around cancel calls (collision handling per DD workflow: 503+Retry-After → frontend retries once)
    **Done:** Implemented retry-once. (1) New export postWithRetryOnce<T>(endpoint, body) in frontend/src/api.ts: raw fetch POST; checks res.status===503 && res.headers.get('Retry-After')!=null BEFORE handleError; sleeps parsed Retry-After seconds (parseInt; fallback 1000ms for unparseable/HTTP-date form — backend sends integer seconds '1' per ADVERSARIAL L1104) via setTimeout; retries EXACTLY ONCE (2 total attempts max, never loops); then handleError + res.json(). (2) editor-pipeline.ts pipelineCancelRender now calls API.postWithRetryOnce('/api/pipeline/cancel_render', {job_id}) — used by BOTH explicit user Cancel (btn click → cancelPipelineRender() skipApi=false) and pipelineRenderAll finally; the silent finally-path calls cancelPipelineRender(true) with skipApi=true → makes NO API call, so retry-once is not applicable there (nothing to retry; also _currentRenderJobId is nulled before it). Documented decision: retry-once applies uniformly to the single API-calling path (explicit cancel); the finally path stays API-free for consistency and to avoid a redundant POST after terminal state. (3) script.ts pipelineCancelWalks now calls API.postWithRetryOnce('/api/pipeline/cancel_walks', {book_id}); handleCancelWalks delegates to it (toast paths unchanged). (4) editor.ts: added pipelineCancelRender to the './editor-pipeline' import + 'Re-export pipeline TTS rendering' block (testability per file convention 'Tests import all pipeline functions from ../tabs/editor'). Backend untouched. No legacy endpoints introduced.
- [x] TDD GREEN: run cancel tests with mocked fetch returning 503 then 200; assert retry-once and correct terminal state
    **Done:** TDD GREEN: test_editor.test.ts + test_script.test.ts both green (126/126, up from 113 baseline in those files). All 9 new tests pass: 503-then-200 → resolves with {status:'cancelled'} and exactly 2 fetch calls; both-503 → rejects with 'transaction contention' and exactly 2 calls, no 3rd; 503-without-Retry-After → 1 call, rejects. Fake timers used (vi.advanceTimersByTimeAsync(0) + 1000) for the Retry-After sleep. Two iteration fixes during GREEN: (1) editor tests initially failed 'pipelineCancelRender is not a function' — the re-export needed the symbol added to editor.ts's import-from-editor-pipeline block (TS2304), not just the export block; (2) rejection-path tests produced unhandled-rejection errors because expect(promise).rejects attached its handler AFTER the rejection settled during advanceTimersByTimeAsync — fixed by constructing the assertion (handler attach) BEFORE advancing. Full suite: 235/235 (was 226).
- [x] Verify: `npx tsc --noEmit` exit 0; vitest green
    **Done:** Verification passed: npx tsc --noEmit → exit 0 (0 errors). npx vitest run → 235/235 passing across 6 files (baseline 226 + 9 new). npm run build → vite build succeeded, exit 0 (21 modules; app/static/dist regenerated — generated build output only, not source). No commit made (working tree left dirty; P7-S6 owns the commit). Frontend-only changes; no backend source modified.

### Phase 3: Walk runs list in Script tab
- [x] TDD RED: write/extend frontend/tests/frontend/test_script.test.ts asserting the Script tab fetches GET /walks/{book_id}/runs and renders a run list (walk_name, status badge, created/finished times) below the existing per-walk status badges; empty state when no runs
    **RED:** TDD RED: added describe 'Script Tab — Walk Runs List (Plan F, Phase 3)' to test_script.test.ts — 12 tests: (1) pipelineWalkRuns GETs /api/pipeline/walks/{book_id}/runs, (2-3) formatWalkRunTime 'YYYY-MM-DD HH:MM' + em-dash for falsy ms, (4) run-row rendering (WALK_DISPLAY_NAMES label, badge, created/finished times, escaped error text, '—' for unfinished), (5) raw walk_name fallback, (6) badge class map for all 5 statuses (completed bg-success / running bg-warning text-dark / failed bg-danger / cancelled bg-dark / pending bg-secondary), (7) empty state 'No walk runs yet', (8) missing-container no-op, (9) loads-on-onboarding + refresh-on-tick (runs endpoint call count 1 after onboard → 2 after one 2000ms tick; walk-status poll stays alive via one 'running' walk), (10) empty state through the real onboard UI, (11) tab-load fetch for a restored session (state.pipelineBookId) WITHOUT starting walk polling (asserts walk_status NOT fetched), (12) index.html declares #walk-runs-container below #walk-status-container. New exports imported DYNAMICALLY (await import, same pattern as the file's existing showToast/showConfirm dynamic imports) so RED is surgical: verified 12 failed / 34 passed — pre-existing tests stay green while only new ones fail.
- [x] Implement: add the runs list to script.ts (fetch on tab load + after each walk poll tick), reuse the existing status badge renderer
    **GREEN:** Implemented in frontend/src/tabs/script.ts: (a) exported WalkRunRow interface + pipelineWalkRuns(bookId) API fn → API.get('/api/pipeline/walks/{bookId}/runs'). (b) Extracted shared buildStatusBadge(status) used by BOTH renderWalkStatuses and the new renderWalkRuns so classes are byte-identical; added an explicit cancelled→bg-dark + fa-stop branch (previously fell into the else bg-secondary else-branch — no existing test pinned that, so zero regression; noted as a deliberate deviation). (c) Exported formatWalkRunTime(ms) → 'YYYY-MM-DD HH:MM' local time, '—' for falsy ms (finished_ms=0 while running). (d) renderWalkRuns(runs) renders into new #walk-runs-container: WALK_DISPLAY_NAMES label (raw fallback), shared badge, 'Created X · Finished Y' times, error text escaped via escapeHtml, empty state 'No walk runs yet', no-op when container missing. (e) refreshWalkRuns(bookId?) internal helper (no-op without a book; logs errors without clobbering the list) wired into the existing startWalkPolling poll() AFTER updateWalkButtons (awaited, so every tick refreshes runs alongside walk status) AND into initPipelineUI for restored sessions: if state.pipelineBookId is set, fetch runs WITHOUT starting walk polling (walk-status behavior unchanged). frontend/index.html: added a 'Walk Runs' card (id=walk-runs-container) inside #walk-execution-section directly below the Walk Status card. No backend files touched.
- [x] TDD GREEN: run test_script tests with mocked fetch (runs payload) and fake timers for polling; assert list rendering and empty state
    **GREEN:** TDD GREEN: test_script.test.ts 46/46 (34 pre-existing + 12 new). Full suite 247/247 across 6 files (was 235 — +12, zero regressions). UI-flow tests use fake timers (vi.advanceTimersByTimeAsync(0) flushes onboard + immediate poll; 2000 for one poll tick) and an API.get mockImplementation keyed on endpoint suffix ('/runs' → payload, else walk statuses with walk_2a running to keep polling alive). The onboarding/tick/tab-load tests REUSE the Phase 2 describe's single initScript() DOMContentLoaded registration — deliberately NOT registering a second one (stacked listeners would double-wire buttons and double-fire handleOnboard; see discovery log on stacked DOMContentLoaded listeners). The Phase 2 cancel-walks tests stay green: their fixture lacks #walk-runs-container so renderWalkRuns early-returns even though the runs fetch now fires inside poll() (API.get mock resolves the statuses object there — harmless).
- [x] Verify: `npx tsc --noEmit` exit 0; vitest green
    **DONE:** Verification complete: npx tsc --noEmit → exit 0 (0 errors); npx vitest run → 247/247 across 6 files (235 baseline + 12 new, no regressions); npm run build → exit 0 (vite 8.2.0, 21 modules; dist regenerated: index.html 70.39 kB + assets/index-B9F8fScJ.js 77.56 kB). Dist NOT committed (P7-S6 owns the single implementation commit). No backend source modified (app/pipeline/*, app/app.py, app/tts.py untouched); working tree left dirty by design. Bundle smoke-checked for new strings ('No walk runs yet', 'walk-runs-container').

### Phase 4: Export M4B form + cover upload + chapter markers
- [x] TDD RED: write tests asserting the Export M4B form submits title/author/narrator/year/description + optional cover file to POST /export/m4b (multipart) and renders the resulting audio artifact; chapter marker generation is backend (Plan C) — frontend asserts the form payload shape and success state
    **RED:** TDD RED: added describe 'Editor Tab — Export M4B (Plan F, Phase 4)' to test_editor.test.ts — 9 tests: (1) pipelineExportM4b posts multipart FormData to /api/pipeline/export/m4b with EXACTLY job_id + title/author/narrator/year/description (6 entries, no cover), (2) cover File appended when provided (7 entries, instanceof File + name/type) and omitted otherwise, (3) index.html declares the form (5 fields + #export-m4b-cover + #btn-export-m4b type=submit) hidden idle (display:none) inside the editor tab, (4) form submit via initEditor wiring posts job_id + values and reveals play/download (data-job-id) + success toast + form reset, (5) backend 409 detail surfaces via error toast and the form stays usable (values preserved, button not disabled, no reveal), (6) M4B-only response.message surfaced in #export-m4b-info, (7) render completion reveals #export-m4b-card (same moment as download/play), (8) new render start hides+resets the stale card synchronously then re-reveals on completion, (9) no completed job → warning toast, no POST. New exports referenced via DYNAMIC import (await import('../../src/tabs/editor')) so RED is surgical. Verified RED: 9 failed / 92 passed in test_editor.test.ts (all 9 failures are the new tests; 92 pre-existing green). Fixture mirrors the Phase 1 result-surface fixture + export card; afterEach restores global fetch + mockReset API get/post (Phase 1 isolation pattern).
- [x] Implement: add the Export M4B form to the result surface in index.html + editor-pipeline.ts (title/author/narrator/year/description inputs + cover file input + submit), POST multipart to /export/m4b, handle success (play/download the artifact) and error states
    **GREEN:** Implemented in frontend/src/tabs/editor-pipeline.ts: (a) module docstring + '- POST /api/pipeline/export/m4b (multipart form: metadata + optional cover; raw fetch + FormData, NOT JSON)'. (b) New export interface M4bExportResult {status, output_path, mp3, mp3_path, audacity, audacity_path, message?} + ExportM4bPayload {jobId, title, author, narrator, year, description, cover?: File|null} + pipelineExportM4b(payload) — raw fetch + FormData (job_id + 5 text fields ??'', cover appended only when truthy), throw Error(body.detail||statusText) on !ok, return res.json(); follows pipelineOnboard raw-fetch pattern in script.ts (API.post would JSON-stringify and break the route). (c) New 'Export M4B form (Plan F, Phase 4)' section: revealResultSurface(jobId) (download inline-block + data-job-id, play-book inline-block, export-m4b-card block), resetExportM4bForm() (form.reset() + hide #export-m4b-info), hideExportM4bForm() (card none + reset), handleExportM4bSubmit(e?) exported (warning toast 'No completed render to export. Render the audiobook first.' when state.pipelineRenderJobId null; success → toast 'M4B export complete' + revealResultSurface + resetExportM4bForm THEN surface response.message in #export-m4b-info (reset must run before the message survives); error → toast 'Export failed: '+msg, form stays usable/values preserved), initExportM4bForm() exported (wires #export-m4b-form submit, no-op when absent). (d) pipelineRenderAll: hideExportM4bForm() right after resetRenderProgress() (new render start), and the two separate download/play reveal blocks replaced by a single 'if (_currentRenderJobId) revealResultSurface(_currentRenderJobId)'. frontend/index.html: #export-m4b-card (display:none) card between the Span Editor card and the Confidence Review section — form#export-m4b-form[enctype=multipart/form-data] with title/author/narrator/year inputs, description textarea, cover file input accept='image/*', #btn-export-m4b type=submit, hidden #export-m4b-info alert-warning. frontend/src/tabs/editor.ts: import group + 'Re-export Export M4B (Plan F, Phase 4)' block (pipelineExportM4b, handleExportM4bSubmit, initExportM4bForm) + initEditor DOMContentLoaded calls initExportM4bForm() after the play-sequence wiring. mergePipelineAudiobook untouched (still download-only reveal). No backend files touched.
- [x] TDD GREEN: run export form tests with mocked fetch; assert multipart body shape (FormData with the 5 fields + cover) and success rendering
    **GREEN:** TDD GREEN: test_editor.test.ts 101/101 (92 pre-existing + 9 new), full suite 256/256 across 6 files (247 baseline + 9 new, zero regressions). All 9 Phase 4 tests pass: FormData shape on the fetch body inspected via [...formData.entries()] (6 entries without cover sorted == author/description/job_id/narrator/title/year; 7 entries with cover, File instanceof + name/type 'cover.jpg'/'image/jpeg'), success path reveals play/download (data-job-id) + 'M4B export complete' toast + form reset, backend 409 detail surfaced via 'Export failed: format mismatch: cover must be jpeg/png' error toast with form kept usable, M4B-only response.message rendered in #export-m4b-info, render lifecycle (card hidden idle → revealed on completion → hidden+reset on new render → re-revealed), no-completed-job warning without POST. One iteration fix during GREEN: resetExportM4bForm() hid #export-m4b-info AFTER the message was set, so the M4B-only message test failed (display stayed 'none') — reordered success path to reset fields first, then surface the message. Static imports of the 3 new exports (dynamic import used only during RED).
- [x] Verify: `npx tsc --noEmit` exit 0; vitest green; `npm run build` + dist commit
    **DONE:** Verification complete: npx tsc --noEmit → exit 0 (0 errors); npx vitest run → 256/256 across 6 files (247 baseline + 9 new, zero regressions); npm run build → exit 0 (vite 8.2.0, 21 modules; dist regenerated: index.html 73.89 kB + assets/index-Cf4jX9tv.js 79.20 kB; smoke-checked — 'export-m4b' present 16× in dist index.html, 1× in JS bundle). Dist NOT committed (P7-S6 owns the single implementation commit; working tree left dirty by design — deviation from plan text 'dist commit', per dispatch instructions). No backend source modified (app/pipeline/*, app/app.py, app/tts.py untouched; git status shows only frontend/ + app/static/dist/ generated output + plan file). No legacy endpoints/modules reintroduced. Phases 1-3 and Plan E UX preserved (all prior tests green).

### Phase 5: MP3 + Audacity ZIP_STORED (feature-detect, degrade)
- [x] TDD RED: write tests asserting the MP3/Audacity buttons appear only when the backend reports support (feature-detect endpoint or capability flag in the export response); when unsupported, a clear message explains M4B-only
    **RED:** TDD RED: added describe 'Editor Tab — Export Capabilities MP3/Audacity (Plan F, Phase 5)' to test_editor.test.ts — 6 tests: (1) index.html declares #export-mp3-link + #export-audacity-link inside #export-m4b-card, hidden by default, (2) mp3:true+audacity:true submit flow shows both links with href = response artifact paths verbatim (getAttribute('href') — jsdom resolves anchor.href against the base URL, so raw attribute asserted) + no degrade message, (3) mp3:false surfaces response.message in #export-m4b-info + suppresses MP3 link while Audacity stays, (4) mp3:false with NO message key → frontend default 'MP3 export unavailable — M4B-only', (5) audacity:false hides the Audacity link (per-flag feature-detect), (6) new render start (pipelineRenderAll) hides + clears stale capability links + info message synchronously. Verified RED: 6 failed / 101 passed in test_editor.test.ts (all failures are the new tests — elements/behavior absent). Isolation mirrors Phase 4: originalFetch save/restore, fetchSpy, API.get/post mockReset in afterEach, fake timers for the lifecycle test.
- [x] Implement: capability feature-detect (libmp3lame availability surfaced via a backend capability check or the /export/m4b response); MP3 button requests MP3 output, Audacity button requests ZIP_STORED audacity export; unsupported → disabled buttons + messaging
    **Implement:** Implemented in editor-pipeline.ts: (1) renderExportCapabilities(result) — internal helper, feature-detects from the /export/m4b response flags (the response IS the capability carrier; no new endpoint): mp3:true && mp3_path → #export-mp3-link shown with href=mp3_path; audacity:true && audacity_path → #export-audacity-link shown with href=audacity_path; mp3:false → MP3 link suppressed + #export-m4b-info surfaced with result.message or frontend default 'MP3 export unavailable — M4B-only'. (2) resetExportCapabilities() — hides + clears hrefs of both links; called from hideExportM4bForm so a new render start (pipelineRenderAll) resets the capability row alongside the existing hideExportM4bForm logic. (3) handleExportM4bSubmit success path: resetExportM4bForm() then renderExportCapabilities(result) — replaces the Phase 4 message-only block (Phase 4 behavior preserved: message still surfaces via the same #export-m4b-info element). index.html: added #export-capabilities-row with hidden-by-default #export-mp3-link ('Download MP3') + #export-audacity-link ('Download Audacity bundle') anchors (download attr) inside #export-m4b-card after the form. editor.ts: NO new exports needed — all Phase 5 logic is internal to the already-exported handleExportM4bSubmit/pipelineRenderAll flows; tests exercise the public surface per Phase 4 conventions.
- [x] TDD GREEN: run the capability tests with mocked capability responses; assert button visibility and degrade messaging
    **GREEN:** TDD GREEN: full suite 262/262 passing across 6 files (256 baseline + 6 new; zero regressions). All 6 Phase 5 tests pass: capability links render from response flags (mp3/audacity true → both visible with href = artifact paths verbatim; mp3:false → MP3 suppressed + M4B-only message, response.message preferred, frontend default 'MP3 export unavailable — M4B-only' when key absent; audacity:false → Audacity hidden), and a new render start resets the stale capability row synchronously via hideExportM4bForm → resetExportCapabilities.
- [x] Verify: `npx tsc --noEmit` exit 0; vitest green; `npm run build` + dist commit
    **Verify:** Verification (P5-S4): `npx tsc --noEmit` exit 0 (zero type errors); `npx vitest run` full suite 262/262 across 6 files (256 baseline + 6 new, no regressions); `npm run build` exit 0 (vite 8.2.0, dist bundle regenerated — NOT committed; P7-S6 owns the commit, working tree intentionally dirty). Backend gap logged as observation: mp3/audacity artifacts have no HTTP serving route — affordances link to the backend-returned paths verbatim (documented + pinned in tests).

### Phase 6: README doc-drift correction
- [x] Update README.md: replace the progress-UI overstatement with the real per-chunk progress description (individual mode) and job-level (batch mode); remove any false claim of a voice review surface or alias editing in the UI (actual surfacing lands in Plan H/D; alias picker is Plan H); note the new export surface
    **Done:** README.md (root) +44/-20 across 10 edits and frontend/README.md +12/-8 across 6 edits. (1) Progress: replaced generic 'progress tracking' with per-chunk completed/total + failure badges (individual) vs job-level (batch) in Features, Quick Start Step 4, Editor Tab, frontend TTS Rendering table + endpoint table + render-body paragraph. (2) Voice-review/alias: NO voice-review-surface claim existed in either README (verified); alias-editing claims (Quick Start Step 3, Voices Tab Speaker Aliases block) reworded to automatic walk-2c resolution with aliases shown as badges. Features L25 alias bullet reworded in the QA Round 1 fixer pass to remove the manual alias-mapping implication: 'Speaker-name variants are resolved automatically during annotation into aliases of a single character (e.g. "YOUNG ELENA" → "ELENA"), so variants share one voice configuration' — now consistent with the automatic wording at README.md L201 and L269. Also removed 2 additional Plan H drifts per task item 6: Voices Tab 'Create and manage voice configs' → browse/preview/assign; frontend Voices tab-table row's fake 'GET/POST/PUT/DELETE /api/pipeline/voices' management endpoints → real endpoints (GET voices, PUT voices/NARRATOR, POST voices/{id}/preview). (3) Export surface: Export M4B metadata form (title/author/narrator/year/description + optional cover) → chaptered M4B, MP3 (libmp3lame) and Audacity ZIP_STORED bundle conditional on backend support, M4B-only degrade — added to Features Export Options, Quick Start, Editor Tab, Output Files (audiobook.m4b/mp3/audiobook-audacity.zip), API Reference export/m4b multipart curl, frontend tables. (4) Script tab: walk runs history bullet (GET /api/pipeline/walks/{book_id}/runs, created/finished times + status badge). (5) Cancel: retry-once-on-503 noted for cancel_walks + cancel_render in Script/Editor bullets and API Reference. (6) Result tab: no stale claims existed (verified). (7) render_status: mode + per-chunk counts (completed/total/failed) documented in API Reference + frontend endpoint table. Confidence Review claims (real, Plan D) left intact. Docs only, no code; no commit; working tree left dirty.
- [x] Verify: grep README.md for the corrected sections; confirm no other stale claims about the removed Result tab remain
    **Done:** Verified via rg + read over README.md and frontend/README.md. All empty in both READMEs: 'spans loaded' / 'N spans' / 'progress bar.*100%' (no static-progress overstatement), 'set as an alias' / 'Map one speaker' / 'alias picker' / 'alias editing' / 'Create and manage' / 'manage voice configs' (no Plan H alias/manage drift), 'Result tab' / 'Result Tab' (no stale Result-tab references), 'voice review' surface claims (only legitimate voice-preview and designer-tab matches remain — L48, L197, L266, L295, L510, frontend L13/L15). Present and accurate: per-chunk progress (completed/total + failure badges, job-level for batch) in Features/Quick Start/Editor Tab/frontend tables; Export M4B form + MP3 (libmp3lame) + Audacity ZIP_STORED + M4B-only degrade in Export Options/Editor Tab/Output Files/API Reference; walk runs (GET /api/pipeline/walks/{book_id}/runs) in Script tab + API Reference; cancel retry-once for cancel_walks/cancel_render; render_status mode + per-chunk counts. Full section re-reads (L45-54, L196-214, L252-272, L333-345, L412-426, L470-486, L624-650) confirm prose style matches existing feature-bullet format. Confidence Review claims intact. No code changed; git diff confirms only README.md + frontend/README.md touched by this phase.

### Phase 7: Regression gate, guard suite and verification
- [x] Run `npm test` (vitest) and record the pass count (167 baseline + new); `npx tsc --noEmit` exit 0
    **DONE:** Regression gate green. `npx vitest run` (run from frontend/ — vitest config lives there; a repo-root run picks up no config and fails 101 tests with 'document is not defined'): 262/262 passing across 6 files (218 baseline + 44 new across phases 1-5; zero regressions). `npx tsc --noEmit` exit 0 (no type errors).
- [x] Run `npm run build` and verify `git diff --exit-code app/static/dist/` is clean after committing dist
    **DONE:** Build verified: `npm run build` (from frontend/, where vite.config.ts lives) exit 0 — vite 8.2.0, 21 modules transformed, dist regenerated in 241ms: app/static/dist/index.html 75.13 kB (gzip 12.00 kB) + assets/index-CGCTafV9.js 79.79 kB (gzip 19.54 kB). Dist state checked: `D app/static/dist/assets/index-Dx_qAJuB.js`, `M app/static/dist/index.html`, `?? app/static/dist/assets/index-CGCTafV9.js` — dirty by design (NOT committed; the plan's `git diff --exit-code app/static/dist/` clean check is a P7-S6 post-commit gate, out of my scope). No source files changed by this step; working tree left dirty.
- [x] Run `pytest tests/pipeline/test_legacy_removed.py -q` and verify 12/12 still green (frontend-only plan; no legacy /api/export_audacity or /api/merge_m4b references introduced)
    **DONE:** Guard suite green: `python -m pytest tests/pipeline/test_legacy_removed.py -q` → 12 passed in 1.68s (9 legacy-file-absent + 1 live route scan + 1 state.ts pipelineEnabled + 1 project_manager identifier). Legacy-reference scan (scoped grep, frontend/src): pattern `export_audacity|merge_m4b|/api/chunks|editor-legacy|tabs/audio` → No files found — zero frontend references to legacy endpoints or legacy editor modules introduced by Plan F. NOTE: aft_search's path param is a project-root override, not a scope filter (prior discovery L70) — used indexed grep with path=frontend/src instead. Verified dist state: build regenerated app/static/dist (D index-Dx_qAJuB.js, M index.html, ?? index-CGCTafV9.js) — dirty by design; the `git diff --exit-code app/static/dist/` clean check runs at P7-S6 after the implementation commit, not now.
- [x] Security review: cover upload validation (size/type limits enforced client-side, backend validates too), ffmpeg invocation stays server-side list-args (no shell), no secrets in the export form; document findings
    **DONE:** Security review (documentation only, no code change): all 4 areas PASS, zero findings. (1) Cover upload: client accept='image/*' UX hint (index.html L1067) + backend enforcement (api_export.py export_m4b L1550-1563): PIPELINE_MAX_COVER_BYTES default 20MiB, read with cap+1 sentinel → 400 oversize; content_type image/* prefix → 400; written as tmp_dir/cover.img (tmp dir rmtree'd in finally), ffmpeg decodes as mjpeg attached_pic — content-type is client-declared/spoofable but ffmpeg decode is the real format gate; file never executed. (2) ffmpeg: all 5 subprocess sites in api_export.py are list-args (L1200 merge, L1276 _run_media_command shared runner, L1297 static probe, plus concat/mux/mp3/ffprobe via the runner) — no shell=True, no os.system/Popen; metadata control-char-stripped (0x00-0x1F+0x7F) + ffmetadata-escaped (\\ = ; #) into a file passed via -i; concat paths containment-checked (_resolve_within_run_dir) + demuxer quote-escaped; frontend adds no shell (fetch+FormData only). (3) Secrets: form fields exactly job_id + 5 metadata + cover; no credentials/keys (keys live in backend config via Setup → POST /api/config; job_id is a server-assigned UUID). (4) XSS: renderWalkRuns escapes error/label/run_id/status via escapeHtml; renderExportCapabilities message via textContent (no HTML parsing) + hrefs via property assignment (not innerHTML → no markup injection; backend builds mp3_path/audacity_path from hardcoded os.path.join constants L1546-1547, never user input, so no URL-injection surface); showToast escapes via escapeHtml. Residual documented: hrefs are backend-returned paths with no serving route (404 in deployment — known Plan C gap L146, not a vuln). No backend or frontend files changed by this step.
- [x] Code review pass via exec-manager QA-Reviewer; fix MINOR findings
    **DONE:** QA gate PASSED. Round 1 (exec-manager dispatch): ISSUES_FOUND MINOR — 2 actionable (orphaned duplicate docstring editor-pipeline.ts L1225-1229 deleted; README.md:25 Speaker Aliases bullet reworded to automatic walk-2c resolution) fixed by Exec-Fixer; 2 backend-gap observations routed (not defects of this frontend-only plan, disclosed in READMEs): 503+Retry-After mapping absent in app/ (retry path untriggerable until backend exception handler lands), mp3/audacity paths lack HTTP serving route (links 404 in deployment). Round 2 (full fresh review): PASS — 0 issues; TestAnalyzer MINOR_ISSUES_PASS (44 new tests discriminating, 262/262, 6 log-only notes); DocsAnalyzer PASS (2 MAJOR + 1 MINOR findings repaired in place and verified: README no-route disclosures for MP3/Audacity, stale 'Phase 5' docstring sentence removed from M4bExportResult, Audacity 'produced alongside the M4B' wording). All gates re-verified: tsc exit 0, vitest 262/262, build + dist byte-identical, guard 12/12, backend diff empty.
- [x] Commit: `feat(frontend): real progress, cancel and export UI for pipeline renders`
    **DONE:** Committed as 5dd2a3a4ae9ed3a459a732d7e14bbfdf6c8b48e8 with exact message `feat(frontend): real progress, cancel and export UI for pipeline renders`. 12 files, 2377 insertions(+), 238 deletions(-): frontend/src/tabs/editor-pipeline.ts, script.ts, api.ts, editor.ts, frontend/index.html, frontend/tests/frontend/test_editor.test.ts, test_script.test.ts, README.md, frontend/README.md, app/static/dist/{index.html, assets/index-CGCTafV9.js (new), assets/index-Dx_qAJuB.js (deleted)}. Staged explicitly (no `git add -A`); plan file artifacts/plans/pending/TASK-universal-upgrade-F-progress-cancel-export-ui.md left unstaged (archived separately). Post-commit verification: (1) `git status --short` clean except the unstaged plan file; no backend source (app/*.py, app/pipeline/*) staged or modified. (2) Fresh `npm run build` from frontend/ exit 0 (vite 8.2.0, 21 modules: index.html 75.13 kB / assets/index-CGCTafV9.js 79.79 kB) then `git diff --exit-code app/static/dist/` exits 0 — committed dist byte-identical to fresh build (reproducibility gate, satisfies P7-S2). (3) `npx tsc --noEmit` exit 0. (4) `npx vitest run` 262/262 across 6 files (218 baseline + 44 new, zero regressions). QA Round 2 already PASS (tsc 0, vitest 262/262, build 0, guard 12/12) before this commit; no backend files touched.

## Completion Criteria

- Progress bar reflects real per-chunk counts (individual) / job-level (batch); failures surfaced
- Result surface plays the whole book and provides a reachable Download button
- Cancel buttons wired to cancel_render/cancel_walks with retry-once on 503+Retry-After
- Script tab shows the walk runs list; export M4B form (5 fields + cover) submits multipart; MP3/Audacity feature-detected with M4B-only degrade messaging
- README progress/voice-review claims corrected
- tsc exit 0, vitest green, build + dist committed, guard 12/12

## References

- `artifacts/designs/pending/DD-universal-upgrade.md` — P4 C3 phase, cap3, FP6+FP2, UX workflow (Progress, Export), decision #2, open item #8 (result-surface placement)
- `artifacts/designs/parts/universal-upgrade/CONTRACTS.md` — export/m4b, export/audio, walks/runs contracts
- `artifacts/designs/parts/universal-upgrade/README.md` — Plan F row
- Prior: `TASK-universal-upgrade-E-audio-surface-player-tabs.md` (completed)
