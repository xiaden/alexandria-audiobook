# Task: Voice-Persona-Prompt Parity D — external capability discovery, clone/design/LoRA integration tests, and final closure

## Problem Statement

This is part D (final) of the implementation plan for `artifacts/designs/pending/DD-voice-persona-prompt-parity-browser-external-validation.md`. Parts A–C are prerequisites. This part delivers the external-validation harness and integration coverage that the DD requires while the earlier parts delivered the pipeline-native surfaces: (1) external-engine capability discovery that reports a clear matrix of supported/unavailable/failed capability states (clone, design, builtin LoRA, LoRA, media decode/encode, required model/runtime features), (2) clone/design/LoRA integration tests that exercise the seams only when the capability is actually available — an unavailable capability is NEVER a green pass, and real-engine smoke tests are opt-in and isolated from deterministic CI, and (3) final project-wide closure: update docs, confirm all gates (test/lint/type/coverage/build/security), truthful environmental reporting, archive the completed plans with evidence, and hand back a director-level report.

## Dependencies

- **Prerequisite:** `artifacts/plans/pending/TASK-voice-persona-prompt-parity-A-...`, `-B-...`, `-C-...` (all backend + frontend delivered).
- Design: `artifacts/designs/pending/DD-voice-persona-prompt-parity-browser-external-validation.md`.
- `app/pipeline/tts_integration.py`, engine factory (`get_tts_engine`), `app/pipeline/adapter.py` (InMemorySQLiteAdapter for fixtures), fake-engine pattern already used by `tests/pipeline/test_tts_integration.py` (2230 lines) and `test_voices.py`. Real-engine surfaces (Artifactory/local Ollama) are opt-in via explicit environment markers; the deterministic suite never requires them.
- Invariants: no unavailable capability produces a green test (unavailable → marked `unavailable`, not `passed`); real-engine smoke tests opt-in and isolated with fixture provenance + tool versions; deterministic Python + TypeScript suites, deterministic build + committed-dist gate, and explicit environment-only failure classification retained; `app/tts.py` byte-identical; legacy guard 12/12; protected combined-walks workbench unchanged.

## Phases

### Phase 1: External capability discovery harness (S1)
- [x] Add an external-validation module (test/validation harness OUTSIDE production routers, e.g. `tests/external/` with `capability_matrix.py` or reuse a markers file): `discover_capabilities()` probes and returns per-capability `{capability, status: supported|unavailable|failed, detail, evidence}` for clone, design, builtin_lora, lora, media_decode, media_encode, and required model/runtime-features; each probe is isolated so a missing engine marks only that capability unavailable (or failed if the engine exists but errors) — never silently skipped into a green pass. Include fixture provenance (which media fixture/license) and tool versions in the report.
    **Note:** Created tests/external/capability_matrix.py — external capability discovery harness OUTSIDE production routers (per DD layer-mapping). discover_capabilities(enable_external=None, engine_provider=None) returns per-capability {capability, status: supported|unavailable|failed, detail, evidence} for clone, design, builtin_lora, lora, model-runtime (engine-gated) and media_decode, media_encode (real ffprobe/ffmpeg probes). Deterministic CI: engine caps report 'unavailable' unless ALEXANDRIA_EXTERNAL=1; never a fabricated green. Real-engine probing opt-in + availability-aware (missing engine→unavailable with clear reason; present-but-broken→failed; present+capable→supported). Probe isolation: media probes write only to a temp dir; engine probe inspects public surface (generate_clone_voice/generate_voice_design/generate_lora_voice/mode) via injectable engine_provider defaulting to app.engine.get_tts_engine (lazy import). Fixture provenance: deterministic in-memory make_wav(0.05s, 8000Hz) PCM (license: none/external-asset-free). Tool versions captured via tool_versions(): ffmpeg 7.1.5, ffprobe 7.1.5, python 3.13.5, torch 2.13.0+cu130. capability_report() returns matrix + fixture_provenance + tool_versions. No production code touched.
    **Notes:** The harness lives outside the production routers (DD layer-mapping: "Test/validation harness outside production routers"). Deterministic CI runs discovery and asserts that unimplemented capabilities report unavailable (not passed); real-engine availability is environment-gated via an opt-in env marker (e.g. `ALEXANDRIA_EXTERNAL=1`).
- [x] Verify: a new test `tests/external/test_capability_discovery.py` green in deterministic CI asserting the matrix reports `unavailable` when engines are absent (never green), `failed` on a present-but-broken engine, and that tool-version/fixture-provenance fields are present in the report.
    **Note:** Added tests/external/test_capability_discovery.py (17 tests). Deterministic CI green: engine capabilities report 'unavailable' (marker off, autouse fixture forces ALEXANDRIA_EXTERNAL off), never 'supported'; media capabilities report supported/unavailable consistent with real binary presence (ffprobe 7.1.5, ffmpeg 7.1.5 present here → supported). Supported/failed paths exercised deterministically via injected fake engines (FakeSupportedEngine→supported; FakeBrokenEngine surface raises→failed; factory raising→failed; provider None→unavailable). Fixture provenance + tool_versions fields asserted present; two-run determinism asserted. GATES: pytest -q tests/external/ → 17 passed (both with and without ALEXANDRIA_EXTERNAL=1, CI-relevant assertions hold); full suite /tmp/qa-venv/bin/pytest -q → 1642 passed (1625 pre-existing + 17 new); git diff --exit-code app/tts.py → empty (byte-identical); no production code changes (git status shows only untracked tests/external/).
    **Notes:** This is the DD acceptance "browser/external checks distinguish pass, fail, unavailable; no unavailable engine produces a green test."

### Phase 2: Clone/design/LoRA integration tests (S2)
- [x] Add integration tests under `tests/external/` (or `tests/pipeline/test_external_integration.py`) that exercise the clone/design/builtin_LoRA/LoRA seams through the existing engine factory and `tts_integration.py` public seams, using the fake engine implementing the existing generate contract for deterministic behavior and the fixed media fixtures. Cover: clone reference rendering path (preview through TTS seam), design voice rendering, builtin LoRA + LoRA adapter rendering, and narrator fallback. Each availability-aware test propagates the capability status — when the engine is the deterministic fake it always passes; when gated to a real engine it is opt-in and reports unavailable/failed distinctly.
    **Note:** Added tests/external/test_external_integration.py (11 tests) exercising clone/design/builtin-LoRA/LoRA/narrator seams through the EXISTING engine factory + tts_integration public seams, using a deterministic fake engine (RecordingEngine) that implements the existing generate contract (generate_voice dispatches by type like app.tts.TTSEngine; also generate_batch/generate_clone_voice/generate_lora_voice/generate_design_voice/generate_voice_design) and writes the fixed make_wav() fixture. Coverage: (1) clone reference rendering preview — GET /api/pipeline/voices/{voice_id}/references/{reference_id}/preview streams stored WAV bytes, 404 on missing file, 404 on cross-voice, plus clone preview through TTS seam (POST /voices/{id}/preview dispatches to generate_clone_voice with ref_audio/ref_text); (2) design voice rendering — design preview dispatches to generate_design_voice with description, output WAV deterministic, generate_voice_design returns (wav_path, sr); (3) builtin LoRA + LoRA adapter rendering — both dispatch to generate_lora_voice carrying adapter_id/adapter_path; (4) narrator fallback — unattributed span resolves to NARRATOR_VOICE constant when no voice_config NARRATOR row exists (render_audiobook batch). Availability-aware: autouse fixture forces ALEXANDRIA_EXTERNAL off (deterministic CI), and a capability-contract test asserts the fake engine reports 'supported' for every engine capability through the phase-1 engine_provider seam (never a fabricated green). Uses TestClient over api_voices router with get_storage + api_export.get_tts_engine DI overrides; CLONE_REFERENCE_ROOT and RENDER_ROOT pinned to tmp; api_voices._PREVIEWS_DIR monkeypatched to tmp so no repo pollution. No production code touched.
    **Notes:** These are "integration tests" in the DD sense (clone/design/LoRA integrations against the existing TTS seam + engine factory), not new production paths. No arbitrary engine arguments; only existing public seams. `app/tts.py` unchanged.
- [x] Verify: `pytest -q tests/external/` in deterministic CI green using fake engines; optional real-engine marker run is documented and reports unavailable where deps are missing (never false-green).
    **Note:** GATES verified. (1) pytest -q tests/external/ — 28 passed (17 phase-1 + 11 new), run twice for determinism (both green). (2) Full backend suite: /tmp/qa-venv/bin/pytest -q → 1653 passed (1642 pre-existing + 11 new; no regressions). (3) git diff --exit-code app/tts.py → empty (byte-identical). (4) No production code changes: git status shows only untracked tests/external/ (the modified artifacts/plans/pending/...md is plan-tooling state, not source). Real-engine marker run (ALEXANDRIA_EXTERNAL=1) on tests/external/test_external_integration.py → 11 passed deterministically (fake engine always injected, so the integration tests never require a real engine; real-engine capability status is reported distinctly by the phase-1 matrix). No stray test artifacts (tests/external/ contains only the two test modules + capability_matrix.py).

### Phase 3: Documentation, security, and final gates (S3)
- [x] Update project docs to match code: confirm CONTRACTS.md has all three contracts + delivered routes/DTOs/tables documented (parts A/B appended; C delivery lines), and add/refresh any pipeline documentation index/codemap touched by the new clone/persona/prompt surfaces (use the `update-docs` skill convention if a codemap/tab mapping doc exists). No legacy prompt-module references.
    **Done:** Docs updated (append-only). CONTRACTS.md: verified all three parity contracts (PipelineVoiceCloneReferenceAPI.v1, PipelineCharacterPersonaAPI.v1, PipelineWalkPromptConfigRevisionAPI.v1) present at lines 1124/1130/1136 with routes/DTOs/tables. Appended a delivery-status note BELOW the parity section (after line 1140) confirming all three delivered + no-arbitrary-engine-args + app/tts.py byte-identical + legacy guard 12/12 + verification counts. git diff shows ONLY +10 insertions at lines 1141-1150, ZERO deletions; workbench section (lines 1090-1140) byte-unchanged (verified via git diff -U0: 0 deleted lines). No codemap/INDEX.md exists in repo (glob **/codemap*, **/INDEX.md, docs/ all empty) — per update-docs skill convention, noted; no codemap to refresh. Legacy prompt-module references: grep for default_prompts|review_prompts|persona_prompts|project_manager|pipelineEnabled across app/ and frontend/src/ returns ZERO matches (legacy prompt files already deleted in Plan Q); only historical references remain in completed-plan/design docs describing the deletion, not live references. No legacy references introduced by this append.
- [x] Run the full security/compatibility gate: confirm no secret/raw-path exposure in responses, path/size/duration constraints enforced, owner checks on every reference/revision, `app/tts.py` byte-identical (`git diff --exit-code`), no legacy endpoint/manifest/prompt-file module, no auto-cascade, no arbitrary engine args. Confirm `pytest -q` (full), legacy guard, `tsc --noEmit`, `vitest run`, `npm run build && git diff --exit-code app/static/dist/`, and coverage.
    **Done:** Full security/compatibility gate — ALL GREEN. (1) No secret/raw-path exposure: CloneReferenceDTO surfaces only contained application-relative_path (never absolute); _resolve_reference_path runs canonical_contain(root,dest) and 404s on any escape; preview/download/delete all resolve via owner-scoped get_clone_reference(reference_id, _LOCAL_OWNER_ID) + voice_id check (cross-owner/cross-voice=404). (2) path/size/duration constraints active: validate_and_copy enforces extension allow-list (media_type_from_path), magic-byte sniff, byte-size bound (configured_max_bytes), decoded-duration bound (configured_max_duration_ms), canonical_contain escape rejection; cleanup_expired_references bounded. (3) app/tts.py byte-identical: git diff --exit-code empty. (4) No legacy endpoint/manifest/prompt-file module: legacy guard 12/12; grep for legacy prompt modules in app/ + frontend/src/ zero matches. (5) No auto-cascade: rerun_persona (api_characters.py) and rerun_scoped_walk (api_walks.py) both require confirm=True (422 otherwise); no voice_assignment_id field in PersonaRerunRequest/ScopedWalkRerunRequest payloads; reruns never auto-run a walk. (6) No arbitrary engine args: rerun/validate endpoints use typed Pydantic request models only; no engine kwargs accepted. Full gate runs: backend pytest -q = 1653 passed; legacy guard 12/12; frontend npx tsc --noEmit exit 0; npx vitest run = 498 passed (12 files); npm run build clean + git diff --exit-code app/static/dist/ empty (dist hashed asset index-lVnMUa2G.js); tests/external/ = 28 passed run twice (deterministic). Coverage (NEW): backend pytest --cov TOTAL 65% (8939 statements, 3084 missed); frontend vitest --coverage Statements 54.75% Branches 44.31% Functions 62.82% Lines 57.84%. pytest-cov installed into /tmp/qa-venv (coverage 7.15.4, pytest-cov 7.1.0). Environmental note: real-engine smoke (Ollama/Artifactory) NOT run — engines unavailable; deterministic fake-engine suite covers integration deterministically; no false-green. Coverage byproduct frontend/coverage/ (untracked, not gitignored) removed post-run to keep tree clean.
    **Notes:** Record truthfully which environment-dependent checks (real-engine smoke) were NOT run because engines are unavailable — environmental reporting must be explicit, never a silent pass. Coverage gate: frontend `--coverage`, backend `pytest --cov`.
- [x] Verify all gates green and record exact counts/deltas (backend test count, frontend vitest count, coverage %, dist hash, legacy guard 12/12, tts SHA).
    **Done:** All gates green with exact counts/deltas: Backend pytest = 1653 passed (delta +28 vs 1625 baseline = 17 capability discovery + 11 integration; no regressions). Frontend vitest = 498 passed (12 files). Frontend coverage (vitest --coverage, @vitest/coverage-v8): Statements 54.75% (2422/4423), Branches 44.31% (1375/3103), Functions 62.82% (387/616), Lines 57.84% (2224/3845). Backend coverage (pytest --cov=app): TOTAL 65% (8939 statements, 3084 missed; 1653 passed). Dist hashed asset: app/static/dist/assets/index-lVnMUa2G.js (git diff --exit-code app/static/dist/ empty after build). Legacy guard: 12/12. app/tts.py SHA: bdec340fff96064d66a7a6672aaa4aa5436d3a4a (byte-identical, git diff empty). tsc --noEmit exit 0. tests/external/ = 28 passed (deterministic, run twice). No production app/ or frontend/src/ code changed. Not committed (exec-manager is sole committer).

### Phase 4: Archive plans and director report (S4)
- [x] Archive the four completed TASK-voice-persona-prompt-parity plans (`plan_archive`) with evidence from annotations (commit SHAs, test counts, gate outputs), moving them from `artifacts/plans/pending/` to `artifacts/plans/completed/`.
    **Committed:** Closure executed 2026-08-17 by Director. All four parity plans (A/B/C/D) archived to artifacts/plans/completed/ with plan_archive after this step. Commit SHAs: A = 586d8e4 (docs contracts) + dd53d41 (clone refs) + 12a584f (QA fix); B = 74d176b + 6978c0b + 8485ab8 + a720ca3; C = 79e5bf9 + a509f94 + 8b9f663 + 20437b8 + 1aa780d (dist) + 5469e92 (plan C completion); D = 5349379 (docs delivery note) + 3143765 (tests/external). Gate evidence recorded in phase annotations: backend 1653, frontend 498, external 28, legacy guard 12/12, app/tts.py byte-identical. Third DD (DD-pipeline-ux-manual-walk-execution.md) never present in repo / untouched. Parity DD (DD-voice-persona-prompt-parity-browser-external-validation.md) was never committed (noted in A/B annotations); plan text + CONTRACTS registrations served as operative spec.
- [x] Commit closure: scoped commit(s) for docs update + final gate evidence if any source changed (docs-only otherwise), and produce the director-level report: plan paths, commits, tests, blockers, changed files, environmental notes, and confirmation the third DD is untouched.
    **Committed:** Closure commits landed: 5349379 docs(contracts): append voice/persona/prompt parity delivery status (appends +10 lines below parity registrations; workbench section byte-unchanged) and 3143765 test(external): capability matrix + parity integration harness (tests/external/: capability_matrix.py, test_capability_discovery.py 17 tests, test_external_integration.py 11 tests, __init__.py). Director report: plans A-D all complete + archived; blockers none; changed files = 2 docs/tests-only commits, no production code; environmental note = real-engine smoke not run (engines unavailable), deterministic fake-engine suite covers integration; third DD untouched. README.md left unstaged for its own commit.

## Completion Criteria

- External capability discovery matrix reports `supported|unavailable|failed` distinctly for clone/design/builtin_lora/lora/media-decode/media-encode/model-runtime; unavailable is never a green pass; real-engine smoke opt-in and isolated with fixture provenance + tool versions.
- Clone/design/LoRA integration tests pass deterministically via fake engines through existing public seams; availability-aware and opt-in; `app/tts.py` byte-identical.
- Docs updated (CONTRACTS parity-owned deliveries; no legacy references); security/compatibility gates pass.
- All four plans archived with evidence; director report delivered with plan paths, commits, tests, blockers, changed files.
- Deterministic suites (Python + TS), committed-dist gate, legacy guard 12/12, and explicit environment-only failure classification all green/nodegrading.

## References

- `artifacts/designs/pending/DD-voice-persona-prompt-parity-browser-external-validation.md`
- `artifacts/plans/pending/TASK-voice-persona-prompt-parity-A-...` / `-B-...` / `-C-...`
- `artifacts/plans/completed/TASK-combined-walks-workbench-workbench-foundation.md`
- `tests/pipeline/test_tts_integration.py`, `tests/pipeline/test_voices.py` (fake-engine conventions)
