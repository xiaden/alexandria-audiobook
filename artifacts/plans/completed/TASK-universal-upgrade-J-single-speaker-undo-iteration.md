# Task: Single-Speaker, Undo & Iteration UX

## Problem Statement

Five iteration utilities from the pre-rewrite app are still missing and land here (DD cap7, P6-C7b, FP3+FP4+A4-r):

1. **Single-speaker mode** — the `book.single_speaker` column (created in Plan A) is never written or enforced. Enforced at the **render boundary only** (tts_integration.py): when single_speaker=1, render_audiobook forces all spans to the NARRATOR voice config; the annotated-script export stays faithful (DD decision #11, open item #7). The UI gains a toggle that writes book.single_speaker.
2. **Undo** — no undo exists anywhere in editor-pipeline.ts. The design's undo = **transactional value-restore** (backend, built in Plan D: walkitem reject/override restores prior_value within a transaction) + **snapshot restore** (Plan I) + a frontend Undo button wired to those primitives for span-text edits (the only reversible mutation surface in the editor today is span text, saved via PUT /span/{id}/text on focusout).
3. **Pause-after** — the pre-rewrite per-span pause_after field is NOT restored (cannot-restore #5); pause is global via TTSConfig `pause_between_speakers_ms`/`pause_same_speaker_ms` (setup.ts already submits these with 500/250 ms defaults — verified in Plan G). This plan **verifies** the global pause path end-to-end (config → tts_integration → generated audio) and polishes the Setup tab UI if needed; no per-span field.
4. **Doc drift cleanup** — archive the stale `designs/completed/DD-frontend-rebuild-per-task-llm-config.md` (276 lines) and fix remaining README claims that don't match the delivered UI.
5. **Contextual review** — DD UX workflow #5 requires the Review tab to show the ±2 neighboring span texts around each item's target span; the delivered review surface (Plan D) carries no span context, so this plan enriches the review GET response with neighbor texts (review.py / api_review.py) and renders them in renderReviewItem.

This plan is the terminal plan of the feature: after it, all 8 DD capabilities are delivered and the whole feature can be smoke-tested end to end.

## Dependencies

- Plan D (transactional value-restore primitive) and Plan I (snapshot restore)
- `artifacts/designs/parts/universal-upgrade/CONTRACTS.md` — book.single_speaker, value-restore, snapshot restore

## Phases

### Phase 1: single_speaker render-boundary enforcement (backend)
- [x] TDD RED: write tests in tests/pipeline/test_tts_integration.py asserting render_audiobook with a book where single_speaker=1 forces every chunk's voice config to NARRATOR (the NARRATOR_VOICE config), while export_annotated_script output remains faithful (unchanged); with single_speaker=0 behavior is unchanged
    **Note:** TDD RED: appended TestSingleSpeakerRenderBoundary to tests/pipeline/test_tts_integration.py (6 tests). Confirmed 4 fail (batch forcing, individual forcing, DB NARRATOR row wins, no-NARRATOR-span still forces) and 2 pass (export fidelity + single_speaker=0 unchanged — behaviors that must NOT change). Uses existing fixtures/helpers; minimal character-only spine helper mirrors _populate_clone_storage pattern.
- [x] Implement: in tts_integration.py at the _build_voice_config/_build_chunks boundary (the only allowed seam — app/tts.py untouchable), read book.single_speaker via storage and override the per-chunk voice config to NARRATOR when set; keep _build_voice_config and _build_chunks signatures compatible
    **Note:** Implemented in app/pipeline/tts_integration.py. Extracted NARRATOR resolution from _build_voice_config into new _resolve_narrator_config(storage) (DB row wins, NARRATOR_VOICE fallback — identical semantics, shared so enforcement and script-present paths resolve identically). Added _enforce_single_speaker(voice_config, storage, book_id): parameterized SELECT single_speaker FROM book WHERE id=?, no-op when 0/missing row, else ensures a NARRATOR entry (covers no-NARRATOR-span scripts) and replaces every entry with a copy of the narrator config. render_audiobook calls it at step 4b, after the empty-script early return and before chunk dispatch — both batch (generate_batch) and individual (generate_voice) paths receive the same dict so one override covers both modes. _build_voice_config/_build_chunks signatures unchanged; app/tts.py untouched; export_annotated_script untouched (chunk speaker fields stay faithful).
- [x] TDD GREEN: run test_tts_integration tests; assert NARRATOR enforcement and export fidelity
    **Note:** TDD GREEN: pytest tests/pipeline/test_tts_integration.py -q → 70 passed (64 baseline + 6 new TestSingleSpeakerRenderBoundary). All 4 previously-failing tests now pass; export-fidelity and single_speaker=0 tests still pass. ruff check app/pipeline/tts_integration.py clean.
- [x] Verify: `ruff check app/pipeline/tts_integration.py` clean; `pytest tests/pipeline -q` green
    **Note:** Verified: ruff check app/pipeline/tts_integration.py clean (All checks passed); pytest tests/pipeline -q → 1241 passed (1235 baseline + 6 new), guard tests/pipeline/test_legacy_removed.py 12/12. Only app/pipeline/tts_integration.py and tests/pipeline/test_tts_integration.py modified (plan file carries annotations). app/tts.py byte-for-byte untouched.

### Phase 2: Single-speaker toggle UI
- [x] TDD RED: write frontend tests asserting the toggle (in editor-pipeline.ts or a settings row) writes book.single_speaker via a backend write path (PUT /operation or a small endpoint in api_operations.py) and reflects the saved value on load
    **Note:** TDD RED done. Wrote 14 spec-first tests in frontend/tests/frontend/test_editor.test.ts under new describe 'Editor Tab — Single-Speaker Toggle (Plan J, Phase 2)': pipelineSetSingleSpeaker write via API.put (on + off), pipelineGetSingleSpeaker read via API.get (1→true, 0→false), loadSingleSpeakerToggle reflects saved value / defaults off on 0 / defaults off + error toast on read failure / no-fetch + off without a book, handleSingleSpeakerToggleChange persists new value / reverts + error toast on write failure, initEditor wires change event to backend write, tab-switch handler loads toggle, frontend round-trip (save→reload reflects), index.html exposes label 'Single-speaker render' + tooltip 'forces NARRATOR at render; script stays faithful' inside editor tab. Added put: vi.fn() to the API partial mock factory (test_editor had no put mock). Confirmed RED before implementation: 14 failed / 107 passed.
- [x] Implement: add the toggle + label ('Single-speaker render' with tooltip 'forces NARRATOR at render; script stays faithful'); wire load/save; add the minimal backend write (PUT /operation for book attributes or dedicated PATCH) to api_operations.py if not already covered
    **Note:** Implemented. Backend (app/pipeline/api_operations.py, behind existing /api/pipeline router — no new module per CONTRACTS #1): chose two dedicated small endpoints over extending OperationRequest (no semantic change to split/merge/move/delete). NEW ENDPOINTS: GET /api/pipeline/book/{book_id}/single_speaker → 200 {book_id, single_speaker: 0|1}, 404 'Book ... not found' for unknown book (parameterized SELECT, defaults 0); PUT /api/pipeline/book/{book_id}/single_speaker body {single_speaker: bool} → 200 {status:'ok', book_id, single_speaker: 0|1}, 404 unknown book (parameterized UPDATE). Request model SingleSpeakerUpdateRequest. Backend tests: TestSingleSpeakerFlagEndpoint in tests/pipeline/test_api.py (6 tests: default 0, PUT true + DB verify + GET round-trip, PUT false, GET/PUT 404, missing-body 422). Frontend: index.html settings row in editor card-body above spans table — form-switch checkbox #single-speaker-toggle, label 'Single-speaker render', title 'forces NARRATOR at render; script stays faithful'; frontend/src/tabs/editor-pipeline.ts adds pipelineGetSingleSpeaker/pipelineSetSingleSpeaker API helpers + loadSingleSpeakerToggle (reflect saved value; default off when no book/unset/read failure w/ toast 'Failed to load single-speaker setting') + handleSingleSpeakerToggleChange (revert + toast 'Failed to save single-speaker setting' on write failure); frontend/src/tabs/editor.ts imports + re-exports all 4, wires change listener in initEditor, calls loadSingleSpeakerToggle() in tab-switch handler (after loadSpans/loadReviewItems); frontend/src/tabs/projects.ts calls loadSingleSpeakerToggle() after loadSpans() on snapshot load (cross-tab refresh).
- [x] TDD GREEN: run the toggle tests; assert round-trip persistence and render-boundary effect documented
    **Note:** TDD GREEN. Full frontend suite 328/328 passed (314 baseline + 14 new) via npx vitest run from frontend/. Round-trip persistence asserted at both layers: frontend test writes via handleSingleSpeakerToggleChange then reloads via loadSingleSpeakerToggle and sees the flag; backend TestSingleSpeakerFlagEndpoint PUT→GET→DB-row verification. Render-boundary effect documented (tts_integration.py NOT touched — Phase 1 owns it): code comments in editor-pipeline.ts helper section, index.html markup comment, and editor.ts wiring comment all state enforcement happens ONLY at the render boundary via tts_integration._enforce_single_speaker and the script/annotated export stays faithful (CONTRACTS decision #9). Note: first green run surfaced 2 failures (tab-switch wiring + round-trip) caused by an incomplete DOM fixture (missing #spans-table-body/#review-items-container made loadSpans/loadReviewItems early-return, shifting API.get call order) and a leaked mockResolvedValueOnce queue; fixed with a URL-dispatch mockImplementation (order/count-agnostic), complete editor fixture, and vi.mocked(API.get/put).mockReset() in beforeEach for hermeticity.
- [x] Verify: `npx tsc --noEmit` exit 0; `npm test` green; `npm run build` + dist committed
    **Deviation:** Verification complete: npx tsc --noEmit exit 0; npm test (vitest) 328/328 green; npm run build (vite) succeeded — regenerated app/static/dist/index.html (83.46 kB, contains single-speaker-toggle markup) + assets/index-EKpi5Aqd.js, old index-CUJ1rnnc.js removed; pytest tests/pipeline -q: 1247 passed (1241 baseline + 6 new) — backend green. DEVIATION from plan text 'dist committed': dist is intentionally NOT git-committed per exec-manager instruction — the single feature commit happens at P5-S6 and will include the dist (tree intentionally dirty; no commits made). No files changed outside scope; CONTRACTS.md untouched (final stamp after QA) — new endpoints to register: GET + PUT /api/pipeline/book/{book_id}/single_speaker.

### Phase 3: Undo wiring (span-text edits)
- [x] TDD RED: write frontend tests asserting an Undo button in the editor reverts the last span-text edit to its prior value (calls the Plan D value-restore / Plan I snapshot-restore primitives or PUT /span/{id}/text with the prior value) and shows a toast; disabled when there is nothing to undo
    **Note:** RED confirmed: 10 new tests fail (9 in new 'Editor Tab — Span-Text Undo (Plan J, Phase 3)' describe at test_editor.test.ts L2835+; 1 in test_projects.test.ts loadProject describe). Failures are exactly the missing implementation: undefined pushUndoEntry/undoLastSpanEdit/clearUndoStack/getUndoStack (dynamic imports), unwired #btn-pipeline-undo click, button absent from index.html, no clearUndoStack call in loadProject. All 146 pre-existing tests in these files still pass. Tests assert: (a) edit→Undo reverts via PUT with PRIOR value; (b) success toast 'Undo: span text reverted'; (c) button disabled→enabled→disabled transitions; (d) stack cleared on snapshot load (loadProject), render start (pipelineRenderAll), and spans (re)load; (e) empty-text edit does NOT push; (f) failed undo re-pushes entry + 'Failed to undo span text edit' toast; (g) index.html button markup disabled initially. Mock convention: beforeEach vi.mocked(API.get/put/post).mockReset(); MOCK_SPANS_WITH_IDS_RAW fixture with id fields (file-level MOCK_SPANS_RAW has no ids). Also fixed latent test_projects mock gap: editor-pipeline mock now provides loadSingleSpeakerToggle (P2 added the call but not the mock key — the throw was silently swallowed post-assertions) + clearUndoStack: vi.fn().
- [x] Implement: maintain an in-memory undo stack of span-text edits (prior value per span id); Undo button reverts via the existing pipelineUpdateSpanText path (PUT /span/{id}/text); clear stack on snapshot load/render
    **Note:** Implemented in-memory undo stack in editor-pipeline.ts (state `_undoStack: UndoEntry[]` declared in Module state next to _cachedSpans; new section 'Span-text undo (Plan J, Phase 3)' after the single-speaker section). Exports: UndoEntry interface, pushUndoEntry(spanId, priorValue), undoLastSpanEdit(), clearUndoStack(), getUndoStack(); internal syncUndoButton() mirrors stack emptiness to #btn-pipeline-undo.disabled. undoLastSpanEdit pops the entry, reverts through the EXISTING pipelineUpdateSpanText path (PUT /span/{id}/text — the server-validated primitive; no backend changes, audit-journal replay not used per DD evidence trail), then restores cached span text + the visible row (querySelector div.span-text[data-index=...]) so UI matches server; on PUT failure re-pushes the entry + 'Failed to undo span text edit' error toast. CLEAR POINTS: (1) projects.ts loadProject — clearUndoStack() alongside loadSingleSpeakerToggle (mandatory snapshot-load clear); (2) pipelineRenderAll — clearUndoStack() right after the render/regen/cancel button swap (mandatory render-start clear); (3) loadSpans — clearUndoStack() in BOTH the no-book branch and the success branch (my documented book-switch/tab-switch choke-point choice: the cache is replaced exactly there, so the stack can never outlive the span set it references; the explicit loadProject + pipelineRenderAll calls remain for plan-mandated clarity). PUSH POINT: editor.ts focusout handler — prior value captured from getCachedSpans() BEFORE the PUT, pushUndoEntry(spanId, priorValue) AFTER the PUT succeeds and only when prior !== newText (no-op edits not pushed); empty-text branch untouched (no push). editor.ts re-exports all 4 + UndoEntry type; initEditor wires #btn-pipeline-undo click → undoLastSpanEdit(). index.html: btn-pipeline-undo added to editor toolbar after btn-pipeline-merge, btn-outline-secondary btn-sm, fa-undo icon, '>Undo</button>', disabled + title + aria-label. projects.ts imports clearUndoStack.
- [x] TDD GREEN: run the undo tests; assert revert, disabled state, and stack clearing on load
    **Note:** GREEN: all 10 new tests pass. Full frontend suite 338/338 (7 files; 328 baseline + 10 new). Dynamic imports converted to top-level (pushUndoEntry/undoLastSpanEdit/clearUndoStack/getUndoStack added to the '../../src/tabs/editor' import block; showToast stays dynamic per P2 convention). Debugging notes worth recording: (1) jsdom swallows clicks on disabled buttons — test (a) waits for btnUndo.disabled===false inside the waitFor before clicking (the push runs in the microtask after the PUT resolves); (2) mock promise continuations can interleave AFTER a waitFor that resolves on a toHaveBeenLastCalledWith(API call) assertion — the DOM restore (undoLastSpanEdit's post-PUT code) was observed running after the test's single-shot assertion, so test (a) polls the DOM state itself (expect .span-text textContent === prior) inside the same waitFor, matching the P2 convention of waiting for observable state rather than the API call.
- [x] Verify: `npx tsc --noEmit` exit 0; `npm test` green
    **Note:** Verification complete: (1) `npx tsc --noEmit` exit 0 from frontend/; (2) full vitest suite 338/338 (7 files — 328 baseline + 10 new: 9 undo tests in test_editor.test.ts + 1 loadProject clear-stack test in test_projects.test.ts); (3) `npm run build` exit 0 — vite regenerated app/static/dist (index-CFbi-Aax.js replaces index-CUJ1rnnc.js; dist NOT committed per CONTRACTS rule #13 / P2-S4 deviation, tree stays dirty for P5-S6); (4) sanity: backend pytest suite still 1247 passed (no backend files touched — undo reuses the existing PUT /span/{id}/text endpoint). No commits made.

### Phase 4: Pause-after verification + Setup polish
- [x] TDD RED: write backend tests asserting pause_between_speakers_ms/pause_same_speaker_ms flow from config.json (via resolve_task_config, Plan G) into the TTSConfig used by render_audiobook, and that generated audio includes the pause (or that the TTSConfig carries the fields unchanged through _build_voice_config); assert defaults 500/250 ms
    **Note:** TDD RED done. Trace conclusion first: pause values were LOST between config and audio. Full chain traced: config.json → pydantic TTSConfig (app.py L244-258, defaults 500/250, byte-stable round-trip via /api/config, tested in app/test_api.py) → setup.ts loadConfig/buildConfigPayload (Plan G verified submission). Downstream, NOTHING consumes them: TTSEngine.__init__ (app/tts.py) ignores the pause keys; render_audiobook had no config channel; merge_audiobook (api_export.py) is plain ffmpeg concat (no silence); combine_audio_with_pauses (app/tts.py, defaults 500/250) has had zero callers since legacy generate_audiobook.py was deleted (git b82fdfe). Added 5 RED tests in tests/pipeline/test_tts_integration.py (new class TestPauseConfigBoundary, +96 lines): default 500/250 on batch chunks; configured values flow unchanged; per-field fallback on partial config; end-to-end config.json→load_tts_config→render boundary (ALEXANDRIA_CONFIG_PATH monkeypatch, per test_resolve_task_config.py convention); individual mode accepts tts_config without crashing. All 5 RED pre-fix: 4 TypeError (unexpected kwarg tts_config — boundary had no channel), 1 KeyError (chunk lacked pause keys). app/tts.py untouched.
- [x] Implement: only if the tests expose a break — fix the passthrough at the tts_integration boundary (never in app/tts.py); polish the Setup tab pause fields (labels/tooltips) to make them discoverable
    **Note:** Implement done. The passthrough WAS broken (proven in P4-S1) and is now fixed AT the tts_integration boundary: (1) new module constants PAUSE_BETWEEN_SPEAKERS_MS=500 / PAUSE_SAME_SPEAKER_MS=250 (mirror TTSConfig pydantic defaults); (2) new _resolve_pause_ms(tts_config) helper — per-field fallback to defaults when a field is absent; (3) _build_chunks gained optional keyword-only pause_between_speakers_ms/pause_same_speaker_ms params (backward compatible — existing callers/tests unchanged) and attaches both keys to every chunk (the engine ignores unknown chunk keys — verified _local_batch_custom/_sequential_custom use .get on known keys only); (4) render_audiobook gained tts_config: dict|None keyword param (wire-through only — api_export.py render path does NOT yet pass it, that's a future call-site; documented in docstring). Individual mode accepts tts_config but has no chunk channel (generate_voice signature has no chunk dict — engine contract), so global pauses apply at the batch/merge level only. Setup tab polish: frontend/index.html pause field form-texts now read 'Silence between different speakers during merge (default 500 ms)' / 'Silence when the same speaker continues during merge (default 250 ms)' with title tooltips on labels+inputs (ids/value/min/step unchanged); frontend/src/tabs/setup.ts got doc comments at the load (L212-217 area) and submit (pause_between_speakers_ms/pause_same_speaker_ms) sites — no behavior change. app/tts.py byte-for-byte untouched; TTSConfig schema/defaults untouched; P1's _enforce_single_speaker untouched. RESIDUAL GAP (documented in log L183): combine_audio_with_pauses (app/tts.py) still has no callers — actual silence insertion at merge is out of boundary scope; chunk-carried values are the pass-through contract per the plan's 'or' clause.
- [x] TDD GREEN: run the pause tests; assert global pause config reaches TTS with defaults intact
    **Note:** TDD GREEN done. python -m pytest tests/pipeline/test_tts_integration.py -q → 75 passed (70 pre-existing + 5 new TestPauseConfigBoundary tests), 0 failed. The 5 new tests went RED (P4-S1) → GREEN (P4-S2) without touching app/tts.py or TTSConfig. Global pause config now demonstrably reaches the render boundary with defaults intact: chunks carry pause_between_speakers_ms/pause_same_speaker_ms == 500/250 when config omitted, == configured values when tts_config provided, per-field default fallback on partial config, and the end-to-end config.json → load_tts_config → render_audiobook path carries 900/400.
- [x] Verify: `pytest tests/pipeline -q` green; `npx tsc --noEmit` exit 0; `npm test` green
    **Note:** Verification done. python -m pytest tests/pipeline -q → 1252 passed (baseline 1247 + 5 new), 0 failed, 1 pre-existing deprecation warning. Guard tests/pipeline/test_legacy_removed.py → 12/12 passed. ruff check on changed Python files → All checks passed. Frontend: npx tsc --noEmit → exit 0; npm test → 338 passed / 7 files (baseline 338). npm run build → dist regenerated (frontend/dist: index.html updated with new tooltip/form-text markup + new hashed asset; git status shows D index-CUJ1rnnc.js / M index.html / ?? index-CFbi-Aax.js) — NOT committed per CONTRACTS rule #13 (P5-S6 commits dist). Files changed in scope: app/pipeline/tts_integration.py, tests/pipeline/test_tts_integration.py, frontend/index.html, frontend/src/tabs/setup.ts (+ regenerated app/static/dist/ artifacts). app/tts.py, TTSConfig, schema, api_export.py all untouched.

### Phase 5: Doc drift cleanup + feature-wide smoke verification
- [x] Archive `artifacts/designs/completed/DD-frontend-rebuild-per-task-llm-config.md` (stale, superseded by the universal-upgrade config work) — move to an archive location or annotate superseded per repo convention
    **Note:** Archived stale DD-frontend-rebuild-per-task-llm-config.md (276 lines) via the repo's in-place SUPERSEDED-header convention. Convention research: no artifacts/designs/archived/ dir exists anywhere (designs/ contains only completed/, parts/, pending/, process/); git history of the file is only the initial publish commit 8c3ed21, so no git-move precedent. Precedent found in prior stale artifacts: parts/epub-audiobook-pipeline-rewrite/walk-definitions.md carries a '> **Status:** SUPERSEDED — stale v2 design. Do NOT use.' blockquote, and completed/ DDs (DD-epub-audiobook-pipeline-rewrite v1/v2/v3) stay in place with status headers. Followed that exact pattern: added a SUPERSEDED blockquote under the title (stating supersession by DD-universal-upgrade, Plan G resolve_task_config replacing resolve_task_llm, and that the file stays in completed/ per repo convention) and updated the Status line to 'Superseded (originally Completed...)'. File NOT deleted (record preserved); no move performed (no archive dir exists). This satisfies the DD-universal-upgrade.md L129 doc-drift entry ('stale artifact ... archived/cleaned').
- [x] Fix any remaining README.md claims that mismatch delivered UI (verify voice review surface, alias editing, per-chunk progress wording post-Plans D/F/H)
    **Note:** README audit complete; ONE genuine mismatch fixed. Audit results: (a) voice review surface — NOT claimed anywhere: no 'voice review'/'audio review' claim in top-level README.md or frontend/README.md; L197 'Open the Voices tab to review the character list' means look-at (legitimate), L338 'Confidence review' and API section L469 describe the real junction review (Plan D). No fix. (b) alias editing — NOT claimed: all alias claims (README L25, L201, L268-269) say aliases are 'resolved automatically during annotation (walk 2c)' — matches delivered (not user-editable); no edit-aliases UI claim exists. No fix. (c) per-chunk progress — matches post-Plans D/F/H reality: README L47/L205/L336 and frontend/README.md L56/L79/L86 all state 'per-chunk completed/total counts with failure badges for individual renders, job-level progress for batch renders'; batch-default claim confirmed against code (RenderRequest.use_batch: bool = True, api_export.py L69; pipelineRenderAudiobook useBatch=true default, editor-pipeline.ts L103). Plan F wording preserved — no regression. No fix. (d) seed drift README.md:274 (Plan H) — GENUINE mismatch, FIXED: removed the bullet '- Optionally set a seed for reproducible output' from the Custom Voice Mode section. Evidence: delivered voice edit form (frontend/index.html #voice-edit-form L542-588) has only character-style/ref-audio/ref-text/type/alias-of fields — no seed input; voices.ts VoiceConfigRow.seed (L53-54) is display-only typing, never a form field; per-voice seed exists only as a backend DB column (VoiceUpdateRequest accepts seed, api_voices.py L58-77) with no UI to set it. The 9 pre-trained voices bullet (L272) and character-style bullet (L273) left intact — engine/data-level accurate (voice_config.voice preset names; character_style IS a delivered form field). parts/universal-upgrade/README.md audited fully (95 lines): decomposition-only content (parts table A-J, dependency graph, rounds, quality gates) — zero UI claims, nothing to fix.
- [x] Feature-wide verification: run the full backend suite (`pytest tests/pipeline -q --cov=app/pipeline --cov-report=term-missing`, record count/coverage), the frontend suite (`npm test`, `npx tsc --noEmit`), `npm run build` + `git diff --exit-code app/static/dist/` clean, and the guard `pytest tests/pipeline/test_legacy_removed.py -q` 12/12
    **Note:** Feature-wide verification — ALL GATES GREEN. Backend: `python -m pytest tests/pipeline -q --cov=app/pipeline --cov-report=term-missing` → **1254 passed** (baseline 1254 ✓), TOTAL coverage **93%** (3712 stmts, 278 miss). Per-module: tts_integration.py **97%** (197 stmts, 5 miss — missing 394, 419, 569-570, 740, NONE in the new P1/P4 code ranges L60-61, L167-298, L588-707); api_operations.py **99%** (210 stmts, 3 miss — missing 69, 313-314, NONE in single_speaker endpoints L190-246); api_export.py **92%** (632 stmts, 48 miss — render()/L223-279 and _run_render_job/L156-203 fully covered, incl. the fixer's tts_config wiring; missing lines are unrelated legacy export paths). All new code ≥80% gate: new paths are effectively 100% covered within their ranges; NO new tests needed. Guard: `python -m pytest tests/pipeline/test_legacy_removed.py -q` → **12/12 passed** (file untouched). Frontend: `npm test` (vitest) → **338 passed** (7 files; baseline 338 ✓); `npx tsc --noEmit` → **exit 0**. Build: `npm run build` → **succeeded** (vite v8.2.0, 22 modules, dist regenerated). Byte-reproducibility confirmed (CONTRACTS rule #13): rebuilt bundle hash is IDENTICAL to the already-untracked working-tree file (index-CFbi-Aax.js, 88.99 kB) — a rebuild produces the same artifact, no drift. Dist dirty state (expected, P2-P4 + this phase): `D app/static/dist/assets/index-CUJ1rnnc.js`, `M app/static/dist/index.html` (+20/-202), `?? app/static/dist/assets/index-CFbi-Aax.js`. The `git diff --exit-code app/static/dist/` clean gate is verified post-P5-S6 (exec-manager-owned); I made NO commit.
- [x] Security review: single-speaker toggle writes a book attribute (validate book_id ownership, parameterized SQL), undo uses server-validated primitives only (no client-supplied targets), pause config passes through pydantic validation; document findings
    **Note:** SECURITY REVIEW — verdicts per item (all PASS, 2 residual notes). [1] Single-speaker GET /book/{book_id}/single_speaker (api_operations.py L208-224): PASS — parameterized `SELECT single_speaker FROM book WHERE id = ?` with placeholder binding (L218-220); unknown book → 404 `Book '{book_id}' not found` (L221-222), existence check by id prevents info leak (no row → uniform 404, no content diff); returns 0/1 normalized int. [2] PUT /book/{book_id}/single_speaker (L227-246): PASS — body is pydantic `SingleSpeakerUpdateRequest(BaseModel)` with `single_speaker: bool = Field(...)` (L199-205), so 422 on missing/invalid body, no type confusion; existence pre-check via parameterized SELECT (L238) → 404; value normalized `1 if request.single_speaker else 0` before parameterized UPDATE (L242-244). NO string interpolation of book_id anywhere in either endpoint. Privilege/consistency: write path is book-scoped (id-validated), identical auth posture to the existing PUT /span/{id}/text and other pipeline endpoints (local tooling, no auth layer per Plan C contract) — no escalation vs any existing write; toggling only flips a column, never mutates script (CONTRACTS decision #9, enforcement at render boundary only). [3] Undo (frontend editor-pipeline.ts pushUndoEntry L276-279 / undoLastSpanEdit L298-324): PASS — undo REUSES the existing server-validated PUT /span/{id}/text primitive (pipelineUpdateSpanText), no new backend surface; server re-validates: empty-text → 400 (L175-177), unknown span → 404 (L180-182), parameterized UPDATE (L184). Client supplies only spanId (from server-fetched _cachedSpans) + priorValue (cached span text) — no client-supplied arbitrary targets; failure re-pushes entry + toast, no data loss. [4] Pause config flow: PASS with residual note — config.json tts section → `load_tts_config()` (app/utils.py L228-231, raw dict) → `_resolve_pause_ms` (tts_integration.py L283-298) reads via dict .get() with int defaults PAUSE_BETWEEN_SPEAKERS_MS=500/PAUSE_SAME_SPEAKER_MS=250 → carried on chunk dicts (L238-280) which the engine ignores (unknown keys). Values NEVER reach SQL, shell, or filesystem paths — zero injection surface. config.json is written only via POST /api/config, validated by pydantic TTSConfig (app.py L244-258: pause_between_speakers_ms: int = 500, pause_same_speaker_ms: int = 250). RESIDUAL 1: load_tts_config does not re-validate at read — a hand-edited config.json non-int pause value would land only in engine-ignored chunk keys (no boundary impact). RESIDUAL 2: pydantic bool lax coercion accepts '1'/'0'/'true'/'false'/'yes'/'no'/'on'/'off' strings for the toggle — bounded, all else 422, value normalized to 0/1; informational only. CONTRACT OBSERVATIONS for exec-manager: GET+PUT /api/pipeline/book/{book_id}/single_speaker are NOT yet registered in CONTRACTS.md endpoint tables (P2-S4 flagged this) — recommend registering them at final stamp; rule #1 (7 api_* modules) satisfied, both live in api_operations.py.
### Phase 6: Contextual review — ±2 neighboring spans (DD UX workflow #5)
- [x] TDD RED (backend): write tests in tests/pipeline/test_review.py (new class, e.g. TestReviewItemNeighborContext) asserting get_review_items enriches EVERY item with `neighbors: {"before": [...], "after": [...]}` — up to 2 span texts in presentation order around the item's span reference (character_span junction items resolve via related_entity_id; walk items of kind 'instruction' resolve via target_id; all other kinds — character_book, character_scene, voice_profile, voice_assignment — get empty lists); extend _populate_storage or add a dedicated fixture with ≥5 spans in a defined presentation order so the full ±2 window is exercised; assert edge cases: item targeting the book's FIRST span → empty before, item targeting the LAST span → empty after; also extend tests/pipeline/test_api.py TestReviewEndpoint with an assertion that GET /review/{book_id} items carry the neighbors key
    **Note:** TDD RED confirmed: 10 failures. New class TestReviewItemNeighborContext in tests/pipeline/test_review.py (8 tests) with dedicated _populate_neighbor_storage fixture (5 spans sp-nb1..sp-nb5 in defined presentation order, one span/paragraph, single scene/chapter/book; character_span junctions targeting first/middle/last spans, character_book junction, walk items of kinds instruction/voice_profile/voice_assignment, plus an out-of-book span target test seeded in-test). Tests assert: every item carries neighbors {before, after}; middle span gets full ±2 window in presentation order; FIRST span → empty before; LAST span → empty after; instruction walk item resolves via target_id; character_book + voice_profile + voice_assignment → empty lists; character_scene junction → empty lists; instruction walk item targeting a span outside the book → empty lists. Also updated TestWalkItemsInQueue::test_walk_item_shape_is_table_columns_only to include 'neighbors' in the walk-item shape (contract change: EVERY item now carries neighbors) — this test fails RED until implementation. Added TestReviewEndpoint::test_review_items_carry_neighbors in tests/pipeline/test_api.py asserting GET /review/{book_id} items carry the neighbors key with list-shaped before/after (api fixture's only review item is character_book — no span ref → empty lists; resolution rules covered at manager level). All 10 RED before implementation: 8 new neighbor tests + 1 updated shape test + 1 API test.
- [x] Implement (backend): in review.py add a module-level helper that loads the book's spans in presentation order once per get_review_items call (span_presentation VIEW join chain per walk_2i `_load_spans_in_presentation_order`, ORDER BY sp.global_index) and attaches `neighbors` to each item dict by resolving its span reference (character_span → related_entity_id; instruction walk item → target_id; no span reference → empty before/after lists); wire it into ReviewManager.get_review_items so the GET /review/{book_id} response carries the enrichment — api_review.py needs no change (response = manager output)
    **Note:** Implemented in app/pipeline/review.py only (api_review.py unchanged — GET /review/{book_id} returns manager output verbatim, so the enrichment rides through automatically; CONTRACTS rule #1 satisfied, no new module). Added module-level section 'Contextual review - +/-2 neighboring spans' with: NEIGHBOR_WINDOW = 2; _load_spans_in_presentation_order(book_id, storage) — byte-identical join chain to walk_2i (span_presentation sp JOIN span s ON sp.id=s.id JOIN paragraph_span ps ... JOIN book_chapter bc ON bc.child_id=cs.parent_id WHERE bc.parent_id=? ORDER BY sp.global_index), returning [{id, text}] with text normalized '' for NULL; _resolve_span_reference(item) — character_span junction -> related_entity_id, walk item kind=='instruction' -> target_id, all other kinds -> None; _build_neighbors(span_ref, ordered_spans, window=2) — slice ordered_spans[max(0,idx-window):idx] / [idx+1:idx+1+window], empty lists when span_ref None or not found. get_review_items step 5: loads spans ONCE per call (guarded by `if items:` so empty queues run zero extra queries) and attaches neighbors to every item. Resolution rules implemented exactly per spec. Full backend suite: 1263 passed (1254 baseline + 9 new), ruff clean on review.py + both test files.
- [x] TDD GREEN (backend): run `pytest tests/pipeline/test_review.py -q` and `pytest tests/pipeline/test_api.py -q`; assert neighbors are correct in presentation order, the first/last-span edge cases yield empty before/after, and no-span-ref items carry empty lists
    **Note:** TDD GREEN (backend): pytest tests/pipeline/test_review.py -q -> 68 passed (59 baseline + 9 new/updated incl. the walk-item shape test now asserting neighbors). pytest tests/pipeline/test_api.py::TestReviewEndpoint -q -> 2 passed (original + test_review_items_carry_neighbors). Assertions verified: middle span gets full +/-2 window in presentation order (before=['Span 1 text','Span 2 text'], after=['Span 4 text','Span 5 text']), FIRST span -> empty before, LAST span -> empty after, no-span-ref kinds (character_book/character_scene/voice_profile/voice_assignment) -> empty lists, out-of-book span target -> empty lists. Full suite: 1263 passed (1254 baseline + 9 new). ruff clean.
- [x] TDD RED (frontend): write tests in frontend/tests/frontend/test_editor.test.ts under a new describe (e.g. 'Editor Tab — Review Context Neighbors (Plan J, Phase 6)') asserting renderReviewItem renders a muted context block showing before/after neighbor texts when present (inline item fixtures with neighbors), renders NO context block when neighbors is empty, and renders NO context block when the field is undefined (existing fixtures like MOCK_REVIEW_ITEMS stay valid — field is optional)
    **Note:** TDD RED confirmed (frontend): added describe 'Editor Tab - Review Context Neighbors (Plan J, Phase 6)' in frontend/tests/frontend/test_editor.test.ts (inserted after the existing renderReviewItem describe, 5 tests). Tests: (1) renders muted context block with before/after neighbor texts + existing card markup (confidence badge, accept/reject/override buttons, data-item-id) preserved; (2) NO block when neighbors {before:[],after:[]}; (3) NO block when neighbors undefined (uses MOCK_REVIEW_ITEMS[0] — fixtures stay valid, field optional); (4) block rendered when only one side non-empty; (5) neighbor text escaped. Run: npx vitest run tests/frontend/test_editor.test.ts -t "Review Context Neighbors" -> 3 failed (block-present tests 1,4,5), 2 passed (no-block tests 2,3) — exactly the expected RED.
- [x] Implement (frontend): extend the ReviewItem interface (editor-pipeline.ts) with `neighbors?: { before: string[]; after: string[] }`; update renderReviewItem to render the neighbors as a muted/collapsible context block (e.g. a `<details>` or muted div listing preceding/next span text) when either list is non-empty; keep the existing card markup (confidence badge, accept/reject/override buttons) unchanged
    **Note:** Implemented in frontend/src/tabs/editor-pipeline.ts only. (1) ReviewItem interface extended with `neighbors?: { before: string[]; after: string[] }` — optional, so existing fixtures/payloads stay valid. (2) renderReviewItem now builds a muted collapsible `<details class="review-neighbors mt-2">` block with summary 'Context - neighboring spans' listing before/after neighbor texts (each line prefixed '↑ before' / '↓ after' via muted text-secondary span, text escaped through escapeHtml) when neighbors is present AND either list is non-empty; no block when neighbors is empty ({before:[],after:[]}) or undefined. The block is inserted inside the card-body AFTER the existing d-flex row — the confidence badge, kind/table text, reason, and accept/reject/override button group markup are byte-identical to before. VERIFIED GREEN: npx vitest run tests/frontend/test_editor.test.ts -t "Review Context Neighbors" -> 5/5 passed; npx tsc --noEmit exit 0.
- [x] TDD GREEN + Verify: `pytest tests/pipeline -q --cov=app/pipeline --cov-report=term-missing` green with ≥80% coverage on the new review.py code; guard `pytest tests/pipeline/test_legacy_removed.py -q` 12/12; `ruff check app/pipeline/review.py` clean; `npx tsc --noEmit` exit 0 (from frontend/); `npm test` green
    **Note:** Scope: review response enrichment + frontend rendering + tests ONLY. No changes to walk logic, junction actions, value-restore, snapshots, undo, single-speaker, pause, or app/tts.py (byte-for-byte untouchable). No new backend modules (CONTRACTS rule #1) — enrichment lives in review.py (wired through api_review.py's existing GET /review/{book_id}).
      TDD GREEN + full verification (all pass): (1) pytest tests/pipeline -q --cov=app/pipeline --cov-report=term-missing -> 1263 passed (1254 baseline + 9 new); app/pipeline/review.py 160 stmts / 0 missed = 100% coverage (well above the >=80% gate); TOTAL 93%. (2) Guard: pytest tests/pipeline/test_legacy_removed.py -q -> 12/12 passed. (3) ruff check app/pipeline/review.py -> clean. (4) npx tsc --noEmit (from frontend/) -> exit 0. (5) npm test (frontend/, vitest) -> 343 passed (338 baseline + 5 new), 7 files. Changed-file scope verified via git status: only app/pipeline/review.py, tests/pipeline/test_review.py, tests/pipeline/test_api.py, frontend/src/tabs/editor-pipeline.ts, frontend/tests/frontend/test_editor.test.ts touched by Phase 6 (other working-tree modifications are prior-phase P1-P5 work awaiting the P7-S3 feature commit). api_review.py unchanged (returns manager output verbatim). No git commits made.

### Phase 7: Final QA gate + feature commit
- [x] Code review pass via exec-manager QA-Reviewer; fix MINOR findings
    **Note:** QA-Reviewer Round 3 (terminal gate) PASS, 0 issues (qa-reviewer L48 → fixer L39/L40 → QA R3). All 7 checks PASS: ruff clean on 7 changed backend/test files, tsc exit 0, app/tts.py git-diff EMPTY (rule #2), exactly 7 api_* modules (rule #1), guard 12/12, no legacy resurrection, no .skip/.only/console.log. R2 DOC_GAP re-verified: zero overclaim wording across README.md/README_CN.md/frontend/index.html/setup.ts; dist fresh (index-b5qmNMz7.js). QA-TestAnalyzer PASS (backend 1263 @93% TOTAL — review.py 100%, tts_integration 97%, api_operations 99%, api_export 92% legacy-only; frontend 343/7 files; all 8 new suites discriminating). QA-DocsAnalyzer MINOR_ISSUES_PASS (dist fresh, SUPERSEDED annotation accurate, 6-item P7-S2 CONTRACTS checklist produced). 2 fix cycles used (R1: PLANNING_GAP→AMEND Phase 6 + tooltip overclaim; R2: README DOC_GAP). No MINOR findings to fix — proceeding to P7-S2.
- [x] CONTRACTS final stamp: register the enriched review response in CONTRACTS.md — add a note on the GET /api/pipeline/review/{book_id} contract row and the ReviewItem DTO row describing the new `neighbors: {"before": [...], "after": [...]}` fields (empty lists for items without a span reference)
    **Note:** CONTRACTS final stamp done — ALL SIX QA-DocsAnalyzer (log L96) checklist items registered in artifacts/designs/parts/universal-upgrade/CONTRACTS.md, append-only, verified against code before writing (api_operations.py L199-246 endpoints, tts_integration.py helpers, review.py neighbors). (1) GET /api/pipeline/book/{book_id}/single_speaker added to New-endpoints table (api_operations, Plan J; 200 {book_id, single_speaker: 0|1}, 404 unknown book, parameterized SELECT, unset reads 0). (2) PUT /api/pipeline/book/{book_id}/single_speaker added (body {single_speaker: bool} via pydantic SingleSpeakerUpdateRequest, 200 {status:'ok', book_id, single_speaker: 0|1}, 404, 422, parameterized UPDATE, 0/1 normalization). (3) tts_integration section retitled '(Plans B/C; Plan J: pause passthrough + single-speaker enforcement)'; STALE render_audiobook row extended with the tts_config kwarg (resolved by api_export render() via load_tts_config at request time; pause values ride onto batch chunk dicts); new rows added for _enforce_single_speaker (decision #9, step 4b, NARRATOR entry guaranteed, batch+individual one dict), _resolve_pause_ms (per-field fallback 500/250), PAUSE_BETWEEN_SPEAKERS_MS/PAUSE_SAME_SPEAKER_MS constants, _build_chunks keyword-only pause kwargs (backward compatible; per-span pause_after NOT restored — cannot-restore #5). Decision #9 row verified ALREADY present (no change). (4) neighbors enrichment registered in BOTH the GET /review/{book_id} Modified row (character_span->related_entity_id, instruction->target_id, empty lists for character_book/character_scene/voice_profile/voice_assignment + first/last-span edges; api_review.py unchanged) and the ReviewItem DTO row (frontend marks field optional). (5) SingleSpeakerUpdateRequest added to DTOs Created table. (6) Plan J status-line stamp prepended NEWEST-FIRST (convention verified: existing line reads I, H, A, B, C, D, E, F, G — I/H were prepended) as '**Plan J (Single-Speaker, Undo & Iteration UX) IMPLEMENTED + QA-VALIDATED 2026-08-07** — commit hash at P7-S3 (exec-manager-owned feature commit)' + full deliverable summary (enforcement, endpoints, toggle, undo stack, pause passthrough, neighbors, doc drift, gates 1263 pytest/93%/guard 12/12/vitest 343/tsc exit 0/dist index-b5qmNMz7.js, security PASS, QA R3 PASS). Verified post-edit: no duplicate registrations (2 single_speaker endpoint rows exactly; neighbors in exactly the 2 registration spots + stamp prose; 1 Plan J stamp), table structure intact, only CONTRACTS.md modified by this step. Commit (P7-S3) NOT made — exec-manager owns it.
- [x] Commit: `feat(pipeline): single-speaker render, undo, global pause, contextual review and doc cleanup`
    **Note:** Committed per Plan I pattern. feat commit 1ac5ea8 'feat(pipeline): single-speaker render, undo, global pause, contextual review and doc cleanup' (21 files, +1892/-95: backend tts_integration/api_operations/review/api_export + 3 test files, frontend index.html/editor-pipeline/editor/projects/setup + 2 test files, README.md + README_CN.md doc-drift fixes, DD-frontend-rebuild SUPERSEDED annotation, upstream ledger single_speaker endpoint append w/ dated note, plan annotations, regenerated dist index-b5qmNMz7.js replacing index-CUJ1rnnc.js); docs commit 7913e99 'docs(contracts): stamp Plan J IMPLEMENTED + QA-VALIDATED commit hash' (delta ledger Plan J stamp with real hash 1ac5ea8 + 6 registrations). QA: 3 rounds / 2 fix cycles (R1 PLANNING_GAP→AMEND Phase 6 + tooltip overclaim; R2 README DOC_GAP; R3 terminal PASS 0 issues). Gates: pytest 1263 @93% TOTAL, guard 12/12, vitest 343/7 files, tsc exit 0, ruff clean, dist fresh + byte-reproducible, app/tts.py untouched. Feature complete: all 8 DD capabilities delivered.

## Completion Criteria

- book.single_speaker enforced at render boundary (NARRATOR override) with faithful annotated-script export; toggle UI round-trips the value
- Undo button reverts span-text edits via transactional/snapshot primitives with disabled state
- Global pause-after verified end-to-end (config → TTS), defaults 500/250 ms; no per-span field resurrected
- Stale frontend-rebuild design doc archived; README claims match delivered UI
- All suites green, guard 12/12, build + dist committed, security review documented — feature complete (all 8 DD capabilities delivered); contextual review (±2 neighboring span texts) delivered on the Review tab (DD UX workflow #5)

## References

- `artifacts/designs/pending/DD-universal-upgrade.md` — cap7, P6-C7b, FP3+FP4+A4-r, decisions #5/#11, open items #5/#7, doc-drift section
- `artifacts/designs/parts/universal-upgrade/CONTRACTS.md` — single_speaker, value-restore, snapshot restore
- Prior: Plans D, I, G (primitives), Plan Q (audio surface removal baseline)
