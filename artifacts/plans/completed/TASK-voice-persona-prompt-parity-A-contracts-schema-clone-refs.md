# Task: Voice-Persona-Prompt Parity A — CONTRACTS registration, schema, and pipeline-native clone reference backend

## Problem Statement

This is part A of the implementation plan for `artifacts/designs/pending/DD-voice-persona-prompt-parity-browser-external-validation.md` (the second pending DD; the first, `DD-combined-walks-2b-2d-workbench.md`, is complete and committed at `bd133cf`). This part delivers the persistent foundation and the clone-reference backend surface only; persona and prompt-config surfaces land in part B, frontend in part C, external validation in part D. The third DD (`DD-pipeline-ux-manual-walk-execution.md`) is out of scope and must remain untouched.

Three append-only contract registrations MUST be added to `artifacts/designs/parts/epub-audiobook-pipeline-rewrite/CONTRACTS.md` BEFORE any production code is written (the DD requires "append these records to CONTRACTS.md before code is written"): `PipelineVoiceCloneReferenceAPI.v1`, `PipelineCharacterPersonaAPI.v1`, `PipelineWalkPromptConfigRevisionAPI.v1`. New tables for clone references, persona revisions, and prompt-config revisions are added idempotently to `app/pipeline/schema.py`. The advertised surface is pipeline-only, preserves `app/tts.py` byte-for-byte and the protected combined-walks workbench, and introduces no legacy endpoint, manifest, prompt-file module, or standalone persona subsystem.

## Dependencies

- Design: `artifacts/designs/pending/DD-voice-persona-prompt-parity-browser-external-validation.md`.
- Contracts ledger: `artifacts/designs/parts/epub-audiobook-pipeline-rewrite/CONTRACTS.md` (append-only; prior Combined Workbench § must not be amended).
- `app/pipeline/schema.py` (idempotent DDL + guarded ALTER migrations), `app/pipeline/adapter.py` (`PipelineStorage` ABC + `SQLiteAdapter`/`InMemorySQLiteAdapter`, `transaction()` = BEGIN IMMEDIATE owner-thread, `ConcurrentTransactionError` → 503 + `Retry-After: 5`), `app/pipeline/api_voices.py` (existing voice CRUD unchanged), `app/pipeline/api_export.py` (`_AUDIO_EXTENSION_MEDIA_TYPES`, `_media_type_from_path`, containment helpers, `get_tts_engine`), `app/pipeline/api.py` (`get_storage`, combined router re-exports).
- Invariants: routes stay under `/api/pipeline` in existing `api_*` modules; all five voice types and existing voice CRUD unchanged; `app/tts.py` byte-identical; legacy guard `test_legacy_removed.py` 12/12; voice reference content stored beneath an approved application root; ownership/path/content/size/duration checks enforced at the boundary; no filesystem-path exposure in any API response; no legacy manifest.

## Phases

### Phase 1: Contract registration and schema foundation (S1)
- [x] Append the three design-time contract entries (`PipelineVoiceCloneReferenceAPI.v1`, `PipelineCharacterPersonaAPI.v1`, `PipelineWalkPromptConfigRevisionAPI.v1`) to `artifacts/designs/parts/epub-audiobook-pipeline-rewrite/CONTRACTS.md` exactly as specified in the DD (schemas, request/response, owners, compatibility), noting they are parity-owned and do not amend the combined-walks workbench contract. Validate the exact CONTRACT blocks against DD lines 107-125.
    **Committed:** Verified by exec-manager (2026-08-14): the three contract entries (PipelineVoiceCloneReferenceAPI.v1, PipelineCharacterPersonaAPI.v1, PipelineWalkPromptConfigRevisionAPI.v1) are present in CONTRACTS.md § Voice / Persona / Prompt Parity Contracts at lines 1120-1140 with exact schemas/routes/OWNER/COMPATIBILITY matching plan spec. Confirmed committed in 586d8e4 via git show (3 'CONTRACT ID: Pipeline' hits). Step was left unmarked despite completed work; no re-execution needed. DD-voice-persona-prompt-parity-browser-external-validation.md is MISSING from designs/pending/ — entries validated against plan text + CONTRACTS registration note instead of DD lines 107-125 (recorded blocker).
    **Notes:** Contract IDs, schema lists, route paths, and OWNER/COMPATIBILITY strings must match the DD verbatim. No prior contract lines amended; append-only.
- [x] Add three new tables to `app/pipeline/schema.py` DDL with the existing idempotent/guarded-migration pattern: `clone_reference`, `persona_revision`, `prompt_config_revision`. Define columns per the DD contracts (e.g. clone_reference: `reference_id PK, voice_id FK voice_config, owner_id, relative_path, original_filename, media_type, byte_size>=0, duration_ms>=0, sha256, created_ms, deleted_ms NULL`, unique sensible index; persona_revision: `persona_id PK, character_id FK, book_id NULL, revision>=0, fields_json, evidence_json, aliases_json, scene_scope, review_state, protected CHECK(0|1), voice_consequences_json, author_id, created_ms, superseded_by NULL`, index by character; prompt_config_revision: `revision_id PK, book_id FK, task, base_revision NULL, source_layers_json, effective_prompt NULL, settings_json, raw_json NULL, validation_json, author_id, created_ms, superseded_by NULL`, index by book+task). Include any additive migration to backfill nothing (no data backfill needed).
    **Committed:** clone_reference, persona_revision, prompt_config_revision tables added idempotently to schema.py with FK/CHECK/index. Committed in dd53d41.
    **Notes:** Match the `walk_override`/`workbench_*` DDL style: parameterized, no FK cycles, CHECK constraints for enums (scene_scope `book|scenes`, review_state `draft|needs_review|accepted|rejected`). The base tables are created in `create_schema()` so new DBs get them; guarded ALTER/INSERT migrations keep existing DBs consistent. `owner_id`/`author_id` are strings (no auth/principal infra exists — single-user local; principal defaults to a stable sentinel like `"local"`).
- [x] Verify: `pytest -q tests/pipeline/test_schema.py tests/pipeline/test_schema_migration.py` green; new tables present on fresh `create_schema()`; schema tests cover the three new tables' columns/CHECK constraints.
    **Committed:** Schema + migration tests cover the 3 new tables; test_schema.py + test_schema_migration.py pass (in the 232 focused + full 1530 suite). Committed in dd53d41.

### Phase 2: Clone reference storage adapter methods (S2)
- [x] Add `PipelineStorage` ABC methods for clone references: `insert_clone_reference`, `list_clone_references(voice_id, owner_id)`, `get_clone_reference(reference_id)`, `tombstone_clone_reference(reference_id, owner_id)` (returns row or raises), `delete_clone_reference_row(reference_id)` (idempotent hard delete of a tombstoned row for cleanup), and `get_tombstoned_references_unreferenced(older_than_ms)` for bounded cleanup. Implement in both `SQLiteAdapter` and `InMemorySQLiteAdapter`. Use parameterized SQL, ensure owner scoping in list/get/delete.
    **Committed:** Adapter clone-reference methods (insert/list/get/tombstone/hard-delete/tombstoned-unreferenced lookup) in SQLite + InMemory; path-containment, media-sniff, size/duration helpers in clone_reference_media.py. Committed in dd53d41.
    **Notes:** `list` returns only the owner's references (DELETE is idempotent for the owner's already-deleted reference — the tombstoned row is returned for the owner and hidden from others; cross-owner access → 404). Schema: follow the existing per-method pattern in `adapter.py` (each method a thin prepared statement). Keep `ConcurrentTransactionError` behavior for writes inside `transaction()`.
- [x] Add a module-level application root + path-containment utility for reference storage (in `api_voices.py` or a small helper): a configured reference root (e.g. `designed_voices/references/` style under the same storage root as `_PREVIEWS_DIR`), `_canonical_contain(dest_root, candidate)` using `realpath` containment (reject traversal, symlinks), `_media_type_from_path`, and allow-list constants. Mirror the existing helpers in `app/pipeline/api_export.py` without moving those code paths.
    **Committed:** reference_root(), canonical_contain (realpath containment, reject traversal/symlink), configured_max_bytes/configured_max_duration_ms in clone_reference_media.py. Committed in dd53d41.
    **Notes:** Content verification (magic-byte/media sniff where feasible), byte-size limit, and decoded-duration limit come from the configured pipeline limits; reject symlinks and traversal; write partial-file removal on any failure. `relative_path` is the contained path stored, never exposed verbatim.
- [x] Implement clone-reference domain helpers in `app/pipeline/tts_integration.py` or a new storage helper module: `compute_sha256(fileobj)`, `probe_duration_ms(path)` (decode via the TTS engine seam or a bounded media probe), `copy_to_reference_root(src_upload, dest_root, original_filename)` returning `{relative_path, original_filename, media_type, byte_size, duration_ms, sha256}` with validation; enforce configured max bytes + max duration; wrap failures to remove partial files.
    **Committed:** compute_sha256, probe_duration_ms (via tts_engine seam), validate_and_copy (contained write, partial-file cleanup on failure) in clone_reference_media.py. app/tts.py untouched. Committed in dd53d41.
    **Notes:** No arbitrary engine arguments; use existing public seams (`get_tts_engine`). `app/tts.py` is NOT modified.
- [x] Verify: pytest green for new adapter + helper tests (use `InMemorySQLiteAdapter` and a temporary real directory for path-containment tests); coverage for new modules strong (`pytest --cov=app.pipeline.api_voices --cov=app.pipeline.adapter`).
    **Committed:** test_clone_reference_media.py (217 lines) + adapter tests green; coverage exercised. Committed in dd53d41.

### Phase 3: Clone reference API routes (S3)
- [x] Add routes in `app/pipeline/api_voices.py` (no new router family): `POST /api/pipeline/voices/{voice_id}/references` (multipart audio + optional `ref_text`; ownership bound to principal; validates voice_config exists and type in the five voice types; validates allow-list ext + content + byte-size + duration; writes file under reference root; inserts `clone_reference`; updates `voice_config.ref_audio` to the canonical relative reference selected by its owner if resolved; returns `201 {reference: CloneReferenceDTO, voice: VoiceConfigDTO}`), `GET /api/pipeline/voices/{voice_id}/references` (only owner's references → `CloneReferenceListResponse`), `GET /api/pipeline/voices/{voice_id}/references/{reference_id}/preview` (inline audio stream; JSON 404 on missing media), `GET .../download` (attachment-only via `Content-Disposition: attachment`), `DELETE .../{reference_id}` (204 after tombstoning metadata + deleting owned file; idempotent for owner's already-deleted; 404 cross-owner/cross-voice). Add a bounded startup/invoke cleanup that removes tombstoned unreferenced files after retention period, never following links.
    **Committed:** Routes POST/GET /voices/{id}/references + preview/download/delete added in api_voices.py: owner sentinel 'local', 201 DTO+voice, inline preview FileResponse w/ Range, attachment download, delete tombstone+file removal idempotent/cross-owner 404, invoke-time bounded cleanup, 503 contention. Committed in dd53d41.
    **Notes:** Use `storage.transaction()` for the insert+tombstone; map `ConcurrentTransactionError` → 503 `Retry-After: 5`; multipart upload via FastAPI `UploadFile` + `aiofiles`/sync `.read()` pattern consistent with legacy `clone_voices_upload` but pipeline-native (no manifest, no `CLONE_VOICES_DIR`). DTOs exported from the module. Pydantic DTO types: `CloneReferenceDTO{reference_id,voice_id,owner_id,relative_path,original_filename,media_type,byte_size,duration_ms,sha256,created_ms,deleted_ms}`, `CloneReferenceUploadRequest` (multipart, not JSON), `CloneReferenceListResponse{references: list[CloneReferenceDTO]}`, `CloneReferenceDeleteResponse` (204, empty). No filesystem path exposed in responses (return `relative_path` only as the contract-defined field).
- [x] Verify: `pytest -q tests/pipeline/test_voices.py tests/pipeline/test_api.py` green plus new test file `tests/pipeline/test_clone_references.py` covering upload/list/preview/download/delete, ownership, path traversal/symlink/malformed-media/size/duration rejection (422/400), partial-file cleanup on failure, cross-owner 404, idempotent delete, live 503+Retry-After:5 on contention, and that existing voice CRUD + five voice types remain selectable/previewable/assignable-by-resolved-ID.
    **Committed:** test_clone_references.py (439 lines) covers upload/list/preview/download/delete, ownership, traversal/symlink/malformed/size/duration rejection, partial-cleanup, cross-owner 404, idempotent delete, 503 contention; test_voices.py + test_api.py regression green. Committed in dd53d41.

### Phase 4: Gates and scoped commit (S4)
- [x] Run gates on committed state: `pytest -q` (full backend, must include prior 1447 + new tests), `pytest -q tests/pipeline/test_legacy_removed.py` (legacy guard must stay green), Pydantic/schema load, and confirm `git diff --exit-code` on `app/tts.py` is empty (byte-immutability).
    **Committed:** Full suite 1530 passed (from 1447 baseline; +83 new); legacy guard 12/12; git diff --exit-code app/tts.py empty; workbench CONTRACTS section byte-unchanged. Committed dd53d41.
    **Notes:** Record exact new-test delta and gate outputs. Ensure no persona/prompt API or frontend changes leak into this part (out of scope for A).
- [x] Commit part A as one or more scoped commits (e.g. `docs(contracts): register clone/persona/prompt parity v1 contracts`, `feat(pipeline): clone reference schema + adapter + API`). Note commit SHAs in plan annotations.
    **QAFix:** QA Round 2 PASS (2026-08-14): fix commit 12a584f (dedupe _VOICE_PERSONA_PROMPT_DDL in schema.py + TestCleanupExpiredReferences in test_clone_reference_media.py). Gates: 224 focused, 12/12 legacy, 1625 full suite, app/tts.py byte-identical.
    **Committed:** Two scoped commits: 586d8e4 (docs contracts) + dd53d41 (feat clone refs).
    **Notes:** Director performs the commit (sole git owner).

## Completion Criteria

- The three contract entries are appended verbatim to `CONTRACTS.md` before any production code; no earlier contract lines amended.
- `clone_reference`, `persona_revision`, `prompt_config_revision` tables exist on fresh `create_schema()` and are covered by schema/migration tests.
- Clone reference upload/list/inline-preview/download/delete enforces ownership, allow-listed bounded validated media, path/size/duration limits, partial-write cleanup, idempotent owner-safe delete (cross-owner 404), and 503 `Retry-After: 5` contention; no manifest dependency, no filesystem-path exposure.
- Existing five voice types and voice CRUD unchanged; reset resolved-ID assignment/narrator/preview behavior preserved; `app/tts.py` byte-identical.
- Legacy guard stays 12/12; full backend suite green; part A scoped commits recorded.
- No persona/prompt API surface, frontend, or external-validation changes in part A.

## References

- `artifacts/designs/pending/DD-voice-persona-prompt-parity-browser-external-validation.md`
- `artifacts/designs/parts/epub-audiobook-pipeline-rewrite/CONTRACTS.md` (§ Voice clone reference / character persona / walk prompt config revision)
- `artifacts/designs/completed/DD-epub-audiobook-pipeline-rewrite-v3.md`
- `artifacts/plans/completed/TASK-combined-walks-workbench-workbench-foundation.md` (prior completed plan conventions)
