# Task: Voice Config Edit Form

## Problem Statement

`PUT /api/pipeline/voices/{id}` exists in the backend but the Voices tab has **no edit form**: voices.ts renders cards with only name, type badge, and a Preview button (createVoiceCard L240). The `VoiceConfigRow` interface (L31) types only {id, name, voice, type?, description?} — the other 7 columns (character_style, ref_audio, ref_text, seed, adapter_id, adapter_path, alias_of) exist in the DB (voice_config table, 12 cols) and in the backend PUT contract but are unreachable from the UI. The only voice mutation in voices.ts is the NARRATOR selector (handleNarratorVoiceChange L445 sends {voice} only). Meanwhile the pre-rewrite app had a full voice edit surface that was lost — the design restores a **minimal** version first (DD cap4, FP5, decision #6): style (character_style), reference audio (ref_audio) and ref_text, the 5-type switch (custom/clone/builtin_lora/lora/design), narrator override, preview; the alias picker (dropdown of existing voices — no free-form map editor, DD open item #5) is included where feasible. Precedence: NARRATOR DB row wins over the UNKNOWN→NARRATOR fallback.

This is a pure frontend plan (Plan H): the backend PUT /voices/{id} and POST /voices/{id}/preview already exist and are tested. It depends on Plan E only (tab navigation must work before the Voices tab is reachable/editable).

## Dependencies

- Plan E (tab-navigation foundation — Voices tab must be reachable)
- `artifacts/designs/parts/universal-upgrade/README.md` — Plan H row
- Upstream: `artifacts/designs/parts/epub-audiobook-pipeline-rewrite/CONTRACTS.md` — VoiceConfig (12-col) + PUT /voices/{id} contract

## Phases

### Phase 1: Widen VoiceConfigRow and load full voice configs
- [x] TDD RED: write tests in frontend/tests/frontend/test_voices.test.ts asserting voice cards render style and type metadata when present, and that the edit form is populated from a fetched voice config (mock GET /voices returning a 12-column row)
    **Note:** TDD RED: appended 6 tests to frontend/tests/frontend/test_voices.test.ts (2 new describes: 'voice config style/type metadata (Plan H phase 1)' + 'voice config full-row carry for the edit form (Plan H phase 1)'). Fixture FULL_CATALOG_ROW carries all 12 voice_config columns (id, name, description, type, voice, character_style, seed, ref_audio, ref_text, adapter_id, adapter_path, alias_of). Assertions: (1) createVoiceCard renders a voice-style-badge with character_style text when present and omits it when absent; (2) type badge renders from a 12-column row ('>clone<'); (3) after loadVoices with a mocked GET /voices returning the 12-col row, getVoiceConfigRow('alice') returns the full row (edit-form data path) and undefined for unknown ids; (4) the rendered catalog card surfaces style+type text. Per task direction, the 'edit form populated' assertion is scoped to data-carrying (getVoiceConfigRow export) since the form markup is Phase 2. Result: 4 failed | 68 passed (66 baseline green) — RED is surgical; getVoiceConfigRow imported dynamically (established script.ts pattern) so the missing export cannot break module load of the whole file.
- [x] Implement: extend `VoiceConfigRow` (voices.ts L31) to all 12 columns (type, character_style, ref_audio, ref_text, seed, adapter_id, adapter_path, alias_of); ensure loadVoices (L305) keeps the existing catalog+narrator+character ledger wiring
    **Note:** Implemented in frontend/src/tabs/voices.ts: (1) VoiceConfigRow widened from 5 to all 12 voice_config columns — added character_style, seed, ref_audio, ref_text, adapter_id, adapter_path, alias_of (all optional string|null, keeping type?/description? optional and id/name/voice as before, so existing fixtures and the Phase-23 catalog tests compile unchanged). (2) New module-level voiceRowsById Map populated in registerVoiceCatalog (id → full row); exported getVoiceConfigRow(voiceId) returns the full 12-column row (undefined for unknown ids) — the data-carrying surface Phase 2's edit form consumes. (3) createVoiceCard now renders an optional voice-style-badge (bg-light border badge with title="Character style") when character_style is present; omitted when absent so existing card markup stays byte-compatible. loadVoices wiring untouched: registerVoiceCatalog still populates state.voicesNames + voiceNameToId, NARRATOR row tracking, renderNarratorSelector + renderVoiceCatalog all unchanged.
- [x] TDD GREEN: run test_voices tests; assert the extended type renders existing cards unchanged (backward-compatible rendering)
    **Note:** TDD GREEN: npx vitest run tests/frontend/test_voices.test.ts → 72/72 passed (66 baseline + 6 new). Backward-compatible rendering verified: all Phase-23 catalog/preview tests (createVoiceCard name/type-badge/preview-button assertions, NARRATOR exclusion, 'No voices available yet' empty state, '>unknown<' fallback for type-less rows) still pass unchanged — the style badge is conditionally rendered only when character_style is present. All 4 RED tests now green (style badge render/omit, full-row getVoiceConfigRow round-trip + undefined for unknown ids, catalog card surfacing style+type text). npx tsc --noEmit exit 0.
- [x] Verify: `npx tsc --noEmit` exit 0; `npm test` green
    **Note:** Phase 1 verification complete: (1) npx tsc --noEmit exit 0. (2) npm test (vitest run, full suite) → 6 files, 271/271 passed (265 baseline + 6 new from P1-S1). (3) git status: only frontend/src/tabs/voices.ts + frontend/tests/frontend/test_voices.test.ts modified plus the plan file (annotations) — zero backend files, zero dist changes (no build run this phase; build/dist commit is P5-S2/P5-S6). Working tree left dirty per task direction (single implementation commit at P5-S6). No legacy endpoints/modules touched; no file-upload surface added (ref_audio stays a path/URL string).

### Phase 2: Edit form markup and card wiring
- [x] TDD RED: write tests asserting clicking an edit action on a voice card opens a form (in voices tab) pre-filled with the voice's current values (style, ref audio, type, alias)
    **Note:** TDD RED: appended 6 tests to frontend/tests/frontend/test_voices.test.ts in a new 'voice config edit form (Plan H phase 2)' describe. Tests: (1) createVoiceCard renders a data-action="edit-voice" button with data-voice-id + fa-edit icon; (2) clicking Edit on the alice card opens #voice-edit-form (display != none) pre-filled from FULL_CATALOG_ROW — character_style 'warm', ref_audio '/refs/alice.wav', ref_text 'This is the reference line.', type 'clone', alias_of 'alice-base' (stale alias NOT in catalog still surfaced as the selected option), plus save/cancel buttons present; (3) type select exposes exactly ['custom','clone','builtin_lora','lora','design']; (4) alias_of select lists other existing voices, excludes the edited voice itself + NARRATOR, first option clears (''); (5) cancel closes the form; (6) openVoiceEditForm('ghost') shows 'Voice config not found: ghost' error toast and keeps the form closed. Per log L161 conventions: beforeAll registers initVoices() ONCE for the describe (each test dispatches DOMContentLoaded to wire the current fixture DOM — no stacked per-test listeners, log L141), openVoiceEditForm imported dynamically so RED cannot break module load. Result: 6 failed | 72 passed — surgical RED (all failures are 'expected null not to be null' on the missing edit button / missing export).
- [x] Implement: add edit-form markup in index.html (Voices tab region, L493) — fields: character_style, ref_audio (file or path text), ref_text, type select (custom/clone/builtin_lora/lora/design), alias_of select (populated from GET /voices — existing voices only, no free-form map), save + cancel buttons; wire each card's edit button in voices.ts (delegated click like preview-voice [data-action])
    **Note:** Implemented: (1) frontend/index.html — single shared #voice-edit-form card (hidden, style="display:none;") inserted inside #pipeline-voices-section between the Voice Catalog block and #character-ledger, with the 5 type options static (custom/clone/builtin_lora/lora/design), character_style + ref_audio + ref_text text inputs (ref_audio is a path/URL string — NO file upload), alias_of select (populated at open time), Save [data-action="save-voice"] + Cancel [data-action="cancel-voice"] buttons, #voice-edit-title span. (2) frontend/src/tabs/voices.ts — module-level editingVoiceId; exported openVoiceEditForm(voiceId) pre-fills from getVoiceConfigRow (error toast + stays closed for unknown ids) and shows the form; exported closeVoiceEditForm hides it; private populateAliasOptions builds alias options from voiceRowsById (option value = voice_config id per the file's name→id convention, label = name; excludes the edited voice itself + NARRATOR; '' first option clears the alias; a stale current alias absent from the catalog is appended selected so the row's real alias_of shows); exported saveVoiceConfig(voiceId) is a Phase-2 placeholder that closes the form — the real exclude_unset PUT body construction lands in P3-S2 per task direction (save button wired but not dead); createVoiceCard now renders the Edit button next to Preview inside a d-flex gap-2 wrapper (Preview classes unchanged — Phase-23 tests byte-compatible); initVoices gained the [data-action="edit-voice"] branch in the existing #voice-catalog delegated click handler (checked before preview) plus ONE new delegated click listener on #voice-edit-form for save/cancel — no extra DOMContentLoaded listener added (the wiring lives inside the existing initVoices callback).
- [x] TDD GREEN: run test_voices tests; assert form opens pre-filled and type select has the 5 options
    **Note:** TDD GREEN: npx vitest run tests/frontend/test_voices.test.ts → 78/78 passed (72 baseline incl. all Phase-1 tests + 6 new). Form opens pre-filled (style/ref-audio/ref-text/type/alias assertions green), type select verified to expose exactly the 5 supported types, alias options exclude self + NARRATOR with '' first (clear) option, cancel closes, unknown-id toast path green. Backward compatibility confirmed: all Phase-23 catalog/preview tests (createVoiceCard name/type-badge/preview-button assertions, NARRATOR exclusion, empty state, '>unknown<' fallback) pass unchanged — the Edit button addition is additive and the delegated preview branch still fires only for [data-action="preview-voice"].
- [x] Verify: `npx tsc --noEmit` exit 0; `npm test` green
    **Note:** Phase 2 verification complete: (1) npx tsc --noEmit exit 0. (2) npm test (full vitest run from frontend/) → 6 files, 277/277 passed (271 baseline + 6 new from P2-S1). (3) git status: only frontend/index.html + frontend/src/tabs/voices.ts + frontend/tests/frontend/test_voices.test.ts + plan file modified — zero backend files, zero dist changes (no build this phase; build/dist commit is P5-S2/P5-S6). Working tree left dirty per task direction. No legacy endpoints/modules touched; ref_audio stays a path/URL string (no file-upload surface added).

### Phase 3: PUT save with exclude_unset + type-aware validation
- [x] TDD RED: write tests asserting saving the form PUTs only changed fields (exclude_unset semantics preserved like handleNarratorVoiceChange L445), and that the type switch changes the voice_config type
    **Note:** TDD RED: appended 6 tests to frontend/tests/frontend/test_voices.test.ts in a new 'voice config save (Plan H phase 3)' describe. Tests: (1) PUTs only the edited fields — change ref_text only, assert API.put('/api/pipeline/voices/alice', { ref_text: 'A new reference line.' }) with NO other keys (exclude_unset pinned); (2) type switch sends the chosen type verbatim ({ type: 'design' }); (3) blanked character_style → { character_style: null } (clears per contract); (4) cleared alias selection ('' on the pre-filled stale 'alice-base') → { alias_of: null }; (5) success re-renders the catalog card from the PUT response (type 'design' + style 'calm' on the alice card), closes the form (display none) and fires the 'Voice config saved: Alice' success toast; (6) PUT rejection (422 via handleError) shows 'Failed to save voice config: 422: invalid voice type' error toast and KEEPS the form open. Per log L162 gotcha: the describe registers NO initVoices() and never dispatches DOMContentLoaded (the Phase-2 beforeAll listener persists for the rest of the file — a click-through save would fire saveVoiceConfig twice), so tests call the exported saveVoiceConfig directly with a constructed form DOM and use toHaveBeenCalledWith (call-count agnostic); vi.mocked(API.put).mockReset() in beforeEach prevents unconsumed mockResolvedValueOnce queues leaking between tests (log L139). saveVoiceConfig imported dynamically (established convention) so RED cannot break module load. Result: 6 failed | 78 passed — surgical RED (all 6 failures are 'saveVoiceConfig is not a function' since the export is the P3-S2 placeholder).
- [x] Implement: add save handler calling PUT /voices/{id} with the edited fields only (character_style, ref_audio, ref_text, type, alias_of); re-render the card from the response; error toast on failure (utils.ts toast pattern)
    **Note:** Implemented in frontend/src/tabs/voices.ts — replaced the Phase-2 saveVoiceConfig(voiceId) placeholder with the real exclude_unset PUT handler. Behavior: (1) guards getVoiceConfigRow(voiceId) — unknown id → 'Voice config not found' error toast and return; (2) reads the 5 form fields (character_style/ref_audio/ref_text inputs, type/alias_of selects) and diffs each against the current fetched row — a shared diffText closure handles the 3 text inputs (trimmed value vs row[key] ?? ''; changed → body[key], blanked → body[key] = null per 'setting a field to null clears it'); type is sent verbatim only when it differs from row.type ?? 'custom' (the select only offers the 5 static valid types, so no free-form type can be submitted); alias_of sent only when it differs from row.alias_of ?? '', with '' → null; untouched fields are omitted entirely (exclude_unset mirrors handleNarratorVoiceChange's {voice}-only body); (3) API.put<VoiceConfigRow>(`/api/pipeline/voices/${encodeURIComponent(voiceId)}`, body) — on success: voiceRowsById.set(voiceId, updated) so a subsequent edit pre-fills fresh values, the single card is re-rendered IN PLACE via card.outerHTML = createVoiceCard(updated) (chosen over re-fetch/re-render — no extra GET round-trip, the catalog's delegated click listener lives on #voice-catalog so Edit/Preview wiring survives; the PUT response IS the 12-column dict per contract), closeVoiceEditForm(), success toast 'Voice config saved: <name>'; (4) on failure: error toast 'Failed to save voice config: <msg>' (backend 422 surfaces through handleError) and the form stays open. handleNarratorVoiceChange and previewVoice untouched (Phase-4 regression concern). No index.html changes this phase (the form markup already carries the 5 type options + save/cancel buttons from P2-S2).
- [x] TDD GREEN: run test_voices tests; assert PUT body contains only edited keys and card re-renders
    **Note:** TDD GREEN: npx vitest run tests/frontend/test_voices.test.ts → 84/84 passed (78 baseline incl. all Phase-1/Phase-2 tests + 6 new). The exclude_unset body assertions are green: PUT body contains ONLY the edited keys (ref_text-only change sends { ref_text: ... } with no character_style/ref_audio/type/alias_of keys; type change sends { type: 'design' }; blanked style sends { character_style: null }; cleared alias sends { alias_of: null }). Card re-render assertion green: after the PUT resolves with the updated 12-column dict (type 'design', character_style 'calm'), the alice card in #voice-catalog shows the new type + style, the form closes, and the 'Voice config saved: Alice' success toast fires. Failure path green: rejected PUT (422 via handleError) → error toast + form stays open. Backward compatibility confirmed: all Phase-23 catalog/preview tests, narrator selector tests, and Phase-2 edit-form tests pass unchanged — saveVoiceConfig's new body only runs on [data-action="save-voice"] clicks (editingVoiceId non-null), and handleNarratorVoiceChange is untouched.
- [x] Verify: `npx tsc --noEmit` exit 0; `npm test` green
    **Note:** Phase 3 verification complete: (1) npx tsc --noEmit exit 0. (2) npm test (full vitest run from frontend/) → 6 files, 283/283 passed (277 baseline + 6 new from P3-S1). (3) git status: only frontend/src/tabs/voices.ts + frontend/tests/frontend/test_voices.test.ts (+ plan file annotations + Phase-2's index.html change still uncommitted from P2-S2) — zero backend files, zero dist changes (no build this phase; build/dist commit is P5-S2/P5-S6). Working tree left dirty per task direction (single implementation commit at P5-S6). No legacy endpoints/modules touched; no file-upload surface (ref_audio stays a path/URL string); handleNarratorVoiceChange + previewVoice untouched.

### Phase 4: Narrator override + preview reuse
- [x] TDD RED: write tests asserting the narrator selector still works (PUT /voices/NARRATOR {voice} unchanged), and the preview button in the edit form reuses POST /voices/{id}/preview with the form's sample text
    **Note:** TDD RED: appended 5 tests to frontend/tests/frontend/test_voices.test.ts in a new 'narrator + form preview (Plan H phase 4)' describe (after the Phase-3 describe). Tests: (1) narrator pin — handleNarratorVoiceChange('Bob') direct call with API.put mockResolvedValueOnce; finds the NARRATOR call via mock.calls.find(url === '/api/pipeline/voices/NARRATOR') and asserts Object.keys(body) === ['voice'] (exclude_unset: no character_style/ref_text/type/alias_of keys leak into the narrator path) + getCurrentNarratorVoice() === 'Bob'; (2) form preview sends the form's ref_text as sample_text — openVoiceEditForm('alice') on FULL_CATALOG_ROW, set #voice-edit-ref-text to 'This is the reference line.', call exported previewVoiceFromForm(button) directly → POST /api/pipeline/voices/alice/preview {sample_text:'This is the reference line.'} + audioInstances[0].src/play assertions (MockAudio) + 'Preview generated successfully' toast + button restored; (3) blanked ref_text → POST with the DEFAULT {sample_text:'This is a preview of the voice.'}; (4) rejected POST (mockRejectedValueOnce 'TTS engine not available') → 'Failed to generate preview: TTS engine not available' error toast + button restored to fa-play; (5) click-through wiring: initVoices registered in beforeAll, per-test dispatch DOMContentLoaded (fires BOTH the persisted Phase-2 beforeAll listener and this describe's listener; the Phase-2 listener no-ops on preview-voice-form clicks so the preview fires exactly once), click edit-voice → form opens → set ref_text 'Click-through sample.' → click [data-action="preview-voice-form"] → POST with that sample (toHaveBeenCalledWith, count-agnostic). Conventions per logs L162/L139: handlers called directly (Phase-3 pattern), toHaveBeenCalledWith everywhere, vi.mocked(API.put).mockReset() + vi.mocked(API.post).mockReset() in beforeEach (queue-leak guard), vi.stubGlobal('Audio', MockAudio) + audioInstances=[] per test, previewVoiceFromForm imported dynamically so RED cannot break module load. RED result: 4 failed | 85 passed — surgical (3× 'previewVoiceFromForm is not a function' + click-through vi.waitFor timeout: POST never fired, no preview branch wired; the narrator pin passes immediately as it is a regression guard on existing behavior).
- [x] Implement: keep handleNarratorVoiceChange behavior; add preview-in-form using existing previewVoice pattern (voices.ts L472, new Audio(…).play() with spinner); ensure NARRATOR row wins over UNKNOWN→NARRATOR fallback is unaffected (backend rule, no frontend change needed beyond not blocking narrator editing)
    **Note:** Implemented (frontend/src/tabs/voices.ts + frontend/index.html): (1) module const DEFAULT_PREVIEW_SAMPLE_TEXT = 'This is a preview of the voice.' (next to NARRATOR_DEFAULT_VOICE); (2) previewVoice gained an optional third param `sampleText?: string` — the POST body is { sample_text: sampleText ?? DEFAULT_PREVIEW_SAMPLE_TEXT }, so the Phase-23 card-preview tests stay byte-compatible (default string unchanged) and only the form path overrides it; spinner/Audio/toast logic untouched; (3) new exported previewVoiceFromForm(button): no-op when editingVoiceId is unset; reads #voice-edit-ref-text (trimmed, non-empty ? value : DEFAULT) and delegates to previewVoice(editingVoiceId, button, sampleText) — reuses the existing spinner/new Audio(...).play()/toast/finally-restore logic verbatim; (4) initVoices: the existing #voice-edit-form delegated click handler now checks [data-action="preview-voice-form"] (calls previewVoiceFromForm) BEFORE the save/cancel branches — no new DOMContentLoaded listeners added, so the stacked-listener gotcha (log L162) is not worsened; (5) index.html: added a Preview button ([data-action="preview-voice-form"], fa-play icon, title explains ref_text-vs-default) inside a d-flex gap-2 wrapper next to the #voice-edit-ref-text input, with a form-text hint. handleNarratorVoiceChange is byte-identical (still PUTs {voice} only) — no frontend change needed. NARRATOR precedence (DB row wins over UNKNOWN→NARRATOR fallback) is a backend rule: NO frontend logic added that blocks narrator editing or reorders the fallback; the narrator selector wiring is untouched.
- [x] TDD GREEN: run test_voices tests; assert narrator PUT unchanged and form preview fires previewVoice
    **Note:** TDD GREEN: npx vitest run tests/frontend/test_voices.test.ts → 89/89 passed (84 baseline + 5 new from P4-S1). Narrator regression pinned: PUT body to /api/pipeline/voices/NARRATOR carries exactly ['voice'] (exclude_unset preserved) and getCurrentNarratorVoice() updates. Form preview verified across all paths: non-empty ref_text → POST {sample_text: ref_text} + audio played via new Audio (MockAudio) + success toast + button restored; blank ref_text → default sample_text; rejected POST → 'Failed to generate preview: ...' error toast + button restored to fa-play; click-through (edit-voice → set ref_text → click [data-action="preview-voice-form"]) fires the preview through the delegated initVoices wiring. Backward compatibility confirmed: all Phase-23 preview tests (default sample_text POST body, MockAudio src/play, spinner restore, 'Failed to play preview' on play() reject) and the Phase-19 narrator selector tests pass unchanged — the sampleText param is additive with a byte-identical default.
- [x] Verify: `npx tsc --noEmit` exit 0; `npm test` green
    **Note:** Phase 4 verification complete: (1) npx tsc --noEmit exit 0 (from frontend/); (2) npm test (full vitest run) → 6 files, 288/288 passed (283 baseline + 5 new from P4-S1). git status: only frontend/src/tabs/voices.ts + frontend/index.html + frontend/tests/frontend/test_voices.test.ts modified (+ plan file annotations) — zero backend files, zero dist changes (no build this phase; build/dist commit is P5-S2/P5-S6). Working tree left dirty per task direction (single implementation commit at P5-S6). No legacy endpoints/modules resurrected (no /api/save_voice_config, no voice_config.json); ref_audio stays a path/URL string — no file-upload surface added. No lint script is configured in frontend (package.json scripts: dev/build/preview/test only, no eslint/prettier/biome config) — tsc + vitest are the verification gates, both green.

### Phase 5: Regression gate, guard suite and verification
- [x] Run `npm test` (vitest 167 baseline + new tests) and `npx tsc --noEmit` (exit 0)
    **Note:** Full regression gate GREEN from frontend/: (1) `npm test` (vitest run v4.1.10) → 6 files, 288/288 passed (baseline 288 — matches Phase 4 count, no new tests this phase; all 23 Phase-1..4 tests included). (2) `npx tsc --noEmit` exit 0. Working tree before gates: only frontend/src/tabs/voices.ts + frontend/index.html + frontend/tests/frontend/test_voices.test.ts + plan file modified; zero backend files, zero dist changes (dist still at HEAD's index-DrlCd_0a.js).
- [x] Run `npm run build` and verify `git diff --exit-code app/static/dist/` is clean after committing dist
    **Note:** `npm run build` from frontend/ exit 0 (vite 8.2.0, 21 modules, 307ms) — dist regenerated. New hashes: app/static/dist/index.html 81.02 kB (gzip 12.71 kB; was 76.67 kB — +4.35 kB = the edit-form markup + bundle ref) and app/static/dist/assets/index-D4Wtskt5.js 83.40 kB (gzip 20.44 kB; was index-DrlCd_0a.js 80.31 kB). Dist diff state vs HEAD: D app/static/dist/assets/index-DrlCd_0a.js (old bundle), M app/static/dist/index.html (+51/−2), ?? app/static/dist/assets/index-D4Wtskt5.js (new bundle). NOT committed — tree left dirty per task direction; the `git diff --exit-code app/static/dist/` clean gate is P5-S6's (exec-manager) job AFTER the commit. Build-content sanity: new bundle contains minification-safe markers (data-action edit-voice/save-voice/preview-voice-form, voice-edit-form, alias_of) and dist/index.html contains the new static labels (Character Style, 'No file upload — a path/URL string only'); HEAD's old bundle ref absent from dist/index.html.
- [x] Run `pytest tests/pipeline/test_legacy_removed.py -q` and verify 12/12 green (no legacy alias-map editor modules; no /api/scripts endpoints)
    **Note:** Guard suite GREEN: `python -m pytest tests/pipeline/test_legacy_removed.py -q` from repo root → 12 passed in 2.89s (12/12). Confirms: all 9 legacy files absent (app/project.py, prompt modules/txt, editor-legacy.ts, audio.ts), app.app imports + exposes NONE of the 26 unique legacy route paths / 18 legacy route families (incl. /api/save_voice_config, /api/scripts family, /api/voices exact-match only — /api/pipeline/voices preserved), state.ts has no pipelineEnabled, no project_manager identifier in app/*.py. Guard file NOT modified. Plan H adds no backend surface; the only voice API the frontend touches are the preserved pipeline endpoints (GET/PUT /api/pipeline/voices, POST /api/pipeline/voices/{id}/preview).
- [x] Security review: voice config edits are data writes — confirm the frontend sends only documented fields, ref_audio is a path/URL string (no file upload surface added here), and no new secrets are rendered into the DOM; document findings
    **Note:** Security review of voice config edits (data writes) — ALL PASS, no fix required. (a) PUT body fields: saveVoiceConfig (voices.ts L474-542) diffs exactly 5 form fields against the fetched row — character_style/ref_audio/ref_text (diffText closure), type, alias_of; all five are documented VoiceUpdateRequest keys; no extra keys; blanked text + cleared alias → null (contract); untouched keys omitted (exclude_unset); typeSelect only offers the 5 static valid types. handleNarratorVoiceChange still PUTs {voice} only. (b) ref_audio = path/URL string: index.html L553 is `<input type="text" id="voice-edit-ref-audio">` with placeholder '/path/to/reference.wav or https://...'; NO file-upload surface added — the 6 pre-existing type="file" inputs (epub, prep-audio, prep-batch, lora-dataset, dsb-import, m4b-cover) are all in OTHER tabs, none in #voice-edit-form (verified via git diff: index.html change = the edit-form block only). (c) No new secrets in DOM: the form renders only user-editable config fields; no api_key anywhere; adapter_path/adapter_id/seed/voice are NOT rendered by the form or cards (only name/type/character_style badges). Pre-fill via .value assignment (openVoiceEditForm L437-441) and title via textContent (L442). (d) No legacy surface resurrected: grep of frontend/src + index.html for save_voice_config|voice_config.json|/api/scripts|editor-legacy|tabs/audio → zero hits; guard 12/12 (P5-S3) confirms backend. (e) XSS: every interpolated value in new markup goes through escapeHtml — createVoiceCard (name L294, type L295, style L288, data-voice-id L291/301/307), populateAliasOptions (id/name/stale alias L397/407), form pre-fill is .value/textContent not innerHTML, all toasts pass through showToast which escapeHtml's the message (utils.ts L39); the save re-render reuses createVoiceCard. Residual (documented, NOT a vuln, matches pre-existing pattern at handleCharacterVoiceChange L640): saveVoiceConfig's success handler interpolates voiceId raw into a CSS selector (querySelector `.voice-card[data-voice-id="..."]`, L531) — a voice id containing selector metacharacters (created by the operator via an unsanitized backend id/name, api_voices.py L149 `request.id or request.name`) would throw SyntaxError → caught by the .catch → error toast + form stays open though the PUT succeeded server-side (minor self-inflicted UX edge; no markup/script execution possible since querySelector is parse-only and all rendering goes through escapeHtml). Left unfixed: not a security issue, out of scope, and the pattern is pre-existing.
- [x] Code review pass via exec-manager QA-Reviewer; fix MINOR findings
    **Note:** QA-Reviewer Round 2 PASS (qa-reviewer L43): 0 issues. Round 1 (L42) found 4 MINOR issues → Exec-Fixer cycle (exec-fixer L35) fixed all 4: (1) saveVoiceConfig re-render querySelector raw interpolation → iterate .voice-card elements comparing dataset.voiceId; (2) test comment honesty + audioInstances hardening; (3) docstring gaps (module header, registerVoiceCatalog, createVoiceCard); (4) isolated click-through save-voice DOM test. Round 2 full re-review: lint PASS (tsc 0), layerCompliance PASS (pure frontend, 3 source files + dist), contracts PASS (exclude_unset 5 keys, null clears, type verbatim, alias ''→null, preview {sample_text}, NARRATOR {voice}-only pinned), codeQuality PASS (0 TODO/FIXME/console.log/type:ignore; 18 escapeHtml sinks), completeness PASS (22 steps), testCoverage PASS (289/289, TestAnalyzer MINOR_ISSUES_PASS, 6 log-only LOW gaps), documentation PASS (DocsAnalyzer MINOR_ISSUES_PASS, 7/7 IDs + 5/5 actions cross-check clean, one comment-only header edit in test_voices.test.ts L4-9 applied post-fixer, tsc+vitest re-verified). Security re-verified PASS. Proceeded to P5-S6 commit.
- [ ] Commit: `feat(frontend): voice config edit form with style, reference audio, type switch and alias picker`

## Completion Criteria

- Voices tab is reachable (Plan E) and each voice card opens an edit form pre-filled from the fetched voice config
- Form edits character_style, ref_audio, ref_text, type (5 options), alias_of (dropdown of existing voices) and saves via PUT /voices/{id} with exclude_unset semantics
- Narrator override and preview (POST /voices/{id}/preview) continue to work; NARRATOR precedence untouched
- vitest green, tsc exit 0, build + dist committed, guard 12/12, security review documented

## References

- `artifacts/designs/pending/DD-universal-upgrade.md` — cap4, P6-C4, FP5, decision #6, open item #5
- `artifacts/designs/parts/universal-upgrade/CONTRACTS.md` — PUT /voices/{id} contract (existing), VoiceConfigRow extension
- Prior: Plan E (tab navigation), Plan Q (voice parity, voice_config 8 missing columns added)
