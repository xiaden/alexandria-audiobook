# Task: Unified Review Queue & Supersede

## Problem Statement

Today the review queue is junction-only: `ReviewManager.get_review_items` (app/pipeline/review.py:112-149) queries character_book/character_scene/character_span junctions with confidence in [0.5, 0.7) and returns `junction_table:character_id:entity_id` ids. The walks 2g (voice audition), 2h (voice assignment), and 2i (delivery) compute `profiles_for_review` / `assignments_for_review` / `instructs_for_review` counters and return them from `execute()` — but the return dict is dropped by the background task, so low-confidence voice-profile, voice-assignment, and instruction items never reach the queue. There is also no supersede mechanism (re-running a walk leaves stale low-confidence items from earlier runs in the queue) and no undo/value-restore.

This plan (DD A3-r Unified Review Queue Honest Union + Completion-Time Supersede) makes the queue honest: walks 2g/2h/2i write `walk_review_item` rows in the same transaction as their junction writes (kind voice_profile/voice_assignment/instruction, prior_value captured pre-write); at completion the walk's FINAL transaction supersedes prior pending items of the same kind for targets it regenerated; `GET /review/{book_id}` returns the union (junction live query + pending walk_review_item rows) with `walkitem:`-prefixed ids; accept/reject/override dispatch on the prefix and walk-side actions transactionally restore `prior_value` (undo). This is the backend half — frontend review surfacing lands in Plan F.

## Dependencies

- Plan A (transaction(), walk_review_item table) and Plan B (walk_run rows, walk-side retry, runner lifecycle) — completed and archived
- `artifacts/designs/parts/epub-audiobook-pipeline-rewrite/CONTRACTS.md` § Universal Upgrade — review endpoint registration

## Phases

### Phase 1: walk_review_item writes in walks 2g/2h/2i
- [x] TDD RED: write tests for walk_2g (voice audition), walk_2h (voice assignment), walk_2i (delivery) asserting each writes a walk_review_item row in the SAME transaction as its junction writes (kind voice_profile / voice_assignment / instruction respectively), with prior_value captured from the pre-write state of the target row, status pending, created_ms set
    **TddRed:** Added TestWalkReviewItem classes to tests/pipeline/test_walk_2g.py, test_walk_2h.py, test_walk_2i.py (14 new tests: per walk — review-band item row with prior_value, review-band with no prior (None), auto-accept no row, auto-reject no row, rollback removes item+target write). Added _get_review_items helper + _FailingItemInsert(HeartbeatStorage) wrapper that raises RuntimeError when SQL contains 'walk_review_item'. Wrapped the 3 existing review-band tests (test_confidence_filter_medium_review 2g/2i, test_voice_matched_medium_confidence_review 2h) to pass HeartbeatStorage(populated_storage, 'run-1') so storage.run_id exists (production runner always wraps in HeartbeatStorage). New tests run RED: 8 failed / 6 passed (6 passing are the auto-accept/auto-reject negative guard tests, expected green both sides). Modified existing review-band tests still pass.
- [x] Implement: extend the per-unit savepoint blocks in walk_2g_voice_audition.py, walk_2h_voice_assignment.py, walk_2i_delivery.py to insert walk_review_item rows when the walk marks an item for review; keep LLM calls outside the transaction (per architectural rule #6)
    **Implement:** Extended the per-unit SAVEPOINT blocks in all 3 walks (walk_2g_voice_audition.py, walk_2h_voice_assignment.py, walk_2i_delivery.py). Per walk: module-local _get_prior_*(target_id, storage) SELECTs the pre-write value OUTSIDE the savepoint; _insert_review_item(storage, book_id, target_id, prior_value) runs INSIDE the existing try/RELEASE/ROLLBACK block after the target write, guarded by is_review. LLM calls remain outside any transaction (rule #6) — no storage.transaction() added, existing SAVEPOINT mechanism reused so a simulated unit failure (ROLLBACK TO SAVEPOINT) removes the item row atomically with the target write. _process_character/_process_span gained a book_id parameter (needed for the item row); call sites updated with keyword args. Item columns: id=f'{run_id}:{kind}:{target_id}', run_id=storage.run_id (HeartbeatStorage attribute; production runner always wraps), status='pending', created_ms=int(time.time()*1000). Kind/target_table: 2g voice_profile/character_metadata, 2h voice_assignment/character, 2i instruction/span. Auto-accept (>=0.7) and auto-reject (<0.5) paths unchanged — helper only reached on review band.
- [x] TDD GREEN: run walk 2g/2h/2i tests; assert item rows exist with correct kind/target/prior_value and that a rollback (simulated unit failure) removes the item row (same transaction)
    **TddGreen:** Implemented in-walk walk_review_item writes. Each walk: added 'import time'; _process_character/_process_span now receive book_id; prior value SELECTed OUTSIDE the savepoint (_get_prior_voice_profile → character_metadata.value for key voice_profile; _get_prior_voice_assignment → character.voice_assignment_id; _get_prior_instruct → span.instruct); inside the existing per-unit SAVEPOINT block, after the target write, if is_review: _insert_review_item(storage, book_id, target_id, prior_value) inserts row (id f'{run_id}:{kind}:{target_id}', kind voice_profile/voice_assignment/instruction, target_table character_metadata/character/span, status 'pending', created_ms int(time.time()*1000), run_id from storage.run_id). Item insert is INSIDE the savepoint → ROLLBACK TO SAVEPOINT removes both target write and item row. No storage.transaction() introduced (rule #6: LLM stays outside txn — unchanged). Counters and confidence filters untouched. Full walk test files GREEN: 88 passed (74 existing incl. 3 modified to wrap HeartbeatStorage + 14 new). Rollback tests in all 3 files pass: simulated item-insert failure → errors recorded, item rows absent, target write rolled back (2g metadata cnt 0; 2h assignment unchanged; 2i span instruct unchanged, seeded prior survives).
- [x] Verify: `ruff check app/pipeline/walks/walk_2g_voice_audition.py app/pipeline/walks/walk_2h_voice_assignment.py app/pipeline/walks/walk_2i_delivery.py` clean
    **Verify:** ruff check clean on the 3 walk files (also clean on the 3 modified test files). Guard green: pytest tests/pipeline/test_legacy_removed.py -q → 12/12 passed. Full walk test files: 88/88 passed. Broader sanity: full tests/pipeline suite → 1027 passed, 0 failed (baseline 894 before this phase; delta = 14 new walk_review_item tests + pre-existing pipeline tests). Scope check via git diff: only the 3 walk modules, 3 walk test files, and plan annotations changed — runner.py, review.py, api_review.py, adapter.py, schema.py, tts.py untouched; no new .py files.

### Phase 2: Completion-time per-target supersede
- [x] TDD RED: write tests asserting that when a walk run completes, its FINAL transaction supersedes prior pending walk_review_item rows of the same kind whose target_id is in the new run's committed targets (status → superseded); items for targets NOT regenerated stay pending; nothing is superseded on failure/cancel
    **TDDRED:** Wrote failing tests first. Added TestSupersede to test_walk_2g.py (5 tests: rerun-supersedes, target-not-regenerated, same-kind-only, top-level-failure-no-supersede, empty-committed-noop), test_walk_2h.py (3 tests: rerun, not-regenerated, top-level-failure), test_walk_2i.py (3 tests: rerun w/ per-span side_effect keeping auto-rejected span-1b pending, top-level-failure, empty-committed). Added TestSupersedeTargets unit class to test_review.py (6 tests: empty-noop, supersedes-matching, excludes-same-run, excludes-other-kind, excludes-non-pending-statuses, excludes-other-book) + import of supersede_targets. Run: 6 walk tests FAIL (items stay pending), 5 failure/empty-set guards PASS (assert nothing-superseded, which is current behavior), test_review.py RED via ImportError (helper absent). Confirmed RED as required.
- [x] Implement: in the walk's FINAL transaction (after the last unit), run the supersede update: `UPDATE walk_review_item SET status='superseded' WHERE book_id=? AND run_id<>? AND status='pending' AND kind=? AND target_id IN (new run's committed targets)`; add a shared helper in review.py (e.g. `supersede_targets(...)`) called by 2g/2h/2i
    **IMPLEMENT:** Added module-level supersede_targets(storage, *, book_id, run_id, kind, target_ids) to app/pipeline/review.py after _make_item_id (before ReviewManager): empty target_ids → return 0 immediately (no transaction); else builds `UPDATE walk_review_item SET status='superseded' WHERE book_id=? AND run_id<>? AND status='pending' AND kind=? AND target_id IN (?,...)` with one placeholder per target, runs via `with storage.transaction(): return storage.execute_update(sql, params)` (single FINAL transaction, commit on clean exit). No new imports needed (PipelineStorage already imported). Wired all three walks: each execute() now tracks committed_target_ids (append after RELEASE inside the per-unit SAVEPOINT try block — only reached when the target write committed; per-unit errors mean that target is not appended) and, after the loop before `return result`, calls supersede_targets with kind voice_profile/voice_assignment/instruction. Deviation note: run_id read via getattr(storage, 'run_id', None) with supersede skipped when None — production runner always wraps storage in HeartbeatStorage (run_id always present), so production semantics are exactly per contract; the guard only prevents AttributeError for existing direct-call tests that pass a raw adapter (they have no run context to supersede from). Failure/cancel semantics untouched: top-level exception in execute() skips the call; run_walk cancel never invokes execute.
- [x] TDD GREEN: run the supersede tests; assert superseded rows, retained pending rows, and no supersede on simulated failure/cancel
    **GREEN:** All supersede tests now pass. `python -m pytest tests/pipeline/test_walk_2g.py tests/pipeline/test_walk_2h.py tests/pipeline/test_walk_2i.py tests/pipeline/test_review.py -q` → 133 passed. End-to-end verified via walk tests: rerun with new run_id supersedes prior pending same-kind items for committed targets and writes fresh pending items (2g/2h/2i); targets not regenerated keep prior pending items; different kind untouched; top-level failure → no supersede (items stay pending); empty committed set → no-op. test_review.py::TestSupersedeTargets covers the helper directly (empty no-op, IN-clause parameterization across multiple targets, run_id<> exclusion, kind scoping, pending-only, book scoping, rowcount).
- [x] Verify: `ruff check app/pipeline/review.py app/pipeline/walks/walk_2g_voice_audition.py` (and 2h/2i) clean
    **VERIFY:** P2-S4 checks all green: (1) `ruff check app/pipeline/review.py app/pipeline/walks/walk_2g_voice_audition.py app/pipeline/walks/walk_2h_voice_assignment.py app/pipeline/walks/walk_2i_delivery.py` → clean (ruff 0.15.20); (2) guard `python -m pytest tests/pipeline/test_legacy_removed.py -q` → 12/12 passed; (3) full walk + review tests → 133 passed; (4) extra: `python -m pytest tests/pipeline -q` → 1044 passed (full pipeline regression); (5) `--cov=app.pipeline.review` → 100% (86 stmts, 0 miss) — new helper fully covered, review.py coverage gate preserved. No files outside scope modified (only review.py, the 3 walks, and the 4 test files).

### Phase 3: Union review queue (junction live query + walk items)
- [x] TDD RED: write tests asserting ReviewManager.get_review_items returns the honest union: existing junction items (unchanged behavior) PLUS pending walk_review_item rows with ids `walkitem:{id}`; walk items carry kind, target_table, target_id, prior_value, created_ms; filter by walk_name when provided
    **TddRed:** Added TestWalkItemsInQueue (11 tests) to tests/pipeline/test_review.py. Refactored the `manager` fixture: extracted the junction inserts verbatim into module-level `_populate_junction_review_items(storage)` (behavior-preserving; existing 34 tests still pass); added `_insert_walk_review_rows(storage)` seeding walk_run rows (run-1..run-3 for b1 with walk_name walk_2g/2h/2i + run-9 for b2) and walk_review_item rows: pending w1/w2/w3 (one per kind, incl. NULL prior_value), non-pending w4/w5/w6 (resolved/superseded/stale, must be excluded), w7 for book b2 (must be excluded by book scope). New `union_manager` fixture = junction items + walk rows. Tests assert: union = 5 junction + 3 walk items; walkitem: prefixed ids; walk items carry exactly {item_id, kind, target_table, target_id, prior_value, created_ms} (table columns — no confidence/human_override); non-pending and other-book rows absent; junction-first ordering; junction ids byte-identical {table}:{char}:{entity}; walk_name filter narrows BOTH junction (source LIKE) and walk items (walk_run.walk_name LIKE join) — 'walk_2g' collapses union to walkitem:w1; 'walk' returns full 8; no-match returns []; manager-only fixture stays pure junction. RED confirmed: 6 failed / 40 passed (failures are exactly the walk-item-presence/order/filter tests; compat guards pass both sides).
- [x] Implement: extend ReviewManager.get_review_items to query walk_review_item (status=pending) for the book and merge with the junction query; item ids get the `walkitem:` prefix; keep junction ids unchanged (backward compatible with existing frontend)
    **IMPLEMENT:** Extended ReviewManager.get_review_items in app/pipeline/review.py: junction code paths untouched (CharacterLedger delegation, _get_scene_review_items, _get_span_review_items byte-identical); added step 4 — items.extend(self._get_walk_review_items(book_id, walk_name)) — plus the new method _get_walk_review_items: SELECT id AS item_id, kind, target_table, target_id, prior_value, created_ms FROM walk_review_item WHERE book_id=? AND status='pending', then rewrites item_id to f'walkitem:{row_id}'. When walk_name provided, the query joins walk_run ON wri.run_id = walk_run.run_id AND walk_run.walk_name LIKE ? (param %walk_name%) — the honest run_id→walk_name link set by run_walk; without the filter, all pending walk items for the book are included. Params order: [%walk_name%, book_id] because the LIKE placeholder precedes the WHERE book_id placeholder in SQL text. Walk items carry exactly the table columns — no confidence/human_override (they don't exist on walk items). No ORDER BY added to the walk query (union ordering contract: junction first, then walk items; 'do not sort otherwise'). Docstring updated to describe the union. accept/reject/override, _parse_item_id, _VALID_JUNCTION_TABLES, supersede_targets untouched.
- [x] TDD GREEN: run review queue tests with a fixture that has both junction items and walk items; assert union order (e.g. junction first, then walk items), ids, and the walk_name filter
    **TddGreen:** GREEN: tests/pipeline/test_review.py → 46 passed (34 pre-existing backward-compat + 12 new TestWalkItemsInQueue). Union assertions verified: 5 junction + 3 walk items; walkitem: prefixed ids (walkitem:w1/w2/w3); walk items carry exactly {item_id, kind, target_table, target_id, prior_value, created_ms}; resolved/superseded/stale + other-book rows excluded; junction-first ordering (first 5 junction, last 3 walk) with no other sort; junction ids byte-identical {junction_table}:{character_id}:{entity_id}; walk_name filter narrows junction (source LIKE) AND walk items (walk_run.walk_name LIKE via join) — 'walk_2g' → [walkitem:w1], 'walk' → full 8, no-match → []. Backward-compat guards (test_each_item_has_item_id 3-part split, test_each_item_has_required_fields) still green — fixture has no walk items and junction shape unchanged.
- [x] Verify: `ruff check app/pipeline/review.py` clean
    **VERIFY:** All P3-S4 checks green: (1) `ruff check app/pipeline/review.py` → clean; (2) guard `python -m pytest tests/pipeline/test_legacy_removed.py -q` → 12/12 passed; (3) suites: tests/pipeline/test_review.py 46 passed, test_walk_2g.py 36 passed, test_walk_2h.py 31 passed, test_walk_2i.py 32 passed (145 combined); (4) extra regression: full `python -m pytest tests/pipeline -q` → 1056 passed (Phase 2 baseline 1044 + 12 new union tests = 1056, no collateral); (5) `--cov=app.pipeline.review` → 100% (97 stmts, 0 miss — new _get_walk_review_items fully covered; coverage gate preserved). Files changed this phase: app/pipeline/review.py (union), tests/pipeline/test_review.py (+12 tests). No files outside scope touched (api_review.py, adapter.py, schema.py, runner.py, walk modules, tts.py untouched).

### Phase 4: Prefix dispatch on accept/reject/override
- [x] TDD RED: write tests asserting POST /review/accept|reject|override dispatch by id prefix: `junction:` → existing junction behavior; `walkitem:` → walk-side action; unknown/malformed prefix → 400; unknown item id → 404
    **TddRed:** Added TestReviewActionDispatch (13 tests) + helpers _seed_walk_review_items / _walk_status to tests/pipeline/test_api.py, placed after TestReviewOverrideEndpoint. Seeding follows test_api.py's own conventions (direct inserts on the function-scoped storage fixture — no cross-test pollution; walk_run + walk_review_item rows wp1/wa1/wi1 one per kind; current walk-generated values written into targets: character_metadata c1 value '{"voice":"new"}', character c2 voice_assignment_id 'vc1', span sp1 instruct 'slowly' — priors '{"voice":"old"}', NULL, 'cheerfully'). Tests assert: accept walkitem: → 200 + row resolved + target unchanged; reject → 200 + prior restored (incl. NULL prior and instruct); override → 200 + new_value written into target + resolved; walkitem: unknown id → 404; junction well-formed unknown id → 404; malformed id → 400; walk override without value → 400 + nothing resolved; junction override without value keeps flags-only behavior; junction accept still works with walk rows present. RED confirmed: 9 failed / 4 passed (the 4 green are junction-behavior + error-code guard tests, expected green both sides). No existing test modified — the new class only POSTs to the action endpoints, so TestReviewEndpoint.test_review_returns_list (junction-shape-on-every-item) is untouched.
- [x] Implement: extend api_review.py action handlers to branch on the id prefix; junction branch keeps current logic; walkitem branch calls a review.py helper that updates the walk_review_item row (status resolved for accept/reject; resolved + new_value stored for override)
    **Implement:** review.py: added ReviewItemNotFoundError(LookupError) (API maps to 404; ValueError stays 400); module-level _WALK_TARGET_WRITES kind→SQL allowlist (voice_profile→character_metadata.value, voice_assignment→character.voice_assignment_id, instruction→span.instruct; kind-keyed so target_table is never interpolated); ReviewManager.resolve_review_action(action, item_id, new_value=None) — startswith('walkitem:') → _resolve_walk_item (prefix stripped), else junction branch delegating to existing accept/reject/override (unknown action → ValueError). _resolve_walk_item: SELECT kind/target_id/prior_value WHERE id=? AND status='pending' (no row → ReviewItemNotFoundError 404); override with new_value None → ValueError 400; accept = no target write; reject = write prior_value; override = write new_value; kind not in allowlist → ValueError; writes + status='resolved' UPDATE run inside ONE storage.transaction() (P5 rollback tests writable against this). Junction 404: added rowcount==0 → ReviewItemNotFoundError checks to accept/review_item, reject_review_item, override_review_item (junction SQL + confidence/human_override semantics otherwise untouched). api_review.py: all three POST handlers now call manager.resolve_review_action(action, item_id, new_value) and catch ReviewItemNotFoundError → HTTPException 404 (ValueError → 400 unchanged); return shapes unchanged {'status': ..., 'item_id': ...}.
- [x] TDD GREEN: run the dispatch tests; assert correct status transitions per action and correct error codes
    **TddGreen:** GREEN: tests/pipeline/test_api.py + tests/pipeline/test_review.py = 131 passed. TestReviewActionDispatch 15/15 (accept→row resolved + target value unchanged '{"voice":"new"}'; reject→prior restored '{"voice":"old"}' / NULL voice_assignment_id / instruct 'cheerfully'; override→new_value written '{"voice":"human"}' / 'vc1' / 'quickly' + resolved; walkitem unknown → 404; junction accept/reject/override unknown → 404; malformed → 400; walk override w/o value → 400 + item stays pending; junction override w/o value → 200 flags-only; junction accept still works with walk rows present). TestResolveReviewActionDispatch 3/3 in test_review.py (unknown action on junction id → ValueError, unknown action on walkitem id → ValueError, kind missing from patched allowlist → ValueError — covers defensive branches 343/380/390). Coverage of app/pipeline/review.py back to 100% (139 stmts, 0 miss). No existing test modified; existing review endpoint tests all green.
- [x] Verify: `ruff check app/pipeline/api_review.py app/pipeline/review.py` clean; guard suite 12/12 still green
    **Verify:** `ruff check app/pipeline/api_review.py app/pipeline/review.py tests/pipeline/test_api.py tests/pipeline/test_review.py` → All checks passed. Guard `python -m pytest tests/pipeline/test_legacy_removed.py -q` → 12 passed (12/12). Full `python -m pytest tests/pipeline -q` → 1074 passed (1056 Phase-3 baseline + 15 new API dispatch tests + 3 new unit tests), 1 pre-existing StarletteDeprecationWarning. review.py coverage 100%. No files outside scope modified; app/tts.py, adapter.py, schema.py, runner.py, walk modules untouched; no new module files.

### Phase 5: Transactional value-restore (undo backend)
- [x] TDD RED: write tests asserting that rejecting/overriding a walkitem: item transactionally restores `prior_value` into `target_table.target_id` (e.g. restoring a pre-walk voice_config value), with the restore committed atomically with the walk_review_item status update; restore failure rolls back both
    **TddRed:** Wrote 12 new tests FIRST (11 unit in tests/pipeline/test_review.py::TestWalkItemValueRestore, 1 endpoint in tests/pipeline/test_api.py::TestReviewActionRollback). Coverage: reject restores prior_value (non-NULL voice_profile, NULL voice_assignment, instruction) + item resolved; override writes new_value + resolved; accept write-free; SELECT outside txn (call-order lock-in); 3 rollback-failure cases (bogus allowlist SQL → OperationalError; adapter raising ConcurrentTransactionError on the status UPDATE — the DISCRIMINATING case where the restore write SUCCEEDS then the status fails; same for override); retry-after-rollback still actionable; API-level rollback via endpoint. RED result: 0 of 12 genuinely RED — all 12 passed against Phase 4 code because Phase 4 already wrapped value-write + status UPDATE in one storage.transaction() and both adapters roll back on exception + re-raise. Honest reporting per task: rollback tests were not truly RED; they are regression-lock tests. Proved they discriminate via mutation test (removed txn wrap → exactly the 4 discriminating tests FAIL: status_update_failure_rolls_back_restore, override_status_update_failure_rolls_back_write, test_item_select_happens_outside_transaction (StopIteration), test_restore_failure_rolls_back_via_api). Bogus-allowlist tests alone would pass even without a txn (first write fails, nothing written either way) — that's why the status-update-failure mechanism is included. New helper seeds: _seed_value_restore_rows (voice_config vc1 + walk_run run-p5 + 3 walk_review_item rows wp1/wa1/wi1 with walk-generated CURRENT values in targets) and _item_status. Added imports sqlite3 + ConcurrentTransactionError to test_review.py.
- [x] Implement: in review.py add the value-restore helper used by the walkitem branch: within `storage.transaction()`, write prior_value back to target_table/target_id and update the item row to resolved
    **Implement:** Verification only — no code change to review.py required. _resolve_walk_item (L345-398) already: (1) SELECTs kind/target_id/prior_value OUTSIDE the txn (rule #6); (2) builds writes (empty for accept; [(sql,(prior,target))] reject; [(sql,(new,target))] override) and executes them + the status='resolved' UPDATE inside ONE `with self._storage.transaction():` block (L392); (3) no commit between write and status (adapter's execute_update commits only when `not in_transaction`, so inside the txn it does not commit); (4) no try/except swallowing — exceptions propagate out of the txn context so the adapter rolls back (verified in both SQLiteAdapter.transaction and InMemorySQLiteAdapter.transaction: except BaseException → conn.rollback() + clear owner + re-raise; else → commit). HARDENING EVIDENCE: ran a mutation test that temporarily removed the transaction wrap (autocommit style); exactly the 4 discriminating new tests failed (restore autocommitted + visible), proving the wrap is what provides atomicity and the tests detect its removal. Mutation then reverted byte-for-byte — net zero diff to review.py from this phase. No hardening change made: the wrap already satisfies the atomicity contract, and any refactor (e.g. skipping BEGIN IMMEDIATE for write-free accepts) is beyond what the contract requires (task: do not refactor beyond atomicity requirement).
- [x] TDD GREEN: run the value-restore tests; assert the target row has the prior value restored and the item is resolved; simulate a restore failure and assert rollback of both the restore and the status change
    **TddGreen:** All 12 new tests GREEN. Rollback simulated two ways: (a) monkeypatched _WALK_TARGET_WRITES kind→SQL against a missing table ('UPDATE nonexistent ...') → sqlite3.OperationalError inside the txn → item stays 'pending' AND target unchanged (reject + override); (b) adapter's execute_update patched to raise ConcurrentTransactionError on the status='resolved' UPDATE (restore write SUCCEEDS first) → both rolled back (reject + override) — the discriminating case. Also verified via API endpoint (TestClient re-raises ConcurrentTransactionError; wp1 pending; character_metadata value unchanged). Full suites: tests/pipeline/test_review.py + test_api.py = 143 passed (131 baseline + 12 new). After rollback the item is still actionable — retry with real SQL succeeds (test_failed_restore_leaves_item_actionable).
- [x] Verify: `ruff check app/pipeline/review.py` clean; `pytest tests/pipeline/test_review.py -q --cov=app/pipeline --cov-report=term-missing` green (review coverage stays 100%)
    **Verify:** All gates pass. (1) ruff check app/pipeline/review.py + tests/pipeline/test_review.py + tests/pipeline/test_api.py → 'All checks passed!' (fixed one F401: unused `import app.pipeline.review as review_module` in test_api.py, left over from the mechanism switch). (2) Coverage: `pytest tests/pipeline/test_review.py tests/pipeline/test_api.py -q --cov=app.pipeline.review --cov-report=term-missing` → 143 passed, app/pipeline/review.py 139/139 = 100% (unchanged from baseline; the 'STAYING 100%' gate holds — combined suite is the 100% measure; test_review.py alone IMPROVED 88%→94%, 16→8 miss, because the new unit tests now cover the walk write/status lines previously covered only by test_api). Note: coverage for review.py is only 100% when run with test_api.py (endpoint tests cover 8 defensive lines: 251,272,316,337,339,341,366,373). (3) Guard: tests/pipeline/test_legacy_removed.py → 12 passed (12/12). (4) Full suite: python -m pytest tests/pipeline -q → 1086 passed, 0 failures. No changes to review.py this phase (mutation reverted).

### Phase 6: Regression gate, guard suite and verification
- [x] Run `pytest tests/pipeline -q --cov=app/pipeline --cov-report=term-missing` and record pass count + coverage (review.py baseline 100% maintained; walk 2g/2h/2i coverage rises from 82% baseline)
    **Regression:** Full-suite regression gate GREEN. `python -m pytest tests/pipeline -q --cov=app/pipeline --cov-report=term-missing` → 1086 passed, 0 failed (baseline 1086 after Phase 5 confirmed, no collateral). TOTAL coverage 92% (3466 stmts, 285 miss). PER-FILE: app/pipeline/review.py 139/139 = 100% stmt (0 miss — the 'STAYING 100%' gate holds; combined suite is the measure). Walk files all RISEN from 82% baseline: walk_2g_voice_audition.py 92% (137 stmts, 11 miss), walk_2h_voice_assignment.py 92% (140, 11), walk_2i_delivery.py 92% (143, 12). Investigated every uncovered walk line: all 34 miss lines are PRE-EXISTING defensive/parse-failure branches (2g: 186 desc-read alt, 283-289 parse-fail/invalid-dict/confidence-type, 412-414 alias JSON decode, 470-471 parse guard; 2h: 215/221-224 voice-profile read paths, 279-281/286 parse-fail, 358 prior-read tail, 455-456 parse guard; 2i: 220/236/261 helper read alts, 318-319/323-324/328 parse-fail/empty-instruct, 384 prior-read tail, 428 name-read alt, 507-508 parse guard). NONE are Phase-1 walk_review_item write lines (2g _insert_review_item 367-395, 2h 361-389, 2i 387-415 all covered by the 14 P1 tests + supersede tests) → no test gap for the walk-item-write lines; no test additions needed (task allows them only to close a walk coverage gap, and there is none). Also ran `ruff check` on all 5 Plan D diff files → All checks passed. No code changes this step (verification-only).
- [x] Run `pytest tests/pipeline/test_legacy_removed.py -q` and verify 12/12 still green (no legacy review endpoints resurrected)
    **Guard:** Guard suite GREEN: `python -m pytest tests/pipeline/test_legacy_removed.py -q` → 12 passed (12/12). No legacy review endpoint resurrected, no route regression. Per the step contract, a guard failure would have STOPPED the phase — none occurred, so no workaround needed. This is the 7th consecutive 12/12 guard pass across Phases 1-5 + this phase.
- [x] Security review: walkitem id dispatch must scope to the book_id (no cross-book item access — verify item lookup joins book_id), junction vs walkitem prefix cannot be spoofed to reach junction tables via walkitem branch, prior_value restore cannot write outside target_table/target_id (validated table allowlist); document findings
    **Security:** Security review (OWASP methodology via security-review skill) of the Plan D diff — full report in exec-worker log L127 (tag security). Threat model: NO auth anywhere (only dev CORSMiddleware app.py:108, single-user local app); "cross-book" is not a privilege boundary, so assessment is defense-in-depth integrity; all ids uuid4 (character walk_2b.py:273, span extract.py, run_id runner.py:470) → unguessable walkitem ids f'{uuid}:{kind}:{uuid}'.

(a) WALKITEM DISPATCH BOOK SCOPING — VERIFIED with LOW note. (i) VERIFIED: item ids are globally-unique TEXT PKs; _resolve_walk_item (review.py:360) SELECTs by id + status='pending' only and derives the target write from THAT row's target_id → a book-B item id can only mutate book-B's own row+target, never book-A rows (uuid4 ⇒ no id collision). (ii) LOW defense-in-depth note: target writes are keyed by character_id/span_id from the item row, NOT book_id (review.py:143-154 _WALK_TARGET_WRITES has no book scoping); character/character_metadata/span are GLOBAL tables with NO book_id column, so book-scoping the write is schema-impossible — the item row's book_id is queue-scoping only (GET + supersede WHERE book_id=?). A crafted walk_review_item row pointing target_id at another book's entity WOULD mutate it. NOT exploitable: only the walks INSERT walk_review_item rows (no API creates them) and walks use book-scoped loads only (character via character_book WHERE book_id=?, spans in presentation order); crafting requires direct DB write access = full compromise (out of threat model); targets are unguessable uuid4. No fix possible/needed (no book_id column on targets; item book_id is row-authoritative). JUNCTION BRANCH: no book scoping at all (pre-existing Plans B/C accept/reject/override keyed by character+entity; caller knowing a junction id can mutate that junction row regardless of book) — LOW, pre-existing, out of Plan D diff, no auth boundary exists anyway; GET read path IS book-scoped. Documented, not fixed.

(b) PREFIX SPOOFING — VERIFIED both directions. Dispatch (api_review.py → review.py:332) is startswith('walkitem:'); junction ids are {table}:{char}:{entity} with table ∈ _VALID_JUNCTION_TABLES={character_book,character_scene,character_span,character_series} — no junction table is named walkitem, so a junction id can never start with 'walkitem:'. A forged 'walkitem:character_book:c1:b1' hits the walk branch, strips prefix, finds no walk_review_item row (walk ids uuid-prefixed) → 404, and CANNOT reach junction SQL: the walk branch's write SQL derives ONLY from _WALK_TARGET_WRITES.get(kind) (kind schema-CHECK-constrained to voice_profile|voice_assignment|instruction; target_table is not even in the SELECT at review.py:360-364; target_id only a bound ? param; unknown kind → .get()→None → ValueError→400). Reverse: walkitem ids always start with 'walkitem:' → always walk branch; even misrouted, _parse_item_id rejects the uuid prefix (ValueError→400).

(c) VALUE-RESTORE ESCAPE — VERIFIED. prior_value/new_value writes use the same _WALK_TARGET_WRITES kind-keyed allowlist, all values bound via ? (review.py:386-388, 392-398); status='resolved' UPDATE parameterized (WHERE id=?); zero f-string interpolation of prior_value/new_value/target_id → no SQL injection. created_ms/id only written by walks (time.time()*1000, f'{run_id}:{kind}:{target_id}'); no API endpoint INSERTs walk_review_item (only UPDATEs status+targets) → not attacker-influenced; used only in bound WHERE clauses. supersede_targets (review.py:95-127) book-scoped + fully parameterized (per-target IN placeholders). _get_walk_review_items book-scoped (wri.book_id=? AND status='pending').

RESULT: no CRITICAL/HIGH; no genuine in-scope IDOR → no code changes in P6-S3 (verify/audit phase per contract). ruff clean on all 5 diff files (re-verified).
- [x] Code review pass via exec-manager QA-Reviewer; fix MINOR findings
    **QA:** QA gate PASSED (Round 3, 2026-08-06): full fresh review after 2 fix cycles. Round 1: 5 MINOR (CONTRACTS.md L124 DTO id→item_id + junction-only fields; L17 rule #8 reword to unprefixed junction ids; review.py module+class docstrings; api_review.py + _resolve_walk_item docstrings; test_review.py duplicate assert block) — fixed by Exec-Fixer. Round 2: 1 MINOR DOC_GAP (CONTRACTS.md L65 supersede_targets signature omitted required storage param + mislocated in ReviewManager table) — fixed, verified verbatim vs review.py:101-133. Round 3: PASS — code review (ruff clean, 1086 passed, guard 12/12, contracts rules #6/#8/#9 + L63-65/L97-98/L124 zero-drift, layer compliance, security re-verified vs exec-worker L127, all 5 completion criteria met), QA-TestAnalyzer PASS (rollback atomicity discriminating, junction backward-compat intact, no assertions lost), QA-DocsAnalyzer PASS (full A-K signature sweep, no fourth drift). 4 non-blocking observations documented (api_review walk_name param deferred to frontend plan; get_review_manager DI 98%; 2 uncovered prior-read defensive tails; pre-existing ResourceWarning) — no action required. reviewRounds: 3, fixCycles: 2.
- [ ] Commit: `feat(pipeline): unified review queue with walk items, supersede and value-restore`

## Completion Criteria

- Walks 2g/2h/2i write walk_review_item rows in-transaction (3 kinds, prior_value captured); rollback removes them
- Completion-time per-target supersede in the FINAL transaction; nothing superseded on failure/cancel
- GET /review/{book_id} returns the honest union (junction + walkitem: prefixed items), filterable by walk_name
- accept/reject/override dispatch by prefix; junction branch unchanged; walkitem branch transactional with value-restore (undo) committed atomically
- Full pytest suite green, review coverage 100%, walk 2g/2h/2i coverage up, guard 12/12, ruff clean, security review documented

## References

- `artifacts/designs/pending/DD-universal-upgrade.md` — P3 A3-r phase, FP3, workflows (Walk, Supersede, Review), decisions #5/#8
- `artifacts/designs/parts/universal-upgrade/CONTRACTS.md` — walk_review_item schema, supersede rule, union queue, prefix dispatch, value-restore
- `artifacts/designs/parts/universal-upgrade/README.md` — Plan D row
- Prior: `TASK-universal-upgrade-B-render-walk-persistence.md` (completed)
