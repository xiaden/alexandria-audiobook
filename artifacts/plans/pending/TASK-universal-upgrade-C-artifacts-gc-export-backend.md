# Task: Artifact-First Run Directories, GC & Export Backend

## Problem Statement

Today rendered audio lands in `tempfile.mkdtemp(prefix=f'audiobook_{book_id}_')` (tts_integration.py:287) — ephemeral, process-scoped, gone on restart. There is no RENDER_ROOT, no durable manifest, no cleanup (the in-memory `_render_jobs` dict was never evicted), no garbage collection, and no way to stream a single chunk or the whole book back to the browser. Export is a single `ffmpeg -f concat` call (api_export.py:335-346) with no metadata, no cover, no chapter markers, no MP3, no Audacity support, and zero FFMETADATA1 usage anywhere.

This plan (DD A2-r Artifact-First Run Directory with fsync Discipline, rows=truth/manifest=derived, tombstoning GC) makes renders durable under `RENDER_ROOT/book-{id}/{job_id}/`, gives each chunk a crash-safe fsync write, rebuilds manifest.json as a derived cache at startup, garbage-collects artifacts ≥ 7 days old (hourly, env-tunable, tombstoning rows + files in one sweep), and exposes the audio/chunk/export endpoints: bounded-range WAV streaming (`/export/chunk/{job_id}/{idx}`), whole-book playback (`/export/audio/{job_id}`), and the 3-phase FFMETADATA1 M4B export (`/export/m4b`) with MP3/Audacity where supported.

## Dependencies

- Plan A (transaction(), tables) and Plan B (render_job/render_chunk rows, endpoints, reconciliation) — completed and archived
- `artifacts/designs/parts/epub-audiobook-pipeline-rewrite/CONTRACTS.md` § Universal Upgrade — export/chunk/audio/m4b endpoint registration

## Phases

### Phase 1: RENDER_ROOT run directories + fsync discipline
- [x] TDD RED: write tests in tests/pipeline/test_tts_integration.py asserting render_audiobook writes chunk WAVs under `RENDER_ROOT/book-{id}/{job_id}/chunk_{i:04d}.wav`, that each chunk file is written tmp→fsync→rename→fsync-parent before the render_chunk row is marked done, and that a crash between rename and row-update leaves a row status that reconciliation will catch (file exists, row pending)
    **Done:** RED tests written in tests/pipeline/test_tts_integration.py. Extended FakeTTSEngine to write placeholder WAVs (generate_voice writes to the given output_path; generate_batch mirrors app/tts.py naming temp_batch_{idx}.wav) so fsync/rename has real files. Added autouse _render_root fixture (monkeypatch.setenv RENDER_ROOT to tmp_path/render_root) — hermetic, file-backed. New classes: TestRenderRootResolution (env override/default data/render_root/per-call re-read), TestRenderRootRunDirs (batch+individual under RENDER_ROOT/book-{id}/{job_id}/, no .tmp leftovers), TestChunkFsyncDiscipline (order test via patched _fsync_file/_fsync_dir/os.replace event log + raw os.fsync call-count test + crash-between-rename-and-row-update invariant: file exists + row 'pending'), TestBatchFsyncDiscipline (all temp_batch files fsynced before parent), TestManifest (fields incl. relative wav_path, batch listing, atomic no-.tmp-leftover, no manifest on failed). Updated 2 outdated assertions: test_auto_created_output_dir_when_none → test_auto_created_run_dir_when_none (RENDER_ROOT/book-b1/{job_id}); test_generate_voice_output_path_format now expects .tmp sibling paths + final file existence. Confirmed RED: 13 failed / 51 passed before implementation.
- [x] Implement: add `RENDER_ROOT` resolution (env `RENDER_ROOT`, default under `data/`); render_audiobook uses `RENDER_ROOT/book-{id}/{job_id}/` instead of mkdtemp; write chunks with the 2-fsync discipline (fsync file after write, rename into place, fsync parent dir)
    **Done:** Implemented in app/pipeline/tts_integration.py only (app/tts.py untouched — verified git diff empty). Added get_render_root() reading env RENDER_ROOT at call time, default os.path.join(os.getcwd(),"data","render_root") (data/ gitignored, .gitignore line 82); _resolve_run_dir(book_id, job_id) → RENDER_ROOT/book-{id}/{job_id}/ with makedirs(exist_ok=True). Replaced tempfile.mkdtemp at the resolved_dir fallback (was line 431) — explicit output_dir param fully preserved and honored (existing tests with tmpdir still pass). Removed now-unused `import tempfile`, added `import json`. 2-fsync discipline in individual loop: engine writes to tmp sibling chunk_{i:04d}.wav.tmp → _fsync_file(tmp) (os.open O_RDONLY + os.fsync) → os.replace(tmp, final) → _fsync_dir(resolved_dir), and only then _mark_chunk_done. tmp-name decision: suffix `.tmp` on the final name (chunk_0000.wav.tmp) — sibling in the same dir so rename is atomic on one filesystem. Batch mode: _fsync_batch_outputs(resolved_dir) fsyncs every *.wav produced + parent dir once (file-level discipline — no per-chunk rows in batch). _fsync_file/_fsync_dir use os.open(path, os.O_RDONLY) which works on Linux for both regular files and dirs. _mark_chunk_done docstring updated (row update is the very last step; crash between rename and row-update leaves pending row + durable file).
- [x] Implement: after render completes, write `manifest.json` in the run dir (job_id, book_id, mode, chunk count, per-chunk wav paths, status) — manifest is derived, never the authority
    **Done:** manifest.json derived cache implemented in tts_integration.py. _write_manifest(resolved_dir, *, job_id, book_id, mode, chunk_paths, status) writes {job_id, book_id, mode, chunk_count, chunks:[{idx, wav_path}], status, created_ms} atomically: tmp manifest.json.tmp → json.dump(indent=2, sort_keys) + f.write("\n") + flush + os.fsync(f.fileno()) → os.replace → _fsync_dir(resolved_dir). DECISION: chunk wav_path entries are RELATIVE to the run dir (portable across relocation; rows keep absolute paths — documented in docstring). chunk_count = len(chunk_paths) (individual: all final chunk paths; batch: enumerated *.wav files via _batch_output_paths — temp_batch_*.wav for the real engine). Written only for completed renders (never failed/cancelled) and only AFTER _finalize_job sets the row 'completed', so a missing manifest is exactly the state Phase 2 rebuilds at startup; empty-script path also writes a chunk_count=0 manifest. _write_manifest_best_effort wraps it in try/except with a stderr warning (manifest is derived — a cache-write failure must not flip a completed row to failed).
- [x] TDD GREEN: run tts_integration tests with a file-backed tmp RENDER_ROOT; assert paths, fsync order (via mocked os.fsync call sequence), and manifest content
    **Done:** GREEN: python -m pytest tests/pipeline/test_tts_integration.py -q → 64 passed (51 baseline + 13 new). All RENDER_ROOT path assertions, fsync-order assertions (patched helper event log + raw os.fsync call count == 8: 3 chunks × file+dir + manifest file + manifest rename dir), crash-invariant test, batch fsync test, and manifest content tests pass with the file-backed tmp RENDER_ROOT (env monkeypatch, autouse fixture). Two test-only fixes during GREEN: (1) fsync-order test compared identical basename fsync_dir events by first-occurrence index — switched to occurrence-counted dir-fsync positions; (2) os.fsync call count was 8 not 7 because _write_manifest performs its own trailing parent-dir fsync after the manifest rename (implementation correct, test expectation fixed). One cross-module regression surfaced: tests/pipeline/test_api.py TestRenderStatusFromRows::test_row_backed_end_to_end_individual_render failed because the shared tts_engine MagicMock fixture's generate_voice wrote no file, so the new fsync discipline correctly failed the chunk (FileNotFoundError → job failed). Fixed the fixture to write the WAV (honest engine contract), in-scope test change. test_api.py → 67 passed.
- [x] Verify: `ruff check app/pipeline/tts_integration.py` clean; `pytest tests/pipeline/test_tts_integration.py -q` green
    **Done:** All verification gates pass. (1) ruff check app/pipeline/tts_integration.py tests/pipeline/test_tts_integration.py tests/pipeline/test_api.py → All checks passed (ruff 0.15.20, repo defaults E4/E7/E9/F). (2) python -m pytest tests/pipeline/test_tts_integration.py -q → 64 passed. (3) python -m pytest tests/pipeline -q → 907 passed (baseline 894 + 13 new; 1 StarletteDeprecationWarning, pre-existing). (4) git diff --stat app/tts.py → EMPTY (byte-for-byte untouchable — verified). Files changed this phase (all in scope): app/pipeline/tts_integration.py (modified), tests/pipeline/test_tts_integration.py (modified), tests/pipeline/test_api.py (modified — tts_engine fixture only). No new module files created; api_export.py, app.py, app/tts.py untouched. Phases 2-7 untouched.

### Phase 2: Manifest rebuild at startup (manifest = derived)
- [x] TDD RED: write tests asserting that after a simulated restart (new adapter instance over the same file-backed DB + RENDER_ROOT), a startup pass rebuilds manifest.json for every completed render_job that has a run dir but a missing/stale manifest; manifest regeneration is driven by rows, never by scanning the filesystem as authority
    **Notes:** RED: added TestRebuildManifests (13 tests) + TestRebuildStartupWiring (1 test) to tests/pipeline/test_adapter.py. Crash-recovery style per DD: file-backed SQLiteAdapter over tmp_path DB + tmp RENDER_ROOT; simulated restart = NEW adapter instance over same DB file. Coverage: individual manifest rebuilt from render_chunk 'done' rows (rows=truth — 3 wavs on disk but 2 done rows → chunk_count 2); batch enumerates sorted *.wav (non-wav files like audiobook.m4b ignored); run dir derived RENDER_ROOT/book-{id}/{job_id}/ when output_dir NULL; missing run dir → 'expired'; fresh manifest untouched (created_ms preserved); stale manifest (chunk_count mismatch) rewritten; negative test: wav files with NO completed row → nothing written (filesystem never authority); idempotent second call; non-completed rows untouched; individual with zero done chunks → chunk_count=0 manifest; mixed counts {'manifests_rebuilt':1,'jobs_marked_expired':1}; InMemory parity; wiring: rebuild called once with get_render_root() AFTER reconcile. RED confirmed: 14 failed with AttributeError (no rebuild_manifests).
- [x] Implement: extend the startup bootstrap (same reconciliation pass from Plan B) to rebuild manifests for completed render_job rows; if a render_job row is completed but its run dir is gone, mark the job status accordingly (artifact missing)
    **Notes:** Implemented in app/pipeline/adapter.py (no new modules): module-level _rebuild_manifests(conn, render_root) -> {'manifests_rebuilt': n, 'jobs_marked_expired': m} + _manifest_is_stale() + _ARTIFACT_MISSING_ERROR, mirroring _reconcile_stale_runs placement and transaction style (was_in_transaction captured before, trailing commit, same UPDATE shape). rebuild_manifests(render_root) methods added to BOTH SQLiteAdapter and InMemorySQLiteAdapter (parity convention), both guard _ensure_owner_thread(). Row-driven: SELECT completed rows; run dir = row.output_dir or derived RENDER_ROOT/book-{book_id}/{job_id}/; missing dir → UPDATE status='expired', error='artifact missing: run dir not found', finished_ms=now (DECISION: 'expired' is the schema-valid GC terminal state for gone artifacts per DD decision #10); individual chunk list from render_chunk WHERE status='done' ORDER BY idx (rows are truth); batch enumerates sorted *.wav in run dir. Manifest writes REUSE tts_integration._write_manifest (lazy import inside function — tts_integration imports PipelineStorage from this module at load, so top-level import would be circular; format/atomicity not duplicated). Staleness compares job_id/book_id/mode/chunk_count/chunks/status against rows, created_ms excluded so fresh caches survive; valid-JSON-non-dict treated as stale. app/pipeline/api_onboard.py: _get_production_storage() now calls adapter.rebuild_manifests(get_render_root()) as a SEPARATE call AFTER reconcile_stale_runs() (per plan — not merged); RENDER_ROOT read at call time; added top-level import of get_render_root (no cycle: api_onboard→tts_integration→adapter).
- [x] TDD GREEN: run the manifest rebuild tests; assert a job with missing dir is flagged and a job with dir+stale manifest gets a fresh manifest
    **Notes:** GREEN: all 14 new tests pass (14 passed, 83 deselected in tests/pipeline/test_adapter.py -k Rebuild). Key assertions verified: test_rebuild_marks_missing_run_dir_expired — completed job with gone output_dir → row status='expired', error='artifact missing: run dir not found', finished_ms set; test_rebuild_rewrites_stale_manifest — manifest claiming 1 chunk vs 3 done rows regenerated (chunk_count 3, fresh created_ms); test_rebuild_leaves_fresh_manifest_untouched — matching cache preserved (created_ms 12345 survives). Existing reconcile tests untouched and green: TestReconcileStaleRuns + TestReconcileStartupWiring (9 passed); the pre-existing bootstrap test (MagicMock) still passes with the added rebuild_manifests call (mock auto-creates the attribute; reconcile still called exactly once).
- [x] Verify: `ruff check app/pipeline/adapter.py app/pipeline/tts_integration.py` clean
    **Notes:** Verify: ruff check app/pipeline/adapter.py app/pipeline/tts_integration.py app/pipeline/api_onboard.py tests/pipeline/test_adapter.py → "All checks passed!" (ruff 0.15.20, repo defaults). python -m pytest tests/pipeline/test_adapter.py tests/pipeline/test_tts_integration.py -q → 161 passed. Full python -m pytest tests/pipeline -q → 921 passed (907 baseline + 14 new), 1 pre-existing starlette deprecation warning during test_voices collection (unrelated, present on baseline). git diff --stat app/tts.py → empty (byte-for-byte untouched). No new module files created; no threads/schedulers started (phase 3 scope untouched).

### Phase 3: Tombstoning GC (≥ 7 days, hourly, rows + files in one sweep)
- [x] TDD RED: write tests for the GC sweep: a completed render_job older than retention has its run dir deleted and its render_chunk rows tombstoned (status evicted) + render_job row marked expired in the same sweep; jobs younger than retention are untouched; a project_snapshot that references artifacts in the run dir keeps the referenced artifacts in the eligibility union (snapshot refs prevent GC); rows are never time-deleted (tombstoned only)
    **Note:** RED tests written in tests/pipeline/test_adapter.py: TestGCSweep (16 tests, file-backed tmp_path DB + real run dirs/files under tmp RENDER_ROOT) covering: dir deletion + chunk eviction + job expired in one pass; young jobs untouched; rows never time-deleted (row counts stable); non-completed statuses (running/failed/cancelled/expired) never candidates; already-expired jobs skipped; missing run dir still tombstones row without error; batch mode evicts 0 chunks; snapshot-referenced run dir survives (absolute path + URI-embedded refs, whole dir kept, skipped list reports job-ref); unparseable snapshot json does not pin artifacts; empty DB noop; float sub-day JOB_RETENTION_DAYS=0.001 override; CHUNK_RETENTION_DAYS=1000 extends retention (later cutoff wins); NULL output_dir derives run dir RENDER_ROOT/book-{id}/{job_id}/; mixed scenario counts; explicit kwargs beat env; InMemorySQLiteAdapter parity. TestGCScheduler (5 tests): single daemon thread, idempotent start, stop joins, stop-without-start noop, PIPELINE_GC_SCHEDULER=0 opt-out, plus end-to-end loop test (_gc_scheduler_loop with 0.05s interval against a real tmp DB + RENDER_ROOT polled to 'expired'). Helpers added: _insert_done_chunk_at, _insert_snapshot, _make_run_dir, _DAY_MS; _insert_completed_render_job extended with finished_ms kwarg (backward-compatible). Verified RED: collection fails with ImportError on _gc_scheduler_loop/start_gc_scheduler/stop_gc_scheduler (not yet implemented).
- [x] Implement the GC in an existing module (no new module per DD architectural rule — put the sweep in app/pipeline/adapter.py or api_export.py): env-tunable `job_retention_days` / `chunk_retention_days` (defaults ≥ 7 days), hourly schedule (background thread or APScheduler-style loop), never on the hot request path
    **Note:** Sweep implemented in app/pipeline/adapter.py (NO new module — DD rule): module funcs _gc_sweep(conn, render_root, *, job_retention_days, chunk_retention_days), _gc_retention_days(env_name, override) (override > env float > 7.0 default; malformed env falls back), constants _GC_DEFAULT_RETENTION_DAYS=7.0 (locks open item #1: both job+chunk default 7d, env-tunable at call time, float days for sub-day test overrides), _GC_EXPIRED_ERROR='expired by GC: artifacts removed after retention', _MS_PER_DAY. Candidates: status='completed' AND finished_ms IS NOT NULL AND finished_ms < cutoff; non-completed never candidates; already-expired skipped. Effective retention = LONGER of job/chunk (both must elapse — run dir contains chunk files, unit never split). Rows NEVER time-deleted — tombstoned only (chunks->'evicted', job->'expired' with _GC_EXPIRED_ERROR + finished_ms=now). Files deleted FIRST (shutil.rmtree ignore_errors on existing dirs), rows tombstoned after, in _reconcile_stale_runs transaction style (was_in_transaction capture + trailing commit). Crash-safety rationale documented in docstring: crash between file delete and row tombstone leaves 'completed' row with missing run dir = exactly what _rebuild_manifests() flips to 'expired' at next startup (phase-2 safety net); reverse order would orphan unreclaimable dirs. Returns {'run_dirs_deleted', 'chunks_evicted', 'jobs_expired', 'skipped_snapshot_referenced'}; run_dirs_deleted counts swept dirs whether or not dir existed. Adapter methods gc_expired_artifacts(render_root, *, job_retention_days, chunk_retention_days) added to BOTH SQLiteAdapter and InMemorySQLiteAdapter (parity convention), each with _ensure_owner_thread() then module func. Added imports: shutil, sys. Scheduler (start_gc_scheduler/stop_gc_scheduler/_gc_scheduler_loop/_gc_interval_seconds, thread name 'alexandria-gc-scheduler', daemon, idempotent singleton, PIPELINE_GC_SCHEDULER=0 opt-out, GC_INTERVAL_HOURS default 1h, first sweep deferred one full interval, per-sweep short-lived SQLiteAdapter over PIPELINE_DB_PATH, get_render_root() at call time, exceptions swallowed per-iteration) also landed in this step since the plan's S2 covers the scheduling. Fixed mid-flight: UnboundLocalError from missing 'global' in start_gc_scheduler; semantics locked to LONGER-of-two retentions after float-override tests initially failed (job-only override gated by 7d chunk default).
- [x] Implement the eligibility union: project_snapshot manifest artifact refs are collected before deletion so snapshots that reference a run dir keep it alive
    **Note:** Eligibility union implemented as _snapshot_referenced_run_dirs(conn, render_root) + _run_dir_is_referenced(run_dir, refs) in app/pipeline/adapter.py, called at the TOP of _gc_sweep BEFORE any deletion. Parsing is deliberately defensive because Plan I (snapshot projects) is not yet implemented and the snapshot manifest schema is UNKNOWN: snapshot_json parsed with json.loads (rows that fail to parse are skipped — a broken snapshot must never pin artifacts forever), then _walk_json_strings recursively collects EVERY string value anywhere in the document. A string counts as a reference when it names a path at/under render_root: matched by raw substring (render_root in value — covers relative + URI-embedded forms like file://...), absolute-path containment (render_root_abs in value — snapshot stores abspath while RENDER_ROOT env is relative), or scheme-stripped path containment ('scheme://' prefix split off before os.path.abspath). Substring-level false positives are filtered downstream by _run_dir_is_referenced (ref must equal the run dir abspath or start with run_dir_abs + os.sep) — the conservative direction (never delete a referenced dir) is safe. Referenced run dirs are SKIPPED this sweep: not deleted, not tombstoned, listed in skipped_snapshot_referenced. Empty project_snapshot table (expected while Plan I pending) => trivially empty union, sweep unchanged. Test coverage: absolute path inside run dir + URI-embedded ref keep whole run dir alive while unreferenced sibling is swept; unparseable snapshot json does not pin artifacts.
- [x] TDD GREEN: run GC tests with a file-backed fixture and a short retention override; assert files deleted, rows tombstoned/expired, snapshot-referenced artifacts survive
    **Note:** GREEN: `python -m pytest tests/pipeline/test_adapter.py tests/pipeline/test_tts_integration.py -q` => 182 passed (161 adapter incl. 21 new GC tests + 21 tts_integration), 0 failed. File-backed fixture with short retention override: tmp_path SQLiteAdapter DB + real run dirs/files under tmp RENDER_ROOT, retention overridden via monkeypatched env (JOB_RETENTION_DAYS=CHUNK_RETENTION_DAYS=0.001 float days) and via explicit kwargs (job_retention_days=0.001, chunk_retention_days=0.001). Asserted: files deleted, chunk rows tombstoned 'evicted', job rows 'expired', snapshot-referenced artifacts survive, never-time-deleted row counts stable. All 21 new tests green (16 TestGCSweep + 5 TestGCScheduler).
- [x] Verify: `ruff check` on the GC module clean; `pytest tests/pipeline -q` green
    **Note:** Verification gates ALL green. ruff check app/pipeline/adapter.py app/pipeline/api_onboard.py app/app.py tests/pipeline/test_adapter.py => All checks passed (ruff 0.15.20, repo defaults). `python -m pytest tests/pipeline/test_adapter.py tests/pipeline/test_tts_integration.py -q` => 182 passed. `python -m pytest tests/pipeline -q` => 942 passed, 0 failed (921 baseline + 21 new GC tests; no regressions). `python -m pytest tests/pipeline/test_smoke.py tests/pipeline/test_legacy_removed.py tests/pipeline/test_status_endpoints.py -q` => 19 passed (app.py lifespan addition is test-safe). `git diff --stat app/tts.py` => EMPTY (byte-for-byte untouched). No-thread-at-import check: importing app.app + app.pipeline.adapter spawns zero 'alexandria-gc-scheduler' threads (asserted). Lifespan positive control: `with TestClient(app) as c` starts exactly one daemon scheduler thread on entry and joins it to zero on exit, app routes respond normally. SCHEDULER WIRING CHOICE (documented decision): FastAPI lifespan on the production app + PIPELINE_GC_SCHEDULER=0 opt-out inside start_gc_scheduler. Rationale: the existing tests construct TestClient(app) without entering the context manager, so the lifespan (and thread) NEVER runs during pytest tests/pipeline — no test-safety hazards, no import-time threads, no conftest edits. Production path: uvicorn.run(app) runs the lifespan -> hourly daemon sweep. First sweep deferred one full interval (GC_INTERVAL_HOURS default 1h); daemon thread cannot block process exit.

### Phase 4: Bounded-range chunk streaming endpoint
- [x] TDD RED: extend tests/pipeline/test_export.py with GET /export/chunk/{job_id}/{idx}: 200 with audio/wav and Content-Range for a valid byte range, 206 + Content-Range for a partial range, 416 + Content-Range: bytes */N for a malformed range, 404 for unknown job/idx, 409/410 for evicted chunks (GC tombstone)
    **Note:** RED tests added to tests/pipeline/test_export.py: new class TestExportChunkBoundedRange (20 tests) using the existing storage/client fixtures (InMemorySQLiteAdapter + FastAPI router + dependency_overrides[get_storage]); RENDER_ROOT/PIPELINE_MAX_RANGE_BYTES set via monkeypatch at request time (both read at call time). Covers: 200 full GET audio/wav + accept-ranges; 206 + Content-Range partial range; open-ended bytes=N- 206; 416 + Content-Range bytes */N unsatisfiable; range-end-beyond-EOF clamped to EOF; malformed Range -> 400 (starlette 1.3.1 native — plan wording said 416 for malformed; documented deviation, pin >= 0.49.1 semantics win per ADVERSARIAL P7-2); 404 unknown job / unknown idx / non-int idx / negative idx; 409 pending & failed; 410 evicted; cap clamp (env=4 -> bytes=0-3/18 and bytes=2-5/18); full GET uncapped when larger than cap; relative wav_path resolved against output_dir; derived run dir RENDER_ROOT/book-{id}/{job_id}/ when output_dir NULL; path traversal 404 (relative '..' escape + absolute path outside run dir); missing WAV file 404 JSON; HEAD headers-without-body. RED run: 18 failed / 2 passed (the 2 pass coincidentally — status-only asserts against FastAPI's default JSON 404).
- [x] Implement GET /export/chunk/{job_id}/{idx} in api_export.py: resolve the render_chunk row → wav_path under RENDER_ROOT; serve with bounded-range WAV semantics (parse Range header, seek the file, stream the slice); guard against path traversal (resolve within the run dir)
    **Note:** Implemented in app/pipeline/api_export.py (no new module files): (1) GET /export/chunk/{job_id}/{idx} via @router.api_route(methods=["GET","HEAD"]) — idx validated as int (non-int -> 404 'Unknown chunk idx', plan allowed 404 or 400); job row lookup -> 404 'Unknown job_id'; chunk row lookup (job_id, idx) -> 404; status dispatch: evicted -> 410 Gone (GC tombstone), pending/failed -> 409 Conflict (no file read for non-done); wav_path resolved from the ROW (rows=truth): absolute paths (the row form — _mark_chunk_done stores os.path.join(resolved_dir, chunk_XXXX.wav)) used directly, relative paths (manifest form) joined against the run dir (row output_dir, else derived RENDER_ROOT/book-{id}/{job_id}/ via get_render_root at call time); os.path.realpath containment under the run dir realpath — escape -> 404 'Chunk not found'. (2) BoundedRangeFileResponse(FileResponse): stats the path and JSON-404s missing/vanished/non-regular files (FileResponse404 pattern), then rewrites scope['headers'] Range with the cap clamp before delegating — FileResponse reads Range from the ASGI scope at call time, so the clamp layers on top of starlette's native 200/206/416/400 semantics (no re-implemented parser, no whole-file buffering; 64 KiB seek/read streaming). (3) _cap_range_value/_cap_range_headers: clamp ONLY the strict single-range form bytes=start-end to start+cap-1; multi-range/suffix/malformed pass through untouched to starlette's pinned (>= 0.49.1) DoS-fixed parser. (4) _max_range_bytes: PIPELINE_MAX_RANGE_BYTES env (default 4 MiB = _DEFAULT_MAX_RANGE_BYTES), read at call time; full GET without Range is never capped. media_type audio/wav, no Content-Disposition (inline streaming for <audio>). Existing endpoints (download_render, export_job_chunks, render, merge) untouched.
- [x] TDD GREEN: run the range tests; assert 200/206/416/404/410 status codes and correct Content-Range headers
    **Note:** GREEN: tests/pipeline/test_export.py 27 passed (7 existing TestDownloadRender + 20 new TestExportChunkBoundedRange); status codes and Content-Range headers asserted per the spec (200 audio/wav full, 206 + Content-Range for valid/open-ended ranges, 416 + Content-Range bytes */N unsatisfiable, 400 malformed, 404 unknown job/idx/non-int, 409 pending/failed, 410 evicted, capped slices 206 with clamped Content-Range). Full suite regression: python -m pytest tests/pipeline -q -> 962 passed (942 baseline + 20 new, no regressions). Guard suite tests/pipeline/test_legacy_removed.py -> 12 passed (new route /api/pipeline/export/chunk/{job_id}/{idx} under the allowed /api/pipeline surface; no legacy family match). app/tts.py diff empty.
- [x] Verify: `ruff check app/pipeline/api_export.py tests/pipeline/test_export.py` clean
    **Note:** Verify: ruff check app/pipeline/api_export.py tests/pipeline/test_export.py -> 'All checks passed!' (ruff 0.15.20, exit 0). Starlette pin confirmed: installed starlette 1.3.1 satisfies >= 0.49.1 (GHSA-7f5h-v6xp-fcq8 / CVE-2025-62727 O(n^2) Range-header DoS fixed in 0.49.1; verified native range semantics in the installed responses.py source: malformed -> 400, unsatisfiable -> 416 + Content-Range bytes */N, multi-range -> multipart 206). No files changed outside scope: app/pipeline/api_export.py (+236) and tests/pipeline/test_export.py (+302) only; app/tts.py untouched; no new module files; no subprocess/ffmpeg; no GC/scheduling changes; existing endpoints' behavior untouched.

### Phase 5: Whole-book audio endpoint
- [x] TDD RED: write tests for GET /export/audio/{job_id}: returns the assembled whole-book audio (the job's output artifact, e.g. audiobook.m4b or concatenated WAV) with correct media type; 404 for unknown job; 410 for expired job; supports Range (for seek in the browser audio element)
    **Note:** RED tests: added 27 tests in tests/pipeline/test_export.py (class TestExportAudioWholeBook) + module-level _make_wav(data, sample_rate, channels, bits) 44-byte-header PCM WAV builder and _seed_job/_seed_chunk/_seed_individual_render helpers reusing existing storage/client fixtures (InMemorySQLiteAdapter + dependency_overrides[get_storage]). Coverage: artifact serving (m4b 200 audio/mp4 accept-ranges inline, m4b Range 206, media-type-by-extension .mp3→audio/mpeg, HEAD headers-only, missing artifact FILE→404 JSON, late-merge parity m4b), job-state dispatch (unknown→404, expired→410, running/failed→404 'Job not completed'), synthesized whole-book WAV (full 200 == _make_wav(d0+d1), Range across chunk boundary, Range inside 2nd chunk, open-ended Range, 416 unsatisfiable, 400 malformed, per-request cap via PIPELINE_MAX_RANGE_BYTES=4, HEAD, batch-mode dir enumeration, batch empty→404, evicted chunk→410, pending chunk→409, missing chunk file→404, no chunk rows→404, derived run dir when output_dir NULL, path traversal→404, format mismatch sr 16000 vs 8000→409). RED run: 26 failed / 28 passed (1 coincidental pass: traversal test asserts only 404+json).
- [x] Implement GET /export/audio/{job_id} in api_export.py: read render_job.output_artifact_path (or assemble from chunks for individual mode), stream with Range support, correct Content-Type per artifact extension
    **Note:** Implemented GET /api/pipeline/export/audio/{job_id} in app/pipeline/api_export.py (registered via @router.api_route(methods=['GET','HEAD']) — FastAPI does not auto-add HEAD). Decision: synthesized form uses OPTION (a) — a streaming concatenation response, NO temp file materialization (pure Python WAV handling, no subprocess/ffmpeg). Key components: (1) _media_type_for_artifact — audio/mp4 for .m4b/.m4a/.mp4, audio/mpeg .mp3, audio/wav .wav, etc. (2) _resolve_within_run_dir — phase-4 containment discipline (realpath must stay under run dir; escape→404 JSON 'Audio file not found'). (3) _parse_wav — reads RIFF/WAVE header, locates data chunk, returns (header_bytes, data_offset, data_size, fmt=(audio_format, channels, sample_rate, bits)); data size past EOF clamped; ValueError→409. (4) _patch_wav_header — patches RIFF size (bytes 4-8) and data-chunk size (bytes before data offset) for the summed payload. (5) ConcatenatedWavResponse(Response) — streams chunk 0 header + each chunk's PCM data from disk in 64 KiB slices via iterate_in_threadpool (bounded memory, event loop never blocked); Range parsed with starlette's own FileResponse._parse_range_header (identical 400-malformed/416-unsatisfiable+suffix semantics as phase 4), single ranges capped per-request to PIPELINE_MAX_RANGE_BYTES (default 4 MiB, read at call time) like phase 4; MULTI-RANGE falls back to full 200 (multipart/byteranges unsupported on virtual stream — documented; browser <audio> seeks never send multi-range); HEAD returns Content-Length (full virtual size) without body; Content-Type audio/wav; Content-Disposition omitted (inline playback). Dispatch (rows=truth): no row→404 'Unknown job_id: {id}'; expired→410 'Job expired by garbage collection' (phase-4 evicted precedent); non-completed→404 'Job not completed (status: X)'; file artifact (output_artifact_path not a dir) → BoundedRangeFileResponse with extension media type; else run_dir/audiobook.m4b parity check (POST /merge late m4b); else synthesized: individual mode uses render_chunk wav_path rows (containment-checked; evicted→410, non-done→409, no rows→404), batch mode enumerates sorted *.wav in run dir (empty→404 'No audio files found in output directory'); all chunks must exist (404) and share one fmt (differ→409, documented limitation — no resampler until phase 6). Added imports: struct, Response, starlette.concurrency.iterate_in_threadpool, starlette.datastructures.Headers/MutableHeaders, starlette.responses.MalformedRangeHeader/PlainTextResponse/RangeNotSatisfiable (public, stable under starlette>=0.49.1 pin). Phase-4 BoundedRangeFileResponse untouched (used as-is).
- [x] TDD GREEN: run the audio endpoint tests; assert media types and range behavior
    **Note:** GREEN: python -m pytest tests/pipeline/test_export.py -q → 54 passed (27 existing + 27 new), 1 warning (pre-existing StarletteDeprecationWarning). Two initial failures were test-data bugs, not code bugs: fixture b'M4B-DATA-0123456789' is 19 bytes (not 20) — corrected content-range 'bytes 2-9/19', body b'B-DATA-0', HEAD content-length '19'. Implementation passed on first full run. ruff check on both touched files clean.
- [x] Verify: `ruff check app/pipeline/api_export.py` clean
    **Note:** Verification all green: (1) ruff check app/pipeline/api_export.py tests/pipeline/test_export.py → All checks passed. (2) python -m pytest tests/pipeline -q → 989 passed (962 baseline + 27 new; no regressions), 1 warning. (3) python -m pytest tests/pipeline/test_legacy_removed.py -q → 12 passed (guard suite; new route /api/pipeline/export/audio/{job_id} is outside legacy families). (4) git diff --stat app/tts.py → empty (hard constraint satisfied). Files touched: app/pipeline/api_export.py (imports + new section), tests/pipeline/test_export.py (docstring, struct import, helpers, new class). No new modules; no changes to phase 2-3 GC/manifest code; no subprocess/ffmpeg; no GC/scheduling behavior.

### Phase 6: 3-phase FFMETADATA1 M4B export (concat → metadata → mux)
- [x] TDD RED: write tests for POST /export/m4b with a 2-chunk fixture: phase 1 concat (existing ffmpeg concat), phase 2 FFMETADATA1 generation (validate TIMEBASE, integer ms, chapter END clamped to duration), phase 3 mux with cover art and title/author/narrator/year/description tags; assert output is audio/mp4 and ffmetadata file content is CI-validated on the 2-chunk fixture
    **Note:** TDD RED: added 24 tests in tests/pipeline/test_export.py (class TestExportM4B + module-level _seed_job_row/_seed_chunk_row/_seed_two_chunk_job/_wav_duration_ms/_ffprobe_json/_parse_ffmetadata helpers; new imports os/json/subprocess). Request shape: multipart Form (job_id + title/author/narrator/year/description, optional cover UploadFile). Fixture: 2 real 0.5s PCM WAVs (16000Hz mono 16-bit) via existing _make_wav, written to run dir; individual mode seeds render_chunk rows (rows=truth), batch mode relies on dir enumeration. Coverage: full success + artifact flags (mp3 True on this ffmpeg 7.1.5, audacity True), output_artifact_path row update + m4b servable as audio/mp4 via phase-5 GET /export/audio, CI-validated ffmetadata (header ;FFMETADATA1, TIMEBASE=1/1000, isdigit START/END, per-chunk boundaries [0,500]/[500,1000], last END==1000==header-derived concat duration, last END <= real m4b ffprobe duration), global tag mapping title/artist/album_artist/date/comment + m4b round-trip, escaping round-trip (=;#\ and newline strip), mp3 codec probe, libmp3lame degrade via monkeypatch of api_export._libmp3lame_available (mp3 False/mp3_path None/message present/m4b still ok), ZIP_STORED audacity bundle contents byte-exact, batch mode, derived run dir under RENDER_ROOT env, cover attached_pic via real ffprobe disposition, cover oversize 400 (PIPELINE_MAX_COVER_BYTES env-tunable), cover non-image 400, dispatch 404/410/404/404/409/410/400/409/404/404, ffmpeg-missing 500 via monkeypatched subprocess.run. RED confirmed: all 24 fail with 405 (route not implemented yet).
- [x] Implement the FFMETADATA1 generator (in api_export.py or a helper within an existing module): auto chapter markers from a single ffprobe pass, END clamped to actual duration; embed cover art when provided; MP3 output where libmp3lame is available (feature-detect) and Audacity ZIP_STORED export where supported
    **Note:** Implemented POST /api/pipeline/export/m4b in app/pipeline/api_export.py (sync def, multipart Form: job_id required + title/author/narrator/year/description optional + cover UploadFile optional). New module-level helpers: _max_cover_bytes (env PIPELINE_MAX_COVER_BYTES, default 20MiB, read at call time), _run_media_command (list-args only, capture_output, 300s ffmpeg/60s ffprobe timeouts, FileNotFoundError/TimeoutExpired/non-zero -> JSON 500 with bounded stderr; mirrors merge_audiobook), _libmp3lame_available (per-process cached `ffmpeg -encoders` probe, OSError/timeout degrade to False), _clean_metadata_value (strips 0x00-0x1F + 0x7F incl. newline, trims), _escape_ffmetadata_value (ffmpeg writer convention: backslash FIRST then = ; # — reader unescapes, verified round-trip byte-exact on 7.1.5), _write_ffmetadata (global tags title/artist(author)/album_artist(narrator)/date(year)/comment(description); [CHAPTER] blocks TIMEBASE=1/1000 + integer-ms START/END + title; atomic tmp->os.replace), _chunk_duration_ms (reuses phase-5 _parse_wav; data_size/byte_rate math; non-PCM -> 409), _probe_duration_ms (single ffprobe format=duration pass). Endpoint flow: row lookup (rows=truth; unknown 404, expired 410, non-completed 404) -> run_dir = output_dir or derived RENDER_ROOT/book-{id}/{job_id} (missing dir 404) -> chunk sources (individual: render_chunk ORDER BY idx, no rows 400, evicted 410, non-done 409, containment-checked wav_path, missing 404; batch: sorted *.wav isfile, none 400) -> format validation (identical fmt required, 409 mismatch) -> CONCAT via concat demuxer (-c:a copy) into .m4b-export/intermediate.wav with per-demuxer single-quote escaping -> METADATA: chapter boundaries from chunk durations, last END clamped via min() to the ffprobe duration (never exceeds real stream; AAC priming makes the muxed container slightly longer than the source timeline — chapters track the source) -> MUX: -map_metadata 1 -map_chapters 1, aac 128k, -f ipod; cover as -map 2:v -c:v mjpeg -disposition:v attached_pic (content-type must start with image/, size cap +1 sentinel read) -> MP3 via libmp3lame 128k when detected (else m4b-only) -> audiobook-audacity.zip ZIP_STORED (chunk wavs byte-exact + mp3 when produced; always producible — Audacity WAV import is universal; no flag) -> UPDATE render_job SET output_artifact_path=m4b on full success only. Intermediates live in hidden .m4b-export/ subdir (never seen by batch *.wav enumeration or zip fallback) removed in finally via shutil.rmtree; durable outputs at run dir root: audiobook.m4b / audiobook.mp3 / audiobook.ffmetadata / audiobook-audacity.zip. Response {status: ok, output_path, mp3, mp3_path, audacity, audacity_path} + message key only when mp3 degraded (DD open item #8). New imports: shutil, subprocess, fastapi File/Form/UploadFile. No new module files; no legacy routes; POST /merge untouched.
- [x] TDD GREEN: run the m4b export tests with the 2-chunk fixture; assert ffmetadata correctness (TIMEBASE format, integer ms, chapter END clamp) and final artifact media type
    **Note:** TDD GREEN: all 24 TestExportM4B tests pass on first implementation run (21/24 immediately; 3 test-fixture bugs fixed, not implementation bugs — missing-run-dir test seeded WAVs into a non-existent dir, and pending/evicted tests violated render_chunk UNIQUE(job_id, idx) by inserting duplicate idx rows; switched to seed_rows=False + explicit status rows). CI-validated ffmetadata assertions pass on the 2-chunk fixture: ;FFMETADATA1 header, TIMEBASE=1/1000, digit-only START/END, per-chunk boundaries [0,500]/[500,1000], last END==1000 == header-derived concat duration == the single-ffprobe WAV duration, and last END <= real muxed m4b duration (AAC priming ~1.08s). Metadata round-trip verified through real ffmpeg mux: title/artist/album_artist/date/comment present in the m4b tags; =;#\ specials round-trip byte-exact; newline stripped. Cover embedded as mjpeg attached_pic (ffprobe disposition.attached_pic==1). MP3 codec=mp3 via libmp3lame; degrade branch (monkeypatched _libmp3lame_available) returns mp3 False + message, m4b still produced. ZIP_STORED bundle contains byte-exact chunk WAVs + mp3. m4b servable through phase-5 GET /export/audio as audio/mp4 after output_artifact_path row update.
- [x] Verify: `ruff check app/pipeline/api_export.py tests/pipeline/test_export.py` clean; guard suite 12/12 still green (no legacy /api/merge_m4b or /api/export_audacity routes resurrected — new paths are /api/pipeline/export/*)
    **Note:** Verification gates all pass. ruff check app/pipeline/api_export.py tests/pipeline/test_export.py: clean (ruff 0.15.20, repo defaults). python -m pytest tests/pipeline/test_export.py -q: 78 passed (54 existing + 24 new), 1 pre-existing StarletteDeprecationWarning. python -m pytest tests/pipeline -q: 1013 passed (989 baseline + 24 new — exact expected count, zero regressions). python -m pytest tests/pipeline/test_legacy_removed.py -q: 12/12 guard green — no /api/merge_m4b or /api/export_audacity resurrected; only new path is /api/pipeline/export/m4b (safe per guard's exact-match + family rules). git diff --stat app/tts.py: empty (byte-for-byte untouched). No new module files created (helpers live inside app/pipeline/api_export.py). No shell=True anywhere (all subprocess calls list-args). No render_job/render_chunk semantics, GC, or manifest code changed. Phase 7 (security review) not implemented.

### Phase 7: Regression gate, guard suite and verification
- [x] Run `pytest tests/pipeline -q --cov=app/pipeline --cov-report=term-missing` and record pass count + coverage; api_export coverage must rise from 76% baseline
    **Done:** Regression gate + coverage: `python -m pytest tests/pipeline -q --cov=app/pipeline --cov-report=term-missing` → **1013 passed, 0 failed** (37.02s; 517 warnings — pre-existing ResourceWarnings in test_api walk tests + StarletteDeprecationWarning, not new). Path-style --cov=app/pipeline used (module-style app.pipeline gives "no data" per prior plans). Coverage table (Stmts/Miss/Cover): api_export.py **630/48/92%**, adapter.py 354/9/**97%**, tts_integration.py 175/5/**97%**, api_onboard.py 60/3/95%, api_walks.py 55/4/93%, assembly.py 44/0/100%, extract.py 125/9/93%, operations.py 130/13/90%, populate.py 91/5/95%, review.py 78/0/100%, schema.py 15/0/100%, walks/runner.py 226/55/**76%**, walk_2c_alias_resolution.py 172/31/82%, walk_2a 89%, 2b 87%, 2d 88%, 2e 90%, 2f 86%, 2g 86%, 2h 87%, 2i 86%, _llm_helpers.py 90%; **TOTAL 3344 stmts / 304 miss / 91%**. api_export gate: 92% > the 76% pre-Plan-B baseline (gate met). Nuance recorded for QA: 95% at end of Plan B was on a ~350-stmt module; phases 4-6 grew it to 630 stmts (new chunk/audio/m4b code, mostly tested) so the % dipped 95→92 purely by denominator — absolute covered statements rose. Low-coverage modules (record for QA, NOT fixed per instructions): walks/runner.py 76% and walk_2c_alias_resolution.py 82% — both pre-existing Plan-B-era, out of Plan C scope. Coverage misses in api_export (48) are dominated by error-branch lines (evicted/pending 409/410, ffprobe failures, mp3 degrade, cover 400s) exercised via monkeypatch in test_export.py; lines 1217-1228 (merge ffmpeg error paths) and 1262-1263 (cover-bytes-env ValueError) are the notable untested stragglers. No fixes made this step.
- [x] Run `pytest tests/pipeline/test_legacy_removed.py -q` and verify 12/12 still green
    **Done:** Guard suite: `python -m pytest tests/pipeline/test_legacy_removed.py -q` → **12/12 passed** (1.55s): 9 legacy-file-absent + 1 live route scan + 1 state.ts pipelineEnabled + 1 project_manager identifier test. New endpoints verified NOT flagged: /api/pipeline/export/chunk/{job_id}/{idx}, /api/pipeline/export/audio/{job_id}, /api/pipeline/export/m4b are all under the preserved /api/pipeline router — the guard matches the 29 legacy routes by EXACT path plus legacy-family prefixes (never substring), and no legacy family prefix is /api/pipeline, so the three new routes (and the pre-existing /api/pipeline/voices, /api/pipeline/merge) pass clean. Live-route sanity assertion (/api/pipeline/onboard present) confirms the scan is exercising the real route table. No legacy endpoints resurrected: no /api/merge_m4b, /api/export_audacity, /api/m4b_cover. Guard green alongside full suite (P7-S1: 1013 passed).
- [x] Security review: path traversal on RENDER_ROOT resolution (all new endpoints resolve paths within the run dir; reject `..`), Range header DoS (starlette≥0.49.1 pin preserved; malformed ranges → 416 not 500), ffmpeg argument injection (no user-controlled shell; subprocess list-args only), cover upload size/type limits; document findings
    **Done:** Security review — all 5 vectors checked against the phase 1-6 code; verdict: **NO exploitable vulnerability found; no fixes made** (documentation-only step; P7-S4 QA-Reviewer will independently re-review). Per-vector findings:

**1. Path traversal on RENDER_ROOT resolution — CLEAN.** export_chunk: idx int-validated (non-int→404) and NEVER used to build a path; job_id used only in parameterized SQL; wav_path comes from the render_chunk ROW (rows=truth), containment-checked via os.path.realpath under realpath(run_dir) (escape→404, api_export.py:470-475). export_audio: artifact_path/chunk wav_paths go through _resolve_within_run_dir (707-722) — same realpath discipline; run_dir = row output_dir or derived RENDER_ROOT/book-{id}/{job_id} when NULL. export_m4b: same containment on candidates (1496-1498) plus concat-list paths written only after containment. Batch mode enumerates within run_dir only. get_render_root() reads env RENDER_ROOT at call time — operator-controlled env var, documented ACCEPTABLE (attacker without env access cannot set it; default data/render_root under cwd). Note: RenderRequest.output_dir is user-influenced (pre-existing Plan B contract, honored since B) and stored in the row, but containment anchors on the row's OWN output_dir — serving can never escape the named dir; an attacker can only serve files under a dir they themselves chose. DB-row poisoning (wav_path/output_dir) requires DB write access = full server compromise; out of threat model.

**2. Range header DoS — CLEAN.** Installed starlette **1.3.1 satisfies >= 0.49.1** pin (GHSA-7f5h-v6xp-fcq8 / CVE-2025-62727 O(n²) Range parsing DoS fixed in 0.49.1). Verified installed source: _parse_ranges/_parse_range_header are linear (single pass + sort), no quadratic behavior. Malformed Range → 400 (MalformedRangeHeader) — NOT 500; unsatisfiable → 416 + Content-Range: bytes */N (RangeNotSatisfiable). Cap: PIPELINE_MAX_RANGE_BYTES (default 4 MiB, read at call time) clamps single-range requests in BOTH BoundedRangeFileResponse (phase 4) and ConcatenatedWavResponse (phase 5); suffix bytes=-N self-bounded by N; multi-range passes to starlette's bounded multipart handling (or full-200 fallback on the virtual stream — equivalent to an uncapped plain GET, no new exposure). No unbounded buffering: FileResponse and ConcatenatedWavResponse stream via 64 KiB seek/read slices / iterate_in_threadpool. Env has NO requirements.txt (repo never tracked one) — the >=0.49.1 pin is a documented CI/build-time guard (CONTRACTS rule 13 / ADVERSARIAL A2-3); installed version satisfies it.

**3. ffmpeg argument injection — CLEAN.** grep-verified **no shell=True anywhere in api_export.py**; all 4 subprocess.run calls are list-args (merge_audiobook L1199, _run_media_command L1275, _libmp3lame_available L1296 + the libmp3lame probe). User-controlled metadata (title/author/narrator/year/description) is control-char-stripped (_clean_metadata_value: 0x00-0x1F + 0x7F incl. newline) and ffmetadata-escaped (\ = ; #, _escape_ffmetadata_value — round-trip verified against ffmpeg 7.1.5 in phase 6) then written to the ffmetadata FILE, passed as `-i <file>` — never interpolated into argv or a shell. Concat demuxer file list single-quote-escapes `'` → `'\''` (L1561) on containment-checked server paths. Chapter titles are server-generated ("Chapter N"). Cover passed as `-i <fixed cover.img>`. ffmpeg/ffprobe run with capture_output + 300s/60s timeouts; failures → JSON 500 with bounded stderr, never a shell.

**4. Cover upload size/type limits — CLEAN.** PIPELINE_MAX_COVER_BYTES (default 20 MiB, env-tunable, read at call time) enforced with read(max_bytes+1) sentinel → oversize 400 (L1547-1549); content-type must start with image/ → 400 (L1550-1551); the file is NEVER trusted as a path — written to fixed tmp_dir/cover.img (client filename never used) and ffmpeg sniffs the real format (a fake image fails at mux → 500, no code execution).

**5. GC safety — CLEAN.** Scheduler starts ONLY from the FastAPI lifespan (app/app.py L40-54 start_gc_scheduler/stop_gc_scheduler); never at import time; existing tests construct TestClient(app) WITHOUT the context manager so the lifespan/thread never runs under pytest (verified P3: zero 'alexandria-gc-scheduler' threads at import); PIPELINE_GC_SCHEDULER=0 opt-out inside start_gc_scheduler; first sweep deferred one full interval. Rows tombstoned NEVER deleted: _gc_sweep issues UPDATE render_chunk→'evicted' + UPDATE render_job→'expired' (no DELETE anywhere); snapshot-referenced run dirs keep artifacts alive (eligibility union via _snapshot_referenced_run_dirs, skipped_snapshot_referenced); non-completed rows never candidates (WHERE status='completed'); already-expired skipped. Crash window (files deleted → rows tombstoned) self-heals via _rebuild_manifests flipping completed-with-missing-dir → expired at startup.

Files changed: NONE (this step is review-only; no genuine vulnerability found to fix). ruff clean on all in-scope modules (api_export, adapter, tts_integration, api_onboard, app.py + 5 test files); git diff --stat app/tts.py EMPTY.
- [ ] Code review pass via exec-manager QA-Reviewer; fix MINOR findings
- [ ] Commit: `feat(pipeline): durable render artifacts, tombstoning GC and export backend`

## Completion Criteria

- All renders write under RENDER_ROOT/book-{id}/{job_id}/ with the 2-fsync discipline; render_chunk rows marked done only after WAV exists
- manifest.json rebuilt from rows at startup (derived, never authority); completed jobs with missing dirs flagged
- Hourly GC sweep: ≥ 7 day retention (env-tunable), tombstone rows + delete files in one sweep, snapshot-referenced artifacts in the eligibility union, rows never time-deleted
- GET /export/chunk/{job_id}/{idx} (bounded range, 206/416), GET /export/audio/{job_id} (whole-book, range-capable), POST /export/m4b (3-phase FFMETADATA1, chapters, cover, MP3/Audacity where supported) all implemented and tested
- Full pytest suite green, api_export coverage up, guard 12/12, ruff clean, security review documented

## References

- `artifacts/designs/pending/DD-universal-upgrade.md` — P2 A2-r phase, FP2/FP6, workflows (Render, Export), decision #10 (GC), open item #1 (retention defaults)
- `artifacts/designs/parts/universal-upgrade/CONTRACTS.md` — render_chunk done-only-after-fsync, GC rule, 3 export endpoints
- `artifacts/designs/parts/universal-upgrade/README.md` — Plan C row
- Prior: `TASK-universal-upgrade-B-render-walk-persistence.md` (completed)
