# Task: Snapshot Projects

## Problem Statement

The pre-rewrite app had saved-script projects (with a legacy format and localStorage script persistence); the rewrite deleted them and today there is NO persistence of edited script state — `alexandria-pipeline-book-id` (state.ts L176) is the only pipeline persistence, and `a12n-pipeline` edits live only in memory/DB. This plan (DD A4-r Snapshot Projects half, cap7, FP3+FP4+A4-r) restores **snapshot projects** as a pipeline-native feature: a `project_snapshot` table (created in Plan A) stores auto-named snapshots of the script/spans state per book; the API (api_operations.py) gains POST /projects (auto-named, returns name), GET /projects, POST /projects/load (merge-vs-replace: characters never deleted), DELETE /projects/{name}, and PATCH /projects/{name} (rename). Restore is blocked while any active walk_run/render_job row exists (409 + Retry-After); audio missing after restore → explicit 're-render' notice. The frontend gains a Projects tab (Save auto-named / Load / Delete / Rename) wired to these endpoints. The DD user defaults are adopted: **generated snapshot names** for saved projects v1 (no free-form name input).

Contract note: the upstream CONTRACTS.md § Universal Upgrade registration lists 10 new endpoints but NOT the rename PATCH — this plan registers PATCH /projects/{name} in the universal-upgrade CONTRACTS.md and appends it to the upstream registration (append-only rule; the upstream section is an uncommitted working-tree diff).

## Dependencies

- Plan B (active-run blocking check via walk_run/render_job rows; adapter snapshot access builds on Plan A's project_snapshot table)
- Plan E (tab navigation — Projects tab must be reachable)
- `artifacts/designs/parts/universal-upgrade/CONTRACTS.md` — project_snapshot + 5 projects endpoints

## Phases

### Phase 1: Snapshot CRUD backend (api_operations.py)
- [x] TDD RED: write tests in tests/pipeline/test_operations.py (or new test_snapshots.py) asserting POST /projects creates an auto-named snapshot (e.g. `Project {YYYY-MM-DD HH:MM}`) with the current spans/script state; GET /projects lists them newest-first; DELETE removes; PATCH renames (409 on duplicate name)
    **Note:** TDD-RED: Wrote 24 failing tests in NEW tests/pipeline/test_snapshots.py (followed test_api.py TestClient conventions — dependency_overrides[get_storage] with InMemorySQLiteAdapter + minimal spine fixture; chose test_snapshots.py over test_operations.py because the latter tests the OperationExecutor service, not API endpoints). RED confirmed: 21 failed / 3 passed (the 3 passing were 404-assertions that pass vacuously pre-implementation). Covers: POST /projects auto-name + manifest capture (spans text/instruct/speaker, characters voice_assignment_id, book_version), size_bytes == len(snapshot_json), unknown-book 404, same-minute distinct names; GET /projects newest-first + book_id filter + exact DTO keys; DELETE removes + 404; PATCH rename + 409 duplicate + 404 + rename-to-self no-op + parametrized invalid names (empty/whitespace//, \, .., control chars). AUTO-NAME FORMAT DECISION: chose plan-text format `Project {YYYY-MM-DD HH:MM}` (local time) over CONTRACTS example `book-{id}-{timestamp}`; asserted via regex ^Project \d{4}-\d{2}-\d{2} \d{2}:\d{2}( \(\d+\))?$ — the optional ` (N)` suffix documents the same-minute PK-collision disambiguation. Tests use adapter methods (create_project_snapshot/get_project_snapshot) for seeding — RED fails on those missing too, resolved in P1-S2.
- [x] Implement: add snapshot endpoints to api_operations.py (already owns project-related operations) — POST /projects (auto-generate name from book_id + timestamp; store snapshot_json = current span/script manifest), GET /projects, POST /projects/load, DELETE /projects/{name}, PATCH /projects/{name}; all behind the existing /api/pipeline router (api.py aggregator already includes operations)
    **Note:** Implemented: (1) adapter.py — 5 new PipelineStorage abstract methods (list_project_snapshots, get_project_snapshot, create_project_snapshot, delete_project_snapshot, rename_project_snapshot) implemented in BOTH SQLiteAdapter and InMemorySQLiteAdapter, mirroring walk_override conventions (compose execute_* which auto-commit outside transaction() and join an open one; parameterized SQL only; ordering created_ms DESC, name ASC tiebreak; ABC test test_adapter.py uses subset semantics so no breakage). (2) api_operations.py — request models CreateProjectRequest{book_id} / RenameProjectRequest{new_name}; helpers _is_valid_snapshot_name (rejects empty/padded, /, \, .., control chars, >200 chars) and _auto_snapshot_name (Project {YYYY-MM-DD HH:MM} + (N) suffix); 4 endpoints behind the existing APIRouter(prefix=/api/pipeline): POST /projects (validates book exists 404, builds manifest via export_annotated_script + character_book voice-assignment query + get_book_version, json.dumps sort_keys, IntegrityError retry loop for same-minute PK collisions), GET /projects (optional book_id filter, DTO {name, book_id, created_ms, size_bytes} derived from len(snapshot_json)), DELETE /projects/{name} (404 unknown), PATCH /projects/{name} (400 invalid name, 404 unknown, 409 duplicate, rename-to-self no-op). No new module files (architectural rule #1), no /api/scripts/*, no app/project.py. DEVIATION from plan text: POST /projects/load is NOT implemented in this phase — restore semantics (merge-vs-replace, active-run 409, re-render notice) are explicitly phase 2 scope per the task contract; the route would be a dead 404 stub otherwise. snapshot_json is pure data (JSON text), never executed.
- [x] TDD GREEN: run the snapshot tests; assert auto-naming, list order, delete, rename
    **Green:** TDD-GREEN: `pytest tests/pipeline/test_snapshots.py -q` → 24 passed (all auto-naming, manifest-capture, list-order, book_id filter, delete, rename/409/404/400 assertions green). Full `pytest tests/pipeline -q` → 1187 passed (includes the 24 new snapshot tests; baseline was 841 + prior plans' growth — no regressions). Import smoke: `import app.pipeline.api_operations` OK; router exposes exactly the 4 new routes (/api/pipeline/projects POST+GET, /api/pipeline/projects/{name} DELETE+PATCH). Ruff clean on all three touched files.
- [x] Verify: `ruff check app/pipeline/api_operations.py` clean; guard suite 12/12 still green (no /api/scripts/* endpoints resurrected — new paths /api/pipeline/projects/* are guard-legal)
    **Verify:** VERIFY PASSED: (1) `ruff check app/pipeline/api_operations.py` → All checks passed (also ran ruff on app/pipeline/adapter.py and tests/pipeline/test_snapshots.py — clean). (2) Guard suite `pytest tests/pipeline/test_legacy_removed.py -q` → 12/12 green — no /api/scripts/* or legacy family routes resurrected; new /api/pipeline/projects/* paths are guard-legal (verified: family prefixes /api/upload, /api/scripts, /api/voices etc. do not match). (3) `python -m compileall` clean on both modified modules. (4) git status shows only in-scope files: M app/pipeline/adapter.py, M app/pipeline/api_operations.py, ?? tests/pipeline/test_snapshots.py — no frontend, no app/tts.py, no CONTRACTS.md edits. Nothing committed (tree left dirty per instructions).

### Phase 2: Restore semantics (merge-vs-replace, active-run block, re-render notice)
- [x] TDD RED: write tests asserting POST /projects/load (a) returns 409+Retry-After while any active walk_run or render_job row exists, (b) merges snapshot state into the current book without deleting characters, (c) flags missing audio → explicit 're-render' notice in the response
    **Note:** TDD RED: added 21 failing tests to tests/pipeline/test_snapshots.py in 4 classes — TestLoadBlockedWhileActive (409 + Retry-After:5 for active walk_run/render_job parametrized pending/running; 200 when only terminal rows or no rows), TestLoadMergeSemantics (span text/instruct restored; spans absent from snapshot untouched; spans deleted since save skipped, not resurrected; characters absent from snapshot kept; missing character created with name+voice+junction; shared character row gets junction only, voice untouched; existing character voice never clobbered), TestLoadNotFound (404 unknown snapshot/book/cross-book; 500 corrupt JSON), TestLoadReRenderNotice (audio present under RENDER_ROOT -> flag False; GC'd dir, never-rendered, out-of-root ref -> flag True; response key set {status,name,book_id,re_render_required}). RED confirmed: 21 failed, 24 passed (route absent -> 405 via /projects/{name} param-route match).
- [x] Implement: load handler — check active walk_run/render_job rows (query via adapter, same statuses as reconciliation: pending/running), 409 on active; apply snapshot_json merge (spans text/instructions updated; characters kept, never deleted); include re_render_required flag when referenced audio artifacts are absent (RENDER_ROOT check)
    **Note:** Implemented in app/pipeline/api_operations.py (no new modules, all behind existing /api/pipeline router): added SnapshotLoadRequest{name,book_id} DTO, _ACTIVE_RUN_STATUSES=('pending','running'), _RETRY_AFTER_SECONDS=5, _AUDIO_SUFFIXES=('.wav','.mp3','.m4b'); POST /projects/load endpoint (book 404 -> snapshot 404 incl. cross-book same-detail guard -> 409+Retry-After on active rows -> json.loads manifest, corrupt -> 500 -> merge -> response {status,name,book_id,re_render_required}); helpers _book_has_active_runs (existence query on walk_run/render_job status IN pending/running via adapter — same set reconciliation treats as alive), _latest_completed_run_dir (SELECT output_dir FROM render_job WHERE book_id=? AND status='completed' ORDER BY finished_ms DESC LIMIT 1), _audio_reference_missing (None ref, out-of-RENDER_ROOT ref, missing/empty dir, or no audio-suffixed file => True), _apply_snapshot_merge (single storage.transaction(): UPDATE span SET text/instruct WHERE id=? per captured span; characters never deleted — create missing character row + character_book junction (source 'derived', confidence 1.0), existing rows kept verbatim). Save path extended: POST /projects now records manifest['audio_run_dir'] = latest completed render output_dir (None when never rendered) so the restore check has a snapshot-referenced artifact (contract c). Fixed arg-order bug during GREEN run (_latest_completed_run_dir(storage, book_id)).
- [x] TDD GREEN: run the load tests; assert 409, merge-not-replace, and re-render flag
    **Note:** GREEN: pytest tests/pipeline/test_snapshots.py -q -> 45 passed (24 phase-1 + 21 new load tests), 1 pre-existing StarletteDeprecationWarning. All phase-2 behaviors verified: 409 + Retry-After:5 while active walk_run/render_job (pending/running parametrized), 200 when only terminal/no rows, merge-vs-replace (spans restored, absent spans kept, deleted spans skipped, characters never deleted, missing characters restored with junction, shared character rows untouched, existing voices never clobbered), 404s (unknown snapshot/book/cross-book), 500 corrupt manifest, re_render_required flag true/false across 4 audio scenarios.
- [x] Verify: `ruff check app/pipeline/api_operations.py` clean; pytest green
    **Note:** Verified: ruff check app/pipeline/api_operations.py clean (also tests/pipeline/test_snapshots.py); pytest tests/pipeline/test_snapshots.py -q -> 45 passed; guard suite pytest tests/pipeline/test_legacy_removed.py -q -> 12/12; full regression pytest tests/pipeline/ -q -> 1208 passed. No files changed outside scope (api_operations.py + test_snapshots.py only this phase; adapter.py M and test_snapshots.py untracked are phase-1 uncommitted work, intentionally left dirty per plan). Nothing committed.

### Phase 3: Projects tab UI
- [x] TDD RED: write frontend tests (new tests/frontend/test_projects.test.ts) asserting the Projects tab lists snapshots, Save auto-names via POST /projects, Load calls POST /projects/load and surfaces the re-render notice, Delete confirms then DELETEs, Rename PATCHes
    **Note:** TDD RED: created frontend/tests/frontend/test_projects.test.ts (25 tests) asserting the Projects tab: renders snapshot rows (name/date/size), empty state, HTML-escaping of snapshot names; Save → POST /api/pipeline/projects {book_id} + auto-name toast + list refresh; Load → postWithRetryOnce('/api/pipeline/projects/load', {name, book_id}, 409) + re-render warning toast when re_render_required + loadSpans cross-tab refresh; real-helper retry test (fetch spy 409+Retry-After:5 → 200, exactly 2 attempts); Delete → showConfirm then DELETE /projects/{name}; Rename → prompt then PATCH {new_name} with 409-duplicate toast; index.html structure (nav link, pane, save btn, list). RED confirmed: import of missing module failed. Follows test_editor/test_voices mocking conventions (vi.mock api + utils + editor-pipeline, escaping escapeHtml mirror, readIndexHtml for structure).
- [x] Implement: add projects.ts (new tab module) + index.html nav link + tab pane + api.ts typed helpers (get/post/del; add del helper to api.ts — it has none today, only get/post/put); wire Save/Load/Delete/Rename buttons; handle 409 Retry-After with a toast and one retry
    **Note:** Implemented: (1) NEW frontend/src/tabs/projects.ts — initProjects (idempotent flag, DOMContentLoaded, delegated row-action clicks via data-action/data-name), loadProjects (GET /api/pipeline/projects?book_id=), saveProject (POST {book_id}, auto-name toast), loadProject (postWithRetryOnce 409, re-render warning toast, then loadSpans() cross-tab editor refresh), deleteProject (showConfirm → del), renameProject (prompt → patch {new_name}); all names escaped via escapeHtml, no innerHTML with raw names. (2) frontend/index.html — '5. Projects' nav link (data-tab=projects, after 4. Editor) + #projects-tab pane (card, #btn-project-save, #projects-list) after editor-tab. (3) frontend/src/api.ts — ADDED del<T>(endpoint) helper (DELETE, handleError, res.json; api.ts had only get/post/put) and patch<T> (PATCH for rename; none existed); parameterized postWithRetryOnce with optional retryStatus=503 (backward compatible — existing cancel_render/cancel_walks callers pass 2 args) so the load 409+Retry-After:5 contract retries exactly once. (4) frontend/src/main.ts — imported + called initProjects() in DOMContentLoaded init order (after initEditor, before initTabNavigation). No backend files touched. state.ts needed no change (pipelineBookId already exists).
- [x] TDD GREEN: run test_projects tests; assert each action hits the right endpoint with the right payload
    **Note:** TDD GREEN: frontend/tests/frontend/test_projects.test.ts — 25/25 pass. Endpoint/payload assertions verified: loadProjects → API.get('/api/pipeline/projects?book_id=book-123'); saveProject → API.post('/api/pipeline/projects', {book_id:'book-123'}) and toast contains returned auto-name; loadProject → postWithRetryOnce('/api/pipeline/projects/load', {name, book_id}, 409) and re_render_required=true surfaces 're-render' warning toast + loadSpans called; real-helper retry test proves exactly 2 fetch attempts (409+Retry-After:5 → 200) and no 3rd attempt on double-409; deleteProject → API.del('/api/pipeline/projects/'+encodeURIComponent(name)) only after showConfirm true; renameProject → API.patch(endpoint, {new_name}) after prompt, 409-duplicate surfaces 'already exists' error toast. Two test-fixture fixes during green (not code bugs): success-no-rerender mock must echo request name (backend echoes request.name); initProjects test reordered Rename before Delete's microtask flush since delete re-render wipes rows.
- [x] Verify: `npx tsc --noEmit` exit 0; `npm test` green; `npm run build` + dist committed
    **Note:** Verify: (1) `npx tsc --noEmit` exits 0 (strict TS clean). (2) `npm test` (vitest run) green: 7 files, 314/314 passed — 289 pre-existing + 25 new test_projects. (3) `npm run build` (vite build) exits 0 and regenerated app/static/dist/ (dist/index.html 82.50 kB + assets/index-CUJ1rnnc.js 87.32 kB; old index-CtEIBS8P.js removed). Per established plan pattern, the tree is left dirty — dist regenerated but NOT staged/committed; the single implementation commit happens at P5-S6. NOT run per scope: backend tests (guard suite is backend-only), ruff, pytest, phase-4 CONTRACTS.md update, phase-5 regression gate.

### Phase 4: Register rename PATCH in contracts
- [x] Append PATCH /projects/{name} (rename, 409 on duplicate, 404 unknown) to the Universal Upgrade registration in artifacts/designs/parts/epub-audiobook-pipeline-rewrite/CONTRACTS.md (append-only, date-stamped)
    **Note:** Appended PATCH /api/pipeline/projects/{name} to the upstream ledger (epub-audiobook-pipeline-rewrite/CONTRACTS.md, § Universal Upgrade, line 937): the projects New: line now reads `POST /api/pipeline/projects` · `GET /api/pipeline/projects` · `POST /api/pipeline/projects/load` · `DELETE /api/pipeline/projects/{name}` · `PATCH /api/pipeline/projects/{name}` (api_operations). Added dated append note (line 939, blockquote matching the section's contract-gate intro style): `> 2026-08-07: PATCH /api/pipeline/projects/{name} appended (rename; 409 duplicate, 404 unknown) per DD design decision — rename PATCH.` git diff confirms the change is confined to this one hunk (+3/-1): PATCH token on the existing line + note line + preserved blank separator; append-only rule (line 950) respected. New: list now 11 endpoints — PATCH appended beyond the 10 registered (DD decision #7: snapshot projects auto-named + rename PATCH). No other lines in the file touched.
- [x] Confirm the universal-upgrade CONTRACTS.md delta ledger already lists all 5 projects endpoints consistently
    **Note:** Confirmed universal-upgrade/CONTRACTS.md delta ledger (lines 110-114) already lists all 5 projects endpoints consistently: POST /api/pipeline/projects (I — save snapshot, auto-named), GET /api/pipeline/projects (I — list snapshots), POST /api/pipeline/projects/load (I — load snapshot, merge), DELETE /api/pipeline/projects/{name} (I — delete snapshot), PATCH /api/pipeline/projects/{name} (I — rename snapshot, DD design: auto-named + rename). All rows are api_operations / Plan I with behavioral notes. Supporting contracts present: RenameProjectRequest DTO (line 127) and Decisions #7 row. No rows missing or inconsistent — no fix needed (append-only style preserved; nothing rewritten).
- [x] Verify: re-read both ledger sections; confirm no duplicate entries and full signatures
    **Note:** Verified both ledger sections post-edit. Upstream (epub-audiobook-pipeline-rewrite/CONTRACTS.md lines 931-941): no duplicate endpoint entries — PATCH appears exactly once in the New: list (line 937) plus the dated append note (line 939); all 11 New entries carry full method+path and module attribution; "future DD updates append, never rewrite" rule (line 950) intact. Delta ledger (universal-upgrade/CONTRACTS.md lines 101-114): no duplicate rows; each of the 5 projects rows has full signature (METHOD path | Module | Plan | behavioral note). Cross-ledger consistency: both ledgers list the same 5 projects endpoints with identical paths and api_operations module; upstream carries the PATCH behavioral note (rename; 409 duplicate, 404 unknown) in the dated append line. P4-S1 diff is append-only: old line `- `POST /api/pipeline/projects` · `GET /api/pipeline/projects` · `POST /api/pipeline/projects/load` · `DELETE /api/pipeline/projects/{name}` (api_operations)` → new line `- `POST /api/pipeline/projects` · `GET /api/pipeline/projects` · `POST /api/pipeline/projects/load` · `DELETE /api/pipeline/projects/{name}` · `PATCH /api/pipeline/projects/{name}` (api_operations)` + appended note `> 2026-08-07: PATCH /api/pipeline/projects/{name} appended (rename; 409 duplicate, 404 unknown) per DD design decision — rename PATCH.`

### Phase 5: Regression gate, guard suite and verification
- [x] Run `pytest tests/pipeline -q --cov=app/pipeline --cov-report=term-missing` and record pass count + coverage (api_operations coverage maintained)
    **Passed:** `pytest tests/pipeline -q --cov=app/pipeline --cov-report=term-missing` from repo root: 1210 passed (1208 prior + 2 new cap tests from P5-S4 fix), 615 warnings (pre-existing ResourceWarnings). TOTAL coverage 92% (3667 stmts / 289 miss) — matches plan baseline. app/pipeline/api_operations.py: 97% (188 stmts, 6 miss: 69 get_operation_executor, 247-248 _max_snapshot_json_bytes ValueError fallback [defensive line, mirrors _max_cover_bytes], 263, 531, 538) — no regression vs prior 98%; ≥80% gate exceeded. adapter.py 96%. Run 2 (final, post cap-fix) recorded.
- [x] Run `pytest tests/pipeline/test_legacy_removed.py -q` and verify 12/12 green (no legacy saved-script format compatibility, no /api/scripts/*)
    **Passed:** `pytest tests/pipeline/test_legacy_removed.py -q`: 12/12 passed (1.62s first run; re-ran 12/12 after the P5-S4 cap change to api_operations.py — still green). No legacy saved-script format compatibility, no /api/scripts/* endpoints resurrected. Guard gate holds.
- [x] Run `npm test` + `npx tsc --noEmit` + `npm run build` with `git diff --exit-code app/static/dist/` clean after commit
    **Passed:** Frontend gates from frontend/: (1) `npm test` (vitest): 314/314 passed across 7 test files (baseline 314, no phase-5 additions — test_projects.test.ts 25 tests included). (2) `npx tsc --noEmit`: exit 0. (3) `npm run build`: exit 0 (280ms). BYTE-REPRODUCIBILITY: built twice; sha256 of all files under app/static/dist/ identical between builds (index-CUJ1rnnc.js 42da049077b8451c43090b3178380707fd46cb2101d371f8bca88aed402bbaee, index.html be9c4c18cb15fc66b51f91eaa1aa642f2040ceaffffa1f9be0655796e40a3572) — deterministic output, CI gate `npm run build && git diff --exit-code app/static/dist/` will pass post-commit. Pre-commit dist diff (expected): D stale index-CtEIBS8P.js, M index.html (Projects nav/tab + new bundle ref), ?? index-CUJ1rnnc.js. Clean-after-commit check is P5-S6 (out of scope).
- [x] Security review: snapshot_json is data (spans/instructions) — confirm size limits, no HTML/script injection rendered from snapshot content (escape when rendering), name validation on PATCH (no path traversal via snapshot name), merge-vs-replace cannot delete characters; document findings
    **Reviewed:** Security review (6 items, all evidence from code + empirical probes): (1) FIX APPLIED — save path had no snapshot_json size cap (SQLite TEXT unlimited); added _max_snapshot_json_bytes() (env PIPELINE_MAX_SNAPSHOT_BYTES, default 20 MiB, ValueError fallback mirroring _max_cover_bytes) + 400 rejection in POST /projects; 2 tests added (test_snapshots.py now 47). (2) VERIFIED — projects.ts escapeHtml's every rendered name (display, title, data-name); innerHTML only from escaped renderProjectsList + static strings; toasts escape via showToast→escapeHtml, showConfirm uses textContent; snapshot CONTENT (spans/instructions) never rendered in projects tab; editor re-render (load path) escapes text/instruct/speaker. (3) VERIFIED — PATCH new_name validated by _is_valid_snapshot_name on the decoded value (400 on / \ .. control >200); path names can't contain separators by construction (auto-name/PATCH); empirical: %2F unroutable (404), %5C reaches handler but no row matches, double-decode can't bypass — names only hit parameterized SQL, never filesystem paths. LOW note: literal '%' names (validator-allowed) are unaddressable via URL round-trip — documented, no code change (log L175). (4) VERIFIED — _apply_snapshot_merge only UPDATEs span text/instruct and INSERTs missing characters/character_book rows; zero DELETE statements. (5) VERIFIED — load scoped by book: cross-book request → 404 with same detail as unknown snapshot (no probing); DELETE/PATCH are name-keyed per contract; names are DB PKs only. (6) VERIFIED — all adapter snapshot methods (SQLiteAdapter + InMemory) use ? placeholders; only f-string interpolates a hardcoded ('walk_run','render_job') tuple (not user input). Files touched: app/pipeline/api_operations.py (+cap), tests/pipeline/test_snapshots.py (+2 tests). ruff clean, 47/47 snapshots green.
- [ ] Code review pass via exec-manager QA-Reviewer; fix MINOR findings
- [x] Commit: `feat(pipeline): snapshot projects with save, load, delete, rename`
    **Committed:** Committed as 2a2a645 "feat(pipeline): snapshot projects with save, load, delete, rename" (14 files, +3026/-219): backend app/pipeline/api_operations.py + adapter.py, tests/pipeline/test_snapshots.py (47) + test_adapter.py (file-backed snapshot tests); frontend/src/tabs/projects.ts, frontend/tests/frontend/test_projects.test.ts, api.ts (del+patch helpers), main.ts, index.html; dist regenerated (new index-CUJ1rnnc.js, old index-CtEIBS8P.js deleted, dist/index.html updated); upstream CONTRACTS.md PATCH registration (phase 4); plan file annotation record. Dist gate verified clean after commit: git diff --exit-code app/static/dist/ -> exit 0. Plan file included per repo convention (prior impl commits 2e211c8/583844d/d06ef53 all carry their plan file). P5-S5 (QA-Reviewer pass) left unmarked — out of assigned scope; QA Round 2 PASS recorded in the docs stamp commit. Followed by docs stamp 2a2a645-next (CONTRACTS.md Plan I IMPLEMENTED + QA-VALIDATED 2026-08-07).

## Completion Criteria

- POST/GET/load/DELETE/PATCH /projects endpoints live in api_operations.py, registered in both ledgers (rename PATCH appended upstream)
- Auto-named snapshots (no free-form name input); load is merge-vs-replace (characters never deleted); 409 while active runs; re-render notice when audio missing
- Projects tab in the frontend (Save/Load/Delete/Rename) with 409 retry-once; api.ts gains del helper
- Full pytest + vitest green, guard 12/12, ruff + tsc clean, build + dist committed, security review documented

## References

- `artifacts/designs/pending/DD-universal-upgrade.md` — cap7 (saved scripts), P6-C7, A4-r, FP3+FP4, cannot-restore #3/#10, open item #5
- `artifacts/designs/parts/universal-upgrade/CONTRACTS.md` — project_snapshot table + 5 endpoints
- Prior: Plan B (walk_run/render_job rows), Plan E (tab navigation)
