# Task: Overlay Config & Walk Overrides

## Problem Statement

`POST /api/config` (app.py:475-482) is destructive: it validates the request through `AppConfig` (which is llm+tts only) and `json.dump(config.model_dump(), CONFIG_PATH)` — pydantic `model_dump()` strips any generation/prompts keys the user had, so saving config **wipes** unknown sections (the L4 data-loss bug, locked in by test_setup.test.ts L296 "ignoring legacy prompts/generation keys"). `resolve_task_llm(task_name, config_path=None)` (app/utils.py:102-139) has a dead `config_path` param that is always None from all 9 walks — per-request config dicts are never consulted for LLM resolution. There is no way to override prompt/generation settings per walk from the UI.

This plan (DD A4-r Overlay Config + Snapshot Projects; here config half, FP5) makes config overlay-based: `POST /api/config` parses raw JSON, validates known keys through `AppConfig(extra='ignore')` (validation only, output never serialized), recursively deep-merges unknown paths, stamps `schema_version`, and atomically writes — byte-stable round-trip guaranteed (CI test fixture MUST include unknown keys). A single `resolve_task_config(task, storage, book_id)` helper replaces the dead-param function: it applies on-disk config → `llm.task_overrides` → `walk_override` rows, snapshotted per walk-unit start, and is called from all 9 walks. The Setup tab gains per-walk override fields (temperature/prompts) and saving preserves unknown keys. Test `test_setup.test.ts` L296 behavior lock is updated to the new byte-stable contract.

## Dependencies

- Plan A (walk_override table) and Plan B (walk_run lifecycle, runner integration) — completed and archived
- `artifacts/designs/parts/epub-audiobook-pipeline-rewrite/CONTRACTS.md` § Universal Upgrade — config contract

## Phases

### Phase 1: Raw-JSON merge for POST /api/config (byte-stable)
- [x] TDD RED: write tests in app/test_api.py (or tests/pipeline) asserting POST /api/config with unknown top-level keys (e.g. generation, prompts) round-trips byte-stable: GET /api/config returns the same unknown keys with the same values after a save; known keys are validated through AppConfig; schema_version is stamped; atomic write (no partial file on failure)
    **TddRed:** RED tests written: new tests/pipeline/test_config_byte_stable.py (7 pytest tests via TestClient(app.app) with CONFIG_PATH monkeypatched to tmp — unknown-key byte-stable round-trip, merge preserves on-disk unknowns, GET preserves unknowns + materialises H1 known-key defaults, known-key 422 validation, schema_version stamp, atomic-write-no-partial-file). Also rewrote the OLD destructive locks in app/test_api.py harness: test_get_config no longer asserts prompts/generation/current_file absent (asserts task_overrides present instead); test_save_config_roundtrip now POSTs _test_generation/_test_prompts unknown sections and asserts they round-trip + schema_version stamped (restore preserved). RED confirmed: 6 failed / 1 passed (422-validation test already green) against current code.
- [x] Implement: rewrite POST /api/config in app.py — parse raw JSON, validate known keys via AppConfig(extra='ignore') (never serialize the validation output), recursive deep-merge unknown paths onto the current file, stamp schema_version, atomic write (tmp+rename per existing atomic_json_write in app/utils.py); keep reset_tts_engine() call
    **GREEN:** Implemented raw-JSON merge in app/app.py: (1) AppConfig now has model_config = ConfigDict(extra="ignore") — validation-only, output never serialized; (2) new _deep_merge_unknown(base, overlay, known) helper — known keys replace verbatim, unknown keys deep-merge dict-wise onto disk, returns new dict (immutable); (3) POST /api/config takes request: Request param, reads raw body via await request.json() (safe: body already parsed+validated by FastAPI's AppConfig param, 422 on malformed/invalid), merges onto current on-disk config, stamps schema_version=1, writes via save_json (atomic_json_write tmp+rename), KEEPS reset_tts_engine(); (4) GET /api/config still materialises pydantic defaults via AppConfig.model_validate().model_dump() but now re-adds unknown top-level keys from the on-disk file. Added imports: Request (fastapi), ConfigDict (pydantic), save_json (utils). Known-key set derived from AppConfig.model_fields.
- [x] TDD GREEN: run the config tests; assert unknown keys preserved byte-stable and schema_version present
    **Green:** Config tests GREEN: `python -m pytest tests/pipeline/test_config_byte_stable.py -q` → 7 passed, 1 warning (StarletteDeprecationWarning re httpx, pre-existing). All byte-stable assertions pass: unknown top-level keys (generation/prompts) round-trip through POST+GET identical, survive later known-only saves and on-disk-omitting saves, schema_version==1 stamped on disk and in GET, failed saves leave file byte-unchanged, known-key 422 validation intact, H1 defaults materialised.
- [x] Verify: `ruff check app/app.py app/utils.py` clean; guard suite 12/12 still green (no legacy prompt-file modules resurrected — unknown keys live in config.json only)
    **Verify:** All verification green: (1) `ruff check app/app.py app/utils.py` → All checks passed (fixed 3 PRE-EXISTING lint errors in app/utils.py, untouched by my diff: E402 moved `import re`/`import gc` to top import block, F541 removed f-prefix from placeholder-less f-string at L432); (2) guard suite `pytest tests/pipeline/test_legacy_removed.py -q` → 12/12 passed (no legacy prompt-file modules resurrected — unknown keys live in config.json only); (3) full `pytest tests/pipeline -q` → 1093 passed, 1 pre-existing warning; (4) `pytest app/test_api.py -q` collects 0 (live-server requests harness, expected — verified it compiles and test logic via py_compile). No config.json written to repo on disk (tests use tmp CONFIG_PATH via monkeypatch). No commits made (P6-S6 owns commit).

### Phase 2: resolve_task_config single helper
- [x] TDD RED: write tests for resolve_task_config(task, storage, book_id) asserting precedence on-disk config → llm.task_overrides → walk_override rows (walk_override wins), and that the result is snapshotted per walk-unit start (a mid-walk config change does not alter in-flight units)
    **TddRed:** RED confirmed: new tests/pipeline/test_resolve_task_config.py (13 pytest tests, file-backed SQLiteAdapter fixture with walk_override rows inserted via execute_insert, ALEXANDRIA_CONFIG_PATH monkeypatched to tmp). Collection error before implementation: ImportError cannot import name 'resolve_task_config' from 'app.utils'. Covers: (a) precedence on-disk config → task_overrides → walk_override rows (walk_override wins for temperature AND model_name); (b) explicit 0.0 honored at both task-override and walk_override tiers (is-not-None semantics); (c) snapshot independence from later storage mutation (DELETE after resolve) and later config rewrite; (d) missing config/empty overrides → hardcoded fallbacks; plus scoping (other book/task rows ignored), corrupt value_json skipped, missing walk_override table guarded (sqlite3.OperationalError stub → falls back).
- [x] Implement: replace resolve_task_llm's dead config_path param usage with resolve_task_config in app/utils.py; apply the 3-tier precedence; return the effective LLM config dict for the task
    **GREEN:** Implemented in app/utils.py: resolve_task_config(task, storage, book_id) -> dict with 3-tier precedence — (1) on-disk llm global defaults via load_app_config() (hardcoded fallbacks model richardyoung/qwen3-14b-abliterated:Q8_0, reasoning None, temp 0.6); (2) llm.task_overrides[task] (temperature is-not-None so 0.0 honored; model/reasoning truthiness — semantics carried over from legacy resolve_task_llm); (3) walk_override rows SELECT key, value_json FROM walk_override WHERE book_id=? AND walk_name=? (walk_override WINS; value_json parsed via json.loads; temperature is-not-None, model/reasoning truthiness). Missing-table/DB-error guard: execute_query wrapped in try/except → [] (graceful tier-1/2 fallback, config resolution never crashes a walk unit). Corrupt value_json rows skipped. Returns fresh dict = value snapshot. resolve_task_llm kept as a thin compatibility wrapper delegating to resolve_task_config(task, _EmptyStorage(), None) — _EmptyStorage.execute_query returns []; all 9 walks still import/call it with config_path=None (verified via grep: walks are the only remaining callers after harness rewrite) so it stays until Phase 3 migrates the call sites; config_path param documented as dead, signature preserved for call-site compat.
- [x] TDD GREEN: run resolve_task_config tests with a file-backed storage fixture containing walk_override rows; assert precedence and snapshot behavior
    **Green:** GREEN: `python -m pytest tests/pipeline/test_resolve_task_config.py -q` → 13 passed in 1.53s. Precedence asserted: walk_override row beats task_overrides for temperature (0.9) and model_name (gpt-4o-mini); partial override leaves other keys at tier 2; explicit 0.0 honored from both task override and walk_override rows; other-book/other-task rows ignored; snapshot tests pass — after resolve, deleting the walk_override row changes the NEXT resolution (0.1) but the previously returned dict stays 0.9; rewriting config.json changes the next resolution but not the earlier snapshot. Missing-table stub and malformed value_json both fall back cleanly.
- [x] Verify: `ruff check app/utils.py` clean; existing tests/pipeline/test_resolve_task_llm.py (app/test_resolve_task_llm.py) updated to the new helper or kept passing via compatibility
    **Verify:** All verification green: (1) `ruff check app/utils.py tests/pipeline/test_resolve_task_config.py app/test_resolve_task_llm.py` → All checks passed (ruff default E4/E7/E9/F set — repo has no ruff config per exec-worker L96). (2) `python app/test_resolve_task_llm.py` → passed=13 failed=0 — harness REWRITTEN to test resolve_task_config with a file-backed SQLiteAdapter fixture (tmp db, walk_override rows via execute_insert, ALEXANDRIA_CONFIG_PATH pinned to tmp) covering fallbacks, task-override temp 0.1/0.3, global inherit, global 0.5, explicit 0.0, walk_override wins (temp 0.9 + model gpt-4o-mini), snapshot-after-DELETE, and a resolve_task_llm compat smoke check (empty storage, no config → fallbacks). Script-style main()/check() harness retained — `pytest app/test_resolve_task_llm.py` collects 0 tests, expected. (3) `pytest tests/pipeline -q` → 1106 passed, 1 pre-existing warning (1093 baseline + 13 new resolve_task_config tests; all 9 walk suites green — wrapper change is behavior-identical since it delegates with empty storage and walks' tests patch app.utils.resolve_task_llm). (4) Guard `pytest tests/pipeline/test_legacy_removed.py -q` → 12/12. resolve_task_llm NOT removed: the 9 walks remain its only callers (verified by grep — all import inside execute() and call with config_path=None); it stays as the documented compat wrapper until Phase 3 migrates call sites and deletes it. Pyright diagnostics in app/utils.py (openai/torch/triton/huggingface_hub resolution + _json) verified pre-existing — none fall in the added diff hunks. No commits (P6-S6 owns commit).
- [x] TDD RED (prompt tier): extend tests/pipeline/test_resolve_task_config.py with prompt-precedence tests asserting resolve_task_config surfaces an effective "prompt" key: (1) on-disk config top-level walk_override section config["walk_override"][task]["prompt"] (string) is honored; (2) llm.task_overrides[task].get("prompt") honored when no top-level walk_override section entry exists; (3) walk_override DB row for (book_id, task) with key="prompt" WINS over both config tiers; (4) None returned when prompt is unset or empty at all tiers; (5) snapshot independence — a later storage or config mutation does not alter an already-resolved prompt. Guard cases: missing walk_override table degrades to config tiers/None (never raises); corrupt value_json for key="prompt" is skipped. Existing {model_name, reasoning_effort, temperature} behavior must remain unchanged.
    **TddRed:** RED confirmed: extended tests/pipeline/test_resolve_task_config.py with TestPromptPrecedence (14 new tests): (a) config top-level walk_override[task].prompt honored and beats tier2; (b) llm.task_overrides[task].prompt honored when no tier-1 entry; (c) walk_override DB row key="prompt" WINS over both config tiers and leaves model/reasoning/temperature untouched; (d) None when unset everywhere; empty-string treated as unset (tier1 "" falls through to tier2; empty row does not override config prompt; non-string row value 123 skipped); (e) snapshot independence for prompt (storage DELETE and config rewrite leave earlier snapshot unchanged); guards: missing walk_override table keeps config prompt / yields None (never raises), non-dict top-level walk_override section degrades to tier2, corrupt value_json for key="prompt" skipped. Also updated the single exact-dict-equality assertion (test_no_config_empty_storage_falls_back_to_hardcoded) to the new 4-key contract {model_name, reasoning_effort, temperature, prompt: None} — the return shape is contract-mandated by the amended CONTRACTS.md row. Run: 15 failed (14 prompt tests KeyError "prompt" + shape-updated fallback), 12 passed — RED exactly as expected since the resolver returns no "prompt" key yet.
    **Notes:** Amendment Round 2 (Plan G prompt-consumption gap, DD decision #6 + cannot-restore #4): the plan under-specified prompt override consumption — these new steps add the prompt tier to resolve_task_config. P2-S1..S4 (model/temperature/reasoning precedence) remain complete; this is additive scope.
- [x] Implement (prompt tier): extend resolve_task_config in app/utils.py to surface an effective "prompt" key with 3-tier precedence: (1) on-disk config top-level walk_override section — config.get("walk_override", {}).get(task, {}).get("prompt") when it is a non-empty string; (2) llm.task_overrides[task].get("prompt") when a non-empty string; (3) walk_override DB rows for (book_id, task) with key="prompt" — value_json parsed via json.loads; a non-empty string row WINS over both config tiers. Return the effective prompt string, or None when unset/empty at all tiers (walks then fall back to their built-in system_prompt). Keep the existing snapshot semantics and the {model_name, reasoning_effort, temperature} keys unchanged; missing-table/DB-error and corrupt value_json guards degrade gracefully (row tier skipped, config tiers/None remain).
    **Green:** GREEN: implemented the prompt tier in app/utils.py resolve_task_config. Return dict now {'model_name', 'reasoning_effort', 'temperature', 'prompt'}. Prompt chain per manager directives: tier1 = config.get("walk_override", {}).get(task, {}).get("prompt") with isinstance dict-guards on both levels, honored only when a non-empty string; tier2 = llm.task_overrides[task].get("prompt") honored when a non-empty string and no tier-1 value; tier3 = walk_override row key="prompt" for (book_id, task) parsed via json.loads — a non-empty string row WINS over both config tiers; empty/non-string rows treated as unset and leave the config-tier prompt untouched. Effective prompt is None when unset/empty at all tiers (walks fall back to built-in system_prompt in Phase 3). Existing {model_name, reasoning_effort, temperature} resolution kept byte-identical (is-not-None 0.0 temperature, truthiness model/reasoning, missing-table try/except -> [], corrupt value_json skip, fresh-dict snapshot). Docstring updated to document the prompt chain and 4-key shape. ruff check clean on app/utils.py.
- [x] TDD GREEN (prompt tier): run the extended resolve_task_config tests; assert prompt precedence (top-level walk_override section → task_overrides → DB row wins), None when unset everywhere, snapshot independence for prompt, and no regression in the existing 13 model/temperature tests.
    **Green:** GREEN: `python -m pytest tests/pipeline/test_resolve_task_config.py -q` -> 27 passed in 2.92s (13 original model/temperature tests — incl. the shape-updated fallback — plus 14 new prompt-tier tests). Asserted: prompt precedence tier1 -> tier2 -> row-wins, None-when-unset-everywhere, empty-string-as-unset with fall-through (tier1 "" -> tier2, empty row -> config tier), non-string and corrupt value_json rows skipped, missing-table degradation to config tiers/None, non-dict walk_override section guard, and prompt snapshot independence (later storage DELETE and config rewrite change the NEXT resolution but not the already-resolved dict). Zero regression in the 13 pre-Amendment tests. Legacy harness app/test_resolve_task_llm.py passed=11 failed=0 (key-based asserts unaffected by the 4th key).
- [x] Verify (prompt tier): `ruff check app/utils.py tests/pipeline/test_resolve_task_config.py` clean; `pytest tests/pipeline -q` green; guard suite `pytest tests/pipeline/test_legacy_removed.py -q` 12/12.
    **Verify:** All verification green: (1) `ruff check app/utils.py tests/pipeline/test_resolve_task_config.py` -> clean. (2) `python -m pytest tests/pipeline -q` -> 1151 passed, 1 pre-existing warning (1137 baseline + 14 new prompt tests), 32.80s. (3) Guard `python -m pytest tests/pipeline/test_legacy_removed.py -q` -> 12/12. (4) `python app/test_resolve_task_llm.py` -> passed=11 failed=0. Pyright diagnostics in app/utils.py (openai/torch/triton/huggingface_hub unresolved + _json possibly unbound at L236/323/339/385/397/410/421/423/481) verified pre-existing per prior phase logs — none fall in the added diff hunks (L101-213). No commits (P6-S6 owns commit). Walks untouched (Phase 3 territory — they will consume llm_config.get("prompt") in P3-S5..S8). Cosmetic note: AFT flags a 22-line duplicate between the two new inline _NoWalkOverrideTable stubs — this mirrors the pre-existing inline stub pattern in the same file (test scaffolding); left as-is to avoid touching existing tests.

### Phase 3: Wire all 9 walks to resolve_task_config
- [x] TDD RED: update walk tests (2a-2i) asserting each walk's LLM resolution now consults walk_override rows for its book; a walk_override row overrides the task temperature/model
    **TddRed:** RED confirmed: `pytest tests/pipeline/test_walk_2a.py ... test_walk_2i.py -q` → 9 failed, 221 passed. Updated all 9 walk test files: (1) every monkeypatch target renamed app.utils.resolve_task_llm → app.utils.resolve_task_config with mock signatures (task_name, config_path=None) → (task, storage, book_id) — mandatory because the compat wrapper itself calls resolve_task_config with 3 positional args, and after P3-S2 the walks do too (2-param mocks would TypeError); (2) test_walk_2c.test_uses_script_alias_resolution_task updated (docstring + mock body param rename task_name → task). Added 1 new test per file, test_walk_override_drives_llm_config (9 total): pins ALEXANDRIA_CONFIG_PATH to nonexistent tmp file (→ fallbacks), inserts walk_override rows (temperature 0.9, model_name gpt-4o-mini) for the walk's TASK name (scene_segmentation/character_discovery/script_alias_resolution/scene_presence/span_attribution/character_description/voice_audition/voice_assignment/delivery) with the book id each walk executes (book-1; b1 for 2c), leaves resolve_task_config REAL (only create_llm_client + chat_completion patched via capture mock), executes the walk, asserts the LLM call received temperature 0.9 and model gpt-4o-mini. Each fails in RED exactly as designed: the walk still calls resolve_task_llm → compat wrapper → _EmptyStorage → no walk_override rows → fallback temperature 0.6 (assert 0.6 == 0.9). Fixes 2c's mock body param bug caught by the run (NameError task_name).
- [x] Implement: change the 9 call sites (walk_2a:70, 2b:72, 2c:81, 2d:76, 2e:79, 2f:76, 2g:77, 2h:80, 2i:81) from `resolve_task_llm(task_name, config_path=None)` to `resolve_task_config(task_name, storage, book_id)`; keep the per-unit snapshot semantics (resolve once per unit start)
    **TddGreen:** Migrated all 9 walk call sites to resolve_task_config(task, storage, book_id): walk_2a:70 ('scene_segmentation'), 2b:72 ('character_discovery'), 2c:81 ('script_alias_resolution'), 2d:76 ('scene_presence'), 2e:79 ('span_attribution'), 2f:76 ('character_description'), 2g:83 ('voice_audition'), 2h:86 ('voice_assignment'), 2i:87 ('delivery'). Note: plan's P3-S2 listed 2g:77/2h:80/2i:81 — stale; actual verified lines are 2g:83/2h:86/2i:87. Each walk: inside-execute import now 'from app.utils import create_llm_client, resolve_task_config'; snapshot semantics preserved (llm_config resolved ONCE before the per-unit loop; create_llm_client(config_path=None) usage kept as-is). Module docstrings + execute() docstrings updated (config param documented as runner-contract-only). DELETED resolve_task_llm + _EmptyStorage from app/utils.py (net -26 lines; comment updated to drop 'legacy resolve_task_llm' reference) after verifying ZERO remaining code callers repo-wide (incl. tests and app/): removed the last reference — the compat smoke check (#8) in app/test_resolve_task_llm.py, whose docstring/import were updated (harness now 11 checks, all resolve_task_config). Remaining 'resolve_task_llm' mentions are non-code (docstrings describing the deleted wrapper, design/plan docs, one frontend comment in frontend/src/tabs/setup.ts:13 — Phase 5 territory, left untouched). Deviation: manager expected the harness to already be free of resolve_task_llm; it still had the compat check, so it was removed to enable the deletion (goal: no dead code).
- [x] TDD GREEN: run all walk tests; assert override-driven resolution and no behavioral regression in the 9 walks
    **TddGreen:** All walk tests GREEN: tests/pipeline/test_walk_2a.py through test_walk_2i.py = 230 passed (9 new test_walk_override_drives_llm_config tests included) in 2.68s; tests/pipeline/test_resolve_task_config.py = 13 passed. Override-driven resolution confirmed: each walk's new test inserts walk_override rows (temperature 0.9, model_name 'gpt-4o-mini') for its book+task, pins ALEXANDRIA_CONFIG_PATH to a nonexistent tmp file (fallbacks would be 0.6/default model), runs the real walk, and asserts the LLM call received temperature==0.9 and model_name=='gpt-4o-mini' — proving the walk_override row beats on-disk fallbacks and flows through resolve_task_config → chat_completion. No behavioral regression: all pre-existing walk tests (patch-based resolution) still pass.
- [x] Verify: `ruff check app/pipeline/walks/*.py app/utils.py` clean; `pytest tests/pipeline -q` green
    **Verified:** Verification gates all pass. (1) ruff check app/pipeline/walks/*.py app/utils.py → 'All checks passed!'; ruff also clean on the 9 modified walk test files + app/test_resolve_task_llm.py. (2) Full pytest tests/pipeline -q → 1115 passed (1106 baseline + 9 new override tests), 1 pre-existing warning, 26.58s. (3) Guard suite tests/pipeline/test_legacy_removed.py → 12/12 passed. (4) Harness python app/test_resolve_task_llm.py → passed=11 failed=0 (compat check #8 removed with the deleted wrapper; remaining 11 checks all target resolve_task_config). No files outside scope changed (app/tts.py and app/app.py untouched — app/app.py shows modified only from Phase 1's uncommitted work; commit deferred to P6-S6).
- [x] TDD RED (prompt consumption): extend walk tests (2a-2i), one new test per file: insert a walk_override DB row with key="prompt" (value_json = a distinctive override string) for the walk's (book_id, task) AND/OR pin a top-level config walk_override section prompt via a tmp ALEXANDRIA_CONFIG_PATH file; leave resolve_task_config real; patch chat_completion via the existing capture mock; execute the walk; assert the captured system_prompt equals the override prompt. Include a companion assertion (same test or a second one) that the walk's built-in system_prompt is used when no prompt override is set at any tier. Each new test fails in RED while the walk still passes its hardcoded system_prompt.
    **Note:** Added 12 new RED tests across the 9 walk test files: one test_prompt_override_drives_system_prompt per file (two phases — asserts built-in system_prompt when no override at any tier, then inserts a walk_override row key="prompt" with value_json "You are a TEST override prompt." and asserts it flows into the captured chat_completion system_prompt), plus config-section tests (test_prompt_override_config_section_drives_system_prompt) in walk_2a, walk_2c, walk_2i pinning a tmp ALEXANDRIA_CONFIG_PATH file with top-level walk_override[task].prompt. resolve_task_config left REAL; only create_llm_client + chat_completion patched via capture mocks returning '[]'. All 12 fail in RED (assert captured system_prompt == override; walk passes hardcoded). Verified failure kind: 'You are a li...ve structure.' != 'You are a TE...rride prompt.'
    **Notes:** Amendment Round 2 — prompt consumption depends on the Phase 2 prompt-tier work (P2-S5..S8). The capture-mock pattern (patched create_llm_client + chat_completion, real resolve_task_config) mirrors the existing test_walk_override_drives_llm_config tests added in P3-S1.
- [x] Implement (prompt consumption): in each of the 9 walks (walk_2a..2i), capture the effective prompt ONCE per unit start from the already-resolved llm_config — prompt = llm_config.get("prompt") (None when unset) — alongside model_name/temperature/reasoning_effort, thread it into the per-unit processing function, and at the chat_completion call site replace the hardcoded system_prompt argument with `system_prompt=prompt or "<existing built-in system_prompt>"` (override wins, built-in is the fallback). Do NOT re-resolve per chapter — the prompt comes from the same unit-start snapshot as model/temperature (snapshot semantics preserved).
    **Note:** Implemented prompt consumption in all 9 walks (walk_2a..2i). Pattern: in execute(), after reasoning_effort capture, added `prompt = llm_config.get("prompt")` (None when unset) from the same unit-start llm_config snapshot — no re-resolution per unit. Threaded into each per-unit processing function via a NEW `system_prompt_override: str | None` parameter (added after reasoning_effort in the signature and the call site) — named system_prompt_override to avoid collision with each function's local user-prompt var `prompt`. At the single chat_completion call site per walk, replaced the hardcoded system_prompt with `system_prompt=(system_prompt_override or "<built-in>")` so the override wins and the built-in is the fallback. 2c special case: chat_completion is called directly inside execute() (GLOBAL scope, no per-unit function) — captured as `system_prompt_override = llm_config.get("prompt")` directly in execute() and consumed at the same call site. Ruff clean on app/pipeline/walks/*.py + app/utils.py; all 12 new prompt-override tests pass (GREEN).
- [x] TDD GREEN (prompt consumption): run all 9 walk suites (test_walk_2a.py..test_walk_2i.py); assert the prompt override flows into the captured chat_completion system_prompt (from both the walk_override row and the config walk_override section), the built-in fallback is used when unset, and no behavioral regression in the existing walk tests.
    **Note:** All 9 walk suites (test_walk_2a.py..test_walk_2i.py) + test_resolve_task_config.py pass: 269 passed in 5.81s. Prompt override flows into the captured chat_completion system_prompt from both the walk_override row path (9 tests) and the config walk_override section path (3 tests: 2a/2c/2i); built-in fallback asserted when unset at every tier (first phase of each new test); no regression in existing walk tests (all 242 pre-existing walk tests + 27 resolve_task_config tests still green).
- [x] Verify (prompt consumption): `ruff check app/pipeline/walks/*.py app/utils.py` clean; `pytest tests/pipeline -q` green; guard suite `pytest tests/pipeline/test_legacy_removed.py -q` 12/12.
    **Note:** Verify gate: `ruff check app/pipeline/walks/*.py app/utils.py` — all checks passed (0 errors). `pytest tests/pipeline -q` — 1163 passed (1151 baseline + 12 new prompt-consumption tests), 1 pre-existing warning. Guard `pytest tests/pipeline/test_legacy_removed.py -q` — 12 passed. All files changed are within scope (9 walks + 9 walk test files).

### Phase 4: walk_override CRUD
- [x] TDD RED: write tests for walk_override row access (read/write/delete) via the adapter (PK book_id+walk_name+key, value_json column); assert upsert semantics and JSON round-trip
    **TddRed:** RED confirmed: new tests/pipeline/test_walk_override_crud.py — 22 tests parametrized over BOTH adapter fixtures (sqlite_adapter file-backed tmp + memory_adapter), following test_adapter.py conventions (request.getfixturevalue pattern). Coverage: (a) upsert inserts a row; (b) upsert on existing PK updates value_json with row count staying 1 (true upsert); (c) JSON round-trip json.dumps->upsert->get->json.loads identical (dict + explicit 0.0 scalar); (d) read-all-for-book returns only that book's rows + full row shape {book_id, walk_name, key, value_json}; (e) delete removes the row; (f) delete absent row is a no-op; (g) missing-table grace — get_walk_overrides on an uninitialized adapter (init_db never called, both file and memory backends) returns []. Also: delete scoped to exact composite PK, distinct keys coexist, empty book returns []. Run: 22 failed — AttributeError 'SQLiteAdapter' object has no attribute 'upsert_walk_override' (methods don't exist yet), exactly as designed.
- [x] Implement: add walk_override access methods to SQLiteAdapter (or a small helper in utils.py using execute_*) — read all for a book, upsert one, delete one
    **GREEN:** Implemented in app/pipeline/adapter.py (+112 lines): get_walk_overrides / upsert_walk_override / delete_walk_override declared as @abstractmethod on PipelineStorage ABC AND implemented on both SQLiteAdapter and InMemorySQLiteAdapter (mirror docstring convention, same schema/interface). get_walk_overrides(book_id) -> list[dict] SELECTs book_id, walk_name, key, value_json WHERE book_id=? — value_json returned as RAW TEXT (caller json.loads; resolve_task_config keeps its own reads); missing-table grace: try/except sqlite3.OperationalError -> [] (precise catch; table-missing is an OperationalError — deliberate narrowing vs resolve_task_config's broad except Exception which guards duck-typed callers). upsert_walk_override composes execute_insert with INSERT ... ON CONFLICT(book_id, walk_name, key) DO UPDATE SET value_json = excluded.value_json (true upsert, rowid stable — chose ON CONFLICT over INSERT OR REPLACE which delete+reinserts). delete_walk_override composes execute_delete (no-op when absent). Write path: composing execute_* means each op joins an open transaction() when one is active (owner-thread guard + no commit inside txn via in_transaction check) and auto-commits standalone — consistent with established adapter write discipline (contract rule #6 pattern); no new transaction() wrapping needed for single-statement ops. ABC extension is safe: only the two concrete adapters subclass PipelineStorage (verified repo-wide); test_adapter.py ABC assertions are issubset-based so unaffected. Verified only 2 PipelineStorage subclasses exist.
- [x] TDD GREEN: run the override CRUD tests; assert upsert + JSON round-trip + delete
    **Green:** GREEN: `python -m pytest tests/pipeline/test_walk_override_crud.py -q` -> 22 passed in 1.45s. All assertions pass on BOTH adapters: upsert inserts row with correct PK + value_json; re-upsert same PK updates value_json with row count staying 1; JSON round-trip (dict and explicit 0.0 scalar) identical through json.loads; read-all-for-book returns only book-1's rows with exact {book_id, walk_name, key, value_json} shape; delete removes only the exact composite PK (other keys/books survive); delete absent row no-op; get on uninitialized adapter (no walk_override table, both file and memory) returns [] instead of raising.
- [x] Verify: `ruff check app/pipeline/adapter.py app/utils.py` clean
    **Verify:** All verification green: (1) `ruff check app/pipeline/adapter.py app/utils.py tests/pipeline/test_walk_override_crud.py` -> 'All checks passed' (ruff default E4/E7/E9/F set — repo has no ruff config, consistent with prior phases). (2) Full `pytest tests/pipeline -q` -> 1137 passed, 1 pre-existing warning (1115 baseline + 22 new CRUD tests), 33.89s — matches manager's expected '1115 + new'. (3) Guard suite `pytest tests/pipeline/test_legacy_removed.py -q` -> 12/12 passed. (4) `pytest tests/pipeline/test_resolve_task_config.py -q` -> 13 passed (Phase 2 suite unaffected — get_walk_overrides is additive; resolve_task_config still does its own SELECT). AFT diagnostics on adapter.py: no new errors introduced. No commits (P6-S6 owns commit); app/tts.py, resolve_task_config, and walk call sites untouched.

### Phase 5: Setup tab per-walk override fields
- [x] TDD RED: update frontend/tests/frontend/test_setup.test.ts — REPLACE the L296 lock that asserts "ignoring legacy prompts/generation keys" with a test asserting the Setup tab preserves unknown keys on save and renders per-walk override fields (temperature + prompt overrides) from the config
    **RED:** Replaced the L296 lock test ("should accept pipeline-only config shape (llm + tts, ignoring legacy prompts/generation keys)") with two new spec-first tests in frontend/tests/frontend/test_setup.test.ts: (a) "should preserve unknown top-level sections (generation/prompts/walk_override) in the save payload" — loads a config carrying generation/prompts/walk_override via mocked API.get, then asserts buildConfigPayload() returns them verbatim alongside known llm/tts; (b) "should render per-walk override fields (temperature + prompt) from config and include them in the save payload" — asserts temperature renders from llm.task_overrides (existing convention), prompt renders from the top-level walk_override section, and both are included in the save payload (temperature via llm.task_overrides, prompt via walk_override keyed by task name, explicit null for empty). Import line extended with buildConfigPayload; loadConfig DOM fixture rows gained <input data-field="prompt" /> for scene_segmentation + delivery. Old wipe-lock assertions (default-prompt null, max-tokens '') removed — they locked the pre-Phase-1 wipe behavior. Design decision from empirical backend test (POST/GET round-trip): prompt overrides live in a TOP-LEVEL walk_override section, NOT nested inside llm.task_overrides — pydantic model_dump on GET strips nested unknown keys, so nested prompts would not survive a reload. This matches plan wording "map to walk_override sections in the config payload" and the manager's sanctioned alternative.
- [x] Implement: extend setup.ts (loadConfig + handleConfigSubmit) to render per-walk override inputs (temperature/prompts) that map to walk_override sections in the config payload; saving sends the raw merged config (unknown keys preserved); update the test setup.ts uses to reflect byte-stable behavior
    **GREEN:** Extended frontend/src/tabs/setup.ts: (1) Fixed stale L13 comment resolve_task_llm() → resolve_task_config() (was the flagged docstring drift); (2) Added WalkOverride interface {temperature?, prompt?} and extended AppConfig with optional schema_version/walk_override + index signature [key: string]: unknown to carry unknown top-level sections; (3) Added module state retainedConfig: AppConfig|null — loadConfig() now stores the FULL raw GET response (unknown sections included); (4) loadConfig() additionally populates a per-task prompt input ([data-field="prompt"]) from config.walk_override[taskName].prompt with placeholder 'Inherit: ${model}'; (5) New exported collectWalkOverrides() — reads prompt inputs per task row (WALK_TASK_NAMES-filtered), emits {prompt: value||null} keyed by task name (explicit null so backend deep-merge clears stale prompts); (6) New exported buildConfigPayload() — starts from {...retainedConfig} (unknown top-level sections preserved verbatim per byte-stable contract), overlays known llm/tts fields from form values (parallel-workers clamp moved in here), sets task_overrides from collectTaskOverrides(), and walk_override merged {…retained.walk_override, …collectWalkOverrides()}; (7) handleConfigSubmit() now just calls buildConfigPayload() + POST /api/config — removed the old llm/tts-only object construction (that was the wipe behavior). frontend/index.html: added "Prompt Override" column header (widths rebalanced 25/25/15/15/20) and a <input type="text" data-field="prompt" placeholder="Inherit global"> cell appended to all 9 per-task rows; help text updated to mention prompt. No backend files touched. Verified empirically that nested prompt inside llm.task_overrides is STRIPPED by GET /api/config (pydantic model_dump drops nested unknowns; only top-level keys re-added), so prompts are carried in the top-level walk_override section which round-trips byte-stable.
    **Notes:** The P5 payload shape IS the prompt-consumption source: buildConfigPayload() emits walk_override[taskName].prompt (string or explicit null) in the TOP-LEVEL walk_override section — exactly what the amended Phase 2 resolve_task_config reads as its tier-1 prompt source (config["walk_override"][task]["prompt"]). No frontend rework needed for prompt consumption; Phases 2/3 consume this shape as-is.
- [x] TDD GREEN: run test_setup tests; assert override fields render, save payload preserves unknown keys, and the old wipe lock is gone
    **GREEN:** Ran `npx vitest run tests/frontend/test_setup.test.ts` — 16/16 passed (was 14 tests before P5: 2 lock tests replaced with 2 new, +2 from replacing 1). Verified: per-walk override fields render from config (temperature from llm.task_overrides, prompt from walk_override section), save payload preserves unknown top-level sections (generation/prompts/walk_override), and the old wipe lock assertions are gone. Full suite also green (see P5-S4).
- [x] Verify: `npx tsc --noEmit` exit 0; vitest green (167 baseline, test_setup updated); `npm run build` + dist commit
    **Verify:** Verification passed: (1) `cd frontend && npx tsc --noEmit` exit 0; (2) `npx vitest run` full suite — 263 passed across 6 files (baseline was 262; net +1: removed 1 lock test, added 2); (3) `npm run build` exit 0 — dist rebuilt into app/static/dist/ (index.html 76.67 kB, assets/index-DrlCd_0a.js 80.31 kB). NOT committed — P6-S6 owns the implementation commit and dist/ is rebuilt+committed at P6 per manager directive. Files changed in scope: frontend/src/tabs/setup.ts (modified), frontend/index.html (modified), frontend/tests/frontend/test_setup.test.ts (modified). Backend files untouched (their git-modified state is from Phases 1-4, uncommitted prior work).
- [x] Add a frontend contract-lock assertion in frontend/tests/frontend/test_setup.test.ts: buildConfigPayload() emits the prompt override as walk_override[taskName].prompt (string value or explicit null) — the exact task-name-keyed shape the backend resolve_task_config consumes via config["walk_override"][task]["prompt"] — and loadConfig() populates the prompt input from that same path. Run `npx vitest run tests/frontend/test_setup.test.ts` green (assertion-only; no production frontend change).
    **Locked:** Assertion-only contract lock — no production frontend change (setup.ts/index.html untouched this step; their git-modified state is P5-S2's uncommitted work). Added a new 'prompt override contract lock (P5-S5)' describe block to frontend/tests/frontend/test_setup.test.ts (2 tests): (1) buildConfigPayload emits walk_override[taskName].prompt keyed by ALL 9 WALK_TASK_NAMES in the TOP-LEVEL walk_override section, each value a non-empty string or explicit null — the exact shape backend resolve_task_config reads via config["walk_override"][task]["prompt"] (tier-1 prompt source, P2-S5/S6); concrete round-trip asserts: string prompt survives, explicit-null survives, absent entry becomes explicit null (backend: unset -> None -> built-in fallback), and entries carry exactly {prompt} (toEqual). Also locks the negative shape: prompt is NOT nested in llm.task_overrides (Object.keys(entry) not.toContain('prompt') per task) — that path is stripped by GET /api/config (pydantic model_dump) and is not a read source. (2) loadConfig populates each row's [data-field="prompt"] input from config.walk_override[taskName].prompt — override string when set, '' when null/absent — for all 9 tasks (same path). Used 9-row DOM fixture + vi.mocked(API.get) mock, matching existing conventions. Results: test_setup 18/18 (was 16), full vitest 265 passed (263 baseline + 2), `npx tsc --noEmit` exit 0. No commits (P6-S6 owns commit).
    **Notes:** Amendment Round 2 — locks the frontend payload shape to the backend read path defined in P2-S5/S6.

### Phase 6: Regression gate, guard suite and verification
- [x] Run `pytest tests/pipeline -q --cov=app/pipeline --cov-report=term-missing` and record pass count + coverage; all green
    **Verify:** `python -m pytest tests/pipeline -q --cov=app/pipeline --cov-report=term-missing` -> 1163 passed in 44.16s, 620 warnings (pre-existing ResourceWarning: unclosed sqlite3 db in a fixture + StarletteDeprecationWarning; non-fatal). Coverage: TOTAL 3499 stmts, 276 missed, 92%. Top-level per-module highlights: adapter 100%, ledger 100%, review 100%, schema 100%, operations 90%, tts_integration 97%, runner 77%, walks 82-94%. Matches expected 1163 passed exactly.
- [x] Run `pytest tests/pipeline/test_legacy_removed.py -q` and verify 12/12 still green (no default/review/persona prompt modules resurrected)
    **Verify:** `python -m pytest tests/pipeline/test_legacy_removed.py -q` -> 12 passed in 1.65s. Guard confirms: legacy files absent (app/project.py, default/review/persona_prompts modules + txt, editor-legacy.ts, audio.ts), no legacy routes in live table (exact + family prefix match), no pipelineEnabled in state.ts, no project_manager identifier in app/*.py. No default/review/persona prompt modules resurrected — prompt overrides live in config.json walk_override section + walk_override rows only (verified by design + P3 prompt-consumption tests).
- [x] Run `npm test` + `npx tsc --noEmit` + `npm run build` with `git diff --exit-code app/static/dist/` clean after commit
    **Verify:** Frontend gates all green (run from frontend/): (1) `npx vitest run` -> 265 passed across 6 test files (16.65s) — matches expected 265; (2) `npx tsc --noEmit` -> exit 0; (3) `npm run build` (vite 8.2.0, 21 modules, 290ms) -> exit 0, dist rebuilt: index.html 76.67 kB (gzip 12.03 kB) + assets/index-DrlCd_0a.js 80.31 kB (gzip 19.65 kB). DIST STATE (documented, pre-commit): `git status --short app/static/dist/` = `D assets/index-CGCTafV9.js` (old bundle removed), `M index.html` (new bundle ref), `?? assets/index-DrlCd_0a.js` (new bundle). `git diff --exit-code app/static/dist/` -> exit 1 (dirty), 3 entries — dirty ONLY because of the uncommitted P5 build. Fresh-build stability verified: a second `npm run build` produces byte-identical output (same hashes, no further diff) — committed dist at P6-S6 will be byte-identical to a fresh build, so the CI gate `npm run build && git diff --exit-code app/static/dist/` will pass AFTER the P6-S6 commit (diff-clean gate intentionally deferred to post-commit per task directive; dist NOT committed here).
- [x] Security review: config file path resolution (CONFIG_PATH env respected, no traversal), api_key handling (secrets stored in config.json — no logging of full config, redact api_key in any error output), byte-stable test proves no key loss; document findings
    **Security:** SECURITY REVIEW (verification-only; no code changed — no gate failed). (a) CONFIG_PATH resolution — PASS: app.py L62 `CONFIG_PATH = os.environ.get("ALEXANDRIA_CONFIG_PATH") or os.path.join(BASE_DIR, "config.json")`; env var used as-is = the documented config-override mechanism. No path traversal: GET/POST /api/config take NO path/user parameters (module-global CONFIG_PATH only; request body never influences the path), and utils.find_config_path() (read side, used by resolve_task_config) mirrors the same env-or-default logic. Writes never escape CONFIG_PATH's directory: save_json -> atomic_json_write uses tempfile.mkstemp(prefix=".tmp_", suffix=".json", dir=os.path.dirname(target_path)) — tmp created in the SAME dir as target (same-filesystem atomic rename) — then os.replace(tmp, target); tmp removed in finally on failure. Failed save leaves the original byte-unchanged (pinned by test_failed_save_leaves_file_unchanged). (b) api_key handling — PASS: api_key is a declared LLMConfig field stored in config.json (local secrets file, acceptable per CONTRACTS.md rule #11). Repo-wide grep for api_key in app/: only utils.py load_llm_config (read) + create_llm_client (passes to OpenAI constructor) + app.py model definition & default literal — ZERO logging sites. No logger call logs the full config dict (grep `logger.*config` -> only 2 unrelated walk_2h voice_config character warnings). log_llm_response (utils.py L243) has ZERO callers repo-wide and logs only label/text/finish_reason/token usage anyway. No exception handlers registered (grep exception_handler/RequestValidationError -> none) so FastAPI's default 422 bodies never echo the request body or config. The 9 walks destructure llm_config into model_name/temperature/reasoning_effort/prompt and never log them (walk loggers emit IDs + response types + truncated response text only). Residual (documented, not a vuln): GET /api/config returns api_key in its response — it is the llm section the local Setup tab round-trips; this is the documented config surface for a single-user local tool, not an error response or log. (c) Byte-stable proof — PASS: tests/pipeline/test_config_byte_stable.py (7 tests, all green in P6-S1) proves no key loss on round-trip: unknown top-level keys (generation/prompts) POSTed round-trip byte-stable through GET AND persist to disk identically; survive later known-only saves (test_unknown_keys_survive_later_known_only_save) and on-disk-omitting saves (test_unknown_keys_already_on_disk_survive_omitting_save); GET preserves on-disk unknowns while materialising known defaults (H1); schema_version=1 stamped on disk + GET; failed saves leave file byte-unchanged (atomic). OVERALL: PASS on all three items; zero findings requiring code changes.
- [x] Code review pass via exec-manager QA-Reviewer; fix MINOR findings
    **QA:** QA-Reviewer Round 1 status PASS (2026-08-07). testAnalyzerReport PASS (56 new discriminating backend tests: 7 byte-stable + 27 resolver incl. 14 prompt-tier + 22 CRUD; 1163 backend, 265 frontend, guard 12/12, harness 11/11; all 6 completion criteria covered; 0 stale tests; no resolve_task_llm refs in tests). docsAnalyzerReport PASS (CONTRACTS.md resolve_task_config contract + prompt precedence accurate; rule #11/#13, DD cannot-restore #4/#13, cap6, UX #6 match implementation; zero doc/code drift in scope). Direct verification: tts.py byte-identical (0-byte diff), ruff clean 12 files, tsc exit 0, build byte-reproducible (index-DrlCd_0a.js 80.31 kB), no repo residue, no unjustified noqa/TODO/FIXME. 1 non-blocking MINOR observation (app.py L520: known keys llm/tts replaced verbatim in _deep_merge_unknown; partial known-section request could strip defaulted optional keys — documented contract-consistent behavior, near-zero impact, FIX_INLINE optional per reviewer). DECISION: no Exec-Fixer dispatched — PASS status, optional hardening only, no test failure; observation preserved for downstream.
- [ ] Commit: `feat(config): overlay config merge with walk overrides and byte-stable round-trip`

## Completion Criteria

- POST /api/config raw-JSON merges (unknown keys preserved byte-stable), validates known keys via AppConfig(extra='ignore'), stamps schema_version, atomic write
- resolve_task_config(task, storage, book_id) single helper with on-disk → task_overrides → walk_override precedence, snapshotted per walk-unit start; all 9 walks call it
- Per-walk prompt overrides are consumed end-to-end: resolve_task_config returns the effective "prompt" (config top-level walk_override section → llm.task_overrides → walk_override row, row wins; None when unset); all 9 walks pass the effective prompt (or built-in fallback) as system_prompt; backend + frontend tests prove override → LLM call
- walk_override CRUD implemented and tested
- Setup tab renders per-walk override fields and preserves unknown keys on save; test_setup.test.ts L296 lock updated
- Full pytest + vitest green, guard 12/12, ruff + tsc clean, build + dist committed, security review documented

## References

- `artifacts/designs/pending/DD-universal-upgrade.md` — P5 A4-r phase, FP5, cap6, workflow (Config), decision #6, cannot-restore #4/#13
- `artifacts/designs/parts/universal-upgrade/CONTRACTS.md` — resolve_task_config, raw-JSON merge, walk_override table
- `artifacts/designs/parts/universal-upgrade/README.md` — Plan G row
- Prior: `TASK-universal-upgrade-B-render-walk-persistence.md` (completed)
