# Task: Render & Walk-Run Persistence

## Problem Statement

Today render jobs live in a module-level dict `_render_jobs` (app/pipeline/api_export.py:69) that is never cleaned or evicted, walk status lives in WalkRunner in-memory `_status`/`_cancelled` dicts (walks/runner.py:290-292), and cancellation is a `threading.Event` that dies with the process. A process restart loses all job/walk state; there is no per-chunk progress; `GET /download/{job_id}` has zero test coverage; `POST /cancel_render`/`POST /cancel_walks` are ephemeral.

The Universal Upgrade design (DD-universal-upgrade, A1-r Schema-Native Jobs with Reconciliation) makes SQLite rows the source of truth: walk_run and render_job/render_chunk rows, startup-only reconciliation (stale `running` → `interrupted`), persisted cancellation via `walk_run.cancel_requested=1`, a single `is_cancel_requested(run_id)` dispatcher, walk-side retry on `ConcurrentTransactionError`, and the jobs/chunks/runs endpoint surface plus the download rewrite. This plan implements the persistence layer and API surface; the RENDER_ROOT run directories and fsync discipline land in Plan C.

## Dependencies

- Plan A (transaction(), tables, single_speaker) — completed and archived
- `artifacts/designs/parts/epub-audiobook-pipeline-rewrite/CONTRACTS.md` § Universal Upgrade — endpoint registration

## Phases

### Phase 1: Smoke-check harness re-establishment (baseline)
- [x] Research where the smoke-check harness should live (DD test strategy requires smoke checks; none exist today) — propose `tests/pipeline/test_smoke.py` booting the FastAPI app via TestClient with stubbed TTS engine and asserting /api/pipeline/onboard, /api/pipeline/render 503-without-engine, /api/pipeline/export/{book_id} routes are reachable; document the chosen location in the step annotation
    **Note:** Research confirmed: NO smoke-test files exist anywhere in the repo (glob **/*smoke* → no files; tests/pipeline/ has 30+ test files, none smoke). Chosen harness location: tests/pipeline/test_smoke.py — same directory as the pipeline suite so it runs with the baseline, and it reuses established conventions there (module docstring coverage list, fixtures, TestClient + dependency_overrides from test_api.py, guard-compatible import path from test_legacy_removed.py). Proposed harness: boot the REAL production app (app.app) via TestClient, stub get_storage (InMemorySQLiteAdapter) + get_tts_engine (MagicMock) via DI overrides, assert /api/pipeline/onboard (400 on non-EPUB), /api/pipeline/export/{book_id} (200 []), /api/pipeline/render (503 without engine, real dependency resolution). No production code touched.
- [x] Create the smoke harness file with the app boot (guard-compatible import path: preload app/utils.py + app/hf_utils.py before importing app.app) and the initial route-reachability checks; TDD GREEN: run it and confirm it passes with the current in-memory implementation
    **Note:** Created tests/pipeline/test_smoke.py (new file, ~160 lines). Guard-compatible import path exactly per test_legacy_removed.py: insert repo root on sys.path, preload app/utils.py + app/hf_utils.py into sys.modules via importlib.util.spec_from_file_location (guarded by `if _name not in sys.modules`), then `import app.app as app_module` with # noqa: E402. No app.tts / soundfile imports anywhere in the file. Fixtures: `storage` (InMemorySQLiteAdapter + init_db, hermetic — no data/pipeline.db) and `smoke_client` (TestClient over the REAL app.app with dependency_overrides for get_storage → fresh in-memory adapter and get_tts_engine → MagicMock stub; overrides popped in teardown). Tests: (1) test_onboard_route_reachable — POST non-EPUB → 400 'must be an EPUB'; (2) test_export_route_reachable — GET /export/nonexistent-book → 200 []; (3) test_render_route_returns_503_without_engine — REAL dependency resolution: stub override popped, engine_factory.reset_tts_engine() before/after (test_api.py TestGetTTSEngineProduction precedent), engine resolves to None because app.tts import fails on missing pydub → 503 'TTS engine not available'. Deterministic in this environment (verified: app.engine.get_tts_engine() → None). TDD GREEN: python -m pytest tests/pipeline/test_smoke.py -q → 3 passed.
- [x] Verify: `ruff check tests/pipeline/test_smoke.py` clean; `pytest tests/pipeline/test_smoke.py -q` passes
    **Note:** Verification green: (1) `ruff check tests/pipeline/test_smoke.py` → All checks passed (ruff 0.15.20, defaults — no ruff config file exists in repo; existing tests/ suite also clean under defaults). (2) `python -m pytest tests/pipeline/test_smoke.py -q` → 3 passed. (3) `python -m pytest tests/pipeline -q` → 844 passed, 1 warning (13.88s) = 841-test baseline + 3 new smoke tests. Only pre-existing warning is the starlette/httpx deprecation warning (unrelated). No production files modified.

### Phase 2: walk_run persistence and is_cancel_requested dispatcher
- [x] TDD RED: write tests in tests/pipeline/test_runner.py asserting run_walk creates a walk_run row (status running) at start, updates to completed with result_json at end, and records created_ms/finished_ms; run_all_walks does the same per walk; cancel_walks persists cancel_requested=1 and is_cancel_requested(run_id) returns True afterwards
    **TddRed:** Added 9 spec-first tests to tests/pipeline/test_runner.py (2 new classes: TestWalkRunPersistence, TestCancelDispatcher) asserting: run_walk creates a walk_run row (status=running, created_ms, heartbeat_ms) at start; completed row carries result_json (json.loads round-trip) + finished_ms; exception → failed row + error + finished_ms; run_all_walks writes one row per walk; each run gets a fresh uuid4 run_id; is_cancel_requested(run_id) False by default; cancel_walks persists cancel_requested=1 on ACTIVE (pending/running) rows + is_cancel_requested True afterwards; stop-file alone marks cancelled (tmp_path via runner.stop_file_dir override); in-process event alone marks cancelled (cancel-after-completion leaves row flag 0 → event is the deciding source). Baseline before RED: 31 passed, runner coverage 67% (149 stmts / 49 miss — misses are the 9 _verify_* functions, unrelated to persistence). RED confirmed: 9 failed, 31 passed (failures are IndexError on empty walk_run and AttributeError on missing is_cancel_requested).
- [x] Implement: WalkRunner.run_walk writes walk_run row (running) before executing the walk module; on completion writes status=completed + result_json; on exception writes status=failed + error; heartbeat_ms updated inside each per-unit transaction
    **Implemented:** run_walk now persists each invocation: fresh uuid4 run_id → INSERT walk_run (status='running', created_ms, heartbeat_ms) BEFORE executing the walk module → on success UPDATE status='completed' + result_json (json.dumps of result dict) + finished_ms + heartbeat_ms; on exception/ImportError/verification-failure UPDATE status='failed' + error + finished_ms. run_all_walks inherits this per walk (each walk gets its own row; abort-on-first-failure preserved — now the loop simply breaks on a 'failed' result). Heartbeat mechanism choice: the plan's per-unit transaction is forward-looking (Plan D) — verified zero transaction() uses in app/pipeline/walks/ — so heartbeat is implemented as a HeartbeatStorage proxy (defined in runner.py) passed to walk_module.execute() in place of the raw adapter; it overrides execute_insert/execute_update/execute_delete to stamp heartbeat_ms on the run's row after each write, and delegates everything else (__getattr__) including execute_query/transaction. Per-write granularity without touching the 9 walk module signatures. Rows always carry heartbeat_ms at start, after each walk write, and at every final transition. Two existing tests updated (test_run_walk_calls_execute, test_run_walk_passes_config): storage-identity assertions now expect a HeartbeatStorage wrapping runner._storage (call args[1].storage is runner._storage). Added test_walk_writes_refresh_heartbeat proving a wrapper write bumps the row's heartbeat_ms. All 41 test_runner.py tests green at this boundary.
- [x] Implement `WalkRunner.is_cancel_requested(run_id) -> bool` as the single dispatcher: reads walk_run.cancel_requested + stop-file + event; replace direct `_cancelled` dict reads in run_walk/run_all_walks/cancel_walks with it; keep clear_cancel semantics
    **Implemented:** WalkRunner.is_cancel_requested(run_id) -> bool is the single dispatcher, reading in order: (1) walk_run.cancel_requested flag (SELECT run_id/book_id/cancel_requested WHERE run_id=?), (2) persisted stop-file at data/cancel/{run_id}.stop, (3) the in-process per-book event (self._cancelled, looked up via the row's book_id). Stop-file location: neither DD nor CONTRACTS define one — chose data/cancel/{run_id}.stop to match the adapter's ./data/pipeline.db convention; data/ is gitignored; overridable per-instance (stop_file_dir class attr, tests set tmp_path). All direct _cancelled reads replaced: run_walk now writes the running row FIRST, then checks is_cancel_requested(run_id); if cancelled it flips the row to 'cancelled' (via _finalize_run with error), sets _status cancelled, returns {'status': 'cancelled', 'error': 'Walk cancelled by user'} — cancellation honored before walk execution with the row recorded. run_all_walks no longer has its own _cancelled pre-check — each walk delegates to run_walk (which handles cancel per walk); 'continue' semantics replaced by per-walk cancelled results; abort-on-first-failure preserved. cancel_walks sets the event (kept — test_api.py asserts _cancelled dict directly) AND persists cancel_requested=1 + writes a stop-file on each ACTIVE (pending/running) walk_run row for the book — persisted cancel survives restart. clear_cancel semantics unchanged (pop only). get_walk_status still reads the in-memory _status dict (frontend contract unchanged); already-running refusal preserved (checked before row insert). All 41 tests green: existing cancel tests (test_cancel_walks_sets_flag, test_run_walk_checks_cancel_flag, test_run_all_walks_stops_on_cancel, clear_cancel tests) + new dispatcher tests (default False, persisted flag, stop-file, event).
- [x] TDD GREEN: `pytest tests/pipeline/test_runner.py -q --cov=app/pipeline --cov-report=term-missing` passes with runner coverage rising from 67% baseline
    **TddGreen:** GREEN verified. python -m pytest tests/pipeline/test_runner.py -q --cov=app.pipeline.walks.runner --cov-report=term-missing: 41 passed (31 pre-existing + 10 new). Runner coverage rose 67% → 73% (149→206 stmts; 49→55 miss). New persistence + cancel paths all covered: running-row-at-start, completed row (result_json json.loads round-trip, finished_ms), failed row on exception (error + finished_ms), run_all_walks row-per-walk, fresh run_id per invocation, heartbeat refresh on wrapper write + at final transition, is_cancel_requested true/false (default), persisted cancel_requested=1 on active rows, stop-file source, event source. Remaining 55 misses are: the 9 pre-existing _verify_walk_2b..2i functions (need DB fixtures; pre-existing gap, out of scope) + 4 lines of trivial wrapper delegation (execute_insert/execute_delete overrides) + 1 _set_status branch. Full tests/pipeline suite 854 passed (844 gate exceeded).
- [x] Verify: `ruff check app/pipeline/walks/runner.py` clean; guard suite still 12/12
    **Verified:** ruff check app/pipeline/walks/runner.py tests/pipeline/test_runner.py → All checks passed (no repo ruff config; defaults E4/E7/E9/F, E501 not enforced). Guard suite: python -m pytest tests/pipeline/test_legacy_removed.py -q → 12 passed (12/12). Full regression: python -m pytest tests/pipeline -q → 854 passed (gate ≥844; +10 from new Phase-2 tests). Test artifacts cleaned (data/cancel/ stop-files removed; data/ gitignored anyway). Changed files only app/pipeline/walks/runner.py and tests/pipeline/test_runner.py.

### Phase 3: render_job / render_chunk rows and per-chunk progress
- [x] TDD RED: write tests in tests/pipeline/test_tts_integration.py asserting render_audiobook(job_id=...) writes a render_job row (running), writes render_chunk rows per chunk in individual mode (status pending then done), and sets render_job completed + output_artifact_path in the final transaction; batch mode writes job-level status only (no chunk rows)
    **Complete:** TDD RED done. Added TestRenderJobPersistence (7 tests) to tests/pipeline/test_tts_integration.py: job row exists with status 'running' DURING generation (via _RowInspectingBatchEngine spy that reads render_job inside generate_batch); batch mode creates zero render_chunk rows; individual mode chunk rows pending during generate_voice and done only after it returns (spy reads render_chunk at entry+exit: both 'pending'; final rows done with wav_path chunk_{i:04d}.wav); final transaction sets completed + output_artifact_path (falls back to output dir when no audiobook.m4b) + finished_ms; generate_voice exception → failed chunk row (error recorded) + failed job row + re-raise; generate_batch all-failed → job row failed with error; empty-script book still gets a completed row. Ran against current impl: 6 failed (IndexError: no rows), 1 vacuous pass (batch no-chunk-rows counts 0).
- [x] Implement: api_export POST /render creates the render_job row + passes job_id into render_audiobook; render_audiobook (app/pipeline/tts_integration.py) writes render_chunk rows per chunk in individual mode (chunk status done only after the WAV exists — fsync discipline lands in Plan C; here set done after write returns), discards the old silent ignore of generate_batch's completed/failed lists and records failures
    **Complete:** Implemented. tts_integration.py: added _now_ms() (int(time.time()*1000), mirrors walks/runner.py), _ensure_render_job_row (SELECT-first, INSERT status='running' + created_ms/started_ms; reuses API pre-created row untouched), _insert_chunk_row/_mark_chunk_done/_mark_chunk_failed (render_chunk pending→done/failed with wav_path/error; parameterized SQL), _format_batch_failure, _finalize_job (single storage.transaction(); completed→output_dir+output_artifact_path [audiobook.m4b path when file exists else output dir]+finished_ms; failed→error+finished_ms; cancelled→finished_ms). render_audiobook PUBLIC SIGNATURE UNCHANGED: render_audiobook(book_id, storage, tts_engine, *, use_batch=True, output_dir=None, batch_seed=BATCH_SEED_RANDOM, job_id=None, cancel_check=None) -> str. Body rewritten: row ensured at start; whole body (script fetch, voice config, dispatch) wrapped in try so ANY exception (incl. export_annotated_script failure) finalizes the row failed before re-raising; individual mode inserts pending row per chunk, marks failed+error on generate_voice exception then re-raises, marks done AFTER generate_voice returns (fsync = Plan C); batch mode writes NO chunk rows; CancelledError → row cancelled. job_id=None direct calls still write rows (documented choice: rows=truth unconditionally; direct-call tests use fresh storage per test so additive rows are invisible). generate_batch interpretation (documented in code): None result (API test MagicMock) treated as all-completed; 'failed and not completed' (every reported outcome a failure) → RuntimeError('Batch render failed: all N chunks failed (chunk i: err; ...)') → job row failed; any completed → job completed (partial batch failures not representable without chunk rows — noted for Plan C). api_export.py: added import time, _now_ms(); POST /render now INSERTs render_job row (job_id, book_id, mode=batch|individual from use_batch, status='running', output_dir from request, created_ms=started_ms) BEFORE registering _render_jobs entry, keeps 503 check + {job_id, status:'started'} response shape exactly; _run_render_job mirrors terminal states into the row via _mark_job_row_terminal (idempotent safety net for exceptions escaping render_audiobook's own row handling, e.g. patched callers).
- [x] Extend GET /render_status/{job_id} to read the render_job row and return per-chunk counts (completed_chunks/total_chunks/failed_chunks) for individual mode; keep the existing response fields for backward compatibility with the frontend poll
    **Complete:** render_status now reads the render_job row first (rows=truth): GET /render_status/{job_id} gained storage: PipelineStorage = Depends(get_storage); SELECT mode/status/error/output_dir FROM render_job WHERE job_id=?; row found → response {job_id, status, output_dir, error, mode}; individual mode adds completed_chunks (COUNT status='done'), total_chunks (sum of all chunk rows), failed_chunks (COUNT status='failed') via GROUP BY on render_chunk; batch mode returns the base 5 fields only (no chunk rows exist by contract). Existing fields {job_id, status, output_dir, error} preserved for frontend poll compatibility; 404 with 'Unknown job_id' preserved when neither row nor dict entry exists. Dict/_render_jobs interaction decision (documented in docstring): rows=truth for status; the dict remains solely as the in-process cancel_event channel plus fallback for row-less entries (pre-patch jobs; test_render_failure's patched-render case reads the row 'running' which the test allows, and the row mirror in _run_render_job keeps them convergent). Added TestRenderStatusFromRows (3 tests) to tests/pipeline/test_api.py: row-only job served from row (mode batch); individual row with 2 done + 1 failed chunks → completed_chunks=2/total_chunks=3/failed_chunks=1; end-to-end individual render via API → completed with completed_chunks=1/total_chunks=1 (test_api storage fixture book b1 has exactly 1 span).
- [x] TDD GREEN: run tts_integration + api_export tests; coverage of api_export.py rises from 76% baseline; all green
    **Complete:** TDD GREEN: `python -m pytest tests/pipeline/test_api.py tests/pipeline/test_tts_integration.py -q --cov=app.pipeline.api_export --cov-report=term-missing -W ignore::ResourceWarning` → 109 passed; api_export coverage 78% (142 stmts, 31 miss), up from the 76% baseline. All new render_job/render_chunk paths covered by the new tests (row-backed status, per-chunk counts, end-to-end individual render, batch/individual failure row finalization, _run_render_job row mirror via patched-render test). Uncovered lines are pre-existing gaps only: 161-162 (API in-flight CancelledError branch — no test cancels a running job; old code had the same uncovered branch), 285 (download 404 missing output dir), 306-307 (cancel_render already_finished path), 324-358 (ffmpeg failure/timeout branches), 387/432-443 (download zip / merge error branches) — none introduced by this phase.
- [x] Verify: `ruff check app/pipeline/api_export.py app/pipeline/tts_integration.py` clean
    **Complete:** `ruff check app/pipeline/api_export.py app/pipeline/tts_integration.py tests/pipeline/test_api.py tests/pipeline/test_tts_integration.py` → 'All checks passed!' (no ruff config in repo; defaults E4/E7/E9/F). Full suite: `python -m pytest tests/pipeline -q` → 864 passed (854 baseline + 10 new: 7 TestRenderJobPersistence + 3 TestRenderStatusFromRows), 0 failures, 14.59s. git status confirms only the 4 in-scope files modified by this phase (app/pipeline/tts_integration.py, app/pipeline/api_export.py, tests/pipeline/test_tts_integration.py, tests/pipeline/test_api.py); app/tts.py untouched; walks/runner.py + test_runner.py + test_smoke.py + plan md modifications are Phase 2's prior uncommitted work, not this phase.

### Phase 4: Startup reconciliation (rows = truth)
- [x] TDD RED: write tests asserting `reconcile_stale_runs()` flips running→interrupted for stale render_job and walk_run rows (stale = started before a cutoff, no heartbeat), leaves completed/failed/cancelled rows untouched, and is invoked once at startup before API requests are accepted
    **TddRed:** 8 spec-first tests added to tests/pipeline/test_adapter.py (2 new classes): TestReconcileStaleRuns (7 tests) + TestReconcileStartupWiring (1 test). Stale predicate per DD/contracts analysis: render_job has NO heartbeat column (schema registered in CONTRACTS.md) so its only liveness signal is started_ms → predicate 'started_ms < cutoff'; walk_run carries created_ms + heartbeat_ms → predicate 'created_ms < cutoff AND (heartbeat_ms IS NULL OR heartbeat_ms < cutoff)'. Tests: (1) crash-recovery scenario FILE-BACKED (tmp_path/db.sqlite): writer adapter inserts stale running rows (render_job started 1h ago; walk_run old-heartbeat; walk_run NULL-heartbeat), close = process death, REOPEN a NEW SQLiteAdapter on the same file, init_db, reconcile → counts {'render_job': 1, 'walk_run': 2}, all flipped to interrupted with error 'interrupted by process restart' + finished_ms set; (2) fresh running rows (started just now, within grace window) untouched, counts 0; (3) completed/failed/cancelled/interrupted (+ pending walk_run) rows untouched even with old timestamps; (4) idempotent — second reconcile call flips nothing (0/0); (5) safe on empty DB; (6) parametrized over both adapter fixtures (mirror parity — InMemorySQLiteAdapter is a MIRROR of SQLiteAdapter, NOT a subclass; it needs the method too); (7) startup wiring: monkeypatch api_onboard._storage=None + SQLiteAdapter→MagicMock, _get_production_storage() calls init_db() once AND reconcile_stale_runs() once; second call short-circuits (assert_called_once). RED confirmed: 8 failed (7 AttributeError on missing method, 1 mock assert), 75 pre-existing tests still pass.
- [x] Implement `SQLiteAdapter.reconcile_stale_runs() -> dict[str, int]` (one pass, per contract rule #5 — no on-read sweeper, no periodic reaper); wire it into the production storage bootstrap in api_onboard.py (or the startup path where _get_production_storage is called)
    **Implemented:** Implemented in app/pipeline/adapter.py: (1) module constants _STALE_RUN_GRACE_MS = 5*60*1000 (5 min) and _INTERRUPTED_ERROR = 'interrupted by process restart'; (2) shared module-level helper _reconcile_stale_runs(conn) — EXACTLY ONE UPDATE per table (contract rule #5, one pass), owner-thread guard honored in the public method, commit-if-not-in-transaction mirrors execute_update discipline; render_job UPDATE: "UPDATE render_job SET status='interrupted', error=?, finished_ms=? WHERE status='running' AND started_ms < ?"; walk_run UPDATE: "UPDATE walk_run SET status='interrupted', error=?, finished_ms=? WHERE status='running' AND created_ms < ? AND (heartbeat_ms IS NULL OR heartbeat_ms < ?)". All variable values parameterized (error text, finished_ms, cutoff); status literal is a fixed enum constant of the operation. cutoff = now_ms - 5min. Returns {"render_job": N, "walk_run": M} always both keys. (3) reconcile_stale_runs() -> dict[str,int] public method added to BOTH SQLiteAdapter and InMemorySQLiteAdapter (mirror — separate class, not subclass, so both need it; contract ledger registers it on SQLiteAdapter; deliberately NOT added to PipelineStorage ABC so no other implementer breaks). Wired into production bootstrap in app/pipeline/api_onboard.py _get_production_storage(): local `adapter = SQLiteAdapter(db_path); adapter.init_db(); adapter.reconcile_stale_runs(); _storage = adapter` — runs exactly once (inside `if _storage is None:`) at first storage acquisition, BEFORE any request is served. NOT wired into test/DI path (tests override get_storage; smoke/API tests never touch production bootstrap). TDD GREEN: 8/8 reconciliation tests pass; ruff clean on adapter.py + api_onboard.py + test_adapter.py.
- [x] TDD GREEN: run reconciliation tests with a file-backed SQLiteAdapter(tmp_path) fixture (per DD test strategy — :memory: conceals crash recovery); assert stale rows flipped and fresh rows untouched
    **TddGreen:** TDD GREEN verified per DD test strategy ('one file-backed SQLiteAdapter(tmp_path) fixture for reconciliation/crash tests — :memory: conceals crash recovery'). The crash-recovery tests use the exact reopen pattern: TestReconcileStaleRuns._reopen(db_path) constructs a NEW SQLiteAdapter on the same file after the writer adapter closed (= process death), calls init_db() then reconcile_stale_runs(). Test test_crash_recovery_flips_stale_running_rows_on_reopen writes stale running rows (render_job started 1h ago, walk_run old heartbeat, walk_run NULL heartbeat) via a writer adapter at tmp_path/crash.db, closes, reopens, reconciles → asserts counts {'render_job': 1, 'walk_run': 2} and all three rows flipped to interrupted with error 'interrupted by process restart' + finished_ms set. Fresh-rows-untouched is asserted by test_reconcile_leaves_fresh_running_rows_untouched (rows started within the grace window survive, counts 0/0). Full suite: python -m pytest tests/pipeline/test_adapter.py -q → 83 passed (75 pre-existing + 8 new). InMemorySQLiteAdapter confirmed as a MIRROR class (separate, not a subclass of SQLiteAdapter) so the method was added to both; the parity test (parametrized over both fixtures) exercises the in-memory path while all crash-recovery tests stay file-backed.
- [x] Verify: `ruff check app/pipeline/adapter.py app/pipeline/api_onboard.py` clean; `pytest tests/pipeline/test_adapter.py -q` green
    **Verified:** Verification complete. ruff check on all changed files (app/pipeline/adapter.py, app/pipeline/api_onboard.py, tests/pipeline/test_adapter.py) → 'All checks passed!' (no repo ruff config; ruff 0.15.20 defaults E4/E7/E9/F, per prior discovery L96). python -m pytest tests/pipeline/test_adapter.py -q → 83 passed (75 pre-existing + 8 new reconciliation tests). python -m pytest tests/pipeline -q → 872 passed, 1 pre-existing starlette import warning (full-suite gate ≥ 864 met; = 864 prior baseline + 8 new). No regression: P2/P3 suites (test_runner, test_tts_integration, test_api, test_smoke=3 passed) all green — reconcile touches only status='running' rows older than the cutoff, and the production bootstrap is never invoked by tests (get_storage overridden), so fresh rows created by prior-phase tests are untouched. Change footprint is exactly 3 files, all in scope: app/pipeline/adapter.py (+74), app/pipeline/api_onboard.py (+9/-2), tests/pipeline/test_adapter.py (+286). No changes to app/tts.py; no new modules under app/pipeline/. No Phase 5+ scope touched (no endpoints, no cancel_render row writes, no download rewrite).

### Phase 5: Persisted cancel + endpoints (render_status, jobs, chunks, runs)
- [x] TDD RED: write tests/pipeline/test_api.py tests asserting POST /cancel_render marks the render_job row cancelling + persisted cancel flag; POST /cancel_walks writes walk_run.cancel_requested=1; GET /export/jobs/{job_id} returns job detail; GET /export/jobs/{job_id}/chunks returns chunk rows; GET /walks/{book_id}/runs returns walk_run rows newest-first
    **TddRed:** 9 spec-first tests added to tests/pipeline/test_api.py in 5 new classes. RED run (python -m pytest tests/pipeline/test_api.py -q): 7 failed + 2 passed — exactly the manager's predicted split. (1) TestCancelRenderPersistence.test_cancel_running_job_marks_intent_and_row_cancelled — PASSED immediately (verification test): the P3 CancelledError path in _run_render_job (lines 160-162, previously uncovered) already lands the row on 'cancelled'; the test proves the cancel_render event-set + row-transition end-to-end per the L20 schema-compatible interpretation (cancel intent carried by in-process event; row reaches terminal 'cancelled' when the task observes it; 'cancelling' intermediate is NOT storable under the render_job.status CHECK). (2) TestCancelWalksPersistence — PASSED immediately (verification test): P2 already persists walk_run.cancel_requested=1 on active (pending/running) rows and leaves terminal rows untouched; test also pins stop-file_dir to tmp_path to keep data/cancel/ clean. (3) 7 genuine RED failures: all new GET endpoint tests fail with 404 'Not Found' — /api/pipeline/export/jobs/{job_id} (detail+404), /api/pipeline/export/jobs/{job_id}/chunks (rows+empty-batch+404), /api/pipeline/walks/{book_id}/runs (newest-first + empty-book) routes do not exist yet. Existing 404-unknown (test_cancel_unknown_job) and already_finished (test_cancel_running_job_via_api) cancel_render tests preserved unchanged.
- [x] Implement the endpoint surface in api_export.py (export/jobs/{job_id}, export/jobs/{job_id}/chunks) and api_walks.py (walks/{book_id}/runs), plus the persisted cancel behavior on cancel_render/cancel_walks; register routes via the existing pipeline router (api.py aggregator)
    **Implemented:** Endpoint surface added to the EXISTING sub-routers (no new modules; api.py aggregator auto-registers since it include_routers these routers): (1) app/pipeline/api_export.py — GET /api/pipeline/export/jobs/{job_id} (export_job_detail): SELECTs the full ExportJobDetail field set (job_id, book_id, mode, status, error, output_dir, output_artifact_path, created_ms, started_ms, finished_ms) from the render_job row; rows=truth; 404 detail 'Unknown job_id: {id}' when no row. (2) GET /api/pipeline/export/jobs/{job_id}/chunks (export_job_chunks): 404 when the job row is absent; otherwise returns ChunkRow rows (job_id, idx, status, wav_path, error) ORDER BY idx — batch jobs (no chunk rows by contract) yield []. (3) app/pipeline/api_walks.py — GET /api/pipeline/walks/{book_id}/runs (get_walk_runs): WalkRunRow fields (run_id, walk_name, status, heartbeat_ms, created_ms, finished_ms, error) ORDER BY created_ms DESC (newest-first — CONTRACTS DTO carries no ordering key, so the plan's created_ms DESC stands); unknown book → 200 []. All SQL parameterized. Both routers are APIRouter(prefix='/api/pipeline') instances already include_router'd by api.py — registration verified via route enumeration + TestClient (P5-S3). Persisted-cancel behavior: cancel_render needed NO behavioral change (L20 interpretation — in-process event carries the intent; P3 CancelledError path already writes row 'cancelled'); added the schema-compatible interpretation comment to the cancel_render docstring documenting why 'cancelling'/cancel-flag is NOT storable under the render_job.status CHECK and that startup reconciliation handles crash-survival of stuck running rows. cancel_walks unchanged (P2 already persists cancel_requested=1). Response shapes are plain dicts/list-of-dicts, consistent with existing endpoint style (render_status, export_script).
- [x] TDD GREEN: run the new endpoint tests; assert 404 for unknown job_id/run_id and 200 for known; keep FastAPI TestClient usage consistent with existing test_api.py patterns
    **TddGreen:** TDD GREEN verified. python -m pytest tests/pipeline/test_api.py -q → 67 passed (58 baseline + 9 new; the 7 RED endpoint tests now green). 404 semantics: GET /export/jobs/{job_id} and GET /export/jobs/{job_id}/chunks return 404 'Unknown job_id: {id}' for unknown jobs; GET /walks/{book_id}/runs returns 200 [] for unknown books (consistent with test_characters_empty_book/test_export_empty_book precedent — the contract has no 404 for unknown book on runs). 200 semantics: export/jobs returns the full ExportJobDetail field set; chunks returns ChunkRow rows ordered by idx (batch job → []); walks/runs returns WalkRunRow rows ordered by created_ms DESC (newest first, exact key-set lock-in asserted in the test). TestClient usage stays consistent with existing test_api.py patterns (fixture client with dependency_overrides, direct _render_jobs seeding + render_audiobook patching for the cancel task-observation path, storage.execute_insert seeding for row fixtures). No regression: full suite python -m pytest tests/pipeline -q → 881 passed (872 gate exceeded by +9).
- [x] Verify: `ruff check app/pipeline/api_export.py app/pipeline/api_walks.py app/pipeline/api.py` clean
    **Verified:** ruff check app/pipeline/api_export.py app/pipeline/api_walks.py app/pipeline/api.py → 'All checks passed!' (ruff 0.15.20, no repo config — defaults E4/E7/E9/F per discovery L96); ruff check tests/pipeline/test_api.py → clean too. Route-registration evidence: python -c enumeration over app.pipeline.api router confirms all three new GET routes present through the api.py aggregator: /api/pipeline/export/jobs/{job_id}, /api/pipeline/export/jobs/{job_id}/chunks, /api/pipeline/walks/{book_id}/runs. Guard suite tests/pipeline/test_legacy_removed.py → 12/12 (new routes are /api/pipeline/* so exact-match and family checks pass). Full suite tests/pipeline -q → 881 passed (≥872 gate). Change footprint: app/pipeline/api_export.py, app/pipeline/api_walks.py, tests/pipeline/test_api.py only; app/tts.py untouched; no new modules under app/pipeline/.

### Phase 6: Download rewrite (FileResponse-404) and job-status endpoint mapping
- [x] TDD RED: create tests/pipeline/test_export.py covering GET /download/{job_id}: present-file 200 (audio/mp4 when .m4b exists), zip fallback (no m4b, zips wav/mp3/m4a/flac), unknown job 404, and the new FileResponse-404 subclass behavior for missing artifact with known job
    **Note:** RED confirmed. Created NEW tests/pipeline/test_export.py (module docstring per suite convention; no test file existed for export/download — verified zero 'download' references anywhere in tests/pipeline). 5 tests: present-m4b 200 audio/mp4+filename audiobook.m4b+exact bytes; zip fallback (wav/mp3/m4a/flac, ignore.txt excluded, ZIP_STORED, namelist asserted); unknown job 404 'Unknown job_id: {id}'; missing-artifact 404 + JSON detail (FileResponse-404 — the key test); not-completed 404 'Job not completed (status: X)'. Pre-implementation run: 4 failed / 1 passed (unknown-job is a contract lock-in that passes both ways, same pattern as P5 cancel tests). Verified the defect empirically before writing tests: current code with completed job + existing-but-empty output dir returns 200 application/zip with a 22-byte EMPTY audiobook.zip (broken 200) when the artifact file is missing; and row-only jobs 404 'Unknown job_id' because the dict is consulted, not rows. The missing-artifact test mirrors the legacy dict entry so the RED run hits the actual empty-zip defect, not just the dict-backing. Fixtures: local storage (InMemorySQLiteAdapter().init_db(), no spine needed — download reads only render_job rows) + client (combined app.pipeline.api router, get_storage override only).
- [x] Implement: GET /download/{job_id} reads the render_job row; serves output_artifact_path via a FileResponse-404 subclass (returns 404 with detail when the file is missing instead of a broken 200); keep the zip fallback; use rows, not the in-memory dict
    **Note:** Rewrote GET /download/{job_id} in app/pipeline/api_export.py — no new module created (per constraint). download_render now takes storage: PipelineStorage = Depends(get_storage) and reads the row: parameterized 'SELECT status, output_dir, output_artifact_path FROM render_job WHERE job_id = ?' (rows = truth; _render_jobs dict is never consulted for download — retained only for cancel_event, documented in docstring). 404s preserved, adapted to row fields: unknown (no row), 'Job not completed (status: {row[status]})' (same format as before), 'Output directory not found' (row output_dir). Added class FileResponse404(FileResponse) in api_export.py: __init__ takes detail= kwarg (default 'File not found'); __call__ re-checks os.path.isfile(self.path) before delegating — missing file sends JSONResponse 404 {'detail': ...} instead of starlette 1.3.1's RuntimeError('File at path ... does not exist.') which FastAPI surfaces as 500 (version behavior verified against installed starlette source: stat is lazy in __call__, NOT in __init__, so construction never raises). Flow: artifact_path from row; if it is not a dir (i.e. the recorded audiobook.m4b — exists OR vanished) serve via FileResponse404 with media_type audio/mp4 + filename audiobook.m4b + detail 'Audio file not found' (subclass decides file-vs-404 — this is the new missing-artifact behavior); if artifact is the output dir (no m4b at finalize), first serve output_dir/audiobook.m4b if POST /merge produced one later (parity with old m4b-first behavior), else the unchanged zip fallback (output_dir/audiobook.zip, ZIP_STORED, *.wav/*.mp3/*.m4a/*.flac sorted, application/zip, detail 'Zip archive not found'); final 404 'No audiobook file found in output directory' tail preserved. Route registered identically (same decorator); signature return type FileResponse unchanged (FileResponse404 subclasses it).
- [x] TDD GREEN: `pytest tests/pipeline/test_export.py -q --cov=app/pipeline --cov-report=term-missing` all pass (closes the pre-existing zero-test gap on download)
    **Note:** GREEN. tests/pipeline/test_export.py: 7 passed (5 planned + 2 added to lock in explicitly-preserved P6-S2 checks: test_missing_output_dir_returns_404 — completed row with nonexistent output_dir → 404 'Output directory not found', proving the check reads the path from the row; test_serves_m4b_produced_by_merge_after_finalize — artifact=dir row but dir/audiobook.m4b now exists → 200 audio/mp4, locking the m4b-first parity path for merge-after-render). Coverage run per plan command (--cov=app/pipeline --cov-report=term-missing): 7 passed; api_export.py 47% on the test_export-only run (169 stmts; the file's other endpoints are covered by test_api.py). Download section coverage: all branches exercised except line 491 (final 'No audiobook file found in output directory' tail — unreachable in normal flow because the zip build always succeeds when the dir exists; kept as defensive tail). The pre-existing zero-test gap on download/{job_id} is closed — the path now has real behavioral coverage including the FileResponse-404 missing-artifact case (404 + {'detail': 'Audio file not found'}).
- [x] Verify: `ruff check app/pipeline/api_export.py tests/pipeline/test_export.py` clean; full `pytest tests/pipeline -q` green (≥ 768 baseline + new)
    **Note:** Verification green. ruff check app/pipeline/api_export.py tests/pipeline/test_export.py → 'All checks passed!' (0 errors, default rule set per repo — no ruff config file exists, per prior discovery L96). Full suite python -m pytest tests/pipeline -q → 888 passed, 0 failed (881 baseline + 7 new from test_export.py) — above the ≥768 floor and ≥881 target. Guard tests/pipeline/test_legacy_removed.py → 12/12 passed. No regressions: TestMergeEndpoint (dict-based merge tests) and all render/cancel/export-jobs tests unaffected — POST /merge, POST /render, render_status, and Phase 5 GET endpoints untouched. Only line 491 of api_export.py (defensive unreachable tail) uncovered; no new modules; app/tts.py untouched; render_audiobook signature untouched.

### Phase 7: Walk-side retry on ConcurrentTransactionError
- [x] TDD RED: write a test simulating a walk unit whose idempotent write raises ConcurrentTransactionError (non-owner thread) and asserting the walk retries the write with 50-100ms backoff up to 3 times before failing the unit
    **Note:** TDD RED done. Added class TestConcurrentTransactionRetry (6 tests) + module helper _FlakyStorage to tests/pipeline/test_runner.py. _FlakyStorage simulates the non-owner-thread raise condition from adapter.py (write from another thread while a transaction is open): first N non-heartbeat writes raise ConcurrentTransactionError, later ones delegate to the real InMemorySQLiteAdapter. Runner's own walk_run bookkeeping SQL always contains 'heartbeat_ms', so it passes through and never pollutes attempt counters. RED run: 5 failed / 1 passed (test_happy_path_writes_once_no_retry passes pre-impl — it is a verification-style guard). The 5 failures are exactly the retry-behavior tests: retry-up-to-3-then-fail, succeed-on-retry-2, backoff timestamps, sleep-in-contract-range (patches app.pipeline.walks.runner.time.sleep, captures args), and LLM-never-reinvoked.
- [x] Implement the retry wrapper at the walk-unit write boundary in walks/runner.py (or the shared walk helper used by 2a-2i) — applies only to the idempotent write phase, never re-invokes the LLM call; after 3 retries, fail the unit and record the error
    **Unmarked:** [exec-manager] Contract deviation: implemented _MAX_WRITE_ATTEMPTS=3 (initial + 2 retries), but CONTRACTS rule #6 and DD line 105 require "retry idempotent write ×3, then fail unit" = 3 retries = 4 total attempts. Plan P7-S1 also says "retries the write up to 3 times before failing". Fix: _MAX_WRITE_ATTEMPTS → 4, align the fail-unit test (3 retries then failure), keep 50-100ms backoff and LLM-never-reinvoked guarantees.
    **Note:** Retry wrapper implemented in app/pipeline/walks/runner.py (only file changed, no new modules, walk_2a..2i untouched, run_walk signature untouched). Design: extended Phase 2's HeartbeatStorage proxy (the write-boundary interception point walks write through) — its execute_insert/execute_update/execute_delete each dispatch the underlying write via new module-level _retry_write() helper. _retry_write is a pure re-dispatch loop: the thunk closes over ONLY (sql, params) for one execute_* call; on ConcurrentTransactionError it sleeps random.uniform(0.05, 0.10) and re-dispatches, up to _MAX_WRITE_ATTEMPTS=3 total attempts (initial + 2 retries), then re-raises so run_walk's existing except-Exception path marks the walk_run row failed and records str(exc). LLM non-reinvocation guarantee: the SELECT → LLM (chat_completion in _llm_helpers) → write flow lives entirely inside each walk module; the retry loop wraps only the single write-method dispatch, never the walk's execute flow — a retry cannot re-run any walk code. Also wrapped _touch()'s heartbeat UPDATE through _retry_write (same idempotent write boundary; observability stamp gets identical protection). Reads (execute_query) never retried — contract applies to the write phase only. Added runtime import of ConcurrentTransactionError (previously TYPE_CHECKING-only) + import random; verified no import cycle (adapter does not import walks). Interpretation note: P7-S2's 'up to 3 attempts' taken literally as 3 total attempts (= 2 retries); the contract's 'retry ×3' read as bounded-by-3. If 3 retries (4 attempts) were intended, bump _MAX_WRITE_ATTEMPTS to 4.
      FIXED contract deviation (unmarked by exec-manager): retry count corrected to match CONTRACTS rule #6 / DD-universal-upgrade.md line 105 ("retry idempotent write x3, then fail unit"). runner.py now declares _MAX_WRITE_RETRIES = 3 and _MAX_WRITE_ATTEMPTS = _MAX_WRITE_RETRIES + 1 (4 total attempts = initial + 3 retries) — _retry_write loop, backoff range uniform(0.05, 0.10), LLM-never-reinvoked guarantee, and heartbeat _touch() wrapper behavior all unchanged. Tests aligned: test_concurrent_error_retries_up_to_3_then_fails_unit now asserts 4 write attempts (3 retries) before unit failure; test_backoff_timestamps_50_100ms_apart asserts 4 stamps / 3 gaps; test_retry_sleeps_50_100ms_per_contract asserts len(sleeps) == 3 each in [0.05, 0.10]. write-succeeds-on-retry-2, LLM-never-reinvoked, and happy-path tests untouched (still 2 / 2 / 1 attempts). Verification: python -m pytest tests/pipeline/test_runner.py -q -> 47 passed; python -m pytest tests/pipeline -q -> 894 passed (>= 894 gate); ruff check runner.py + test_runner.py -> clean. Interpretation note in the code comment corrected to "x3 retries = 4 total attempts".
- [x] TDD GREEN: run the retry tests; assert backoff timestamps are 50-100ms apart and the write eventually succeeds on retry 2
    **Note:** TDD GREEN: all 6 new retry tests pass; test_runner.py now 47 passed (41 baseline + 6 new). test_write_succeeds_on_second_attempt proves a write that fails once succeeds on retry 2 (2 write attempts, unit completes). test_backoff_timestamps_50_100ms_apart records time.monotonic() at each attempt in _FlakyStorage and asserts consecutive gaps >= 0.045s (50ms contract minus 5ms scheduler tolerance) and < 1.0s (loose CI upper bound — timing sensitivity noted; the exact 50-100ms ceiling is pinned by test_retry_sleeps_50_100ms_per_contract, which captures sleep() args via patch('app.pipeline.walks.runner.time.sleep') and asserts each in [0.05, 0.10]). test_retry_never_reinvokes_llm asserts the walk's chat_completion marker fires exactly once across a fail-then-succeed write. One test-side fix during GREEN: the fail-unit test originally asserted 'ConcurrentTransactionError' in error, but the runner records str(exc) (the adapter raise message) — assertion changed to the real message text 'transaction is owned by thread', matching adapter.py's non-owner-thread wording.
- [x] Verify: `ruff check app/pipeline/walks/runner.py` clean; `pytest tests/pipeline/test_runner.py -q` green
    **Note:** Verification passed. `ruff check app/pipeline/walks/runner.py` → All checks passed; `ruff check tests/pipeline/test_runner.py` → All checks passed (defaults; no ruff config in repo — see discovery L96). `python -m pytest tests/pipeline/test_runner.py -q` → 47 passed (41 baseline + 6 new). Full suite `python -m pytest tests/pipeline -q` → 894 passed, 1 warning (888 baseline + 6 new; ≥888 gate met; the 1 warning is pre-existing). Guard suite included in the 894. app/tts.py untouched, walk_2a..2i untouched, no new module files, run_walk/render_audiobook signatures untouched. Only files changed this phase: app/pipeline/walks/runner.py, tests/pipeline/test_runner.py. No commit (Phase 8 handles it).

### Phase 8: Regression gate, guard suite and verification
- [x] Run `pytest tests/pipeline -q --cov=app/pipeline --cov-report=term-missing` and record pass count + coverage (baseline 768 / 89%; runner and api_export coverage must rise)
    **Note:** Regression gate PASSED. `python -m pytest tests/pipeline -q --cov=app/pipeline --cov-report=term-missing` → 894 passed, 0 failed (20.84s). vs plan baseline 768 → +126; also exceeds the actual pre-Plan-B suite of 844. Overall coverage 90% (2659 stmts / 253 miss) vs 89% baseline → ROSE. Per-file vs baselines: app/pipeline/walks/runner.py 75% (222 stmts / 55 miss) vs 67% baseline → ROSE +8pp (task expected ~73%); app/pipeline/api_export.py 95% (169 stmts / 8 miss) vs 76% baseline → ROSE +19pp (task expected ~78%+). Other notable: adapter.py 100%, schema.py 100%, api_onboard.py 95% (miss 65,137-138), api_walks.py 93% (58-60,67), tts_integration.py 98% (307,332,468). Remaining runner.py misses are the 9 pre-existing _verify_* functions (need DB fixtures; documented since P2) + 4 wrapper-delegation lines. Warning note: the --cov run reports 436 warnings — this is cov-plugin multiplication of the single pre-existing starlette/httpx deprecation warning (plain `pytest tests/pipeline -q` → 894 passed, 1 warning); no new warnings introduced.
- [x] Run `pytest tests/pipeline/test_legacy_removed.py -q` and verify 12/12 still green (no legacy endpoint resurrection; new /api/pipeline/export/* and /api/pipeline/walks/* paths are guard-legal per pre-verified simulation)
    **Note:** Guard suite PASSED. `python -m pytest tests/pipeline/test_legacy_removed.py -q` → 12 passed in 1.47s (12/12 green). All 9 legacy-file-absent parametrized cases + test_app_imports_and_exposes_no_legacy_routes (exact-match on 26 legacy paths + family prefix check) + test_state_ts_has_no_pipeline_enabled + test_no_project_manager_identifier_in_app all pass. Confirms: no legacy endpoint resurrection; the new Plan B routes (POST /api/pipeline/render, /api/pipeline/render/cancel, /api/pipeline/export/jobs/{job_id}, /api/pipeline/export/jobs/{job_id}/chunks, /api/pipeline/export/jobs/{job_id}/download, /api/pipeline/walks/{book_id}/runs, etc.) are all under the /api/pipeline/ family so the exact-match + family checks treat them as legal; /api/pipeline/onboard sanity assert still mounted.
- [x] Security review: confirm all new queries are parameterized (no f-string SQL), download path resolution cannot escape the run directory (path traversal check), cancel endpoints require a valid job/run id (404 not 500 on unknown); document findings
    **Note:** Security review of Plan B (Phases 1-7) changed files: app/pipeline/walks/runner.py, app/pipeline/tts_integration.py, app/pipeline/api_export.py, app/pipeline/api_walks.py, app/pipeline/adapter.py, app/pipeline/api_onboard.py. Method: git diff of all 6 files + per-file scan of every SQL execute_* call site + full read of download_render/FileResponse404/cancel endpoints. Findings:

CHECK 1 — Parameterized SQL: PASS. Every execute_query/execute_insert/execute_update/execute_delete added by Plan B uses ? placeholders exclusively: api_export.py (8 calls: render_status SELECT ×2, export_job_detail SELECT, export_job_chunks SELECT ×2, download_render SELECT, _mark_job_row_terminal UPDATE, render INSERT), tts_integration.py (8 calls: _ensure_render_job_row SELECT+INSERT, _insert_chunk_row INSERT, _mark_chunk_done/_mark_chunk_failed UPDATE, _finalize_job completed/failed/cancelled UPDATE), runner.py (INSERT walk_run, cancel_walks SELECT+UPDATE, is_cancel_requested SELECT, heartbeat + _finalize_run UPDATEs — all via _retry_write which closes over sql+params only), adapter.py (_reconcile_stale_runs 2 UPDATEs), api_walks.py (get_walk_runs SELECT). Status literals ('running','completed','failed','cancelled','interrupted','pending','done') are fixed constants inside SQL strings, never interpolated user input. The only f-string SQL in the pipeline (tts_integration.py:131 IN-clause, api_voices.py:255, assembly/ledger/operations/review) is PRE-EXISTING and outside the Plan B diff; the tts_integration one interpolates only a '?, ?, ...' placeholder-count string with values passed as a params tuple — no user value enters SQL text. f-strings added by Plan B are error text, tempdir prefix (f'audiobook_{book_id}_'), chunk filename, and stop-file name (f'{run_id}.stop' where run_id is a server uuid4) — none are SQL.

CHECK 2 — Download path resolution / traversal: PASS (with pre-existing caveat, not a Plan B regression). (a) job_id from the URL is NEVER used to construct a filesystem path — only `WHERE job_id = ?` parameterized lookup; URL traversal like job_id='../../etc/passwd' is impossible (DB key only). (b) output_dir/output_artifact_path are read from the render_job row. output_dir originates from the optional RenderRequest.output_dir at POST /render, or defaults to tempfile.mkdtemp(prefix=f'audiobook_{book_id}_') in render_audiobook when omitted — so default runs live in temp dirs; a caller who explicitly supplies output_dir designates that directory themselves (there is NO server-side RENDER_ROOT confinement; that arrives in Plan C). (c) Resolution logic in download_render: artifact_path not-a-dir → FileResponse404(artifact_path) serving the recorded m4b (audio/mp4, filename audiobook.m4b); m4b-first parity branch serves os.path.join(output_dir, 'audiobook.m4b') (constant filename); zip fallback enumerates sorted(os.listdir(output_dir)) on the ROW's output_dir, filters .wav/.mp3/.m4a/.flac, writes zf.write(os.path.join(output_dir, name), arcname=name) — arcname is the basename so no zip-slip on extraction. All path components appended are constants or os.listdir basenames. (d) VERIFIED via `git show HEAD:app/pipeline/api_export.py`: the pre-Plan B download_render had IDENTICAL resolution logic (same os.listdir zip fallback, same user-suppliable output_dir read from the _render_jobs dict) — Plan B changed only the data source (dict → row) and added FileResponse404, which IMPROVED the vanished-artifact behavior from broken 200/500 to a proper JSON 404. Residual exposure: a caller who drives their own render to 'completed' with a hostile output_dir could cause the zip fallback to enumerate that dir and follow symlinks (zipfile follows links) — but that requires write access to the served directory, is 100% pre-existing, and unchanged by Plan B. Verdict: no Plan B regression; recommend Plan C's RENDER_ROOT confinement as the durable fix (out of scope here).

CHECK 3 — Cancel + Phase 5 GET endpoints, unknown ids → 404 not 500: PASS. POST /cancel_render: `_render_jobs.get(request.job_id)` → None → HTTPException(404); note it consults the in-process dict not the row, so a row-only job (e.g. post-restart) yields 404 — documented behavior, no crash. POST /cancel_walks: broadcast-by-book_id per contract (no unknown-id 404 required); runner.cancel_walks does parameterized SELECT then per-row UPDATE; zero rows → clean no-op returning {'status':'cancelled'}; no 500 path. GET /export/jobs/{job_id}: parameterized SELECT → no row → 404. GET /export/jobs/{job_id}/chunks: job-presence SELECT → 404 unknown job, else parameterized chunk SELECT. GET /walks/{book_id}/runs: parameterized SELECT → 200 [] for unknown book (contract behavior, not 404/500). GET /render_status/{job_id}: row → dict fallback → 404. GET /download/{job_id}: row → 404 for unknown/not-completed/missing-output-dir; FileResponse404 returns JSONResponse 404 when the artifact vanished (avoids the pre-fix starlette RuntimeError→500). All id lookups parameterized; 404 paths raise HTTPException (FastAPI → JSON 404) or return JSONResponse; none raise unhandled exceptions.

CONCLUSION: No security defects found in Plan B changes. No fixes applied (nothing met the one-line obvious-parameterization criterion; none needed). No production code modified during this verification phase.
- [x] Code review pass via exec-manager QA-Reviewer; fix MINOR findings; escalate PLANNING_GAP items if any
    **Note:** QA gate: PASS on Round 2 (full fresh re-review after Round-1 fix cycle). QA-Reviewer verdict PASS: checks lint/layerCompliance/contracts/codeQuality/completeness/testCoverage/documentation all green; testAnalyzerReport PASS (9 log-only MINORs, no dispatch per analyzer routing); docsAnalyzerReport PASS (2 INFO observations). All 8 Round-1 MINOR fixes verified correct (dead TYPE_CHECKING block removed, docstring drift corrected on _run_render_job/module list/Raises/cancel_walks, __getattr__ annotated, _format_batch_failure typed, stop-file best-effort removal in _finalize_run). app/tts.py byte-for-byte preserved (md5 435696434e5141d70f2c20efa8e35f12 == HEAD). Guard 12/12. 894 tests, 90% overall cov (runner 75% vs 67%, api_export 95% vs 76%). Fix cycles used: 1.
- [ ] Commit: `feat(pipeline): persist render/walk jobs to SQLite with reconciliation and cancel`

## Completion Criteria

- walk_run, render_job, render_chunk rows are written and read by runner/api_export; rows = truth for status and progress
- `is_cancel_requested(run_id)` is the single cancel dispatcher; cancel_walks/cancel_render persist their intent
- `reconcile_stale_runs()` flips stale running→interrupted once at startup
- Endpoints: render_status per-chunk counts, export/jobs/{job_id}, export/jobs/{job_id}/chunks, walks/{book_id}/runs implemented and tested; download rewritten with FileResponse-404 and full test_export.py coverage
- Walk-side retry on ConcurrentTransactionError with 50-100ms backoff ×3
- Smoke harness re-established and green; full pytest suite green; guard 12/12; ruff clean

## References

- `artifacts/designs/pending/DD-universal-upgrade.md` — P1 A1-r phase, FP1/FP2, workflow (Render/Walk), decisions #1/#2/#3/#4
- `artifacts/designs/parts/universal-upgrade/CONTRACTS.md` — walk_run/render_job lifecycle, is_cancel_requested, 4 endpoints
- `artifacts/designs/parts/universal-upgrade/README.md` — Plan B row
- Prior: `TASK-universal-upgrade-A-schema-transaction-foundation.md` (completed)
